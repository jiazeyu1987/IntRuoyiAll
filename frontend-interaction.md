# eDHR 偏差管理前端交互说明

## Purpose and Scope

将已确认的业务规则落实到用户能看到的页面、字段、按钮、签名、状态和失败反馈。PRD 为需求事实源；本文件固定实现交互取舍，不把推测写成用户新要求。本轮交付为 Markdown 设计，不包含界面代码或真实浏览器验收。

## Evidence Reviewed

- 现有 UnifiedListTemplate 提供筛选、排序、列设置和分页。
- BatchExecutionActiveOrderDetailPage 使用 ActiveOrderSubmissionDetailPanel，接收 batchExecutionId 或 activeOrderId。
- ActiveOrderSubmissionDetailPanel 被多个页面复用，FORMAL_BATCH_SOURCE_DETAIL 也用于历史只读内嵌表单，不能单靠此值判断可编辑性。
- 历史 FormTraceAuditTab/ChangeTab/ReleaseTab 打开 BatchExecutionTraceDrawer；BatchExecutionDetailPage 的 traceRecordDrawer 是另一个独立追溯入口。
- PRD FR-01～FR-12、常规关闭签名清单和关键偏差直接转审规则。

## Navigation and Page Scope

### 页面入口与返回

| 场景 | 偏差展示位置 | 动作与身份 |
| --- | --- | --- |
| 偏差管理 | 全部、未处理、已处理 Tab | 一条偏差一行，打开偏差详情；有发起权限显示“发起偏差”。 |
| 批记录/活跃表单详情 | 与总表、生产表单等平级的“偏差”主 Tab | 当前批记录固定；有权限且未上市放行可发起偏差，QA可选择关键偏差转审。 |
| 历史追溯列表 | BatchExecutionTraceDrawer 中只读“偏差”Tab | 只读，不在其内嵌逐工序表单重复显示另一偏差 Tab。 |
| 批次详情追溯 | BatchExecutionDetailPage 自身 traceRecordDrawer 中只读“偏差”Tab | 只读，数据范围与外层正式批记录一致。 |
| 现有普通订单/PQC/NCR共享详情 | 不因组件复用自动新增偏差入口 | 保留原功能；从关联偏差链接进入授权的偏差详情时仍按其批记录身份。 |

新增 BatchDeviationPane 只接收正式 batchExecutionId 和 readOnly。宿主显式决定是否显示主 Tab：正式顶层详情显示；历史或逐工序内嵌预览不显示主 Tab。FORMAL_BATCH_SOURCE_DETAIL 是记录语义，不能直接赋予写权限；write action 同时依赖宿主非只读、服务端 allowedActions 和用户权限。

正常入口不要求用户复制/输入技术 ID。若路由只有 activeOrderId，后端按正式来源关系解析并返回 batchExecutionId（建议 deviationContext），前端再查询偏差。解析到多个批记录时要由正式详情明确当前对象，不取第一条/最新一条；缺失或歧义显示错误。开发必须补齐正常入口身份传递，不能以“缺 ID”页面作为完成标准。

从列表进入详情再返回，恢复该页签、筛选、排序和页码；若当前页因偏差关闭已无记录，重新请求上一有效页。偏差详情的关联批记录和关联NCR编号可点击返回正式详情，保留来源位置。

## Page Layout

~~~text
偏差管理                                      [发起偏差]
[全部] [未处理] [已处理]
[编号] [批记录] [等级] [发起时间]    [查询] [重置] [列设置]
编号 | 关联批记录 | 等级 | 发起人/时间 | 偏差进度 | 关闭方式 | 评审状态 | 操作
标准分页

偏差详情：PC-202609-0001   重大（关键）   未处理/已处理
关联批记录：可点击的正式编号    关联不合格审批：可点击编号/无
[发起信息]  [唯一处理记录]  [签名与审计]
当前可执行动作与未满足条件；转审后持续显示评审状态和批次限制
~~~

详情分区可以用现有页签/分节组件，无需复制 Word 的合并单元格布局；字段语义和签名位置完整保留。列表使用 UnifiedListTemplate，不能另建简化表格代替标准模板。

## Deviation List Interactions

- 查询权限可查看当前租户全部偏差，不自动限定本人；没有 query 权限显示无权访问。
- 全部=status ALL，未处理=status OPEN，已处理=status CLOSED，均由后端分页；每个Tab结果正确独立计算total。
- 切换状态Tab保留用户显式搜索条件但重置页码为1；状态过滤只来自Tab。重置清空搜索条件、保留当前Tab并回到第1页。排序/列宽/可见列沿用标准列表约定。
- 搜索字段：偏差编号、批记录编号、等级、发起时间。级别仅普通/重大（关键）；不得出现第三档“主要”或“重大/关键”两个选项。
- 默认列：编号、批记录号、产品/批号、等级、发起人/时间、处理进度、关闭方式、NCR编号/状态、操作。
- closeReason=NORMAL_COMPLETED 显示“常规闭环”；TRANSFERRED_TO_NCR 显示“转不合格审批关闭”。后者仍显示“待处置/让步放行/返工/作废”，不能把它渲染成“整改验证合格”。
- 旧请求晚返回不能覆盖新Tab/批次结果；加载失败不显示旧列表作为当前结果。详情请求失败不清空用户正在编辑的内容。

## Create Deviation Interactions

1. 从管理页发起：打开完整表单，搜索选择批记录。从批记录详情发起：固定当前批记录，不允许改绑到另一份。
2. 展示服务器返回的批记录号、产品/规格、批号；无正式来源的字段由用户填写，不按相邻订单或模板推算。
3. 字段完整呈现如下。首版校验取舍属于工程设计：身份、等级、发现和描述等核心项必填；客观不适用的设备/系统或措施不得伪造值，用明确“不适用”及说明保存。最终业务事实由填写人确认。
4. “提交并签名”校验字段后打开签名确认。用户取消签名时保留输入、不创建；成功后展示正式偏差编号并跳转详情。
5. 不提供发起草稿持久化；未提交前不预分配编号。网络结果不明时用同幂等键重试/回读，不能点击一次产生两条偏差。

| 发起区 | 控件与校验 |
| --- | --- |
| 发现部门、发现人、接收人 | 正式组织/用户选择；发起编制签名人由系统认证确定，不能输入姓名冒充。 |
| 发现时间、上报日期 | 日期时间/日期控件；业务日期和服务器签署时间独立显示，不改服务器时间。 |
| 发现地点/工序、设备/系统 | 文本/正式选项。地点/工序仅描述，不建立工序级主关联。 |
| 产品名称/规格、批号 | 从所选批记录带出可用正式快照，缺少业务说明时明确填写，绝不改关联主键。 |
| 偏差类别 | 八类多选，至少一类；类别完整名单见PRD。 |
| 现象、紧急措施、分级依据 | 多行文本；现象与等级依据必填，措施不存在时明确记录未采取及原因。 |
| 等级 | 单选普通/重大（关键），必填；不自动映射原Word的“主要偏差”。 |
| 编制人/编制日期 | 电子签名区显示真实人、时间与签署含义；发起签名成功后锁定发起原文、等级和关联。 |

## Single Handling Record Interactions

未开始处理时显示“尚未填写处理内容”和“填写处理”（有handle权限时）。第一次保存建立唯一记录，后续“编辑处理”更新同一ID，不显示第二份处理单、处理轮次或第二条业务结果。

| 处理区 | 控件与条件 |
| --- | --- |
| 调查日期、计划完成、实际完成、调查组成员 | 日期与人员选择；未完成时允许缺实际完成日期，常规提交完整性检查不能等同于提前填造完成时间。 |
| 根因与影响 | 展示直接/根本原因、涉及人机料法环测、范围/数量/批次/流向、质量安全有效性、同类历史。长文本有明确标签。 |
| 风险评估 | 三项原文选项：无质量风险可正常放行、轻微风险整改后放行、重大风险隔离/报废/追溯排查。选项不直接执行系统放行/报废。 |
| 产品处理 | 措施、是否执行不合格品程序、评审编号、整改责任人、预计完成日期。是否=是时关联编号必填；不替代QA转NCR动作。 |
| CAPA | 是否执行纠正预防、CAPA编号、附件；是时要求编号及原表要求的随附材料；使用受控文件预览，不以文件名拼URL。 |
| 整改验证 | 核查内容、合格/不合格、验证日期、验证签名。不合格时保留OPEN并允许修改同一记录再验证。 |
| 处理结论 | 闭环完成/持续跟踪及部门负责人、QA签署；持续跟踪不能常规关闭。 |
| 批准 | 质量负责人签名；仅重大（关键）常规路径展示管理者代表必签节点。 |

“保存处理”只保存当前内容，不自动签名/关闭。“签署处理编制”先做本节点完整性校验。进入编辑已签内容时提示将使本处理版已有签名失效；保存必须有修改原因。服务端返回新contentVersion后清除前端有效签名标记，保留历史签名查看入口。未保存变更离开页面需明确放弃或继续编辑；口令永不保留。

处理人只通过“保存处理”修改调查、风险、措施和CAPA正文。验证结论由验证人签署提交，部门处理结论由部门负责人签署提交，再投影到同一处理记录；后续批准在这些事实之上签署。第一次新增验证/部门/批准不更改处理正文版本；修订验证或部门结论必须给原因，并由服务端让依赖旧结论的下游签名失效。界面不允许处理人直接覆盖他人的已签结论。

## Sign and Close Interactions

| 节点/按钮 | 前置与成功效果 |
| --- | --- |
| 提交并签名（发起） | create权限、批记录合法未上市放行；成功生成编号和发起签名。 |
| 签署处理编制 | handle权限、当前内容完整；保存该处理版编制签名。 |
| 签署验证 | verify权限、当前处理编制已签；记录合格/不合格和签名。不合格不关闭。 |
| 部门负责人确认 | department-confirm权限、处理与验证事实可审；记录结论签名。 |
| QA关闭确认 | qa-close权限、当前处理/验证/部门签名有效；可以先于质量负责人完成。若还缺其他节点，提示“已确认，等待其余签名”，仍未处理。 |
| 质量负责人批准 | quality-approve权限、当前处理/验证/部门签名有效；不要求QA已签。 |
| 管理者代表批准 | critical-management-approve权限且重大（关键）、常规路径；不对普通偏差显示必签。 |
| 最后必要签名完成 | 服务端满足FR-06才转CLOSED；QA/质量/管理者新增同版签名不改变正文哈希。 |

同一账号有多角色时可逐个完成节点，但一次签名不会自动填写其他节点。签名确认显示偏差编号、动作含义、内容版本和受控再验证；错误保留原表单并要求重新输入口令，成功显示姓名、服务器时间/时区、意图、图样（既有签名支持时）和证据链接。

关闭后详情只读。转审关闭保留已有处理内容/签名，没有完成的节点显示“已转不合格审批，未执行此节点”；不得出现虚构姓名、默认合格或空处理行冒充处理完成。

## Critical Deviation to Nonconformance Review

1. QA可在尚未推送PQC的批记录详情发起，不要求先进入PQC放行列表。偏差详情的入口固定当前批记录并预选当前关键偏差，允许选择同批其他开放关键偏差。
2. 选择器至少显示编号、等级、发起摘要和状态，使用标准列表模板；只选同租户同批OPEN+CRITICAL。按钮为“发起不合格审批”，创建后自动关闭的是明确勾选项。
3. 填评审原因并进行NCR发起签名；无需先完成偏差表处理、验证、部门/QA/质量/管理者批准。取消或失败均不关闭偏差。
4. 确认提示：“成功发起后，所选偏差将以转不合格审批方式关闭，批次继续受限，直至评审处置。”确认一次提交，pending期间禁连点。
5. 成功后打开已有统一NCR详情，并刷新偏差/批次状态；已处理列表显示“转审关闭”，NCR待处置继续阻止四项动作。未选偏差保持OPEN。
6. 同批已有待审NCR时显示其编号和“查看评审”，拒绝第二张，不静默追加/关闭新偏差。评审处置仍可执行；之后未关闭关键偏差可再发起下一张。首版一次选择多条满足一评审多偏差要求。

该等级限制只针对“从偏差转NCR”的入口。现有PQC检验不合格等独立NCR来源仍按其原规则使用，不得因为本模块而强制所有NCR都先有关键偏差。

## Disposition and Batch Controls

QA或获PQC生产放行处置授权的用户，在统一NCR页按现有材料、意见和签名要求选择：

- 让步放行：解除该评审的待审阻断，保留其他开放偏差/外部冻结及PQC、上市放行原前置；不自动完成上市放行。
- 返工：按正式返工状态/任务进入受控返工；不以一个“恢复”提示替代实际状态回读。
- 作废：终止批次后续生产和放行，历史偏差与评审保留可查。

四项动作拦截提示说明阻断来源及编号，提供有权限的详情链接。未关闭偏差拦截一线生产提交、一线PQC提交、PQC生产放行、上市放行；处理、签名、发起NCR和处置按钮不被该门禁一并禁用。

## Empty, Loading and Error States

| 场景 | 展示/操作 |
| --- | --- |
| 偏差首次加载 | 独立loading，不阻塞主批记录详情；必要时单独重试。 |
| 查询成功且为空 | “没有偏差”；有create权且未上市放行可发起。 |
| query无权限 | “无权查看偏差”，不显示“没有偏差”。 |
| 来源缺失/歧义 | 明确身份错误；正常入口要补齐正式来源合同，错误态不是交付替代方案。 |
| 请求失败 | 显示错误和重试；不回退到空数据、不显示前一批次数据。 |
| 已上市放行 | 偏差历史可查，所有新增关联动作禁用，服务端仍校验。 |
| 乐观锁/内容版本冲突 | 保留本地输入并提示重新加载差异，用户确认后重新提交；不自动覆盖他人修改。 |
| 网络响应不明 | 使用原幂等键核对/重试，结果确定前不新建另一请求。 |
| 签名失效 | 标“需重新签署”，历史证据仍可打开，不能继续作为关闭条件。 |

## Responsive and Accessibility Behavior

沿用Element Plus焦点、标签、键盘和表单错误定位；等级/状态同时有文字。桌面显示完整标准表格，小屏横向滚动；长文本、材料名称、签名含义可换行。弹窗默认聚焦首项，确认/取消文本明确。列表无多余仪表盘或另造设计体系。

## Interaction Acceptance Matrix

UI-01 三Tab/筛选/排序/分页/返回状态 → P3-AC1。
UI-02 发起与批记录锁定、上市放行并发拒绝 → P1-AC1/2/3/5、P2-AC1。
UI-03 唯一处理记录、部门签名、重签、QA与质量任意顺序 → P2-AC2/3/4/5/6。
UI-04 推送PQC前直接转NCR、多选/取消/错误/成功 → P1-AC4、P4-AC1/2/3/6。
UI-05 已处理与待评审双状态、四项限制和处置 → P4-AC4/5/7。
UI-06 正式来源透传、共享组件/只读隔离、空/错/无权 → P3-AC3/4/5/6、P5-AC1/2/3。

## Open Questions

交互主流程已明确。首版不增加发起草稿、已签NCR事后追加、偏差重开及PDF导出改造。具体路由注册名、菜单ID和组件命名由开发按现有工程规范确定，不改变上面的行为合同。
