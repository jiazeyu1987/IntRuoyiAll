# 注册证支持材料直接生效且暂缓模板校验

## 决策

2026-08-24，用户明确决定：延续受理单/立卷发补单上传后直接生效；本次暂不实现自动模板校验。

## 影响

- 删除“待文控确认”“确认”“驳回”的业务状态、接口和页面动作。
- 支持材料必须仍通过证书、版本、租户、公司范围、正式业务文件归属及文件状态校验；这些不是模板校验，不能省略。
- 上传成功后，8 个月浅色提醒立即恢复为正常。
- 自动模板校验作为独立的后续需求保留；本次不得把未执行的校验伪装为已通过。

## 非目标

本变更不新增文控审批、不改动下载申请待定策略，也不导入历史附件。
