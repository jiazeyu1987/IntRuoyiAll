# 文控浏览与上传测试项新增

## 任务目标

通过本机 `int_main` 真实前端的“系统管理 > 测试管理”页面，依据文控受控浏览和文件上传用户路径新增 4 个可执行测试项，并核验页面列表中的最终数据。

## 里程碑

- [ ] 完成任务、登录、E2E、测试管理与经验门禁预检。
- [ ] 保存任务开始前的脏工作区基线，并记录提交证据。
- [ ] 核对文控浏览与上传真实页面行为，确定 4 个测试项内容。
- [ ] 通过真实前端新增 4 个任务自有测试项。
- [ ] 通过页面搜索核验名称、项目、方法和检查点。
- [ ] 完成任务文档、清理、提交与推送。

## 预期验证

- 本机前端 `http://localhost:8081` 可访问，后端 `http://127.0.0.1:48081/actuator/health` 为 `UP`。
- 使用本机默认测试身份标签 `芋道源码/admin` 登录。
- 4 个测试项均通过测试管理页面创建，不使用 API 或 SQL 绕过。
- 页面按测试项全名搜索时每项唯一命中，且测试方法和检查点与文控真实路径一致。
- 不执行新建测试项，不向文控业务数据发送写请求。

## Current Status

in_progress

## 设计约束检查

- `是否引入 fallback/降级/吞异常`：否。
- `是否从根因和长期维护角度解决`：是，测试项基于文控真实页面入口、可见动作和明确检查点编写。
- `是否存在临时补丁或绕过`：否。

## 经验门禁

- 测试管理真实操作必须走本机前端页面；API 仅可用于最终只读核验，不得替代页面创建。
- 写入前确认本机前后端入口、目标测试租户和测试管理员身份；测试项名称需带明确业务范围并可追踪。
- Element Plus 表格按可见业务全名定位目标行，不使用数组下标、表头全选或坐标猜测。
- 测试项只描述文控路径，不立即执行 Runner；因此本任务不要求 Runner token、Runner 在线状态或 Codex CLI 前置。
- 当前工作区已有并行脏改动，必须先独立保存基线，不得回滚、覆盖或删除既有改动。
