# 验证报告

## 范围

- 在本机 `int_main` 的 `系统管理 > 测试管理` 中，基于文控受控浏览和上传场景新增 4 个测试项。
- 本任务不执行新增测试项，不启动 Runner，不修改文控业务数据。

## 新增结果

- `文控受控浏览：目录与关键字筛选`：3 个测试方法项，2 个测试目标项。
- `文控受控浏览：在线预览只读限制`：3 个测试方法项，2 个测试目标项。
- `文控上传：普通受控文件提交审批`：4 个测试方法项，2 个测试目标项。
- `文控上传：图纸源文件缺少PDF阻断`：4 个测试方法项，2 个测试目标项。

## 验证命令

- `Invoke-RestMethod http://127.0.0.1:48081/actuator/health` -> PASS，返回 `UP`。
- `Invoke-WebRequest http://127.0.0.1:8081/` -> PASS，返回 HTTP 200。
- 本机 Playwright 真实页面脚本 -> PASS，使用 `芋道源码/admin` 登录 `http://127.0.0.1:8081/system/codex-test-management`，通过页面新增并按全名搜索核验全部 4 项。

## 关键断言

- 每个测试项项目均为 `文控`。
- 每个测试项均可按完整名称唯一检索。
- 每个测试项列表行均展示预期测试方法项和测试目标项文本。
- 未点击执行按钮，未创建 Runner execution，未写入文控业务数据。

## 收尾状态

- 页面写入与验证已完成。
- Cleanup preview/apply 已通过，无删除项、无阻塞、无警告。
- 最终提交/推送暂未完成：共享工作区存在非本任务脏改动和其它线程 ahead 提交，本任务不得混入或推送无关文件。
