# 粗洗设备参数配置

## Task Goal

在前端工序配置中支持配置粗洗工序的 5 个设备参数：清洗次数、清洗介质、清洗功率、室温、清洗时间，并在一线运行页面按配置展示和输入。

## Milestones

- [ ] 定位现有工序配置与一线运行页面参数渲染链路。
- [ ] 补充粗洗参数配置的 BDD 场景与最小 RED 验证。
- [ ] 实现前端配置、默认值、类型、范围和下拉选项。
- [ ] 运行定向验证并记录 GREEN / 回归证据。

## Expected Verification

- 运行定向静态合同或单元测试，证明粗洗 5 个参数均可配置。
- 运行受影响前端验证命令，至少覆盖目标静态合同与 `git diff --check`。
- 若运行真实页面验证所需服务、账号或入口缺失，记录阻塞，不使用 mock 或 API-only 替代真实路径。

## Current Status

in_progress

## 设计约束检查

- `是否引入 fallback/降级/吞异常`：否。
- `是否从根因和长期维护角度解决`：是，目标是复用正式工序配置链路表达粗洗参数约束。
- `是否存在临时补丁或绕过`：否。

## Experience Gate

- 已检查 `docs/experience-index.md`，命中“一线设备参数默认值”门禁。
- 适用约束：设备数值参数默认值必须优先来自正式显式默认值；前端不得重复计算范围中点、不得用 `Number(null)` 生成 `0`、不得用固定常量覆盖显式目标值。
- 本任务实现只能在粗洗参数配置定义中写入用户指定的正式默认值、类型、范围和选项；运行态输入初始化仍应遵守正式配置链路。
