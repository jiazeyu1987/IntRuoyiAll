# 20260729-admin-submitted-content-e2e

## Task Goal

验证批记录管理员在批次执行主区域看到的是当前已提交内容，而不是草稿或静态快照；确认其他账号提交后，管理员只读主区域能读取并显示提交后的单元格值。

## Milestones

- [x] 建立任务记录并读取 E2E、登录、本地运行态和编码门禁。
- [ ] 确认本机前端/后端入口和验证脚本前置条件。
- [ ] 执行真实 Playwright E2E，覆盖普通填写账号提交与管理员只读查看。
- [ ] 记录验证证据、失败位置或阻塞前置。

## Expected Verification

- 使用本机 `http://127.0.0.1:8081` / `http://127.0.0.1:48081` 成对入口。
- 使用真实 Playwright 页面路径登录、打开批次执行、提交填写内容，再以批记录管理员视角查看主区域。
- 断言管理员主区域显示提交后的单元格内容，不读取草稿，不触发 MES 写请求。
- 记录脚本命令、入口 URL、租户/用户标签、目标批次/任务/执行记录、关键断言和 artifact 路径。

## Current Status

in_progress

## 设计约束检查

- `是否引入 fallback/降级/吞异常`：否。
- `是否从根因和长期维护角度解决`：是；本任务为验证任务，按正式运行态和真实页面路径验证。
- `是否存在临时补丁或绕过`：否。

## Applicable Gates

- E2E 必须使用 Playwright 操作真实前端页面，API 只能用于最终状态核验或只读辅助检查。
- 本机验证使用 `int_main` 成对入口 `8081/48081`，不得静默换端口或 API-only 冒充页面通过。
- 批记录相关链路必须区分正式批记录表单、表单槽位和工序开始，不得用 `formBindings` 或草稿数据替代正式生产批记录。
- 管理员只读验证需确认主区域显示当前已提交内容，且无草稿保存、提交等 MES 写请求。
