# 生产放行全流程实施总任务

## Task Goal

依据已评审的 SP-0 至 SP-4 文档，在独立 worktree 内完成生产组长完工、PQC 放行、批次执行与数据映射、四份报告上传、管理者代表放行、可追溯入列的端到端实现和独立验证。

## Milestones

- [x] M0：建立总任务状态、验收门禁与 worktree 约束。
- [x] M1：复核实际代码并批准统一 PRD 与可测试验收标准。
- [x] M2：批准依赖图、接口衔接、开发计划与测试计划。
- [x] M3：完成 SP-0 角色和权限基线。
- [x] M4：完成 SP-1 生产组长到 PQC 待办。
- [x] M5：完成 SP-2 PQC 放行、批次执行创建和三类业务数据映射。
- [x] M6：完成 SP-3 三类负责人上传四份报告及完整性门禁。
- [x] M7：完成 SP-4 管理者代表放行和可追溯入列。
- [ ] M8：完成独立测试、真实用户路径 E2E、回归与总验收。
- [ ] M9：完成任务收尾；未获用户明确授权不得提交、合并或推送 Git。

## Expected Verification

- 每项业务验收标准均有对应的后端测试、前端合同或组件测试、接口集成验证证据。
- 使用真实角色、真实页面入口和任务自有测试数据完成 Playwright E2E；API 只用于最终只读核验。
- 验证重复提交、越权、并发版本冲突、报告缺失、拒绝终态、唯一批次执行、事务回滚等异常路径。
- 运行与 MES 相关的回归测试，确保批记录表单、表单槽位和工序开始三条链路仍保持独立。

## Applicable Experience Gates

- worktree 必须位于 `D:\IntRuoyiWorktree\`，创建前校验绝对路径；启动服务前原子预留 1..19 的运行槽位。
- 子 Agent 只写分配的 worktree 路径和总任务证据文件；发现主工作区溢出或同文件并发冲突立即阻塞。
- 后端、数据库、前端、E2E、登录和本地运行分别在执行前读取对应权威规则；缺前置条件不得降级为 mock、API-only 或默认成功。
- PowerShell 测试命令逐条检查退出码；不得用分号串联后一个 PASS 掩盖前一个 FAIL。
- 工序开始、逐工序批记录表单、`formBindings` 表单槽位分别建模和验证，不得互相替代。

## 设计约束检查

- `是否引入 fallback/降级/吞异常`：否。
- `是否从根因和长期维护角度解决`：是；以统一状态机、幂等键、乐观锁、角色权限和正式数据映射合同为实施基线。
- `是否存在临时补丁或绕过`：否。

## Current Status

blocked：Agent 产品开发交付已完成，T1-T10 和 2026-08-19 生产组长放行回执前端增量修正均已合入 `int_main`。最新提交为 `e5ba7869a fix: align team leader release receipt flow`，仅包含生产组长活跃订单放行申请回执前端、页面和静态合同三处文件；相关 TeamLeader 静态合同、Prettier、SP-1 合同、生产放行总覆盖静态门禁、`pnpm ts:check` 和差异检查均已 PASS。用户于 2026-08-18 明确修正验收前置：从测试租户登录即可限定后续业务操作在测试租户下，“可见租户管理菜单”不再作为当前开发交付或手工验收前置。P11 当前剩余 blocker 仍是测试租户缺少可用于真实主链的正式业务数据底座：现有 ACTIVE 路线版本缺正式工序节点快照，路线缺 DCC 项目绑定和 QA 当前发布规程组合，现有候选工单不可安全加入活跃订单池。订单、附件和其它业务 fixture 写入保持为 0，TC-13 未通过；继续前需先通过真实页面补齐正式路线/规程/来源底座，再创建三组 `PRFLOW-T11-20260817` 任务自有工单、四附件、签核、追溯和清理证据。

## Ownership

- 主 Agent：唯一调度者，维护 `task-state.json`、阶段门禁、冲突协调和最终验收。
- 子 Agent：按单任务写入范围开发或独立测试，不得修改 `task-state.json`。
- 业务代码目标 worktree：`D:\IntRuoyiWorktree\pqc-production-release-flow`。
- 当前 P11 验证 worktree：`D:\IntRuoyiWorktree\r260817i\a`，`int_main slot=1`，前端 8082、后端 48082。
