# PQC 权限角色分配给角色数小于 2 的用户

## Task Goal

- 在本机 `芋道源码` 租户中，随机选择 30 个有效权限角色数小于 2 的用户。
- 若不存在 PQC 权限角色，则创建一个 PQC 权限角色。
- 将这 30 个用户绑定到 PQC 权限角色，并记录可回滚的用户与角色清单。

## Milestones

- [x] 创建任务目录和初始任务文档。
- [x] 只读核对数据库连接、租户、用户/角色/菜单/关联表结构。
- [x] 只读确认是否已有 PQC 权限角色和至少 30 个有效角色数小于 2 的用户。
- [x] 执行最小范围角色创建或角色绑定数据变更。
- [x] 复核 30 个目标用户已拥有 PQC 角色，且非目标用户不受影响。
- [x] 写入验证报告。

## Expected Verification

- 只读 schema 核对：`system_tenant`、`system_users` 或 `system_user`、`system_role`、`system_user_role`、`system_menu`、`system_role_menu`。
- RED：变更前业务 PQC 权限角色不存在，原“无权限用户”候选为 0；用户已改口径为“权限角色小于 2 个”。
- GREEN：变更后恰好 30 个本次选中用户绑定 PQC 角色。
- 角色验证：PQC 角色存在且绑定 PQC 相关菜单权限。
- 安全验证：变更范围限定到 `芋道源码` 租户，不访问远端，不输出数据库密码或其它凭据。

## Applicable Gates

- 数据库门禁：写 SQL 前必须核对真实 schema；写入型数据操作必须确认目标租户，禁止污染生产租户、admin 基线数据或无关业务数据。
- 登录/租户门禁：默认只操作本机环境；远端环境需要明确授权，本任务无远端授权。
- PowerShell/编码门禁：中文文档和 SQL 承载必须 UTF-8；PowerShell 不使用 `&&`。
- 并发 Git 门禁：当前工作区存在并发改动，禁止宽泛暂存、提交或推送。

## Design Constraint Check

- `是否引入 fallback/降级/吞异常`：否。
- `是否从根因和长期维护角度解决`：是，按正式用户、角色、菜单关联表执行数据变更，不通过 mock 或前端假状态实现。
- `是否存在临时补丁或绕过`：否。

## Current Status

ready_for_closeout

- 用户已将目标口径改为“权限角色小于 2 个”；该口径下 `芋道源码` 租户候选用户数为 2045。
- 已创建业务 PQC 权限角色 `PQC权限角色`，角色 ID `910438`，角色编码 `pqc_permission`。
- 已绑定菜单 `5100,900220,900435,900438`，并将该角色分配给随机选中的 30 个用户；复核无重复绑定、无无效用户。
