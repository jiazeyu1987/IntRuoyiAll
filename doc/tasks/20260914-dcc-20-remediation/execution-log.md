# Execution log

## 2026-09-14 task start
- User intent: 在当前分支逐个修复20项DCC问题；完整范围见 task.md。
- Branch verified: int_qms, HEAD 2f7acecdbd4761717b4e063adca63a720b7a824f, tracking origin/int_qms, no ahead reported.
- Existing dirty state: .gitignore; .runtime/; IntRuoyiFronted/scripts/route-view-source-integrity.test.mjs; IntRuoyiFronted/src/views/dcc/controlled-file/logs/; doc/tasks/20260914-fix-dcc-logs-import/.
- Command intent: 读取规则、核对分支、识别基线与构建测试工具；未运行测试或修改生产代码。
- Baseline blocker: two existing .runtime files exceed 100 MB; requesting explicit exclusion of local tools from baseline. Files remain untouched.
- Previous DCC logs task reports ready_for_closeout. Its source restoration is unrelated and will be preserved.
- Validation: task requirements recorded for all 20 items. No item is claimed fixed or verified.
- User approved excluding .runtime/ from Git while retaining all files. Added root ignore rule; remaining existing changes will be committed separately from task implementation.
- Experience path D:/ProjectPackage/Int/IntPP/FRONTEND_STYLE.md is absent; no style changes planned before resolving that gate. Current project frontend/backend rules are available.
- Baseline commit: b0f8119e5. Files: .gitignore; IntRuoyiFronted/scripts/route-view-source-integrity.test.mjs; IntRuoyiFronted/src/views/dcc/controlled-file/logs/index.vue; previous task task.md, execution-log.md, verification-report.md. Branch runtime port guard passed 8061/48061; staged diff whitespace passed.
- BDD: DCC-MAIN-07/08/17 ordinary approval operations removed -> Given ordinary DCC old return/transfer/before-sign/after-sign requests; When submitted, including concurrently; Then reject with the business action-not-allowed error before any mapper, signature or BPM operation. Historical records remain untouched. External review is a separate flow outside the ordinary-file removal scope.
- Test authoring correction: initial compile failed because this branch uses string sign types; corrected test inputs to before/after. This compile error is not counted as RED evidence.
- RED: Maven -pl yudao-module-dcc -am -Dtest=DccOrdinaryApprovalRemovedActionsTest -Dsurefire.failIfNoSpecifiedTests=false test -> 4 executed, 3 failures + 1 concurrent execution error. Removed ordinary actions reached dependencies instead of returning the business rejection. Ancestor modules have no matching test, hence failIfNoSpecifiedTests=false; DCC test count explicitly checked.
- Implemented immediate business rejection at the three ordinary-file service entry points. External-review-specific methods preserved. Generic BPM controller already invokes external-signature guard; form-center-owned exception remains under review.
- GREEN: DccOrdinaryApprovalRemovedActionsTest -> 4 tests passed, no skipped tests.
- RED/GREEN frontend: node --test scripts/dcc-ordinary-removed-actions.test.mjs -> initially ordinary return rendered; after restricting controls to external review, PASS. Added handler-time guards for dialogs opened before a context change. This is visibility-contract evidence, not real E2E.
- RED: BpmTaskExternalSignatureGuardTest -> 7 tests, 1 failure: form-center ownership allowed a generic DCC route mutation. Narrowed mutation guard to reject DCC regardless of form-center ownership; normal approve/reject retains its existing formal signing route.
- REGRESSION: 127 backend tests passed (BPM guard 7; workflow 100; external review 6; approval adapter 10; removed actions 4). Four old ordinary mutation success tests updated to assert rejection/no writes per new requirement; external review tests retained. Frontend visibility tests 2 passed, including generic BPM DCC controls. Real E2E remains pending.
- BDD: DCC-MAIN-13 directory hierarchy -> Given A contains B; When assigning A's parent to A/B; Then fail without updating. Given corrupt cyclic ancestors; When listing child directories; Then fail explicitly within bounded queries. Concurrent topology edits must serialize current hierarchy reads before parent validation.
- RED: DccDirectoryCycleTest -> 3 failures after completing read-path fixture: self/descendant edits accepted; corrupted path exceeded 10-query safety budget. No production data was used.
- Implemented tenant-scoped topology locking via MyBatis FOR UPDATE before parent validation; cycle detection for tree/path/ancestor/subtree traversal. New specific cycle error. No schema modification.
- GREEN: DccDirectoryCycleTest 3 passed. Directory regression found an existing fixture missing assignment_file.master_id although the mapper joins by master_id; added 9100L matching the fixture's actual file master, preserving all authorization assertions.
- RED frontend: dcc-directory-cycle.test.mjs 2 failures: self/descendant selections invoked update API. Added pre-submit hierarchy validation including stale/missing parent and duplicate nodes; no style changes.
- GREEN directory: backend DccDirectoryCycleTest 3 + DccDirectoryAdminServiceImplTest 19 passed; frontend pre-submit 2 passed. Concurrent database topology race test and full real E2E still pending.
- BDD: DCC-MAIN-18 -> Given overlapping directory/page/tenant requests; When old success/failure returns after context changes; Then no old list, permission, total, error or loading state overwrites current context. Current-context errors must remain explicit.
- RED/GREEN DCC-MAIN-18: first 6 list behavior tests failed, then passed with request sequence and directory/page/tenant/user context check. Two additional metadata/cache races failed and passed with context checks. A route-sync overlap test exposed early guard release; replaced boolean-only lifetime with outstanding-navigation count. Superseded failures are logged; current errors stay visible and reject.
- Frontend grouped behavior tests: 13 passed. Full vue-tsc exited with Node default ~4 GB heap exhaustion; not treated as a type-check pass. Will rerun using appropriate memory after confirming machine capacity.
- Current-branch scope discovery: int_qms does not contain the int_main checkout/checkin or project-file-template services. Items 03/04/05/09/10/20 require formal implementation on this branch, not transplantation of conclusions from int_main. Product-code binding also differs; inspect existing product resolver before applying item 15.

## Merge latest int_main into int_qms
- User explicitly requested merging latest int_main. Preserve the entire 20-item goal and all partial remediation before integration.
- Read worktree-restrictions, branch-runtime-ports, powershell-memory and task-closeout rules before merge. No worktree creation, runtime restart or database mutation is involved.
- Pre-merge partial fixes: ordinary mutation guards; directory cycle checks; frontend response ownership guards. These are not the completed 20-item implementation. Save as a separate integration baseline commit under the authorized dirty-baseline rule.
- Verification before preservation: 127 approval/backend tests, 22 directory tests, 13 frontend behavior tests passed. Full type check exceeded default heap; 8GB rerun session was interrupted and completion output unavailable. Do not claim full build/type/E2E verification.
- Merge verification plan: inspect conflict resolution; ensure incoming latest main commit is ancestor; verify branch runtime guard 8061/48061; rerun changed behavior tests and affected backend compilation/tests; confirm checkout/checkin and project templates present; push and compare origin/int_qms.
- Integration baseline commit: f76b1337d (18 remediation source/test/task files). Remote main fetched as 70b53cf270fb989b48c3850e315e691011d69fe4.
- Resolved 25 conflicted paths. Adopted reviewed main v7 runtime profile/guards (QMS stays 8061/48061), full main BPM/ERP runtime implementations and extended logs page. Preserved QMS historical task records as labeled appendices and retained local runtime-source tracking gates. Combined browser async ownership guards with main loaded-state/error projections.
- Error-code collision: moved the local directory-cycle code from 1080000189 to unoccupied 1080000347, preserving main's print error code. All symbolic callers stay intact.
- Merge regression exposed an additional ordinary DCC action button group introduced by main; applied the removal guard to both groups and strengthened the existing test to cover all matching controls. Frontend 15 tests passed after resolution.
- Frozen-lockfile dependency install passed. DCC checkout/checkin/cancel-checkout VO and project-file-template service now exist in the merged tree.
- project-experience-consolidation performed by reviewing this integration's reusable branch-identification/remote-verification gate and adding it to existing powershell-memory.md plus experience-index.md. No new long-term document created; no separate executable of that name is available in this checkout.
- Post-merge backend compile succeeded across 24 reactor modules; 46 targeted tests passed (DCC directory/removal 26, ERP runtime 6, BPM bridge/guard 14).
- Additional imported-feature regression: DCC query including checkout/checkin 131 passed, project template 6 passed, form-center callback/idempotency passed. Workflow 135 tests found two obsolete expectations: removed transfer/sign requests expected a missing-post error. Updated them to the earlier unconditional action-not-allowed result; no-write assertions retained. Re-running that class.
- GREEN workflow regression: 135 tests passed. Combined selected backend classes total 349 passes. Frontend full pnpm build:local completed successfully with exit 0; 15 Node behavior/contracts passed. See verification-report.md for scope and limits.
- Merge milestone ready_for_closeout: source still equals remote main 70b53cf27; no unmerged paths; QMS port guard passed. Cleanup ownership review permits removal only of scripts/resolve-main-merge.mjs. Overall remediation remains in_progress.
- Merge commit: e2b7eac93. Ancestor verification `git merge-base --is-ancestor 70b53cf270fb989b48c3850e315e691011d69fe4 HEAD` passed. Scan of new local history found zero blobs over 100 MB. `git push origin int_qms` succeeded (2f7acecdb -> e2b7eac93); no force push or history rewrite.
- One-use conflict-resolution script removed; build output retained; no unrelated file cleanup. Integration milestone completed. Remaining 20-item implementation/verification continues against the integrated branch.

## Continue remediation after integration
- Current authoritative baseline e07813eea / int_qms is clean and synced. Updated user AGENTS supersedes earlier automatic commit/push requirements. No current-turn commit/push/E2E/database-write authority; proceed with code and local test verification only.
- BDD: DCC-MAIN-01/02/12 final approval prerequisites -> Given a valid stamped PDF ticket and active category default directory binding, with historical needTraining=true and no selected distribution scopes or manual directory; When final approval readiness is evaluated; Then ready, with the formal default directory as destination. Missing default binding still fails explicitly. No old distribution/training task is created.
- RED: DccControlledFileWorkflowServiceImplTest#ordinaryFinalApprovalReadiness* -> 2 failures: required manual directory/training/distribution prevented readiness; missing formal mapping reported manual selection instead of default mapping failure.
- Readiness now resolves only the active formal category-directory binding, with no unclassified-directory inference. Training/distribution request fields no longer gate or create final-approval artifacts. Published PDF ticket validation retained. Completion lifecycle and full regression still pending.
- GREEN readiness: 2 targeted tests passed after import correction. RED lifecycle: ordinaryApprovalImmediatelyActivatesDespiteHistoricalDistributionAndTrainingFlags failed because approval only marked READY_TO_PUBLISH without activation.
- Implementing ordinary-file approval finalization under master/file locks in one transaction, retaining event identity, CAS, stamped PDF evidence, signature binding and publication audit. Historical category training/distribution flags do not dispatch tasks for ordinary files. Nonordinary flows remain separate.
- GREEN lifecycle: ordinary approval activation plus both readiness tests passed. RED DCC-MAIN-12: final approval with another project's same-name Master failed the obsolete category/directory/name conflict check; removed that check from directory relocation. Existing Master ID and formal logical identity are unchanged, backed by 20260906_dcc_new_file_lifecycle_p1.sql replacing the old name index with tenant/project/taxonomy/normalized-number uniqueness. No migration executed.
- Frontend final-approval template RED/GREEN: removed manual-directory and distribution input controls, retained stamped PDF upload and explicit automatic-default-directory explanation. Readiness/submission payloads omit removed inputs. Removed unnecessary manual-directory load on dialog open.
- BDD/RED pending: historical needTraining=true must not redirect ordinary MATRIX_APPROVAL into applicant-training-record wait; updated existing observable transition test to new DOC_CONTROL_APPROVAL expectation.
- RED intermediate training transition reproduced PENDING_APPLICANT_TRAINING_RECORD instead of DOC_CONTROL_APPROVAL; ordinary workflow now bypasses that retired wait, while nonordinary workflow rules remain distinct.
- Regression transition work: updated obsolete ordinary distribution/training success tests to verify immediate ACTIVE, bound signature/PDF, default directory, no distribution/training writes, and no independent publication wait. Restored unrelated no-approval import directory assertions after detecting an overly broad assertion-text replacement.
- RED manual-distribution entry: fully prepared historical ordinary file still accepted manual release; now rejects before state changes. RED query projections: both ordinary publish/manual-release actions still exposed; ordinary projections now suppress those retired actions.
- Frontend detail eslint passed. Current regression is being rechecked against new lifecycle expectations; no full completion claim.
- Latest regression: workflow 138 PASS, query 131 PASS, external review 6 PASS. Finalization's only remaining test issue was stale Mockito stubs after manual release became an early rejection; removed unused fixture setup while retaining explicit zero-interaction checks. Finalization class rerun pending.
- Finalization 39 PASS; grouped backend tests 314 PASS, frontend 14 PASS and detail lint PASS. Subsequently RED proved standalone ordinary publish precheck still allowed a new action; added explicit denial before policy/task creation. Its adjacent legacy tests require alignment.
- BDD DCC-MAIN-10: Given a configured template item with invalid/non-leaf classification or missing active category; When an administrator reads the template; Then preserve item identity and show explicit invalid reason for repair. When selecting it for upload; Then reject. Added explicit validation fields to response contract before implementing read/validation behavior.
- User updated goal: after fixes, continue static code analysis until all 20 logical issues are corrected. Requirement-by-requirement call-chain audit is mandatory; scope remains all 20, no E2E/commit/push authorization inferred.
- RED DCC-MAIN-10 confirmed: administrative read threw instead of returning invalid item; upload accepted configured invalid classification. Implemented shared strict classification/category validation plus explicit invalid-row response for administrative repair. Frontend RED: editor dropped repair reason and upload candidates included invalid item.
- GREEN: template backend 7 PASS, template frontend 2 PASS. Standalone-publication/finalization tests 40 PASS after retaining obsolete fields only as test setup and asserting early new-action denial. Administrative template invalidity is explicit via valid=false/code/message; uploads and saves share strict validation.
- BDD DCC-MAIN-09 -> Given selected replacement source upload fails or returns after dialog context changes; When checkin is attempted or upload completes; Then failed selection cannot silently become metadata-only, and old response cannot bind to a new session/version.
- RED/GREEN DCC-MAIN-09: two frontend behavior tests failed, then passed with source-upload state and request/session/target guards. Frontend changed-file eslint passed.
- Static audit DCC-MAIN-20 found per-ticket cleanup returns 0 for bound/ineligible tickets or lost claims, while controller unconditionally reports CLEANED. BDD: bound historical ticket deletion or lost cleanup claim must fail explicitly without deleting physical history or returning false cleanup success. Added backend tests before fixing.
- RED cleanup backend: both bound-ticket and lost-claim cases accepted false zero-count cleanup. GREEN: all 26 DccUploadTicketServiceTest cases passed after refusing ineligible tickets and accepting zero only for observed CLEANED state.
- RED/GREEN cleanup frontend: deletion previously only cleared form, and failure could not block widget removal. Two new tests passed after formal per-ticket cleanup before removal, recoverable ticket retention, cancel/close cleanup guards, and submit/upload locks. Source/PDF cleanup never uses a bound historical-file delete endpoint. Combined checkin tests 4 PASS.
- BDD DCC-MAIN-06: confirmed ordinary DCC candidate list contains a disabled user; both runtime task creation and preview must reject with an explicit business error and preserve the full list, never removing a required candidate or substituting an empty-candidate strategy. Other BPM process semantics remain unchanged.
- RED: BpmDccRequiredCandidatesTest 2 failures; both task generation and preview silently removed disabled user and continued. Added DCC-specific complete-list validation before disabled-user/start-user filtering and empty-candidate substitution; other process behavior retained.
- GREEN candidate generation: DCC tests and existing BpmTaskCandidateInvokerTest passed. Static finalization audit found no frozen roster check: RED ordinaryApprovalCannotActivateWithoutTheFrozenSignoffRoster accepted activation. Added finalization gate matching all four frozen stages to same-file/same-version valid APPROVE signatures; ALL requires all distinct roster actors, ANY at least one; malformed/missing/duplicate stages fail. Added targeted policy tests for missing mandatory signer, duplicate signatures, wrong version/user/action, invalid evidence and malformed roster.
- GREEN frozen roster: finalization 41 and frozen-signature policy 4 tests passed. Candidate invoker regression also passed. Code audit must still consider signature cryptographic verification and all legacy finalization callers before closing item 06.
- BDD DCC-MAIN-14: Given more than 200 related files; When searching a keyword or moving to the next page; Then server receives keyword/page, selected version labels remain available, and old same-project requests cannot replace newer results. Permission/content boundary audit remains a separate required part.
- RED/GREEN DCC-MAIN-14 pagination: two frontend tests failed with pageNo=1 and omitted keyword; now both pass with server keyword, 50-row pagination, selected-version label retention and per-request sequencing. Upload-page eslint passed.
- Static DCC-MAIN-14/16 findings still open: project associated-file list maps all project records after project access without per-file name visibility filtering; validateAndBindRelatedFiles has no actor parameter/explicit relation permission check. These must be resolved before closing item 14, even though pagination tests pass.
- Static DCC-MAIN-16 root cause: directory access service merges canQuery OR canPreview for both name and content; administration and frontend also copy one grant to both. BDD: query-only department grant remains query-only and never grants content preview; content grant may imply name visibility. Started with existing directory access test changed to the required denial.
- RED name/content boundaries: runtime query-only grant erroneously authorized PREVIEW; frontend normalization changed false content permission to true; H2 save/read test persisted the same escalation. Corrected all three to one-way implication (content includes name, name never includes content); frontend provides distinct switches. Broader file-level access audit remains pending.
- GREEN directory authorization: runtime/admin regression passed, including H2 persistence. RED file name-query test could not find a name-authorized file; added directory QUERY/PREVIEW name visibility while keeping detail/binary access separate and returning minimal name-only projections without hashes, content or action capabilities. Existing query tests exposed repeated permission lookups; reused request permission cache in detail projection checks.
- RED project associated-list test returned unauthorized file name; filter now delegates per-file name visibility to the same query policy before pagination. Related writes still require independent actor/authorization update.
- BDD DCC-MAIN-14 write authorization: Given an actor has only name access, or a submitted target is outside name visibility; When binding uploaded-file relations; Then project EDIT/OWNER and each target's name policy are checked independently before any relation insert. All upload entry points must use one workflow guard. Given a missing or different-project owner; Then the relation persistence service rejects instead of skipping owner validation. No content permission is implicitly granted.
- GREEN DCC-MAIN-14: 152 workflow/relation tests passed. Missing-owner test was RED (orphan insert accepted); authorization helper tests were RED (central guard absent). Three upload callers now share project EDIT/OWNER and all-target name authorization before relation binding; persistence checks source/target project and Master identities. This guard is in workflow orchestration to reuse the query policy without creating a query/related-service dependency cycle.
- BDD DCC-MAIN-15: Given project code differs from the bound product DCC code; When upload resolves project metadata and later submits; Then preview and persistence use the identical server resolver. Invalid bound product fails explicitly. Late resolution for an old project cannot change current form, and unresolved/failed metadata prevents submit. Policy for projects without a product binding is awaiting optional user clarification; associated-product correctness proceeds independently.
- RED/GREEN DCC-MAIN-15 associated product: backend preview tests initially failed because the shared endpoint was absent; frontend returned PROJECT-CODE instead of product DCC code and did not guard old results/errors. Shared server resolution and context-owned frontend loading now pass; 154 workflow/relation tests and 3 frontend behaviors passed. Additional route contract caught a singular/plural URL mismatch; corrected to the actual /dcc/controlled-files mapping. ESLint and related-upload contract passed. Unbound-project policy remains separately pending.
- BDD DCC-MAIN-19: Given a current final approver with content access but no category UPLOAD; When uploading a real PDF for the exact task/version; Then task authorization permits an APPROVAL_PDF ticket. Wrong actor, stage, file/category or session scope fails before storage; final approval rejects ordinary DRAWING_PDF tickets and tickets scoped to another task/version. Upload size policy remains mandatory. Session identity carries the exact file/task scope and is checked again at final binding; no database mutation/migration is required.
- GREEN DCC-MAIN-19: workflow task guard, approval-specific purpose, final-binding session guard, and frontend exact-task/session ownership implemented. 146 workflow tests, 33 upload tests, 3 HTTP contracts and 4 frozen-signature policy tests passed; the command initially used the wrong finalization class name, so finalization rerun remains required. Frontend two behavioral tests and changed-file ESLint passed. Approval actors can access status/cleanup, whose service still enforces uploader/tenant/unbound ownership; ordinary upload menu authorization is preserved. Session prefix length-frames task IDs to prevent delimiter ambiguity.
- Static DCC-MAIN-20 follow-up: session cleanup still reports success when a candidate's cleanup claim is lost; unlike per-ticket cleanup it does not reread terminal state. BDD: Given a session candidate loses its cleanup claim and remains uncleaned; When the user removes/replaces the upload; Then cleanup throws instead of returning CLEANED and discarding recoverable UI state.
- GREEN DCC-MAIN-20 follow-up: session lost-claim test failed because no exception was thrown, then passed with terminal-state reread; cleanup 27, finalization 41 and frozen policy 4 PASS. Combined frontend remediation regression 17 PASS.
- BDD DCC-MAIN-16 version projection: Given a permitted parent file and a different version visible only by name; When version history is built; Then that version exposes index identity and explicit denied actions but no source hashes, change detail or content. Given a version with content access; Then its own action projection is returned, without inheriting the parent version's capabilities.
- RED/GREEN DCC-MAIN-16 partial: historyNameOnlyVersion exposed confidential-hash; historyContentVersion had no actionProjection field. Added per-version content check before content fields and each authorized version's action projection; full DccControlledFileQueryServiceTest passed (134 tests). Remaining: name-only top-level responses currently omit version history, directory PREVIEW is not yet a formal direct content grant at file level, history visibility still partly depends on parent authorization, pending preview action and retired training/manual-release capabilities need final unified-policy audit. Do not close item 16 on this partial pass.
- BDD DCC-MAIN-16 continuation: Given an explicit directory content grant within assigned scope; When viewing active or pending versions; Then detail/preview honor that grant while download remains separate. Given only name rights for a two-version chain; Then browser history includes permitted index identities with denied content actions. Given parent requester access but no target-version name/content grant; Then target identity is excluded. Previous continuation classified as progress: it changed code and produced RED/GREEN evidence; no live build remains at start of this continuation.
- GREEN DCC-MAIN-16 continuation: three RED defects reproduced; independent directory content grant, name-only history and per-version visibility now pass. Existing denial fixtures explicitly removed their default content grant; pending history fixture now contains its mandatory process identity. Pending preview projection separately failed despite authorized binary read, then passed after propagating the authoritative read capability. Query 138 and project 37 tests PASS.
- BDD DCC-MAIN-05 continuation: Given lock-before-read matters under REPEATABLE READ; When checkin or working submission waits for the Master lock after an earlier snapshot; Then it uses the locked current file status and current version chain, rejecting a newly pending version before source-copy/ticket/signature writes. Checkin/cancel follow Master -> file -> checkout lock order, matching checkout/submission; same-key replay rereads committed current facts.
- GREEN DCC-MAIN-05 current reads: working submit RED lacked selectByIdAndTenantForUpdate; checkin RED initially exposed nested Mockito stubbing confusion, corrected with doReturn plus explicit lock-read verification, then reproduced old-state use. Checkin/cancel now lock Master then current file before checkout, replay reads current checkout/iteration, and next-version/submit chain reads use FOR UPDATE. Query + workflow 286 PASS. Submission's conditional write and current idempotency read still need closure, as do old cross-entry mutation paths.
- BDD DCC-MAIN-04: Given metadata-only CAD checkin created a distinct PDF copy; When the same completed checkin is retried; Then compare the copy's actual verified content to the predecessor PDF and return the existing iteration with zero insert/bind/copy; changed or missing companion must fail. Existing comparison incorrectly requires identical PDF IDs, contradicting version isolation.
- GREEN DCC-MAIN-04: same-content isolated PDF replay initially failed with NOT_CHECKED_OUT, then passed with verified content comparison; changed-content replay remains denied and neither path inserts/binds/copies. Query 141 PASS.
- DCC-MAIN-05 conditional write: added claimWorkingIterationSubmission with tenant/id/requester/WORKING/unchecked-out/deleted predicates, used it in working submission, and switched submission replay lookup to current FOR UPDATE. DccWorkingSubmissionConditionTest first RED because conditional mapper was absent; GREEN uses the real MyBatis mapper against isolated H2 and proves only the valid row is claimed once while checked-out, pending, other-tenant, other-requester and deleted rows stay unchanged. Extended regression concurrently races actual checkout and submit SQL using separate sessions and proves exactly one wins. Workflow 147 PASS; condition + lifecycle contract group 5 PASS. This verifies H2 SQL behavior, not a live MySQL/E2E run.
- Remaining next implementation focus: CAD checkin major/minor choice and removal of standalone/upload revision paths (03/11). Current checkin only resolveNextIteration; upload still auto-switches REVISION and initialForNewFile ignores the supplied version. These paths remain open and must be implemented together before claiming the 20-item goal complete.
- BDD DCC-MAIN-03/11: Given an existing checked-out CAD version and fresh source/companion PDF in one session; When choosing MINOR or MAJOR at checkin; Then MINOR increments the iteration, MAJOR requires current project OWNER and advances beyond all existing revisions while preserving the current ACTIVE formal baseline, and both create a WORKING version for reapproval. The default selection is MINOR; unknown types fail. Replay identity includes the version change type, so replaying a completed minor checkin as major cannot reuse or create a version. Close old upload/standalone major entry points after this replacement is verified. Previous continuation was progress, with authoritative SQL/concurrency and replay fixes; no build process remains active at start.
- GREEN DCC-MAIN-03 major/minor core: major owner/baseline/fresh CAD+PDF and minor regression are included in full query 143 PASS. Browser sends explicit MINOR/MAJOR and removed the standalone row button; 3 frontend behavior tests and changed-file ESLint PASS. Generic and impact create-revision backend capabilities and upload REVISION server/UI paths remain to remove; therefore 03/11 are not closed.
- BDD DCC-MAIN-11 initial version: Given a new logical identity; When the user enters A/1, B/1 or another valid Windchill revision at iteration 1; Then persist that exact normalized initial version. Blank public input, numeric legacy versions, or iteration >1 fail. Existing identity still fails instead of becoming revision upload. Current initialForNewFile ignores every client value and is RED for this requirement.
- BDD DCC-MAIN-11/20 rejected replacement: Given REJECTED or historical APPLICANT_REWORK content, or a requested MAJOR checkin; When checkin is attempted without a newly AVAILABLE source ticket; Then reject before source/PDF/version writes. With a new CAD source, the same session must also resolve a new DRAWING_PDF ticket. A bound ticket cannot resolve as AVAILABLE, and the successor records predecessor/version identity before the old process is closed and resubmitted.
- BDD DCC-MAIN-15 unbound product: Given a project has no productMasterId; When resolving upload metadata; Then return UNBOUND with null product identity rather than copying projectCode/projectName into product fields. Product-bound DHF/DMR categories fail before insert; non-product categories may persist null product fields. Disabled/missing bound MDM product remains a hard failure.

- Latest static audit: full changed-path checks exposed stale assertions in old contract scripts after the intended policy changes. Updated contracts for approval quick-review reason validation, template repair visibility, training-session progress reload, drawing preview resolution, major-action projection, and related uploads. The remaining invalid-saved-training-recipient script still encodes the retired distribution/training side-effect path and is recorded as an outdated verifier rather than production evidence.
- GREEN: DccTrainingTaskServiceTest 6; workflow 134; query 146; finalization 42; electronic-signature 39; frozen-roster 4. Frontend relaxed type check completed, and upload/checkin behavior plus the changed static contracts passed. Overall task remains in_progress pending full cross-entry audit and real-page evidence.

- Cross-entry audit found ordinary DCC controller/API remnants for `/return-task`, `/transfer-task`, and `/sign-task` even though their service methods rejected calls. Removed those ordinary controller methods and corresponding frontend wrappers/call sites; external-review endpoints remain separate. Controller/action regression 11 PASS, frontend type/lint PASS.
- Full DCC JavaScript static contract sweep: 21/21 PASS after aligning stale verifiers to the current method names and ordinary-approval policy. The invalid-saved-recipient verifier now checks that nonordinary validation remains strict while ordinary historical training configuration bypasses training side effects.
- A full reactor test command reached an unrelated pre-existing system-module fixture error (`C:\ProjectPackage\erp-invoice-voucher-print-assistant\server.js` missing) before DCC; this is outside the task scope. DCC targeted suites continue to be run with the module and required ancestors.

## Static closure continuation — 2026-09-15

- Previous continuation classified as progress: it changed source and produced targeted GREEN evidence for DCC-MAIN-03/11/15/19 plus frontend/static contracts. No live process was inherited at the start of this continuation.
- Rechecked branch/status: current branch `int_qms`; worktree contains uncommitted task implementation, tests and task records plus deleted obsolete revision-entry classes.
- Full DCC JavaScript static sweep passed: 21 backend static contracts and 35 frontend DCC behavior/contracts. This includes ordinary approval mutation removal, project access closure, checkout/checkin permissions, structured replay payload, major/minor checkin UI, related-file pagination, template invalid-item repair, approval PDF scoping and version-governance contracts.
- First expanded backend regression found two version-format mismatches in `DccControlledFileWorkflowServiceImplTest`: one stale assertion expected `V1.0` for an `A/1` fixture, and withdrawn resubmission rejected historical stored `V1.0` before creating `A/2`.
- Implemented bounded stored-version compatibility: `DccWindchillVersionNumber.parseStoredInitial("V1.0")` maps only the historical initial record to `A/1` for resubmission. Public new-upload validation still rejects `V1.0` and requires `A/1`, `B/1`, etc. Added regression coverage in `DccNewFileInitialVersionTest`.
- GREEN after fix: `DccControlledFileWorkflowServiceImplTest` 134 PASS and `DccNewFileInitialVersionTest` 6 PASS.
- Expanded backend GREEN: Maven reactor `-pl yudao-module-dcc -am` with 16 targeted DCC test classes passed 487 tests, 0 failures, 0 errors, 0 skipped.
- Frontend GREEN: `pnpm ts:check` PASS; `pnpm build:local` PASS; final repeated DCC static/behavior sweep again passed 21 backend + 35 frontend tests.
- Static source sweep reviewed residual `return-task`/`transfer-task`/`sign-task` and `V1.0` occurrences. Ordinary DCC controller/API/page entry points are removed; remaining return/transfer/sign endpoints are external-review specific. Remaining `V1.0` values are external-review or NAS legacy paths, except submit failure wording, which was fixed to show `A/1` rules for ordinary controlled uploads and retain numeric version rules for external review.
- `git diff --check` PASS with only CRLF conversion warnings reported by Git. No database write, runtime restart, real E2E, Git commit or push was performed in this continuation.
- Completion audit: current objective's code/static portion is satisfied by current source and verification evidence. Repository task closeout is not complete because `docs/task-closeout-rules.md` requires commit/push before `completed`, and current `AGENTS.md` requires current-turn authorization for Git commit/push and E2E. No further code defect is currently identified under the 20-item static scope.
