# 来料检报告标题显示修复

## Task Goal

修复附件节点页面顶部红框区域显示为 `-` 的问题：当前节点为来料检报告时，应显示 `来料检报告`。

## Milestones

- [x] 建立任务记录并确认既有脏改动基线已隔离。
- [ ] 定位标题字段来源和渲染逻辑。
- [ ] 增加回归测试，先复现 `-` 的错误显示。
- [ ] 实施最小修复并通过目标验证。
- [ ] 完成收尾记录、清理和最终验证。

## Expected Verification

- 目标回归测试能覆盖来料检报告节点标题显示。
- 前端相关静态测试或类型检查通过。
- 任务文档记录 RED/GREEN 证据。

## Current Status

in_progress

## Experience Gate

- `docs\experience-index.md` 未在当前仓库发现；本任务为小范围前端显示修复，记录缺失事实后继续。

## 设计约束检查

- `是否引入 fallback/降级/吞异常`：否。
- `是否从根因和长期维护角度解决`：是，目标为修正标题字段来源或展示契约。
- `是否存在临时补丁或绕过`：否。
