# 测试服 MES 主数据对齐本机

## Task Goal

将测试服务器 `172.30.30.58` 的芋道源码租户 `tenant_id=1` 中工作站、工序、工艺路线、路线工序绑定的当前有效数据修复为与本机 `E:\IntRuoyi` 对应本地 MySQL 数据一致。

## Milestones

- [x] 建立任务目录和数据修复门禁记录
- [x] 生成修复前差异证据并作为 RED
- [x] 备份测试服受影响表数据
- [x] 将测试服工作站、工艺路线、路线工序有效数据对齐本机
- [x] 复跑差异校验并作为 GREEN
- [x] 记录验证报告和剩余风险

## Expected Verification

- 本机与测试服 `mes_pro_process` 有效工序按编码无缺失、无多余、无差异。
- 测试服 `mes_md_workstation` 有效工作站按编码、名称、班时、状态与本机一致。
- 测试服 `mes_pro_route` 有效工艺路线按编码、名称、状态与本机一致。
- 测试服 `mes_pro_route_process` 有效路线工序按路线编码、排序、工序编码、工作站编码、班时与本机一致。
- 测试服保留本次修复前备份表，可按备份表恢复。

## Current Status

ready_for_closeout

## 设计约束检查

- `是否引入 fallback/降级/吞异常`：否。
- `是否从根因和长期维护角度解决`：是；按真实源库数据对齐测试服数据，不通过前端隐藏、默认值或 API-only 假成功掩盖差异。
- `是否存在临时补丁或绕过`：否。

## Applicable Gates

- 服务器访问门禁：目标环境为测试服务器 `172.30.30.58`，仅执行授权的数据读取与数据修复，不发布、不重启。
- 数据库门禁：写入前核对真实库 schema、生成修复前差异、备份受影响表、写入后复核差异。
- PowerShell/编码门禁：所有中文文档与 SQL/stdin 使用 UTF-8；PowerShell 不使用 `&&`。
- 数据修复临时表排序规则门禁：临时表与字符串比较使用目标库 `utf8mb4_0900_ai_ci`，避免排序规则混用。
- MES 三页签同步门禁：本任务只对齐用户点名的工作站、工序、工艺路线/路线工序范围；不扩展同步排产工单、物料、批记录报表等其它页签数据。

## Verification Summary

- RED：修复前测试服缺少 118 个有效工作站、缺少 1 条有效工艺路线，路线工序绑定存在 40 条本机有而测试服缺失、26 条测试服多余差异。
- Backup：测试服已保留 `zz_bak_ws_20260802_1530`、`zz_bak_proc_20260802_1530`、`zz_bak_route_20260802_1530`、`zz_bak_rp_20260802_1530`。
- GREEN：有效工作站 144/144、有效工序 65/65、有效工艺路线 4/4、有效路线工序绑定 77/77，本机与测试服缺失/多余均为 0。
- Scope note：本机仍有 14 条有效路线工序挂在已删除路线下；页面可见有效路线范围未同步这些孤儿历史行。
