# Verification Report

## Result

PASS for requested data fix. 测试服芋道源码租户的有效工作站、工序、工艺路线、有效路线工序绑定已与本机一致。

## RED Evidence

- 修复前测试服有效工作站 `26`，本机 `144`，测试服缺 `118`。
- 修复前测试服有效工艺路线 `3`，本机 `4`，测试服缺 `TN-ROUTE-STATUS-001`。
- 修复前 `ROUTE-XLSX-00002` 在测试服绑定了 `WS-XLSX-00002-01 ~ 26`，本机对应路线工序未绑定工作站。

## Data Change Evidence

- 测试服备份表保留：
- `zz_bak_ws_20260802_1530`：124 行。
- `zz_bak_proc_20260802_1530`：65 行。
- `zz_bak_route_20260802_1530`：34 行。
- `zz_bak_rp_20260802_1530`：190 行。
- staging 表 `zz_stage_*_20260802_1530` 已清理。

## GREEN Evidence

- 有效工作站：本机 `144` / 测试服 `144`，缺失 `0`，多余 `0`。
- 有效工序：本机 `65` / 测试服 `65`，缺失 `0`，多余 `0`。
- 有效工艺路线：本机 `4` / 测试服 `4`，缺失 `0`，多余 `0`。
- 有效路线工序绑定：本机 `77` / 测试服 `77`，缺失 `0`，多余 `0`。

## Scope Notes

- 本次对齐的是页面可见的有效工艺路线范围。
- 本机仍有 `14` 条 active 路线工序挂在已删除路线下；这类孤儿历史行不属于当前页面有效路线范围，未同步到测试服。
- 同步后测试服 `ROUTE-XLSX-00002` 与本机一致，26 道路线工序均未绑定工作站。
