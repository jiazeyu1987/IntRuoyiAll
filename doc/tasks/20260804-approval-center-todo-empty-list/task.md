# 审批中心待办列表空态修复

## Task Goal

修复审批中心待办页签中左侧徽标显示存在待办数量，但列表区域显示为空或数量为 0 的问题，确保待办列表首屏与待办徽标使用同一正式聚合口径。

## Milestones

- [x] 创建任务记录并记录既有脏工作区基线提交。
- [ ] 复现并用回归测试锁定“总数大于 0 时列表不得为空”的缺陷。
- [ ] 从根因修复审批中心待办分页/筛选/聚合链路。
- [ ] 运行定向 RED/GREEN 和相关回归验证。
- [ ] 完成验证报告、经验沉淀、提交和推送。

## Expected Verification

- 后端定向 JUnit 覆盖全局待办聚合在 provider 总数大于当前窗口时仍能返回首屏/目标页数据。
- 前端静态合同覆盖审批中心待办列表不得在无筛选时用隐藏查询条件或越界页显示空列表。
- 相关审批中心静态合同和必要类型检查通过，若运行态/E2E 前置缺失则记录明确 blocker。

## Current Status

in_progress

已完成既有脏工作区基线提交：`6f9ed0e83 chore: baseline existing workspace changes`。本任务后续改动将单独提交。

## 设计约束检查

- `是否引入 fallback/降级/吞异常`：否。
- `是否从根因和长期维护角度解决`：是，目标是修复正式审批中心聚合/分页或路由筛选状态，不用前端空态兜底掩盖。
- `是否存在临时补丁或绕过`：否。

## Experience Gate

- 适用 `docs/frontend-development.md#前端静态契约隔离门禁`：若全量前端检查有历史失败，必须用任务专用最小静态契约证明 RED/GREEN，并记录无关 blocker。
- 适用 `docs/frontend-development.md#前端服务端分页排序链路门禁` 的分页原则：服务端分页列表不得只修当前页视觉状态，必须保证分页请求和后端聚合结果一致。
- 适用 `docs/frontend-development.md#前端列表跨账号默认列布局统一门禁` 的列表状态边界：显示字段/表格 key 相关改动不得掩盖真实列表数据。
- 适用 `docs/powershell-memory.md#共享分支并发基线提交门禁`：当前分支已有基线提交 `6f9ed0e83`，本任务只提交后续任务自有文件。
