# 损耗原因显示损耗描述

## 任务目标

- 修复工序配置表格的“损耗原因”列，使其显示正式损耗描述，不再显示损耗编码。
- 保持新增、修改、删除损耗及其它工序配置能力不变。

## 里程碑

- [x] M1：定位页面、接口字段与现有测试，确认根因和正式描述来源。
- [x] M2：补充 BDD 场景与失败回归测试（RED）。
- [x] M3：实施最小修复并通过目标测试（GREEN）。
- [x] M4：完成相关回归、验证报告和任务收尾。

## 预期验证

- 独立聚焦静态回归测试证明损耗原因标签只渲染正式描述字段，不拼接或回退显示编码。
- 相关相邻静态测试通过。
- `pnpm ts:check` 通过。
- `git diff --check` 通过。

## 设计约束检查

- 是否引入 fallback/降级/吞异常：否。
- 是否从根因和长期维护角度解决：是；以正式损耗描述字段作为唯一展示来源，并用回归合同锁定。
- 是否存在临时补丁或绕过：否。

## 经验门禁

- 已读取 `docs/experience-index.md`；本任务适用“前端静态契约隔离门禁”：使用工序配置现有聚焦合同锁定当前显示行为，不扩大修改到其它损耗维护、选择或删除场景。
- 已核对生产组长工序配置相关经验：正式统一表响应已经提供 `reasonName` 与 `reasonCode`，本任务只调整用户可见列，保留编码作为内部身份和维护操作数据。
- 收尾复盘结论：本次没有需要新增或合并的长期经验；“聚焦静态合同隔离”和“Windows Playwright CLI 异常后改用项目脚本”均已由现有 `docs/frontend-development.md` 与 `docs/e2e-rules.md` 覆盖，避免把一次性业务显示变更写入长期经验。

## Current Status

completed：实现、严格 TDD、真实页面验证、并发改动后聚焦复验和两次 cleanup 均已完成；最终验证通过。
