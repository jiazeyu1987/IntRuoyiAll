# 一线生产选择陈丽提交系统异常修复

## 任务目标

复现并修复本机 `芋道源码/admin` 登录“一线生产”、选择正式员工陈丽、输入该员工电子签名密码后点击提交出现“系统异常”的问题；保留选择员工签名、电子签名授权、密码校验和单事务提交的正式门禁，不增加 fallback 或默认成功。

## 里程碑

- [x] 核对本机运行态、目标账号/员工、签名授权、人员档案和提交前置。
- [x] 通过用户真实请求和后端日志复现，冻结请求、响应与根因证据。
- [x] 新增失败回归测试并记录 RED。
- [x] 实施最小根因修复并记录 GREEN。
- [x] 运行相邻回归与真实页面复验。
- [x] 完成缺陷证据校验、经验沉淀和任务清理。

## 预期验证

- 本机前端 `8081`、后端 `48081` 与依赖可用，登录身份为 `芋道源码/admin`。
- 一线生产选择陈丽后，提交请求的实际员工和签名员工均为陈丽对应的正式系统用户。
- 正确的陈丽签名密码通过校验，服务端生成签名并完成正式提交；错误密码仍返回明确业务错误且不写入提交链路。
- 页面不再显示“系统异常”，接口返回业务码 `0`，报工/签名/工序池写入保持单事务。
- 不记录明文密码、token 或其它凭据。

## 设计约束检查

- `是否引入 fallback/降级/吞异常`：否。
- `是否从根因和长期维护角度解决`：是；先复现并读取后端真实异常，再以回归测试锁定正式签名链路。
- `是否存在临时补丁或绕过`：否。

## 经验门禁

- 适用 `docs/backend-development.md#一线生产正式提交必须单事务落链并按唯一组长归属可见`：签名主体必须是选择员工；正式员工走系统用户 DCC 授权与本人密码，临时工走人员档案签名密码。
- 适用 `docs/e2e-rules.md`：真实页面复现使用 Playwright；写请求发出后需按响应不确定门禁先只读核对是否已落库，再决定是否重试。
- 适用 `docs/login-access.md`：仅使用本机 `芋道源码/admin`，凭据不写入任务证据。

## Current Status

completed - 用户已明确授权并完成唯一一次真实页面正式提交：HTTP `200`、业务码 `0`、报工 ID `877`、工序池事件 ID `192`。数据库只读核对证明报工、签名、工序池和数量片段单事务落链且选择员工为陈丽；缺陷证据校验、经验沉淀及任务清理预览/应用均已通过。
