# Task: 配置本地运行时环境

## Task Goal

使用用户提供的本地 DCC 下载加密配置，补齐当前用户环境变量并验证 int_main 后端启动前置条件。

## BDD

- Given 用户提供本地开发专用 DCC 配置，When 写入当前用户环境，Then 启动脚本可读取四项配置且不把密钥写入仓库。

## 设计约束检查

- 密钥仅写入当前用户环境，不写入 Git、任务日志或命令输出。
- 不修改源码安全门禁，不生成替代密钥。

## Current Status

completed
