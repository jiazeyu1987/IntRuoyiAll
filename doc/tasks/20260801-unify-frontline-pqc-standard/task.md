# 统一一线 PQC 口径

## Task Goal

根据用户裁定，将生产全流程角色系统操作文档中的 PQC 未来主流程统一为“新的一线 PQC”口径，明确旧 IPQC/待检任务只作为历史或辅助质量单据链路，不作为未来 PQC 一天工作流的主入口。

## Milestones

- [x] 创建任务目录和任务记录。
- [x] 核对产品需求文档技能、任务收尾规则、PowerShell UTF-8 规则和经验索引。
- [x] 更新 `docs/product/production-role-system-operations.md` 的 PQC 角色、流程、规则、状态和验收口径。
- [x] 执行 UTF-8 读取、关键口径搜索和空白检查。

## Expected Verification

- 使用 UTF-8 读取更新后的产品文档和任务记录。
- 搜索确认文档包含“一线 PQC”“工序池 PQC 事件”“旧 IPQC 不作为未来主入口”等关键口径。
- `git diff --check` 不报告空白错误。

## Current Status

completed

## 设计约束检查

- `是否引入 fallback/降级/吞异常`：否。
- `是否从根因和长期维护角度解决`：是，明确未来主流程只采用新的一线 PQC，不用旧 IPQC 单据流兼容替代。
- `是否存在临时补丁或绕过`：否。

## 经验门禁

- `docs/experience-index.md` 命中 PQC、一线报工、工序池、过程检验记录、批记录表单/表单槽位边界相关经验。
- `AGENTS.md#工艺路线三类配置术语契约`：过程检验记录不得误入批记录表单；批记录表单、表单槽位、工序开始不得混用。
- 本任务只修改产品文档和任务记录，不修改生产代码、不运行构建、不触碰数据库、远端服务或本地运行态。
