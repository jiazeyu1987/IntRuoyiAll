# 20260729 production line recording design

## Task Goal

将本次线程中关于“生产一线报工内嵌批记录相关填写、工序池、FIFO 生产工单消耗、审核副本、原始记录修改日志、固定 UI 模板”的业务讨论整理并保存到项目文档中，仅记录本次线程对话，不汇总其它任务或历史对话。

## Milestones

- [x] 建立任务目录和任务记录
- [x] 读取项目任务收尾、PowerShell UTF-8、项目构想文档技能要求
- [x] 明确记录范围为本次线程对话
- [x] 写入业务简述文档
- [x] 写入证据清单与本线程原始对话摘录
- [x] 执行文档结构与 UTF-8 读取验证
- [x] 更新验证报告
- [x] cleanup preview/apply
- [x] 补充现有系统结合方案
- [x] 补充报工与记录本组合入口口径
- [x] 根据压力泵批记录和损耗单抽取一线填写字段
- [x] 补充多人分片填写与数量完成判断口径
- [x] 补充工序池核心概念
- [x] 补充工序池记录 PQC 过程检验记录口径
- [x] 补充工序池统一提交对象命名
- [x] 补充有限设备共享登录账号与账号内切换用户口径
- [x] 核对现有报工工序余量池与新工序池关系
- [x] 固化新增工序池、电子签名、审核副本和 FIFO 后修改限制
- [x] 补充功能实现里程碑划分
- [x] 保存启动子 agent 前的脏工作区基线提交
- [x] 启动 6 个子 agent 分别产出功能点 1、2、3、4、7、8 的 BDD/TDD 草案
- [x] 主线程 review 子 agent 草案并按 21 条需求门禁整合
- [x] 写入专题 BDD/TDD/E2E/测试数据/审查报告文档
- [x] 执行专题文档结构与 UTF-8 验证
- [x] Git closeout

## Expected Verification

- `docs/inception/project-brief.md` 包含项目构想文档要求的固定章节。
- `docs/inception/evidence-inventory.md` 包含证据清单固定章节，并保存本次线程原始对话摘录。
- 文档明确只记录本次线程，不包含其它历史任务或项目全量对话。
- 文档明确一线入口既是报工入口，也是记录本入口；点击确定提交时同时完成报工和记录本写入。
- 文档列出从 `批记录压力泵.doc` 和 `损耗单.doc` 抽取的一线字段归类。
- 文档明确一线字段可由一人或多人分片填写，工序完成由累计完成数量判断。
- 文档明确工序池是数量归集、完工判断和 FIFO 满足的核心层。
- 文档明确工序池需要记录 PQC 过程检验记录。
- 文档明确统一对象命名为“工序池提交事件”，并记录唯一电子签名、唯一提交时间和时间甘特图用途。
- 文档明确有限设备上使用共享登录账号，账号绑定一个或多个工艺路线，员工在账号内切换到本人，UI 随实际员工切换，电子签名绑定实际员工。
- 文档明确现有系统已有 `mes_pro_feedback_surplus_pool` 报工工序余量池，但它不是完整工序池，只能作为复用基础或下游余量表。
- 文档明确工序池必须新增；员工切换到自己时无需额外身份验证，电子签名代表身份；审核副本第一版只处理上下限；原始记录修改必须重新电子签名；已 FIFO 分配给生产工单的原始记录不能修改。
- 文档明确实现建议分为 12 个里程碑，并可合并为 4 个大阶段推进。
- 文档明确 6 个可先做功能点的 BDD/TDD/E2E/测试数据计划，并有 21 条需求门禁 review 结论。
- 使用 UTF-8 方式读取新增文档，确认中文内容可读。
- 运行 `project-inception-docs` 技能提供的结构校验脚本。

## Current Status

completed

## 设计约束检查

- `是否引入 fallback/降级/吞异常`：否。
- `是否从根因和长期维护角度解决`：是，本任务仅做需求事实沉淀，不用其它历史信息替代本次线程证据。
- `是否存在临时补丁或绕过`：否。

## Experience Gate

- 已读取 `docs/task-closeout-rules.md`：本任务修改文件前建立 `doc/tasks/<task-id>/`，收尾前需要记录验证结果。
- 已读取 `docs/powershell-encoding.md`：中文 Markdown 写入使用 `apply_patch`，读取验证使用 UTF-8。
- 已读取 `docs/experience-index.md`：命中“批记录/批记录表单/formBindings/工序开始三类配置不得混用”门禁，本次文档必须保持术语边界。
- 已读取 `project-inception-docs` 技能：本次输出项目构想文档和证据清单，不编造未确认事实。
- 已执行 `project-experience-consolidation` 技能评估：本次新增的是当前线程业务需求证据，不是可复用工程事故或通用流程门禁；未创建新的长期经验文档。

## Closeout Notes

- 文档实现与结构验证已完成。
- 已补充“现有系统结合方案”，记录复用 eDHR 记录本、报工、资源池、生产工单、字段审计和工序任务候选的方向。
- 已按用户纠正更新：不考虑排产系统，订单只考虑生产工单。
- 已按用户最新补充更新：一线入口是报工与记录本组合入口，点击提交后同一事务既报工，也写入记录本。
- 已按用户要求补充：基于 `批记录压力泵.doc` 和 `损耗单.doc` 抽取生产一线需要填写的输入数量、设备参数、输出数量、损耗数量。
- 已按用户补充更新：一线字段支持一人或多人分片填写，工序完成通过生产工单或任务的累计数量判断。
- 已按用户纠正更新：工序池是核心概念，所有一线提交先进入对应工序池，生产工单再按数量满足。
- 已按用户补充更新：工序池还要记录 PQC 的过程检验记录，并作为质量过程明细影响质量状态和可分配数量。
- 已按用户要求补充命名建议：提交到工序池的统一对象叫“工序池提交事件”，每条事件包含唯一电子签名和唯一提交时间，可支撑时间甘特图。
- 已按用户补充更新：设备数量有限时，设备端登录账号绑定一个或多个工艺路线；员工在账号内切换到本人，UI 按员工切换，提交时电子签名和工序池提交事件责任主体绑定实际员工而不是设备登录账号。
- 已按用户追问核对：现有系统存在报工工序余量池和余量分配流水，但缺少工序池提交事件、记录本/PQC/电子签名/生产工单 FIFO 等完整工序池维度。
- 已按用户确认更新：新增工序池与现有报工余量池不是同一概念；切换员工无需额外身份验证；电子签名代表身份；审核副本只处理上下限；原始记录修改必须重新电子签名；已 FIFO 分配给生产工单的原始记录不能修改。
- 已按用户最新问题补充：实现建议拆为 12 个里程碑，覆盖需求冻结、工序池模型、设备账号与权限、固定模板、一线组合提交、数量汇总、PQC 入池、生产工单 FIFO、修改重签名、审核副本、甘特图、现有系统集成和试点验收。
- 已按用户要求启动 6 个子 agent，分别覆盖可先做的功能点 1、2、3、4、7、8。
- 已将子 agent 草案整合为 `docs/acceptance/production-line-process-pool/` 下专题验收文档，并补充 21 条需求门禁审查报告。
- 已完成专题验收文档结构验证、UTF-8 读取验证、21 条门禁关键短语验证和 `git diff --check`。
- `task-closeout-cleanup` preview/apply 已通过，未删除任何文件。
- 实现文档提交：`5006da7a docs: add process pool BDD TDD acceptance plan`。
- 收尾记录提交和远端推送在本记录更新后执行。
