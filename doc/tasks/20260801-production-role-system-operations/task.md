# 生产全流程角色系统操作文档

## Task Goal

根据用户补充的生产订单、物料、报工、PQC、批记录与放行业务口径，整理未来需要使用本系统的角色、界面入口和操作方式，并写入正式产品需求文档。

## Milestones

- [x] 创建任务目录和任务记录。
- [x] 核对适用项目规则、产品需求文档技能结构、编码规则和既有经验门禁。
- [x] 编写角色与系统界面操作文档。
- [x] 执行文档结构和 UTF-8 读取验证。

## Expected Verification

- 使用 UTF-8 读取新增文档，确认中文内容可读。
- 检查文档必须覆盖角色边界：计划排产员、仓库、物料员不直接使用本系统；ERP 数据可直连获取；其他角色未来使用本系统。
- 检查文档必须覆盖三类配置边界：批记录表单、表单槽位、工序开始不得混用。

## Current Status

completed

## 设计约束检查

- `是否引入 fallback/降级/吞异常`：否。
- `是否从根因和长期维护角度解决`：是，本次只整理正式业务流程和界面操作口径，不用临时字段或替代链路掩盖缺失决策。
- `是否存在临时补丁或绕过`：否。

## Applicable Gates

- `docs/experience-index.md` 命中 `批记录表单 / 表单槽位 / 工艺路线 / 过程检验 / 报工` 相关门禁。
- `AGENTS.md#工艺路线三类配置术语契约`：批记录表单、表单槽位 `formBindings`、工序开始配置是三条独立链路，不得互相替代。
- 文档任务不修改生产代码、不运行构建、不触碰数据库、远端服务或本地运行态。
