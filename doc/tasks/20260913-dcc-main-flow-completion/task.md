# DCC主流程闭环修复

## Task Goal

按用户最新要求，使“项目及模板配置 → 上传工作稿 → 送审 → 审核会签 → 批准发布 → 浏览”的主流程具备完整代码闭环，优先修复017、019、021、030相关主线缺口。

## Milestones

- M1 completed：主流程四项缺口均有失败回归；主流程、权限、图纸及并发BDD已记录。
- M2 completed：017、019、021、030修复完成；图纸检入、元数据PDF独立副本、绑定/内容重放及失败清理均已实现。
- M3 completed：最终423项后端回归、13个静态合同、5项前端回归及类型检查PASS；bug状态、报告、证据校验和17份源码指纹验证完成，待清理。

## Expected Verification

- 使用现有Maven/JUnit/H2及前端Node合同，先RED后GREEN，不运行线上接口或数据操作。
- 验证有效配置能保存、无效主体/重复环节提前拒绝，PDF/Office/CAD配套预览正确，送审后不能误检出。
- 核对上传、具体小版本送审、审批结果进入待发布、人工发布及浏览所需的现有回归；新增必要的跨步骤回归，不以只检查关键字证明全链路完成。
- 用户当轮未明确要求E2E，按项目规则不执行浏览器E2E；不宣称已完成真实环境验收或部署。

## 设计约束检查

- 培训、分发及撤回重提不作为本轮修复目标；不改其业务规则。
- 先写Given/When/Then及失败测试，再改生产代码；不增加fallback，不绕过正式权限、版本、签名或发布门禁。
- 尽量复用已有服务、票据和状态约束，不改无关业务或并行改动。
- 未获当轮授权的子Agent、Git提交推送、服务重启、远端及运行库写入不执行；测试自有内存夹具用于针对性回归。
- 下级默认提交要求不扩张本轮用户授权；源码修复交付与实际部署分别报告。

## Current Status

completed - cleanup preview/apply已通过且没有删除文件；2026-09-14 用户授权提交并融合进`int_main`后，DCC相关提交已推送到`origin/int_main`，分支领先/落后均为0。真实E2E、服务重启、数据库写入和部署仍未执行。

## Cleanup Keep

- doc/tasks/20260913-dcc-main-flow-completion/task-state.json
