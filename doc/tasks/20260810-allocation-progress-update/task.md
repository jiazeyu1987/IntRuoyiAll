# 20260810 活跃订单分配满额更新生产进度

## Task Goal

修复生产组长报工分配链路：不管通过 FIFO 自动分配还是手动分配给订单，只要目标订单当前工序累计分配已满，对应活跃订单的生产进度必须更新；分配数量允许为空或 0，空按 0 处理。

## Milestones

- [x] 定位活跃订单分配确认、进度计算与前端分配数量校验入口
- [x] 写入覆盖 FIFO/手动满额分配和空/0 数量的回归测试，并记录 RED
- [x] 修复正式实现，不引入 fallback、吞异常或默认成功
- [x] 跑目标回归和必要静态检查，记录 GREEN/REGRESSION
- [x] 更新验证报告与任务状态
- [x] 执行用户要求的真实页面 E2E 验证

## Expected Verification

- 后端目标测试覆盖：FIFO 自动分配满额后活跃订单生产进度达到 100%；手动分配满额后活跃订单生产进度达到 100%；分配行数量为空或 0 时按 0 参与校验且不报“必须为正数”。
- 前端目标静态/单元验证覆盖：分配弹窗允许空值和 0，并提交前把空值规范化为 0。
- 如全量构建或大范围测试存在历史无关失败，必须记录失败点并保留当前任务的最小 GREEN 证据。
- 真实 E2E：通过本机 `http://127.0.0.1:8081` 页面验证生产组长报工分配弹窗允许空/0 分配输入，并确认目标请求不再被前端旧正数校验阻断；若运行态缺少可安全写入的任务自有分配数据，记录 E2E BLOCKED，不用 API-only 冒充通过。

## Experience Gate Summary

- 命中 `docs/backend-development.md#FIFO 自动分配当前工序快照边界`：FIFO 预览和最终确认需区分宽松候选与指定订单 fail-fast，不能用默认成功掩盖缺失快照。
- 命中 `docs/backend-development.md#生产组长报工分配、共享数量池`：分配确认必须保留共享数量池、活跃订单工序目标、放行和历史投影的正式链路。
- 命中 `docs/frontend-development.md#前端静态契约隔离门禁`：若全量前端检查被历史问题阻塞，使用任务专用最小静态契约证明当前行为。

## 设计约束检查

- 是否引入 fallback/降级/吞异常：否。
- 是否从根因和长期维护角度解决：是，目标是修复分配确认后进度事实链路，而不是前端显示补丁。
- 是否存在临时补丁或绕过：否。

## Current Status

blocked

用户追加要求执行 E2E 验证；官方真实写入型 Playwright 脚本已运行，但当前环境缺少任务自有 `TLW_*` 测试租户、账号、工单、工序、设备、签名等夹具，按 E2E 规则不能用 admin 基线数据或 API-only 替代。
