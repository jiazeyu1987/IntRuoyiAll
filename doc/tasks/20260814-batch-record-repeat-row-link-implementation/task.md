# 批记录重复行组对应关系实现

## Task Goal

在现有“批记录单元格链接”页面实现“重复行组”配置能力：只保存当前路线版本、当前工序、当前正式批记录表单版本下，一线生产字段到重复记录行组的对应关系；不在配置页或一线提交时生成批记录数据。

## Milestones

- M1：冻结 PRD 范围、现有代码边界和 BDD 场景。
- M2：用 RED 证明当前系统没有重复行组保存/读取/页面入口。
- M3：补后端重复行组配置 schema、读写接口和校验。
- M4：补前端重复行组模式、候选记录确认和模板字段映射保存入口。
- M5：运行聚焦后端、前端静态/类型验证；真实 E2E 若缺运行数据则记录 blocker。

## Expected Verification

- 后端：重复行组配置保存/读取测试通过。
- 前端：批记录单元格链接重复行组静态合同通过。
- 前端：类型检查通过或记录无关阻塞。
- 真实页面：能进入现有链接页并看到重复行组入口；若本机运行态或正式测试数据缺失，fail fast 记录 blocker。

## 设计约束检查

- 是否引入 fallback/降级/吞异常：否。
- 是否从根因和长期维护角度解决：是；按路线版本、工序、正式批记录表单版本保存重复行组，不按工序名或固定 4 行硬编码。
- 是否存在临时补丁或绕过：否。

## Current Status

completed

- 已确认 PRD 任务 `20260812-batch-record-repeating-row-link-prd` completed。
- 当前实现范围只做对应关系配置，不做申请放行生成数据。
- PB-01 “不同工序启用前必填映射集合”仍未冻结；本轮不全局硬编码必填字段，仅保存用户明确配置的映射关系。
- 已完成后端重复行组保存接口、前端重复行组入口和任务专用页面入口验证。
- 真实页面验证只确认“现有链接页可切换到重复行组并展示候选区域”，未执行保存、未生成批记录、未触发申请放行。
- 电脑重启后已完成恢复复验；期间发现最新一线生产提交请求漏传会话快照 id/hash，已按既有静态合同补齐并复验通过。
- 2026-08-14 融合后按用户要求在 `芋道源码/admin`、本机 `8081/48081` 执行真实 E2E 复验；仅验证重复行组对应关系入口和页面能力，不生成批记录数据。
- 融合后真实 E2E 已完成：当前 `int_main` HEAD=`33302985228b16faae2695458b03268098a433af`，本机 `8081/48081` 归属 `E:\\IntRuoyi`，admin 登录后进入批记录单元格链接页并切换到“重复行组”，候选区域可见；未点击保存。
- 当前补充目标：选择“粗洗工序生产记录”后，左侧“报工数据”按该工序自动展示数量、人员、时间、设备、勾选项、清场确认和五项粗洗设备参数，供用户逐项建立单元格对应关系。
- 补充实现与代码级验证已完成；真实页面复验阻塞于共享 `int_main` 运行态仍由另一任务使用，且当前 `48081` 是未包含本次修复的旧运行包。为避免中断并发任务，本轮未停止或覆盖该进程。
- 用户已明确授权中断旧共享运行态并重启最新代码；当前等待同一主工作区正在执行的 Maven 测试自然结束，随后执行标准重启与只读页面复验。
- 已修复真实数据链路：历史正式绑定的批记录版本冗余字段为空时，改按正式 `batch_record_report_id` 对应的报表版本取绑定，并按当前路线 ID 限定工序，避免跨路线误配。
- 已补齐重复行组正式迁移元数据并应用到本机开发库；目标配置表为空，未写入任何对应关系或批记录数据。
- 代码级回归、前端类型检查、静态合同和两条真实只读页面 E2E 均已通过；最新后端已标准重启，粗洗工序左侧返回 62 个字段，11 个代表字段全部命中，MES 写请求为 0。
- 适用经验门禁：批记录正式绑定必须按 `batch_record_report_id` 解析报表定义/版本；绑定表冗余版本为空不能丢失工序，且请求带路线时必须按 `routeId` 隔离同报表跨路线绑定。
- 收尾清理已完成：保留任务核心文档与既有真实入口检查脚本，删除本任务临时探针、临时截图和已汇总的中间证据文件；无阻塞项。

## Cleanup Keep

- doc/tasks/20260814-batch-record-repeat-row-link-implementation/repeat-row-group-page-entry-check.cjs
