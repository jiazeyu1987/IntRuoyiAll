# Execution Log

## 2026-09-04

- User requested syncing the registration certificate change worktree to latest code and continuing E2E verification.
- Existing `D:\IntRuoyiWorktree\registration-change-mvp` worktree could not be fast-forwarded because its `.git/worktrees/registration-change-mvp/index.lock` was stale and locked; no product code was modified there.
- Reserved new worktree slot with `scripts\runtime\reserve-worktree-slot.ps1`: name `20260904-registration-change-e2e-sync`, profile `int_main`, slot `20`, frontend `8154`, backend `48154`.
- Created new worktree `D:\IntRuoyiWorktree\20260904-registration-change-e2e-sync` from latest `int_main` HEAD `549b32bf62f46633a6817d583100f2fd92589bf4`.
- Verified latest query behavior in `DccRegistrationCertificateQueryServiceImpl.scopedCompanyIds(...)`: it returns `List.of()` and no longer calls MDM company-scope authorization for list/detail queries.
- Confirmed frontend dependencies and backend executable jar are not present in the new worktree before startup.
- Confirmed current worktree is already up to date with `origin/int_main` at `549b32bf62f46633a6817d583100f2fd92589bf4`; no product code changes were made.
- Runtime preflight: backend `http://127.0.0.1:48154/actuator/health` returned `{"status":"UP"}`; frontend `http://127.0.0.1:8154/login?redirect=/index` returned HTTP 200; change file `e2e_test\registration\biangeng\biangeng.pdf` exists.
- GREEN: `pnpm exec playwright test tests/e2e/registration-certificate-change-ui-smoke-real.spec.js --project=chromium --reporter=line` with fixed applicant credentials injected by environment -> PASS. The current registration certificate list/detail loads without the old authorized-company error, and the change dialog exposes required controls without write requests.
- GREEN/PARTIAL: Real frontend submit path for E2E-1 completed. Applicant `wanglixuan` submitted one product-name change for certificate `沪械注准20212020492`, natural submit request `/admin-api/dcc/registration-certificates/990819128/changes` returned HTTP 200, business code `0`, requestId `224`.
- BLOCKER: E2E-3 registration-manager login preflight for fixed approver `chudongqian` through the real login page -> FAIL, backend returned business code `1002000000` / `登录失败，账号密码不正确`. The same runtime accepts the project baseline `admin` login, so this is not a backend/frontend outage. Per no-fallback policy, approval was not attempted with another account.
- BLOCKED: E2E-4 through E2E-9 require the change request to be approved by the fixed registration manager account before field update, history, direct download, ordinary-user download grant, and 3-day expiry flows can be verified.
- Stopped this task-owned worktree backend/frontend after verification; ports `8154` and `48154` no longer have listeners.
- Project experience consolidation: added a reusable login/E2E rule to `docs\login-access.md` stating that fixed acceptance-document actors may not be silently replaced by `admin` or other accounts when their real login fails.
- User provided corrected approver credentials: `chudongchuan` / redacted password. Restarted the task-owned worktree runtime on slot `20`; backend health returned `UP`, frontend returned HTTP 200.
- Re-ran the approver path through the real frontend with `chudongchuan`: login preflight PASS.
- E2E-3 registration-manager todo visibility: PASS. Approval center showed one pending “注册证变更审批” task for applicant `wanglixuan`, current node `注册证访问审批`, available actions `APPROVE`, `OPEN_DETAIL`, `REJECT`.
- E2E-3 approval submit: FAIL. The real page opened “审核确认”, electronic-signature password was submitted, and the naturally triggered `/admin-api/approval-center/tasks/review` response returned HTTP 200 with business code `1002013000`, message `手机号不存在`.
- Failure analysis: code `1002013000` maps to `SMS_SEND_MOBILE_NOT_EXISTS` in `yudao-module-system`; backend stack reached `SmsSendServiceImpl.validateMobile(...)`, so approval is blocked by a required SMS/mobile precondition during the approval review flow, not by authorized-company configuration and not by `chudongchuan` login.
- Stopped this task-owned worktree backend/frontend after the corrected approver run; ports `8154` and `48154` no longer have listeners.
- User changed scope: registration certificate approval should not require SMS/mobile validation.
- BDD: 注册证审批通知不依赖手机号 -> Given a BPM process with definition key `dcc-registration-certificate-access`, When task assignment, timeout, process approve, or process reject notification is sent, Then BPM uses admin notify inbox templates and never calls `SmsSendApi.sendSingleSmsToAdmin`, so missing user mobile cannot block approval.
- Root cause narrowed: `BpmMessageServiceImpl` defines DCC notify templates for controlled-file process key `dcc-controlled-file-approval`, but does not define or route registration certificate process key `dcc-registration-certificate-access`; existing tests already expect registration-certificate process notifications to use notify inbox.
- RED: `mvn -pl yudao-module-bpm -Dtest=BpmMessageServiceImplTest test` -> FAIL, registration-certificate task assigned and process approve tests expected notify inbox delivery but `NotifyMessageSendApi` was not called.
- Code change: added `dcc-registration-certificate-access` to the DCC notify process routing in `BpmMessageServiceImpl`; expanded `BpmMessageServiceImplTest` with registration-certificate reject and timeout cases so task assigned, approve, reject, and timeout all verify no SMS call.
- GREEN: `mvn -pl yudao-module-bpm -Dtest=BpmMessageServiceImplTest test` -> PASS, 17 tests, 0 failures.
- Package verification: `mvn -pl yudao-server -am -DskipTests package` -> PASS, fresh `yudao-server-exec.jar` produced.
- Runtime note: an E2E retry before repackaging still hit `1002013000 手机号不存在` because the running jar was stale. After rebuilding and restarting from the fresh jar, the old SMS/mobile blocker no longer appeared.
- E2E retry after fresh jar: `/admin-api/approval-center/tasks/review` returned business code `1080000275` / `未配置注册证提醒任务`. This is a new blocker from registration-certificate reminder/business-event notification configuration, separate from SMS/mobile validation.
- Evidence validation: bug-regression evidence validator PASS; backend-api evidence validator PASS.
- `git diff --check` PASS with only Git line-ending warnings.
- Stopped this task-owned worktree backend/frontend after verification; ports `8154` and `48154` no longer have listeners.
- User requested continuing verification.
- Restarted task-owned worktree backend/frontend on slot `20`; backend health returned `UP`.
- Continue-verification rerun: `pnpm exec playwright test tests/e2e/registration-certificate-change-continue-approval-real.spec.js --project=chromium --reporter=line` -> FAIL, stable current blocker `1080000275 未配置注册证提醒任务`.
- Stopped this task-owned worktree backend/frontend after the continue-verification rerun; ports `8154` and `48154` no longer have listeners.
- User changed scope: remove the `1080000275 未配置注册证提醒任务` approval blocker and statically check for other restrictions.
- BDD: 缺少注册证提醒任务配置不阻塞变更审批 -> Given a registration-certificate change approval reaches business-event notification and the reminder job config is absent, When the approval records `CHANGE_APPROVAL_RECORDED`, Then the notification is skipped with a warn log and the approval main flow is not failed by `REGISTRATION_CERTIFICATE_REMINDER_JOB_NOT_CONFIGURED`.
- RED: `mvn -pl yudao-module-dcc -Dtest=DccRegistrationCertificateBusinessEventNotifierTest test` -> FAIL before implementation. Missing reminder-job config still threw `REGISTRATION_CERTIFICATE_REMINDER_JOB_NOT_CONFIGURED`, and invalid event params were only checked after config resolution.
- Code change: `DccRegistrationCertificateBusinessEventNotifier` now validates notification detail params first, catches only `REGISTRATION_CERTIFICATE_REMINDER_JOB_NOT_CONFIGURED`, logs a warn, and returns without sending the optional business-event notification. Other `ServiceException` values and notify-send failures still fail fast.
- GREEN: `mvn -pl yudao-module-dcc -Dtest=DccRegistrationCertificateBusinessEventNotifierTest test` -> PASS, 3 tests, 0 failures.
- Static check: searched registration-certificate approval/change/notification paths for SMS/mobile calls and reminder-job config usage. Current approval path has no direct SMS call, and missing reminder-job config is intercepted in the notifier used by change approval/upload/renewal/activation event notifications. Remaining `REGISTRATION_CERTIFICATE_REMINDER_JOB_NOT_CONFIGURED` throws are in the reminder daily job and business-time simulation service, which belong to reminder execution/simulation rather than the approval main path.
- Static check: searched approval/change hard failures. Remaining blockers are normal business validations: request/process binding state, approval ownership/status, duplicate terminal approval key, revision conflict, malformed change payload, missing required change file, file ownership conflict, pending change conflict, and product-relation requirements when production-related fields are changed.
- Combined verification: `mvn -pl yudao-module-bpm "-Dtest=BpmMessageServiceImplTest" test` -> PASS, 17 tests.
- Combined verification: `mvn -pl yudao-module-dcc "-Dtest=DccRegistrationCertificateBusinessEventNotifierTest,DccRegistrationCertificateBusinessEventNotificationTest" test` -> PASS, 10 tests.
- Package verification: `mvn -pl yudao-server -am -DskipTests package` -> PASS, fresh `yudao-server-exec.jar` produced.
- Static formatting check: `git diff --check` -> PASS with only Git line-ending warnings.
- Runtime verification: started task-owned backend/frontend on slot `20` from the fresh jar; backend health returned `UP`, frontend `http://127.0.0.1:8154/` was ready.
- E2E approval retry: `pnpm exec playwright test tests/e2e/registration-certificate-change-continue-approval-real.spec.js --project=chromium --reporter=line` -> first run reached `/admin-api/approval-center/tasks/review` with business code `0` and `result=true`; it then failed only because the test locator matched the new product name in two visible places.
- E2E script correction: updated the task-owned continue-approval spec to scope the product-name assertion to the detail page and to support idempotent rerun after the target todo has already disappeared because approval succeeded.
- GREEN/E2E: `pnpm exec playwright test tests/e2e/registration-certificate-change-continue-approval-real.spec.js --project=chromium --reporter=line` -> PASS, 1 test. Evidence confirms target approval task is no longer in TODO, product name `变更后产品名称-E2E-CHANGE-20260904-1439` is visible on certificate detail, change history shows `已变更`, and `biangeng.pdf` is visible.
- Runtime closeout: stopped task-owned backend/frontend; ports `8154` and `48154` no longer have listeners.
- User requested continuing E2E verification.
- Runtime verification: restarted task-owned backend/frontend on slot `20`; backend started on `48154`, frontend `http://127.0.0.1:8154/` was ready.
- E2E-6 script preflight: `node --check tests\e2e\registration-certificate-change-remaining-real.spec.js` -> PASS.
- E2E-6 first remaining run: `pnpm exec playwright test tests/e2e/registration-certificate-change-remaining-real.spec.js --project=chromium --reporter=line` -> FAIL before submit because the E2E script selected the production relation dropdown too broadly. The page kept `是否委托生产=是`, raised frontend validation `委托生产为是时，请填写受托企业`, and therefore no change-submit response was emitted.
- E2E script correction: narrowed the production-relation dropdown locator to the exact `el-form-item` label and added resume support so an already submitted real request can continue without creating a duplicate pending change.
- E2E-6 second remaining run: real frontend submit succeeded for certificate `国械注准20223030034`, requestId `256`, selected fields `产品名称/型号规格/结构组成/适用范围/产品技术要求/注册人名称/住所/生产地址`, production relation `委托生产=否/自行生产=是`; detail before approval still did not show the new product name.
- E2E-6 approval attempt with user-specified approver `chudongchuan`: FAIL. Approval center TODO for `chudongchuan` did not contain requestId `256` or certificate `国械注准20223030034`, even when the script retried without keyword filtering.
- Failure analysis read-only DB evidence: request `256` is `UPLOAD_CERTIFICATE/BPM_BOUND`; binding detail contains candidate user ids including `1490/chudongchuan`, but `ACT_RU_TASK` for process `e862b434-a83d-11f1-b19b-00155dc78f1d` has current assignee `1035/wangboxuan`. The registration-certificate approval flow hands BPM a candidate list, while BPM single-user task behavior randomly picks one assignee; approval center only lists tasks assigned to the logged-in user.
- BLOCKED: E2E-6 cannot complete with `chudongchuan` because the live task is assigned to another user. E2E-7 and E2E-8 depend on E2E-6 approval producing an approved change file, so they were not executed. E2E-9 remains blocked because the acceptance requires a grant older than 3 days and no formal page path was found to advance business time; DB/API expiry simulation was not used.
- Runtime closeout: stopped task-owned backend/frontend; `Test-NetConnection` confirmed ports `8154` and `48154` are free. `git diff --check` PASS with only Git line-ending warnings.
- User selected approval option 2: all BPM candidate approvers should see the same registration-certificate task, and the first selected candidate who approves becomes the actual handler.
- BDD: 注册证候选审批人共享待办 -> Given a registration-certificate BPM task has selected candidate user ids including the logged-in approver, When the approval center TODO is queried, Then the task is visible even if Flowable already has another assignee; When that selected candidate approves, Then the task is claimed to the logged-in user and approved; When a non-candidate tries to approve, Then the task is not claimed.
- RED: `mvn -pl yudao-module-bpm "-Dtest=BpmNativeApprovalTaskProviderTest,BpmTaskServiceImplApprovalFilterTest" test` -> FAIL before implementation. Candidate TODO lookup did not include assigned-to-other registration-certificate tasks, and registration-certificate claim logic did not prove non-candidates were rejected.
- Code change: `BpmTaskServiceImpl` now uses Flowable `taskCandidateOrAssigned` for user TODO queries. `BpmNativeApprovalTaskProvider.pageTodo(...)` supplements registration-certificate tasks visible to selected candidates from `PROCESS_START_USER_SELECT_ASSIGNEES`; `claimRegistrationUploadTaskIfPermitted(...)` only claims when the login user is a selected candidate with the required approver role and permission. `DccRegistrationCertificateApprovalService` now writes certificate summary variables for future change approvals so certificate number search can work on newly started tasks.
- GREEN: `mvn -pl yudao-module-bpm "-Dtest=BpmNativeApprovalTaskProviderTest,BpmTaskServiceImplApprovalFilterTest,BpmMessageServiceImplTest" test` -> PASS, 51 tests.
- GREEN: `mvn -pl yudao-module-bpm,yudao-module-dcc "-Dtest=BpmNativeApprovalTaskProviderTest,BpmTaskServiceImplApprovalFilterTest,DccRegistrationCertificateBpmIntegrationTest" test` -> PASS. BPM module ran 34 tests; DCC module ran 14 tests.
- GREEN: `mvn -pl yudao-module-dcc "-Dtest=DccRegistrationCertificateBusinessEventNotifierTest,DccRegistrationCertificateBusinessEventNotificationTest" test` -> PASS, 10 tests.
- Package verification: `mvn -pl yudao-server -am -DskipTests package` -> PASS; restarted task-owned backend/frontend on slot `20`, backend health returned `UP`, frontend returned HTTP 200.
- E2E-6/E2E-7 verification: `pnpm exec playwright test tests/e2e/registration-certificate-change-remaining-real.spec.js --project=chromium --reporter=line` -> PASS, 1 test. The result file records E2E-6 PASS and E2E-7 PASS. Existing request `256` was found by request id because it was started before the certificate summary variable fix and lacks `certificateNo` in BPM variables.
- E2E-8 blocker: ordinary user can see the change file request-download button, but the certificate detail has project code displayed as `—`. `openDownloadRequest(...)` checks `detail.projectCodeId` before submitting and sets `缺少项目代码，无法提交文件下载申请。`, so no `/admin-api/dcc/registration-certificates/access-requests` POST is emitted. The rerun records E2E-8 as BLOCKED instead of masking it as a timeout.
- E2E-9 blocker remains: the acceptance requires a download grant older than 3 days, but no formal page path was found to advance business date/time. Per E2E policy, DB/API expiry simulation was not used.
- User changed scope: missing project code must still allow download application, and acceptance files must say 24 hours instead of 3 days.
- BDD: 缺少项目代码仍可申请下载 -> Given a current registration certificate or applied change approval file has no project code, When an ordinary user clicks request-download from the real detail attachment area, Then the page submits a formal `DOWNLOAD_FILE` access request with the selected business file id and an omitted project code instead of blocking locally.
- BDD: 下载授权有效期文档统一为 24 小时 -> Given registration-certificate upload/change E2E acceptance docs describe ordinary-user download grants, When the docs are read for E2E-8/E2E-9, Then they use 24-hour grant wording and do not retain stale 3-day wording.
- RED: `node IntRuoyiFronted\tests\registration-certificate-download-search-static.spec.mjs` -> FAIL before this slice because the existing static contract still required `projectCodeId: requireProjectCodeId()` for download requests, which contradicts the new optional project-code behavior.
- Code change: `ActionPanel.vue` now allows change-approval-file download requests without `projectCodeId` when a concrete business file id exists; the submitted payload omits an empty project code instead of failing local validation. Backend download request normalization validates project code only when present and resolves formal file ids from certificate/change facts.
- GREEN: `node IntRuoyiFronted\tests\registration-certificate-download-search-static.spec.mjs` -> PASS.
- GREEN: `node IntRuoyiFronted\tests\registration-certificate-download-request-inline-ux-static.spec.mjs` -> PASS.
- BDD: 下载审批候选人覆盖 E2E-8 -> Given a `DOWNLOAD_FILE` registration-certificate request is assigned by Flowable to another selected user, When a registration-certificate approver with access-request approval permission opens TODO by request id, Then the download approval is visible and can be claimed before review.
- RED: `mvn -pl yudao-module-bpm "-Dtest=BpmNativeApprovalTaskProviderTest" test` -> FAIL before the final E2E-8 slice because older download requests beyond the first 200 active tasks were not scanned by the candidate-supplement path.
- Code change: `BpmNativeApprovalTaskProvider` now treats `DOWNLOAD_FILE` as claimable and scans up to 1000 active tasks for registration-certificate candidate supplementation; the regression test covers an older request id `300` behind 250 unrelated tasks.
- GREEN: `mvn -pl yudao-module-bpm "-Dtest=BpmNativeApprovalTaskProviderTest" test` -> PASS, 32 tests.
- Package verification: `mvn -pl yudao-server -am -DskipTests package` -> PASS; fresh `yudao-server-exec.jar` produced and task-owned backend restarted on `48154`, health `UP`.
- E2E-8 historical failure after restart: `chudongchuan` still could not see pre-fix pending request `300`. Read-only DB evidence showed request `300` was created before the access-request candidate fix and its binding detail stored only `candidateUserIds: [1]`; the task was assigned to user `1`, so it was stale pre-fix data rather than a failure of newly created download approvals.
- Cleanup/setup action through real frontend: logged in as the assigned approver account and approved stale request `300` from approval center by its process instance id. This was a real UI approval to clear the pre-fix pending request; it did not use API/DB to perform the approval action.
- User-authorized E2E-9 simulation: after a valid grant existed, the test changed only the grant timestamps for the source request to `NOW() - 25 hours` / `NOW() - 1 hour` to trigger the accepted "over 24 hours" branch. Passwords were injected by environment variables and are not recorded.
- GREEN/E2E: `pnpm exec playwright test tests/e2e/registration-certificate-change-remaining-real.spec.js --project=chromium --reporter=line --workers=1` -> PASS, 1 test. Final result file status `PASS`; E2E-8 PASS using request `377` for authorized ordinary-user download; E2E-9 PASS by expiring request `377`, submitting second request `380`, approving it as `chudongchuan`, and downloading within the new 24-hour grant.
- Project experience consolidation: appended the "写入型审批 E2E 旧在途单据门禁" rule to `docs\e2e-rules.md` so future approval E2E fixes distinguish stale pre-fix running requests from new-code behavior.
- Runtime closeout: stopped task-owned backend/frontend; ports `48154` and `8154` are confirmed free.
- Integration request: user requested merging this worktree into `int_main`.
- Pre-closeout preview before commit: `task_closeout.py --mode preview --task-id 20260904-registration-change-e2e-sync` -> BLOCKED because the worktree still had uncommitted task changes and the main worktree was dirty.
- Task commit: `git commit -m "修复注册证变更下载E2E链路"` -> created task commit, then rebase onto `int_main` hit conflicts in BPM provider/message, DCC access request/notification, static tests, and acceptance docs.
- Conflict resolution: kept the generic registration-certificate selected-candidate TODO supplement for upload/change/download, kept DCC inbox notification routing for `dcc-registration-certificate-access`, preserved missing-project-code download requests, and merged E2E rules/docs additions.
- Rebase verification after resolving conflicts:
  - `node IntRuoyiFronted\tests\registration-certificate-download-search-static.spec.mjs` -> PASS.
  - `node IntRuoyiFronted\tests\registration-certificate-download-request-inline-ux-static.spec.mjs` -> PASS.
  - `mvn -pl yudao-module-bpm "-Dtest=BpmNativeApprovalTaskProviderTest,BpmTaskServiceImplApprovalFilterTest,BpmMessageServiceImplTest" test` -> PASS, 53 tests.
  - `mvn -pl yudao-module-dcc "-Dtest=DccRegistrationCertificateAccessRequestServiceTest,DccRegistrationCertificateBpmIntegrationTest,DccRegistrationCertificateBusinessEventNotifierTest,DccRegistrationCertificateBusinessEventNotificationTest" test` -> PASS, 38 tests.
  - `git diff --check` -> PASS.
- Closeout apply: `python C:\Users\BJB110\.codex\skills\task-closeout-cleanup\scripts\task_closeout.py --mode apply --task-id 20260904-registration-change-e2e-sync` -> APPLIED. It deleted task-local evidence artifacts, fast-forward merged `codex/20260904-registration-change-e2e-sync` into `E:\IntRuoyi`, and removed Git worktree registration.
- Post-merge HEAD: `int_main` -> `7afa6b11c`.
- Post-merge worktree status: `E:\IntRuoyi` clean, ahead of origin by 13 local commits.
- Residual cleanup note: `Test-Path D:\IntRuoyiWorktree\20260904-registration-change-e2e-sync` returned `True`, but `git worktree list --porcelain` no longer includes that path. The residual contains task-owned `IntRuoyiFronted\node_modules`. Attempts to remove the residual directory with guarded PowerShell `Remove-Item -LiteralPath ... -Recurse -Force` were rejected by local execution policy, so it remains as a non-Git physical cache directory requiring manual cleanup.
- Rebased again after `int_main` advanced locally; final ancestry check shows `codex/20260904-registration-change-e2e-sync` is one commit ahead of current `int_main`.
- Final closeout preview: `python C:\Users\BJB110\.codex\skills\task-closeout-cleanup\scripts\task_closeout.py --mode preview --task-id 20260904-registration-change-e2e-sync` -> BLOCKED only because main worktree `E:\IntRuoyi` is dirty and cannot receive ff-only merge. Current main dirty files are unrelated Kingdee/source-governance task files, so they were not modified.
- User authorized committing the main-worktree blocker first. Main-worktree verification before that commit:
  - `git diff --check` -> PASS.
  - `mvn -pl yudao-module-dcc "-Dtest=DccControlledFileSourceGovernanceApiContractTest,DccControlledFileSourceGovernanceManifestServiceTest,DccControlledFileSourceGovernancePreparationServiceTest" test` -> PASS, 11 tests.
- Main-worktree blocker commit: `git commit -m "feat: prepare DCC source governance manifests"` -> `ca4a1fc33`.
- Rebased `codex/20260904-registration-change-e2e-sync` onto the new `int_main`; no conflicts. Final pre-merge verification:
  - `node IntRuoyiFronted\tests\registration-certificate-download-search-static.spec.mjs` -> PASS.
  - `node IntRuoyiFronted\tests\registration-certificate-download-request-inline-ux-static.spec.mjs` -> PASS.
  - `git diff --check` -> PASS.
  - `mvn -pl yudao-module-bpm "-Dtest=BpmNativeApprovalTaskProviderTest,BpmTaskServiceImplApprovalFilterTest,BpmMessageServiceImplTest" test` -> PASS, 53 tests.
  - `mvn -pl yudao-module-dcc "-Dtest=DccRegistrationCertificateAccessRequestServiceTest,DccRegistrationCertificateBpmIntegrationTest,DccRegistrationCertificateBusinessEventNotifierTest,DccRegistrationCertificateBusinessEventNotificationTest" test` -> PASS, 38 tests.

## 2026-09-04 No-Modification E2E Verification

- User requested E2E verification in the worktree according to `e2e_test\registration\biangeng\registration-certificate-change-e2e-acceptance.md`, recording every case result, analyzing failures from code, and not modifying product code.
- Preflight: reused worktree `D:\IntRuoyiWorktree\20260904-registration-change-e2e-sync`, profile `int_main`, slot `20`, ports `8154/48154`; backend health returned `{"status":"UP"}` and frontend login page returned HTTP 200.
- Preflight: source acceptance path in the prompt used `e2e\_test\...`, but the repository path present in this worktree is `e2e_test\registration\biangeng\registration-certificate-change-e2e-acceptance.md`; `biangeng.pdf` exists.
- E2E batch command with 3 parallel workers: `pnpm exec playwright test tests/e2e/registration-certificate-change-ui-smoke-real.spec.js tests/e2e/registration-certificate-change-continue-approval-real.spec.js tests/e2e/registration-certificate-change-remaining-real.spec.js --project=chromium --reporter=line` -> FAIL before business verification because all three Chromium headless launches timed out at 180000 ms.
- Rerun serial E2E smoke: `pnpm exec playwright test tests/e2e/registration-certificate-change-ui-smoke-real.spec.js --project=chromium --reporter=line --workers=1` -> PASS, 1 test. Covers preflight/UI availability for change entry and required controls without submitting.
- Rerun serial E2E continuation: `pnpm exec playwright test tests/e2e/registration-certificate-change-continue-approval-real.spec.js --project=chromium --reporter=line --workers=1` -> PASS, 1 test. Covers E2E-1 through E2E-5 from existing request `224`.
- Rerun serial remaining E2E: `pnpm exec playwright test tests/e2e/registration-certificate-change-remaining-real.spec.js --project=chromium --reporter=line --workers=1` -> FAIL. E2E-6 and E2E-7 remained PASS from evidence; E2E-8 submitted access request `300` and the page approval step returned success for a visible approval-center task, but the target request `300` remained `BPM_BOUND/RUNNING` with no grants; final ordinary-user detail still showed `申请中` and no download button.
- Read-only API evidence after E2E-8 failure: `/dcc/registration-certificates/files/download-grants?businessFileIds=990819182` as `wanglixuan` returned `canDownload=false`, `pendingRequestId=300`; `/dcc/registration-certificates/access-requests/300` returned `requestStatus=BPM_BOUND`, `bpmBindingStatus=RUNNING`, `grants=[]`.
- Read-only approval-center evidence as `chudongchuan`: `/approval-center/tasks/page?viewType=TODO&moduleCode=BPM&keyword=300` returned one unrelated item titled `注册证延续审批`; target access request `300` was not visible as an approvable download request.
- Code analysis: `DccRegistrationCertificateApprovalService.startNativeApproval(...)` creates `DOWNLOAD_FILE` approvals with scoped candidates, but `BpmNativeApprovalTaskProvider.claimRegistrationUploadTaskIfPermitted(...)` only handles `UPLOAD_CERTIFICATE` requestType. If Flowable assigns a download request to another candidate, the shared-candidate claim path used for registration certificate upload/change does not apply to `DOWNLOAD_FILE`.
- Code analysis: `DccRegistrationCertificateAccessPolicyService.canDownloadFile(...)` only returns true after a valid active grant exists. Since request `300` remains running and `DccRegistrationCertificateGrantService.createGrantsForApprovedRequest(...)` was not reached for that request, the frontend keeps the file in `申请中` and cannot show the authorized download button.
- E2E-9 remains BLOCKED by policy: the acceptance requires a grant older than 24 hours, and no formal page path was found to advance business time; DB/API expiry simulation was not used.
