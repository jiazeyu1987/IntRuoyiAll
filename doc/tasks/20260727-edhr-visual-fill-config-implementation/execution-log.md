# Execution Log

## 2026-07-27

- User intent: 在 `D:\IntRuoyiWorktree\202607727_yingshe` 完成设计文档里的开发验证工作，验证后融合进 `int_main`，再执行完整 E2E。
- Worktree: `D:\IntRuoyiWorktree\202607727_yingshe`，分支 `codex/202607727_yingshe`，运行槽位 frontend `8083` / backend `48083`。
- Trigger docs read: `docs/backend-development.md`、`docs/frontend-development.md`、`docs/task-closeout-rules.md`、`docs/powershell-encoding.md`、`docs/powershell-memory.md`、`docs/database-rules.md`。
- Source design: `doc/tasks/20260726-edhr-visual-fill-config-bdd-tdd-design/docs/acceptance/tdd-plan.md`。
- BDD: T01 辅助行模型与规则保存 -> Given 报表已有可填写单元格和已确认单元格规则 / When 管理员提交辅助行描述、排序和单元格坐标 / Then 系统保存 `edhrAssistRows` 并在读取单元格规则时原样返回，且拒绝重复坐标、空描述、空字段、无效坐标和未覆盖的可填写单元格。
- RED: `mvn "-Dtest=MesProBatchRecordCellRuleSupportTest" "-Dsurefire.failIfNoSpecifiedTests=false" test -pl yudao-module-mes -am` -> FAIL, expected missing `BatchRecordReportAssistRowVO` / `assistRows` support before T01 implementation.
- GREEN: `mvn "-Dtest=MesProBatchRecordCellRuleSupportTest" "-Dsurefire.failIfNoSpecifiedTests=false" test -pl yudao-module-mes -am` -> PASS, `Tests run: 33` before later T02 additions.
- GREEN: `mvn "-Dtest=MesProBatchRecordReportServiceImplDbTest#getAndSaveCellRules_suggestsAndPersistsReviewedTypedMetadata" "-Dsurefire.failIfNoSpecifiedTests=false" test -pl yudao-module-mes -am` -> PASS, cell rules and `assistRows` save/read service path verified.
- BDD: T02 类型纠错、下拉框和签名约束 -> Given 管理员纠正文本/数字/日期/签名单元格并维护下拉选项 / When 保存单元格规则 / Then 后端保留现有 `STRING/NUMBER/DATE/SIGNATURE` 模型，拒绝少于两个选项、空值、重复值和缺少签名标记的签名单元格。
- RED: `mvn "-Dtest=MesProBatchRecordCellRuleSupportTest#validateRule_rejectsDuplicateStringOptionValues" "-Dsurefire.failIfNoSpecifiedTests=false" test -pl yudao-module-mes -am` -> FAIL, expected duplicate dropdown option values were not rejected before T02 implementation.
- GREEN: `mvn "-Dtest=MesProBatchRecordCellRuleSupportTest#validateRule_rejectsDuplicateStringOptionValues" "-Dsurefire.failIfNoSpecifiedTests=false" test -pl yudao-module-mes -am` -> PASS, duplicate `STRING` option `value` validation added.
- BDD: T03 按辅助行保存填写责任 -> Given 报表 JSON 已包含两个 `edhrAssistRows` / When 管理员通过 `save-by-report` 提交每个辅助行的填写人 / Then 后端按 `scopeKey` 保存一条启用的 `FILL` 规则，并由服务端生成精确 `fillableScopeJson`，同时拒绝混用旧 `fillRule`、未知 `scopeKey` 和责任覆盖缺失。
- RED: `mvn "-Dtest=MesProEdhrProcessFormPermissionRuleControllerContractTest,MesProEdhrProcessFormPermissionRuleServiceImplTest#saveRuleByReport_persistsAssistRowFillAssignmentsWithServerGeneratedCellScope+saveRuleByReport_rejectsMixedLegacyFillRuleAndAssistRowAssignments+saveRuleByReport_rejectsAssignmentWhenScopeKeyIsNotInAssistRows" "-Dsurefire.failIfNoSpecifiedTests=false" test -pl yudao-module-mes -am` -> FAIL, expected missing `MesProEdhrBatchRecordFormPermissionRuleSaveReqVO.FillAssignment` before T03 implementation.
- GREEN: `mvn "-Dtest=MesProEdhrProcessFormPermissionRuleControllerContractTest,MesProEdhrProcessFormPermissionRuleServiceImplTest#saveRuleByReport_persistsAssistRowFillAssignmentsWithServerGeneratedCellScope+saveRuleByReport_rejectsMixedLegacyFillRuleAndAssistRowAssignments+saveRuleByReport_rejectsAssignmentWhenScopeKeyIsNotInAssistRows" "-Dsurefire.failIfNoSpecifiedTests=false" test -pl yudao-module-mes -am` -> PASS, `Tests run: 5, Failures: 0, Errors: 0`。
- REGRESSION: `mvn "-Dtest=MesProBatchRecordCellRuleSupportTest,MesProBatchRecordReportServiceImplDbTest#getAndSaveCellRules_suggestsAndPersistsReviewedTypedMetadata,MesProEdhrProcessFormPermissionRuleControllerContractTest,MesProEdhrProcessFormPermissionRuleServiceImplTest#saveRuleByReport_persistsAssistRowFillAssignmentsWithServerGeneratedCellScope+saveRuleByReport_rejectsMixedLegacyFillRuleAndAssistRowAssignments+saveRuleByReport_rejectsAssignmentWhenScopeKeyIsNotInAssistRows,MesProBatchRecordVersionPhaseThreeGovernanceContractTest#schemasRetainPhaseThreeGovernanceColumns+processFormPermissionRuleUniqueKeyIsVersionScoped" "-Dsurefire.failIfNoSpecifiedTests=false" test -pl yudao-module-mes -am` -> PASS, `Tests run: 42, Failures: 0, Errors: 0`。
- BDD: T04 旧规则显式迁移 -> Given 旧报表级 `FILL` 规则只有全表填写人且尚无精确范围 / When 执行一次性迁移 / Then 迁移将规则显式标记为 `scopeKey=ALL` 并从不可变 Jimu 报表 JSON 的已确认 `edhrCellRule` 坐标生成 `schemaVersion=2` 精确单元格范围；若缺少版本 ID、Jimu JSON 或已确认坐标则用包含规则 ID 的错误阻断。
- RED: `mvn "-Dtest=MesProEdhrVisualFillConfigScopeMigrationContractTest" "-Dsurefire.failIfNoSpecifiedTests=false" test -pl yudao-module-mes -am` -> FAIL, expected `20260727_mes_edhr_visual_fill_config_scope.sql` did not use `jimu_report.json_str` or backfill explicit `ALL` precise cells.
- GREEN: `mvn "-Dtest=MesProEdhrVisualFillConfigScopeMigrationContractTest" "-Dsurefire.failIfNoSpecifiedTests=false" test -pl yudao-module-mes -am` -> PASS, `Tests run: 1, Failures: 0, Errors: 0`。
- REGRESSION: `mvn "-Dtest=MesProBatchRecordCellRuleSupportTest,MesProBatchRecordReportServiceImplDbTest#getAndSaveCellRules_suggestsAndPersistsReviewedTypedMetadata,MesProEdhrProcessFormPermissionRuleControllerContractTest,MesProEdhrProcessFormPermissionRuleServiceImplTest#saveRuleByReport_persistsAssistRowFillAssignmentsWithServerGeneratedCellScope+saveRuleByReport_rejectsMixedLegacyFillRuleAndAssistRowAssignments+saveRuleByReport_rejectsAssignmentWhenScopeKeyIsNotInAssistRows,MesProBatchRecordVersionPhaseThreeGovernanceContractTest#schemasRetainPhaseThreeGovernanceColumns+processFormPermissionRuleUniqueKeyIsVersionScoped,MesProEdhrVisualFillConfigScopeMigrationContractTest" "-Dsurefire.failIfNoSpecifiedTests=false" test -pl yudao-module-mes -am` -> PASS, `Tests run: 43, Failures: 0, Errors: 0`。
- BDD: T05 工作任务责任快照 -> Given 一个报表仍只创建一个填写工作任务且包含多个辅助行责任 / When 创建初始填写任务 / Then 任务候选人是所有辅助行候选用户并集，`responsibilityScopeJson` 冻结 `schemaVersion=2`、来源、版本、`scopeKey`、解析用户和精确 `cells`；缺失或损坏责任快照时打开/填写任务 fail fast，不读取当前模板 JSON 回退。
- RED: `mvn "-Dtest=MesProEdhrWorkTaskServiceImplTest#createInitialFillTask_freezesAssistRowResponsibilityScopeAndCandidateUnion+getAssignedTaskForDetail_rejectsProcessFormFillTaskWithoutResponsibilityScopeSnapshot" "-Dsurefire.failIfNoSpecifiedTests=false" test -pl yudao-module-mes -am` -> FAIL, expected work task had no `responsibilityScopeJson` snapshot and detail path did not reject missing process-form responsibility scope.
- GREEN: `mvn "-Dtest=MesProEdhrWorkTaskServiceImplTest#createInitialFillTask_freezesAssistRowResponsibilityScopeAndCandidateUnion+getAssignedTaskForDetail_rejectsProcessFormFillTaskWithoutResponsibilityScopeSnapshot" "-Dsurefire.failIfNoSpecifiedTests=false" test -pl yudao-module-mes -am` -> PASS, `Tests run: 2, Failures: 0, Errors: 0`。
- REGRESSION: `mvn "-Dtest=MesProEdhrWorkTaskServiceImplTest,MesProEdhrWorkTaskOwnershipTransferTest" "-Dsurefire.failIfNoSpecifiedTests=false" test -pl yudao-module-mes -am` -> FAIL, `completeRouteFormFillAndCreateNextFill_marksFormCenterFillDoneAndCreatesNextFill` exposed route-form submit path passing no source user into next-fill creation and triggering `账号未登录` outside page login context.
- GREEN: route-form next-fill creation now passes explicit `actorUserId` through the existing work-task progression path; no new task table, per-assist-row task, or login fallback added.
- REGRESSION: `mvn "-Dtest=MesProEdhrWorkTaskServiceImplTest,MesProEdhrWorkTaskOwnershipTransferTest" "-Dsurefire.failIfNoSpecifiedTests=false" test -pl yudao-module-mes -am` -> PASS, `Tests run: 70, Failures: 0, Errors: 0`。
- REGRESSION: T01-T05 aggregate backend scope -> PASS, `Tests run: 53, Failures: 0, Errors: 0`。
- BDD: T06 当前用户有效范围与后端写入授权 -> Given 一个共享表单工作任务冻结了流程填写人责任快照 / When 当前员工尝试写入同一行不同列的单元格 / Then 后端按 `sourceTableIndex + rowIndex + columnIndex` 精确判断责任范围，合法单元格写入并留审计，越权单元格在签名前失败且值与审计不变化。
- RED: `mvn "-Dtest=MesProBatchRecordExecutionFieldAuditServiceTest#saveChanges_rejectsProcessFormResponsibilityOutOfScopeColumnBeforeSignature+saveChanges_allowsProcessFormResponsibilityInScopeCellAndWritesAudit" "-Dsurefire.failIfNoSpecifiedTests=false" test -pl yudao-module-mes -am` -> FAIL, `saveChanges_rejectsProcessFormResponsibilityOutOfScopeColumnBeforeSignature` expected `ServiceException` but no exception was thrown because existing授权只比较行范围，无法区分同一行不同列。
- GREEN: `mvn "-Dtest=MesProBatchRecordExecutionFieldAuditServiceTest#saveChanges_rejectsProcessFormResponsibilityOutOfScopeColumnBeforeSignature+saveChanges_allowsProcessFormResponsibilityInScopeCellAndWritesAudit" "-Dsurefire.failIfNoSpecifiedTests=false" test -pl yudao-module-mes -am` -> PASS, `Tests run: 2, Failures: 0, Errors: 0`。
- REGRESSION: `mvn "-Dtest=MesProBatchRecordExecutionFieldAuditServiceTest#saveChanges_rejectsRecordbookModeWhenExecutionDisabledIt" "-Dsurefire.failIfNoSpecifiedTests=false" test -pl yudao-module-mes -am` -> PASS, `Tests run: 1, Failures: 0, Errors: 0`；测试夹具显式让 `recordbookGlobalSettingService` 按正式禁用错误码 fail fast，确认不会进入签名链路。
- REGRESSION: `mvn "-Dtest=MesProBatchRecordExecutionFieldAuditServiceTest" "-Dsurefire.failIfNoSpecifiedTests=false" test -pl yudao-module-mes -am` -> PASS, `Tests run: 43, Failures: 0, Errors: 0`。
- BDD: T07 执行快照与辅助模式数据 -> Given 执行来自已确认批记录模板且模板包含辅助行 / When 打开或复用批记录执行 / Then `executionSnapshotJson` 在既有 `fields` 旁冻结 `assistRows`，后续模板变更不改变 V1 执行，且缺少辅助行时返回空辅助模式数据而不从字段推导。
- RED: `mvn "-Dtest=MesProBatchRecordExecutionServiceImplTest#openOrCreateByContext_freezesAssistRowsInExecutionSnapshot" "-Dsurefire.failIfNoSpecifiedTests=false" test -pl yudao-module-mes -am` -> FAIL, expected execution snapshot did not persist `assistRows` before T07 implementation.
- GREEN: `mvn "-Dtest=MesProBatchRecordExecutionServiceImplTest#openOrCreateByContext_freezesAssistRowsInExecutionSnapshot" "-Dsurefire.failIfNoSpecifiedTests=false" test -pl yudao-module-mes -am` -> PASS, execution snapshot freezes two assist rows and keeps them after report JSON changes.
- BDD: T07 当前用户辅助行过滤 -> Given 一个共享表单填写任务冻结了 `EDHR_PROCESS_FORM_FILLER` 责任范围和执行快照辅助行 / When 当前员工打开填写任务 / Then `executionPageQuery.assistRows` 只包含当前员工 `responsibilityScopeJson.scopes.scopeKey` 命中的辅助行；缺少执行快照或责任快照时 fail fast，不读取当前模板回退。
- RED: `mvn "-Dtest=MesProEdhrBatchExecutionServiceTest#openTask_exposesOnlyCurrentUsersAssistRowsFromFrozenResponsibilityScope" "-Dsurefire.failIfNoSpecifiedTests=false" test -pl yudao-module-mes -am` -> FAIL, expected `executionPageQuery.assistRows` was missing before T07 implementation.
- GREEN: `mvn "-Dtest=MesProBatchRecordExecutionServiceImplTest#openOrCreateByContext_shouldNormalizeExecutionSnapshotJsonStructure+openOrCreateByContext_freezesAssistRowsInExecutionSnapshot,MesProEdhrBatchExecutionServiceTest#openTask_bindsExistingSingleExecutionContext+openTask_exposesOnlyCurrentUsersAssistRowsFromFrozenResponsibilityScope" "-Dsurefire.failIfNoSpecifiedTests=false" test -pl yudao-module-mes -am` -> PASS, `Tests run: 4, Failures: 0, Errors: 0`。
- REGRESSION: `mvn "-Dtest=MesProBatchRecordExecutionServiceImplTest,MesProEdhrBatchExecutionServiceTest" "-Dsurefire.failIfNoSpecifiedTests=false" test -pl yudao-module-mes -am` -> FAIL, `MesProEdhrBatchExecutionServiceTest#get_releasePendingApproval_locksNormalTaskActions` exposed pending-release action lock was skipped for ordinary active fill tasks because the lock exemption only checked `OPEN_FORM`.
- GREEN: `mvn "-Dtest=MesProEdhrBatchExecutionServiceTest#get_releasePendingApproval_locksNormalTaskActions+openTask_pendingReleaseAllowsApprovedOrdinaryFillCompletedBeforeClose" "-Dsurefire.failIfNoSpecifiedTests=false" test -pl yudao-module-mes -am` -> PASS, `Tests run: 2, Failures: 0, Errors: 0`；action lock exemption now matches the formal submitted ordinary route-form rule.
- REGRESSION: `mvn "-Dtest=MesProBatchRecordExecutionServiceImplTest,MesProEdhrBatchExecutionServiceTest" "-Dsurefire.failIfNoSpecifiedTests=false" test -pl yudao-module-mes -am` -> PASS, `Tests run: 237, Failures: 0, Errors: 0`。

- BDD: T08 前端统一编辑器和辅助模式静态合同 -> Given 管理员在批记录表单列表打开填写配置 / When 在现有规则弹窗中维护字段类型、下拉选项、签名单元格、辅助行描述和单元格归属 / Then 保存仍走 cell-rules 接口并同时提交 assistRows，执行页辅助模式优先使用 openTask 返回的当前用户 assistRows，缺少配置时显示“未配置辅助模式”。
- RED: `node tests\e2e\edhr-visual-fill-config-static.spec.js` -> FAIL, expected 表单列表入口文案仍为“规则”且弹窗/执行页缺少辅助行配置与 assistRows 渲染 token。
- GREEN: `node tests\e2e\edhr-visual-fill-config-static.spec.js` -> PASS, eDHR visual fill config static contract。
- REGRESSION: `node tests\e2e\edhr-cell-rules-static.spec.js` -> PASS, eDHR cell rules static contract。
- REGRESSION: `node tests\e2e\edhr-batch-record-form-list-filler-static.spec.js` -> PASS, eDHR batch record form list filler static contract。
- REGRESSION: `node tests\e2e\batch-record-cell-rule-editor-mode-static.spec.js` -> PASS, batch record cell rule editor mode static contract。
- REGRESSION: `node tests\e2e\edhr-assist-fill-mode-static.spec.js` -> PASS, edhr assist fill mode static contract。
- BLOCKER: `node tests\e2e\edhr-shared-form-binding-static.spec.js` -> FAIL, missing historical target file `IntRuoyiFronted/src/views/mes/pro/route/RouteProcessList.vue`; not counted as T08 failure because the missing file is outside the current visual fill config scope.
- BLOCKER: T09 runtime preflight -> current worktree slot 2 ports 8083/48083 are not listening; `IntRuoyiBackend/yudao-server/target/yudao-server-exec.jar` is missing and `IntRuoyiFronted/node_modules/.bin/vite.cmd` is missing, so real Playwright E2E cannot start without first building/installing runtime prerequisites.
- EXPERIENCE: `docs/worktree-memory.md` updated with Worktree 真实 E2E 运行产物门禁，记录真实 E2E 前必须同时核对后端 Jar、前端 Vite 依赖、登记端口和运行态归属。
- RECHECK: stale T09 runtime blocker -> PASS，`IntRuoyiBackend/yudao-server/target/yudao-server-exec.jar` exists，`IntRuoyiFronted/node_modules/.bin/vite.cmd` exists，8083 PID `60092` belongs to current worktree Vite，48083 PID `57620` belongs to current worktree `yudao-server-exec.jar`。
- RECHECK: runtime health -> PASS，`Invoke-RestMethod http://127.0.0.1:48083/actuator/health` returned `{"status":"UP"}` and `Invoke-WebRequest http://127.0.0.1:8083/` returned HTTP `200`。
- BDD: T08 辅助行填写人配置 -> Given 管理员在同一个“填写配置”弹窗中维护辅助行 / When 为每个辅助行选择个人或角色并点击保存 / Then 前端先保存 `cell-rules + assistRows`，再通过现有 `save-by-report` 提交 `fillAssignments`，任一步失败都不提示整体成功。
- RED: `node tests\e2e\edhr-visual-fill-config-static.spec.js` -> FAIL, expected `BatchRecordCellRulesConfirmDialog.vue` 缺少“辅助行填写人”、`getSimpleUserList/getSimpleRoleList` 和 `EdhrProcessFormPermissionRuleApi.saveByReport`。
- GREEN: `node tests\e2e\edhr-visual-fill-config-static.spec.js` -> PASS，填写配置弹窗已包含辅助行填写人分配并按辅助行提交 `fillAssignments`。
- GREEN: `pnpm ts:check` -> PASS，`edhrWorkTaskNavigation.stringifyQuery` 已显式过滤 `executionPageQuery` 中的对象/数组，避免 `assistRows` 被错误序列化进路由。
- REGRESSION: `node tests\e2e\edhr-cell-rules-static.spec.js`、`node tests\e2e\edhr-batch-record-form-list-filler-static.spec.js`、`node tests\e2e\batch-record-cell-rule-editor-mode-static.spec.js`、`node tests\e2e\edhr-assist-fill-mode-static.spec.js` -> PASS。
- BDD: T09 真实用户路径门禁 -> Given 已登记 worktree 运行态 8083/48083 在线 / When 执行 `edhr-visual-fill-config-real-flow.e2e.js` / Then 脚本必须先校验成对 URL、三账号凭据、显式写入授权和 `CODX-VFC-*` 任务专用报表夹具；缺项时 BLOCKED，不进入 mock 或 API-only 路径。
- GREEN: `node --check tests\e2e\edhr-visual-fill-config-real-flow.e2e.js` -> PASS。
- SUPERSEDED BLOCKER: `node tests\e2e\edhr-visual-fill-config-real-flow.e2e.js` -> BLOCKED，曾缺少 `EDHR_VISUAL_FILL_TENANT`、管理员/员工甲/员工乙用户名与密码环境变量、`EDHR_VISUAL_FILL_ALLOW_WRITE=1` 和 `CODX-VFC-*` 任务专用报表夹具；此凭据/环境变量阻塞已被后续用户授权和本地未跟踪配置方案取代。
- GREEN: `pnpm build:local` -> PASS，Vite build completed successfully after increasing timeout; earlier 240s timeout left task-owned build child processes, which were stopped while preserving current 8083 Vite runtime.
- GREEN: `python C:\Users\BJB110\.codex\skills\frontend-feature-delivery\scripts\validate_frontend_feature.py --evidence doc\tasks\20260727-edhr-visual-fill-config-implementation\frontend-feature-evidence.md` -> PASS。
- GREEN: `python C:\Users\BJB110\.codex\skills\backend-api-delivery\scripts\validate_backend_api.py --evidence doc\tasks\20260727-edhr-visual-fill-config-implementation\backend-api-evidence.md` -> PASS。
- GREEN: `git diff --check` -> PASS，only CRLF conversion warnings were printed; no whitespace errors.
- BLOCKER RECHECK: `node tests\e2e\edhr-shared-form-binding-static.spec.js` -> FAIL, still `ENOENT` for historical target `IntRuoyiFronted/src/views/mes/pro/route/RouteProcessList.vue`; unchanged out-of-scope blocker, not caused by visual fill config changes.
- RECHECK: 2026-07-27 continuation -> backend `http://127.0.0.1:48083/actuator/health` returned `{"status":"UP"}`，frontend `http://127.0.0.1:8083/` returned HTTP `200`，`git status --short --branch` confirmed current branch `codex/202607727_yingshe` still has task-owned implementation/doc changes pending commit.
- SUPERSEDED BLOCKER RECHECK: `Get-ChildItem Env:EDHR_VISUAL_FILL_*` -> `<none>`；当时 T09 真实用户路径 E2E 仍缺少三账号凭据、显式写入授权和 `CODX-VFC-*` 任务专用报表夹具；此凭据/环境变量阻塞已被后续用户授权和本地未跟踪配置方案取代。
- SUPERSEDED BLOCKER RECHECK: `node tests\e2e\edhr-visual-fill-config-real-flow.e2e.js` -> BLOCKED，script reported `edhr_visual_fill_real_e2e_precondition_missing` with missing `EDHR_VISUAL_FILL_TENANT`、管理员/员工甲/员工乙用户名与密码、`EDHR_VISUAL_FILL_REPORT_NAME`、`EDHR_VISUAL_FILL_ALLOW_WRITE=1` and task-owned `CODX-VFC-*` report fixture; this was superseded by the later no-env local config path.
- RECHECK: T09 user authorization -> PASS，用户已授权在 `芋道源码` 租户使用管理员和两名员工账号执行真实 E2E，并明确要求不使用环境变量；真实 E2E 改为只读取未跟踪本地配置 `IntRuoyiFronted/test-results/edhr-visual-fill-config-real-flow/local-input.json`，结果证据继续脱敏秘密。
- BDD: T09 正式路线产品夹具前置 -> Given 真实 E2E 需要导入 `CODX-VFC-*` 任务批记录并生成员工待办 / When 脚本准备 DCC 项目代码、MES 物料、工艺路线产品和金蝶生产订单 / Then 路线产品名必须与批记录夹具名分离，且 DCC 项目名、MES 物料名称、批次绑定和金蝶物料编码必须一致；缺失时提前 BLOCKED，不创建冲突 DCC 数据，不调用 MES 手工工单接口。
- RED: `node tests\e2e\edhr-visual-fill-config-static.spec.js` -> FAIL, expected reason: 真实 E2E 脚本仍缺少 `routeProductName`，并把 `projectName: config.batchRecordName` 用作 DCC 项目编码创建口径，导致 `A001.02.092.60011` 命中“DCC项目代码已存在不同产品名称”。
- GREEN: `node --check tests\e2e\edhr-visual-fill-config-real-flow.e2e.js` -> PASS，真实 E2E 脚本语法有效。
- GREEN: `node tests\e2e\edhr-visual-fill-config-static.spec.js` -> PASS，静态合同确认真实 E2E 已分离 `routeProductName`，不再依赖 `EDHR_VISUAL_FILL_*` 环境变量，且不得再用批记录夹具名创建 DCC 项目编码。
- BLOCKER: T09 route product fixture scan -> BLOCKED，正式 API 只读/前置探测确认当前一致候选 `IDI`、`ID`、`CODXVFC20260726` 均无法通过 `/erp/kingdee-sync/production-order/create` 创建金蝶生产订单，错误均为对应物料编码不存在；`A001.02.092.60011` 对应 MES 物料名为 `血管鞘鞘管显影环` 且 `batchFlag=false`，与当前任务 DCC 项目名不一致，不能作为安全路线产品夹具。
- BLOCKER: `node tests\e2e\edhr-visual-fill-config-real-flow.e2e.js` -> BLOCKED，new fail-fast precondition reported only `local config fixture.routeProductName` missing; script stopped before DCC import, before ERP order creation, and did not enter mock/API-only/fallback path.
- EXPERIENCE: `docs/e2e-rules.md` updated with eDHR 跨系统路线产品夹具门禁，要求真实 E2E 在导入批记录和创建工单前先校验任务批记录名、目标表单名、路线产品名、DCC 项目代码、MES 物料批次属性、路线绑定和金蝶物料一致性。
- RED: `node tests\e2e\edhr-visual-fill-config-real-flow.e2e.js` -> FAIL，管理员保存阶段的两个 `waitForResponse` promise 在页面操作失败后未被统一收敛，页面关闭时产生未捕获拒绝，导致外层恢复和脱敏结果写入无法接管真实失败原因。
- RED: `node tests\e2e\edhr-visual-fill-config-static.spec.js` -> FAIL，点击“填写配置”前未预先标记 `reportId:cellRules` 已消费，页面按钮的 `router.push(action=cellRules)` 与路由监听存在重复打开弹窗风险。
- RED: `node tests\e2e\edhr-visual-fill-config-static.spec.js` -> FAIL，真实 E2E 保存按钮使用全局 `getByRole(...).last()`，在页面存在底层或过期弹窗按钮时可能命中被 overlay 遮罩覆盖的按钮。
- RED: `node tests\e2e\edhr-visual-fill-config-static.spec.js` -> FAIL，填写配置弹窗未显式禁用通用 Dialog 默认全屏，真实 E2E 中页脚保存按钮被全屏 `el-overlay-dialog` 命中测试拦截。
- RED: `node tests\e2e\edhr-visual-fill-config-static.spec.js` -> FAIL，填写配置编辑区高度仍为 `clamp(520px, calc(100vh - 220px), 880px)`，诊断显示保存按钮中心点落在 960px 视口外，导致真实点击不可达。
- RED: `node tests\e2e\edhr-visual-fill-config-static.spec.js` -> FAIL，右侧“填写配置”按钮执行同页 query 路由后，Playwright 默认等待 scheduled navigation，真实点击已完成但 30 秒后超时，未进入弹窗断言。
- RED: `node tests\e2e\edhr-visual-fill-config-static.spec.js` -> FAIL，真实 E2E 未断言可见填写配置编辑器数量，loading 遮罩超时时无法区分重复弹窗和单实例接口加载失败。
- RED: `node tests\e2e\edhr-visual-fill-config-static.spec.js` -> FAIL，页面内填写配置按钮仍先写入 `action=cellRules` query，触发列表重载与弹窗初始化竞态；同页按钮应直接复用现有弹窗，外部深链仍由 `handleTemplateActionQuery` 处理。
- RED: `node tests\e2e\edhr-visual-fill-config-static.spec.js` -> FAIL，官方登录 preflight 外层仅允许 120 秒，独立真实登录验证耗时约 132.7 秒并成功，导致完整 E2E 被过短外层超时误判为无输出 BLOCKED。
- BDD: T09 动态表单中心责任范围快照 -> Given 路线首个待办是已发布动态表单中心任务、权限规则只负责候选人且批次任务已冻结 `fillableScopeJson.ranges` / When 创建并打开填写工作任务 / Then 责任快照使用批次任务冻结范围并保持可校验，批记录辅助行仍只接受权限规则中的精确 `cells`，两类范围不互相回退或推导。
- RED: `mvn "-Dtest=MesProEdhrWorkTaskServiceImplTest#createInitialFillTask_usesRouteVersionedFormBindingRuleForDynamicRouteFormSlot" "-Dsurefire.failIfNoSpecifiedTests=false" test -pl yudao-module-mes -am` -> FAIL，动态表单中心权限规则没有辅助行 `cells` 时仍被统一解析为精确单元格范围，未读取批次任务已冻结的正式 `fillableScopeJson.ranges`，抛出 `scopeKey=ALL`。
- GREEN: `mvn "-Dtest=MesProEdhrWorkTaskServiceImplTest#createInitialFillTask_usesRouteVersionedFormBindingRuleForDynamicRouteFormSlot+createInitialFillTask_usesRouteVersionedRuleWhenFormCenterBindingStoredAsReportId+createInitialFillTask_freezesAssistRowResponsibilityScopeAndCandidateUnion+getAssignedTaskForDetail_rejectsProcessFormFillTaskWithoutResponsibilityScopeSnapshot" "-Dsurefire.failIfNoSpecifiedTests=false" test -pl yudao-module-mes -am` -> PASS，`Tests run: 4, Failures: 0, Errors: 0`；动态表单中心责任范围只读取批次任务冻结的 `ranges`，批记录辅助行责任范围继续只读取权限规则的 `cells`，责任快照校验只接受二者之一且不得混用。
- REGRESSION: `mvn "-Dtest=MesProEdhrWorkTaskServiceImplTest" "-Dsurefire.failIfNoSpecifiedTests=false" test -pl yudao-module-mes -am` -> PASS，`Tests run: 62, Failures: 0, Errors: 0`。
- GREEN: `mvn -pl yudao-server -am "-DskipTests" package` -> PASS，最新 `yudao-server-exec.jar` 生成时间 `2026-07-27T20:08:51+08:00`，SHA256 `1A6455E739BD4DC63A73081661F7AFFC981C917573144D465891FB8F4245D434`。
- GREEN: slot 2 backend controlled restart -> PASS，旧 PID `5148` 的命令行归属当前 worktree 且监听 `48083`；停止后以同一 Jar、同一端口和 `local` profile 启动新 PID `31356`，`http://127.0.0.1:48083/actuator/health` 返回 `UP`，`http://127.0.0.1:8083/` 返回 HTTP `200`。
- GREEN: experience-preflight -> PASS，真实 E2E 使用同一 `int_main slot=2` 的成对 URL `8083/48083`、用户明确授权的 `芋道源码` 三账号、ignored 本地配置和显式写入授权；不读取或记录密码，不使用环境变量，不使用 mock、API-only、直接 SQL 或跨 slot fallback。
- BDD: T09 大型动态表单责任快照摘要 -> Given 动态表单任务冻结 100 个 `ranges` / When 创建填写工作任务 / Then `responsibilitySourceDigest` 使用固定长度 `scopes-sha256=<64 lowercase hex>`，完整范围只保存在 `responsibilityScopeJson`，不得因数据库列长度溢出而失败。
- RED: `mvn "-Dtest=MesProEdhrWorkTaskServiceImplTest#createInitialFillTask_usesCompactDigestForLargeFrozenRangeScope" "-Dsurefire.failIfNoSpecifiedTests=false" test -pl yudao-module-mes -am` -> FAIL，H2 复现 `responsibility_source_digest` 超过 `varchar(1000)`。
- GREEN: `mvn "-Dtest=MesProEdhrWorkTaskServiceImplTest#createInitialFillTask_usesCompactDigestForLargeFrozenRangeScope" "-Dsurefire.failIfNoSpecifiedTests=false" test -pl yudao-module-mes -am` -> PASS，责任摘要改为确定性 SHA-256，完整 100 个范围仍保留在责任快照。
- REGRESSION: `mvn "-Dtest=MesProEdhrWorkTaskServiceImplTest" "-Dsurefire.failIfNoSpecifiedTests=false" test -pl yudao-module-mes -am` -> PASS，`Tests run: 62, Failures: 0, Errors: 0`。
- GREEN: `mvn -pl yudao-server -am "-DskipTests" package` -> PASS，Jar 生成时间 `2026-07-27 20:21:21 +08:00`，SHA256 `FCD2FC092FC506FDD2665C9989149642ECEBADD4EF565097C554396EC18C0353`。
- GREEN: slot 2 backend controlled restart -> PASS，PID `54524` 加载上述 Jar 并监听 `48083`；frontend PID `57116` 监听 `8083`，后端 health `UP`、前端 HTTP `200`。
- BLOCKER: `node tests\e2e\edhr-visual-fill-config-real-flow.e2e.js` -> BLOCKED，旧脚本在创建批次 `900000000886` 后才发现员工无待办；认证只读诊断确认该批次的 10 个任务只包含 6 个 `ROUTE_FORM` 和 4 个特殊节点，唯一活动任务 `6788/2233` 是分配给既有路线用户的“损耗单”，目标“粗洗工序生产记录”未进入批次任务快照。
- BDD: T09 目标报表路线绑定前置 -> Given 管理员已通过真实页面保存目标报表的辅助行填写配置 / When E2E 准备用工单当前激活路线创建任务批次 / Then 必须先通过正式 `/mes/pro/route/flow-config` 只读核对目标报表绑定，缺失时在创建批次和员工验证前 fail fast；创建成功后还必须确认目标 `batchRecordReportId` 的批次任务真实存在。
- RED: `node tests\e2e\edhr-visual-fill-config-static.spec.js` -> FAIL，真实 E2E 尚未包含 `assertTargetReportRouteBinding` 和 `assertTargetReportBatchTask`。
- GREEN: `node tests\e2e\edhr-visual-fill-config-static.spec.js` -> PASS，真实 E2E 已增加路线绑定前置和批次任务后置断言。
- GREEN: `node --check tests\e2e\edhr-visual-fill-config-real-flow.e2e.js` -> PASS。
- BLOCKER: `node tests\e2e\edhr-visual-fill-config-real-flow.e2e.js` -> BLOCKED，准确返回 `task_owned_batch_route_missing_target_report_binding`；路线 `922119 / RT000028 / V15` 的当前 `BATCH` 配置中 `configuredBatchRecordReports=[]`，目标报表 ID `1d05410f1d3140c5b8aa6786887ae69c` 未绑定。本次复跑在 `open-or-create` 前停止，没有新增批次。
- VERIFICATION: 真实浏览器登录 `芋道源码/admin` 后通过认证只读 API 复核路线和任务 -> PASS；租户共 5 条路线，仅 `RT000028` 启用批记录，当前激活版本 14 个工序的 `batchRecordReports` 全为空；工单 `881MO090889` 只有该路线可选。
- RESTORE: 独立认证只读 API 复验原始报表配置 -> PASS；`ruleCount=87`、`assistRowCount=87`、`fillAssignmentCount=0`、`fillRuleStatus=CONFIGURED`，原角色填写规则已恢复。
- BDD: T09 任务专用路线副本 -> Given 本地未跟踪配置显式指定共享来源路线 `sourceRouteCode` 和 `CODX-VFC-*` 任务路线 `preferredRouteCode`，且目标工单可使用来源路线产品绑定 / When 管理员通过真实页面复制路线、创建候选版本、在工序配置中绑定目标报表、电子签名发布并启用副本，再从批次页面选择该精确路线创建任务批次 / Then 共享来源路线不被修改，目标批次必须生成目标报表任务，两名员工只能看到各自冻结的辅助行；无论通过或失败，脚本都恢复原报表配置、作废任务批次并停用删除任务路线副本。
- RED: `node tests\e2e\edhr-visual-fill-config-static.spec.js` -> FAIL，真实 E2E 尚未从本地未跟踪配置读取 `sourceRouteCode`，也未具备精确任务路线选择合同。
- GREEN: `node tests\e2e\edhr-visual-fill-config-static.spec.js` -> PASS，真实 E2E 已读取并校验 `sourceRouteCode`、`preferredRouteCode`，批次创建只接受精确任务路线编码，不再选择任意第一条路线。
- GREEN: `node --check tests\e2e\edhr-visual-fill-config-real-flow.e2e.js` -> PASS。
- BDD: T09 候选路线逐工序批记录表单选择 -> Given 管理员已进入任务专用路线的草稿候选版本并选中目标工序的“批记录表单”字段 / When 管理员搜索并选择已发布的批记录报表后保存路线草稿 / Then 页面只更新该工序正式 `batchRecordReports` 绑定并复用现有 `flow-config/batch-record/save` 合同，`formBindings` 表单槽位保持不变；非草稿候选版本不可编辑。
- RED: `node tests\e2e\edhr-visual-fill-config-static.spec.js` -> FAIL，`RouteFlowGraphDesigner.vue` 尚未接入 `BatchRecordReportApi`，草稿候选版本没有逐工序批记录报表选择器。
- BDD: T09 工艺路线三类配置严格分离 -> Given 工序同时存在工序开始负责人、正式批记录报表和动态表单槽位 / When 管理员查看“批记录表单”字段值、链接和节点配置状态 / Then 这些结果只来自该工序显式 `batchRecordReports`，不得读取、补齐或推断 `formBindings`，缺少显式 `formSlotType` 的旧报表绑定必须明确失败而不是默认归入 `MAIN`。
- RED: `node tests\e2e\mes-route-flow-batch-record-detail-slot-filter-static.spec.js`、`node tests\e2e\mes-route-flow-legacy-batch-record-detail-static.spec.js`、`node tests\e2e\mes-route-flow-batch-record-panel-visible-static.spec.js`、`node tests\e2e\mes-route-flow-batch-record-item-restore-static.spec.js` -> FAIL，当前字段值、链接和节点状态仍通过通用 `buildRecordBinding*` / `isRouteNodeRecordBindingConfigured` 合并 `formBindings`，且旧报表缺少槽位类型时仍默认归入 `MAIN`。
- GREEN: `node tests\e2e\edhr-visual-fill-config-static.spec.js` -> PASS，候选路线逐工序批记录报表选择器已接入正式报表分页 API，并同步到 `legacyBatchRecords/batchRecordReports` 草稿保存链路。
- GREEN: `node tests\e2e\mes-route-flow-batch-record-detail-slot-filter-static.spec.js`、`node tests\e2e\mes-route-flow-legacy-batch-record-detail-static.spec.js`、`node tests\e2e\mes-route-flow-batch-record-panel-visible-static.spec.js`、`node tests\e2e\mes-route-flow-batch-record-item-restore-static.spec.js`、`node tests\e2e\route-batch-record-save-contract-static.spec.js` -> PASS，批记录表单字段值、链接、红绿状态和保存只使用显式 `batchRecordReports`；`formBindings` 继续只属于表单槽位。
- GREEN: `pnpm ts:check` -> PASS。
- BDD: T09 草稿候选正式批记录绑定读写对称 -> Given 管理员在 DRAFT 候选版本的指定工序保存正式 `batchRecordReports` 且未配置 `formBindings` / When 后端写入候选 `batchUseConfigs` 快照并按同一版本读回 / Then 同一批记录报表仍位于 `batchRecordReports`，`formBindings` 保持为空，二者不得转换、补齐或互相替代。
- RED: `mvn "-Dtest=MesProRouteFlowConfigServiceImplTest#saveRouteFlowConfig_shouldRoundTripDraftBatchRecordReportsWithoutConvertingToFormBindings" "-Dsurefire.failIfNoSpecifiedTests=false" test -pl yudao-module-mes -am` -> FAIL，保存后的 DRAFT 候选快照将正式 `batchRecordReports` 从 1 条清空为 0 条。
- GREEN: `mvn "-Dtest=MesProRouteFlowConfigServiceImplTest#saveRouteFlowConfig_shouldRoundTripDraftBatchRecordReportsWithoutConvertingToFormBindings" "-Dsurefire.failIfNoSpecifiedTests=false" test -pl yudao-module-mes -am` -> PASS，DRAFT 候选快照独立保留并读回正式 `batchRecordReports`，未转换为 `formBindings`。
- REGRESSION: `mvn "-Dtest=MesProRouteFlowConfigServiceImplTest" "-Dsurefire.failIfNoSpecifiedTests=false" test -pl yudao-module-mes -am` -> PASS，`Tests run: 35, Failures: 0, Errors: 0`。
- GREEN: `mvn -pl yudao-server -am "-DskipTests" package` -> PASS，最新 Jar 生成时间 `2026-07-27 22:22:17 +08:00`，SHA256 `BFBA86229250A7FB16EA493B9BFE57ACB8F4E7AA57692DE9726CE8399FCF4EDD`。
- GREEN: slot 2 backend controlled restart -> PASS，旧 PID `54524` 经命令行确认归属当前 worktree 后停止，新 PID `64320` 加载上述 Jar 并监听 `48083`；后端 health `UP`，前端 `8083` HTTP `200`。
- BLOCKER: `node tests\e2e\edhr-visual-fill-config-real-flow.e2e.js` -> FAIL，候选路线 `922225 / V2(386)` 的保存请求已携带 1 条正式 `batchRecordReports`，但同名下拉选项命中了报表 `249d8d8d9b3f4041a3e71951bf603a19`，而填写配置目标报表为 `1d05410f1d3140c5b8aa6786887ae69c`；脚本已恢复共享报表配置、删除路线且未创建批次。
- BDD: T09 同名批记录报表精确选择 -> Given 正式报表目录存在名称相同但编码和 ID 不同的已发布报表 / When 管理员在候选路线工序中搜索并选择目标批记录表单 / Then 下拉选项必须展示报表编码，管理员和 E2E 均可按编码唯一识别目标报表，保存后读回的报表 ID 必须与目标一致。
- RED: `node tests\e2e\edhr-visual-fill-config-static.spec.js` -> FAIL，候选路线批记录表单选项尚未展示报表编码，真实 E2E 也仍按同名文案选择第一项。
- GREEN: `node tests\e2e\edhr-visual-fill-config-static.spec.js` -> PASS，批记录表单选项显示报表编码，真实 E2E 按 `reportCode` 精确选择。
- GREEN: `pnpm ts:check` -> PASS；`node --check tests\e2e\edhr-visual-fill-config-real-flow.e2e.js` -> PASS。
- BDD: T09 批次详情支持多辅助行填写规则 -> Given 同一正式批记录报表版本按辅助行保存多条 `FILL` 规则且填写工作任务尚未创建 / When 管理员打开批次详情 / Then 详情聚合所有辅助行规则的候选用户并正常返回，不得调用单条查询抛出 `TooManyResultsException`，也不得只取任意第一条规则。
- RED: `mvn "-Dtest=MesProEdhrBatchExecutionServiceTest#detailTask_includesFillableUsersFromMultipleAssistRowRulesWhenWorkTaskNotCreated" "-Dsurefire.failIfNoSpecifiedTests=false" test -pl yudao-module-mes -am` -> FAIL，`TooManyResultsException: found 2`，稳定复现真实运行态 87 条辅助行 `FILL` 规则导致批次详情失败。
- GREEN: 同一目标 Maven 测试 -> PASS，批次详情改为读取规则列表并按规则顺序聚合候选用户，不再选择任意第一条；工作任务 legacy 版本检查和路线演练就绪检查同步改用多规则查询。
- BDD: T09 任务批次页面清理入口 -> Given 任务自有批次仍存在但批次详情接口可能因业务数据异常暂时不可用 / When E2E finally 执行清理 / Then 必须先通过真实批次列表页面响应精确定位批次并发起作废，不能在进入页面前依赖详情接口；作废后仍通过只读详情复验终态。
- BDD: T09 共享辅助行任务多员工可见 -> Given 一个批记录表单只创建一个填写工作任务，冻结候选员工甲和员工乙及各自辅助行范围 / When 员工甲已打开该共享任务，员工乙进入“我的待办” / Then 员工乙仍看到并可打开同一个工作任务，后端继续按 `responsibilityScopeJson.scopes.resolvedUserIds` 只返回员工乙负责的辅助行，不创建第二个工作任务。
- RED: `mvn "-Dtest=MesProEdhrWorkTaskServiceImplTest#getMyPage_keepsSharedProcessFormTaskVisibleToSecondCandidateAfterFirstCandidateOpensDetail" "-Dsurefire.failIfNoSpecifiedTests=false" test -pl yudao-module-mes -am` -> FAIL，员工乙查询共享流程表单任务时 `expected total=1 but was 0`，现有 `my-page` 只按首位 `assigneeUserId` 过滤。
- GREEN: 同一目标 Maven 测试 -> PASS，活动中的 `EDHR_PROCESS_FORM_FILLER` 共享任务在“我的待办”和待办统计中按冻结 `candidateUserSnapshot` 对所有候选员工可见；已办列表仍保持单一经办人口径。
- DIAG: T09 cleanup-only list blocker -> authenticated read-only probe confirmed admin user has `super_admin` role plus `mes:pro-edhr-batch-execution:query` and `mes:pro-edhr-batch-execution:overview`; target batch `900000000887 / EDHRB-1785162866341` now returns `status=60` from `/get`, while `/page` returns `total=0` because the batch list intentionally excludes `VOIDED` status. The blocker is cleanup idempotency, not admin overview permission.
- BDD: T09 已作废批次清理幂等性 -> Given cleanup-only 读取到上次失败结果里的任务自有批次且该批次已经是 `VOIDED` / When E2E 清理流程启动且列表页因排除终态批次未返回该行 / Then 脚本用只读详情确认终态并返回 `already-voided`，未作废时仍必须进入真实批次列表页面定位并提交作废，不得用 API-only 或 SQL 执行动作。
- RED: `node tests\e2e\edhr-visual-fill-config-static.spec.js` -> FAIL，expected cleanup script to read current batch status when the list page excludes an already-voided task-owned batch; current script only checked list rows and raised `cleanup_task_owned_batch_not_visible_on_batch_page`.
- GREEN: `node tests\e2e\edhr-visual-fill-config-static.spec.js` -> PASS，cleanup-only 现在在列表未命中时只读确认任务自有批次状态，`VOIDED` 返回 `already-voided`，未作废批次仍保留真实列表页作废路径。
- GREEN: `node --check tests\e2e\edhr-visual-fill-config-real-flow.e2e.js` -> PASS，真实 E2E 脚本语法有效。
- GREEN: `node tests\e2e\edhr-visual-fill-config-real-flow.e2e.js --cleanup-only` -> PASS，批次 `900000000887 / EDHRB-1785162866341` 幂等识别为 `already-voided`，任务路线 `CODX-VFC-20260727` 已不存在；未打印或记录密码。
- REGRESSION: `mvn "-Dtest=MesProEdhrRehearsalReadinessServiceTest#preflight_acceptsMultipleAssistRowFillRulesForSameReportVersion" "-Dsurefire.failIfNoSpecifiedTests=false" test -pl yudao-module-mes -am` -> PASS，演练就绪检查可接受同一报表版本的多个辅助行 `FILL` 规则，不再依赖会因多行抛错的单条规则查询。
- REGRESSION: `mvn "-Dtest=MesProEdhrWorkTaskServiceImplTest,MesProEdhrBatchExecutionServiceTest,MesProEdhrProcessFormPermissionRuleServiceImplTest,MesProRouteFlowConfigServiceImplTest,MesProEdhrRehearsalReadinessServiceTest" "-Dsurefire.failIfNoSpecifiedTests=false" test -pl yudao-module-mes -am` -> PASS，`Tests run: 288, Failures: 0, Errors: 0`。
- REGRESSION: `node tests\e2e\edhr-visual-fill-config-static.spec.js`、`node tests\e2e\mes-route-flow-batch-record-detail-slot-filter-static.spec.js`、`node tests\e2e\mes-route-flow-legacy-batch-record-detail-static.spec.js`、`node tests\e2e\mes-route-flow-batch-record-panel-visible-static.spec.js`、`node tests\e2e\mes-route-flow-batch-record-item-restore-static.spec.js`、`node tests\e2e\route-batch-record-save-contract-static.spec.js` -> PASS。
- GREEN: `pnpm ts:check` -> PASS。
- GREEN: `mvn -pl yudao-server -am "-DskipTests" package` -> PASS，最新 Jar 生成时间 `2026-07-27 23:44:09 +08:00`，SHA256 `CB4F4650E6D70806694E76DF4C18A0FEECF8CAEFDC1A67316B45E3064077DAAD`。
- GREEN: slot 2 backend controlled restart -> PASS，旧 PID `67436` 经命令行确认归属当前 worktree 后停止；本地策略拦截宽 `Start-Process` 包装后，改用 scoped `Win32_Process.Create` 后台启动同一 Jar，新 PID `50564` 监听 `48083` 且归属当前 worktree；后端 health `UP`，前端 `8083` HTTP `200`。
- GREEN: `node tests\e2e\edhr-visual-fill-config-real-flow.e2e.js` -> PASS，真实 E2E 使用 `芋道源码` 租户和用户授权账号 `admin` / `jiazeyu` / `wangxin`，本地未跟踪配置 `IntRuoyiFronted/test-results/edhr-visual-fill-config-real-flow/local-input.json`，不依赖环境变量，不打印密码；管理员通过真实页面保存填写配置、复制并发布任务专用路线、创建任务批次，两名员工均打开同一个共享辅助行工作任务并看到各自可填辅助模式，finally 恢复报表配置、作废批次 `900000000889 / EDHRB-1785167428536`、删除任务路线 `922227 / CODX-VFC-20260727`。
- GREEN: project-experience-consolidation -> PASS，按既有归宿更新 `docs/e2e-rules.md` 和 `docs/experience-index.md`，沉淀任务专用路线副本、同名批记录报表精确选择、任务批次清理幂等三条 E2E 门禁；未新建长期经验文档。
- COMMIT: implementation commit `8312a6f0 feat: implement eDHR visual fill config` -> PASS，包含当前任务实现、测试、任务证据和经验门禁；`scripts\preflight\branch-runtime-port-guard.ps1` commit hook PASS。
- PUSH: `git push origin codex/202607727_yingshe` -> PASS，远端分支已创建；随后设置 upstream，`git status --short --branch` 显示 `codex/202607727_yingshe...origin/codex/202607727_yingshe`，无 ahead。

## 2026-07-28

- CONTINUE: 在融合分支 `codex/202607727_yingshe_integration` 继续处理真实 E2E 阻塞；失败证据为 `/mes/pro/edhr-batch-execution/open-or-create failed: 批记录附件负责人配置无效：batchRecordAttachmentOwners`，失败结果文件 `IntRuoyiFronted\test-results\edhr-visual-fill-config-real-flow\result.json`。
- BDD: T09 任务专用路线复制附件负责人快照 -> Given 来源路线 ACTIVE 版本已配置 `configSnapshots.batchRecordAttachmentOwners` / When 管理员通过真实页面复制任务专用路线、发布候选版本并用该路线创建 eDHR 批次 / Then 目标路线 ACTIVE 和候选发布快照必须保留来源附件负责人数组，批次创建继续按正式快照严格校验，不允许放宽校验、默认成功或从其它配置推断。
- RED: `mvn "-Dtest=MesProRouteServiceImplTest#copyRoute_shouldRefreshActiveVersionWithCompleteConfigSnapshotAfterChildConfigsCopied" "-Dsurefire.failIfNoSpecifiedTests=false" test -pl yudao-module-mes -am` -> FAIL，`batchRecordAttachmentOwners` 为 `null`，复现复制路线目标 V1 刷新快照只读取目标旧快照、未继承来源 ACTIVE 路线级附件负责人配置。
- GREEN: `mvn "-Dtest=MesProRouteServiceImplTest#copyRoute_shouldRefreshActiveVersionWithCompleteConfigSnapshotAfterChildConfigsCopied" "-Dsurefire.failIfNoSpecifiedTests=false" test -pl yudao-module-mes -am` -> PASS，复制路线刷新目标 ACTIVE 快照时继承来源 ACTIVE 的合法 `batchRecordAttachmentOwners` 数组，缺失或类型错误仍 fail fast。
- REGRESSION: `mvn "-Dtest=MesProRouteServiceImplTest,MesProRouteFlowConfigServiceImplTest" "-Dsurefire.failIfNoSpecifiedTests=false" test -pl yudao-module-mes -am` -> PASS，`Tests run: 57, Failures: 0, Errors: 0`。
- REGRESSION: `mvn "-Dtest=MesProEdhrWorkTaskServiceImplTest,MesProEdhrBatchExecutionServiceTest,MesProEdhrProcessFormPermissionRuleServiceImplTest,MesProRouteFlowConfigServiceImplTest,MesProEdhrRehearsalReadinessServiceTest,MesProRouteServiceImplTest" "-Dsurefire.failIfNoSpecifiedTests=false" test -pl yudao-module-mes -am` -> PASS，`Tests run: 321, Failures: 0, Errors: 0`。
- REGRESSION: `node tests\e2e\edhr-visual-fill-config-static.spec.js`、`node tests\e2e\mes-route-flow-batch-record-detail-slot-filter-static.spec.js`、`node tests\e2e\mes-route-flow-legacy-batch-record-detail-static.spec.js`、`node tests\e2e\mes-route-flow-batch-record-panel-visible-static.spec.js`、`node tests\e2e\mes-route-flow-batch-record-item-restore-static.spec.js`、`node tests\e2e\route-batch-record-save-contract-static.spec.js` -> PASS。
- GREEN: `pnpm ts:check` -> PASS。
- GREEN: `mvn -pl yudao-server -am "-DskipTests" package` -> PASS；复制到 `output\runtime\slot2\yudao-server-exec.jar` 后 SHA256 `37CC88F87847D7A1965C08A02DDFE285E3414119E2E2FAA1CA4E40045255CAB1`。
- GREEN: slot 2 backend controlled restart -> PASS，旧 `48083` 进程归属当前 worktree 后停止；使用 scoped `Win32_Process.Create` 启动新后端，port PID `42408`，backend health `UP`，frontend `8083` HTTP `200`。
- GREEN: `node tests\e2e\edhr-visual-fill-config-real-flow.e2e.js` -> PASS，真实 E2E 使用 `芋道源码` 租户和授权账号 `admin` / `jiazeyu` / `wangxin`，本地未跟踪配置 `IntRuoyiFronted\test-results\edhr-visual-fill-config-real-flow\local-input.json`，不依赖环境变量，不打印密码；管理员通过真实页面保存填写配置、复制并发布任务专用路线、创建任务批次，两名员工均打开同一个共享辅助行工作任务并看到各自可填辅助模式，finally 恢复报表配置、作废批次 `900000000891 / EDHRB-1785175736424`、删除任务路线 `922229 / CODX-VFC-20260727`。
- GREEN: bug-regression-evidence validation -> PASS，`python C:\Users\BJB110\.codex\skills\bug-regression-fix-loop\scripts\validate_bug_regression.py --evidence doc\tasks\20260727-edhr-visual-fill-config-implementation\bug-regression-evidence.md`。
- GREEN: experience-preflight -> PASS，使用 `project-experience-consolidation` 技能将“任务专用路线副本必须校验并保留来源 `batchRecordAttachmentOwners` 快照”合并到 `docs\e2e-rules.md#edhr-任务专用路线副本-e2e-门禁`，并更新 `docs\experience-index.md` 关键词；未新建长期经验文档。
- GREEN: pre-commit checks -> PASS，`git diff --check` 仅输出 CRLF 转换 warning、无 whitespace error；`powershell -ExecutionPolicy Bypass -File scripts\preflight\branch-runtime-port-guard.ps1` -> PASS，slot 2 frontend `8083` / backend `48083`。
- COMMIT: post-integration fix commit `98bfe0b2 fix: preserve copied route attachment owners` -> PASS，包含融合后复制路线附件负责人快照修复、回归测试、静态合同 CRLF 归一化、任务证据和经验门禁更新；commit hook branch runtime guard PASS。
- READY_FOR_CLOSEOUT: `task.md` 当前状态已更新为 `ready_for_closeout`，并在 `Cleanup Keep` 保留 `bug-regression-evidence.md`。
- CLEANUP PREVIEW: `python C:\Users\BJB110\.codex\skills\task-closeout-cleanup\scripts\task_closeout.py --task-id 20260727-edhr-visual-fill-config-implementation --mode preview` -> BLOCKED；preview keep `task.md`、`execution-log.md`、`verification-report.md`、`bug-regression-evidence.md`，delete 仅限任务目录下 `backend-api-evidence.md`、`frontend-feature-evidence.md` 与 `runtime-logs\*`。
- BLOCKER: cleanup apply / ff-only merge / worktree removal blocked because main worktree `E:\IntRuoyi` is dirty. 按规则不得清理主 worktree、不得覆盖或删除无关脏改动、不得删除当前 linked worktree；当前分支可先推送，最终 `completed` 状态需等待主 worktree 清理后继续 closeout。
- RECHECK: main worktree baseline and cherry-pick -> PASS，`int_main` 已创建 dirty baseline commit `89bded8e`，随后 cherry-pick 任务实现与修复提交到 `8255db08`，当前主分支待最终验证和推送。
- REGRESSION: main backend focused integration regression -> PASS，`mvn "-Dtest=MesProEdhrWorkTaskServiceImplTest,MesProEdhrBatchExecutionServiceTest,MesProEdhrProcessFormPermissionRuleServiceImplTest,MesProRouteFlowConfigServiceImplTest,MesProEdhrRehearsalReadinessServiceTest,MesProRouteServiceImplTest" "-Dsurefire.failIfNoSpecifiedTests=false" test -pl yudao-module-mes -am`，`Tests run: 321, Failures: 0, Errors: 0`。
- REGRESSION: main frontend static contracts + type check -> PASS，6 个相关 `tests\e2e\*.spec.js` 静态合同和 `pnpm ts:check` 均通过。
- GREEN: main backend package -> PASS，`mvn -pl yudao-server -am "-DskipTests" package` 生成 `IntRuoyiBackend\yudao-server\target\yudao-server-exec.jar`。
- GREEN: main stable backend runtime reload -> PASS，复制到 `output\runtime\int_main\yudao-server-exec.jar` 后 SHA256 `38503FDC04059E47E52862B575F097C31289A8AAFCBC4BCA4118E66303A43C51`；旧 `48081` PID `29284` 归属 `E:\IntRuoyi` 后停止，新 listener PID `54028` 加载稳定 Jar，backend health `UP`，frontend `8081` HTTP `200`。
- BDD: T09 融合后主运行态 E2E URL -> Given 任务已融合到 `E:\IntRuoyi` 的 `int_main` / When 使用主工作区稳定 Jar 和主端口执行真实 E2E / Then 脚本必须允许且只允许 `8081/48081` 主运行态或成对 `int_main slot 1..19`，不得拒绝合法主端口，也不得接受随机未配对端口。
- RED: `node tests\e2e\edhr-visual-fill-config-real-flow.e2e.js` -> FAIL，使用 `8081/48081` 时脚本报 `frontend port must be int_main worktree slot 1..19, got 8081`，阻塞融合后主运行态验证。
- RED: `node tests\e2e\edhr-visual-fill-config-static.spec.js` -> FAIL，静态合同新增主运行态 URL 断言后，脚本尚未包含 `const isMainRuntime = frontendPort === 8081 && backendPort === 48081`。
- GREEN: `node tests\e2e\edhr-visual-fill-config-static.spec.js` -> PASS，`node --check tests\e2e\edhr-visual-fill-config-real-flow.e2e.js` -> PASS；真实 E2E 端口校验仅接受 `int_main 8081/48081` 或成对 `int_main slot 1..19`。
- GREEN: `node tests\e2e\edhr-visual-fill-config-real-flow.e2e.js` -> PASS，主端口 `baseUrl=http://127.0.0.1:8081`、`backendUrl=http://127.0.0.1:48081`，tenant `芋道源码`，accounts `admin` / `jiazeyu` / `wangxin`；管理员配置并发布任务专用路线，两名员工均打开同一个共享辅助行工作任务并看到各自可填辅助模式，finally 恢复报表配置、作废批次 `900000000893 / EDHRB-1785177596364`、删除路线 `922231 / CODX-VFC-20260727`。
- GREEN: project-experience-consolidation -> PASS，按既有归宿更新 `docs\e2e-rules.md#worktree--int_main-运行态-url-门禁` 和 `docs\experience-index.md`，沉淀融合后主端口 E2E 与附加 worktree 成对 URL 双模式；未新建长期经验文档。

- PUSH: `git push origin int_main` -> PASS，`int_main` 从 `8beb91e1` 推送到 `6dec5f20`，随后 `git status --short --branch` 显示不再 ahead。
- CLEANUP APPLY: main workspace cleanup -> PASS，`python C:\Users\BJB110\.codex\skills\task-closeout-cleanup\scripts\task_closeout.py --task-id 20260727-edhr-visual-fill-config-implementation --mode apply` 删除 `backend-api-evidence.md` 与 `frontend-feature-evidence.md`，保留 `task.md`、`execution-log.md`、`verification-report.md`、`bug-regression-evidence.md`。
- CLOSEOUT: linked worktree ff-only cleanup path -> BLOCKED_BY_HISTORY，`task_closeout.py --mode preview` 在 `D:\IntRuoyiWorktree\202607727_yingshe` 报 `Current branch codex/202607727_yingshe_integration cannot be fast-forward merged into int_main`；原因是该分支已按 cherry-pick 融合到主分支。
- CLOSEOUT RECHECK: `git cherry int_main codex/202607727_yingshe_integration` -> PASS，5 个分支提交均以 `-` 标记，证明补丁等价已在 `int_main`。
- CLOSEOUT: task-owned worktree runtime stop -> PASS，停止 `8083` PID `57116` 和 `48083` PID `42408`，二者命令行均归属 `D:\IntRuoyiWorktree\202607727_yingshe`，随后 `8083/48083` 端口释放。
- CLOSEOUT: task-owned worktree removal -> PASS，`git worktree remove D:\IntRuoyiWorktree\202607727_yingshe` 注销 worktree；非强制删除留下依赖/生成物残留后，使用路径与端口自检的 UTF-8 Python 清理脚本删除残留目录。
- CLOSEOUT: worktree slot release -> PASS，`D:\IntRuoyiWorktree\.ports\worktree-ports.json` 中 `202607727_yingshe` 已标记 `active=false`、写入 `deletedAt` 与 `cleanupTask=20260727-edhr-visual-fill-config-implementation`；`git worktree prune` 后 `git worktree list --porcelain | rg "202607727_yingshe"` 无匹配，`8083/48083` 空闲。
