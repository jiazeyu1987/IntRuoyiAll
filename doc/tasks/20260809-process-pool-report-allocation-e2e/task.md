# 报工分配数量不足 E2E 核查

## Task Goal

通过本机真实前端路径，核查报工管理中提交时间为 `2026-08-07 13:30:38`、完成数量为 `411111` 的报工在 FIFO 自动分配和手动分配下的实际行为，并确认“剩余数量不足”提示的真实原因。

## Milestones

- [x] 完成本机运行态、登录身份、目标报工和 E2E 前置条件核对。
- [x] 通过 Playwright 验证 FIFO 自动分配路径并记录页面与接口证据。
- [x] 通过 Playwright 验证手动分配路径并确认失败时不产生分配或审核终态。
- [x] 汇总根因、验证结果和剩余阻塞，进入任务收尾。

## Expected Verification

- Playwright 从 `http://localhost:8081` 真实登录并进入生产组长报工管理页面。
- 目标报工由提交时间、员工、工序和完成数量共同定位，不依赖列表顺序。
- FIFO 自动分配记录实际页面提示和对应接口响应。
- 手动分配按目标报工完成数量提交，并核对实际页面提示和对应接口响应。
- 最终核对目标报工是否新增分配记录或进入审核终态。

## 设计约束检查

- `是否引入 fallback/降级/吞异常`：否。
- `是否从根因和长期维护角度解决`：是；同时核对报工需求量、当前工序计划快照和历史已分配量的正式关系。
- `是否存在临时补丁或绕过`：否。

## Experience Gate

- 已读取 `docs/experience-index.md`，本任务命中以下门禁：
  - Playwright 输出限定在本任务目录；CLI 若出现 `UV_HANDLE_CLOSING`，记录工具链失败并改用项目既有 Playwright 运行时，不得降级为 API-only。
  - 登录后必须真实切换到“报工管理”页签，并按提交时间、员工、工序、完成数量定位目标可见行。
  - FIFO 预览必须记录目标接口业务响应；手动确认必须等待确认接口，并在业务拒绝后核对没有新增分配记录或审核终态。
  - 本机默认身份仅用于本次既有报工的负向、无落库验证；不得创建或成功写入 admin 基线数据。
  - 任何响应不确定必须先以全新只读会话复核目标事件状态，禁止盲目重放。

## Current Status

completed

- Playwright 真实页面验证通过，自动与手动路径均已复现，目标报工未产生分配记录或审核终态。
- 只读数据核对确认：7 个有效活跃订单在目标 `routeProcessId=928611 + processId=922987` 下均缺计划数量快照，FIFO 正式剩余量合计为 `0`；订单 ERP 固定数量合计为 `2398`，仍小于报工数量 `411111`。
- 手动确认在数量校验前被 PQC 质量门禁拦截：同工单/工序有 13 个 PQC 候选事件，但 `production_submit_event_id=176` 的正式 PQC 结构化记录为 0 条。
- task-closeout-cleanup preview/apply 已通过；仅删除本任务临时脚本、诊断 SQL 和 Playwright 临时产物，最终验证结果保留在 `verification-report.md`。
