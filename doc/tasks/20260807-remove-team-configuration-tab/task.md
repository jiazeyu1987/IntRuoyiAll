# 删除班组配置页签

## 任务目标

从截图所示的一线生产工作台顶部模块导航中删除“班组配置”页签及其页面切换入口，不改动其它页签和后端接口。

## 里程碑

- [x] M1：定位页签入口、活动状态和关联测试，确认影响边界。
- [x] M2：先补充失败静态契约，再删除班组配置页签入口及不可达页面分支。
- [x] M3：完成目标测试、相邻回归、类型检查和差异检查。
- [x] M4：完成证据归档、清理、提交和推送。

## 预期验证

- 聚焦静态契约证明顶部导航不再渲染“班组配置”。
- 聚焦静态契约证明活动页签联合类型和页面分支不再包含班组配置入口。
- 相邻工作台静态契约通过。
- `pnpm ts:check` 通过。
- `git diff --check` 通过。

## 设计约束检查

- 是否引入 fallback/降级/吞异常：否。
- 是否从根因和长期维护角度解决：是；同步删除可见入口、状态契约和不可达页面分支，避免保留死入口。
- 是否存在临时补丁或绕过：否。

## 适用经验门禁

- 命中 `docs/frontend-development.md#前端角色内容页签拆分口径门禁`。
- 本次“页签”明确指 `TeamLeaderWorkbenchPage.vue` 内生产组长页面的功能模块 `el-tabs`，不是动态菜单或主导航入口。
- 该共享组件在人员、管理、详情和看板等模块块中重复渲染生产组长页签，必须同步核对并删除全部重复的 `config` 页签、活动状态类型、显示 gate 和内容分支。
- 聚焦静态契约必须覆盖目标页签负向断言，并复跑相邻生产组长/PQC 组长页签合同与 `pnpm ts:check`。

## Current Status

completed

实现、验证和任务产物清理已完成；最终提交与推送证据记录在执行日志。

## Cleanup Candidates

- output/playwright/20260807-remove-team-configuration-tab/
