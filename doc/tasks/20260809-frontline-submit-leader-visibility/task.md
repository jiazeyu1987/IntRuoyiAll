# 一线提交到生产组长显示链路修复

## 任务目标

修复一线生产正式提交已经生成报工、电子签名和工序池事件，但唯一对应生产组长在“报工管理”中看不到该事件的问题；保持正式员工范围隔离、唯一组长归属、无工单提交和单事务落链约束，不增加管理员全量可见或其它 fallback。

## 里程碑

- [x] 固化真实页面失败证据与正式行为契约。
- [x] 定位提交写链路与生产组长读链路之间的身份或范围断点。
- [x] 新增数据迁移失败回归测试并记录 RED。
- [x] 实施最小根因修复并记录 GREEN。
- [x] 运行相邻回归并确认本机运行态即时读取迁移结果。
- [x] 使用真实 Playwright 验证报工管理可见且报工历史未提前出现。
- [x] 完成缺陷证据校验、经验沉淀和任务清理。

## 预期验证

- 已存在的一线正式提交事件 `192`、报工 `877` 保持不变，不重复发出正式提交请求。
- `芋道源码/admin` 作为该路线唯一生产组长，在“生产组长 > 报工管理”按正式提交日期能看到员工陈丽、粗洗工序、完成数量 `123`。
- 事件未复核前不出现在“报工历史”；其他不负责该员工的生产组长仍不可见。
- 后端回归覆盖正式员工系统用户 ID 与生产人员档案 ID 的边界，确保写入事件和组长责任范围使用同一员工身份。
- 页面/API 无系统异常，不记录密码、token 或其它凭据。

## 设计约束检查

- `是否引入 fallback/降级/吞异常`：否。
- `是否从根因和长期维护角度解决`：是；以提交事件员工身份和组长责任范围的正式契约对齐为修复目标。
- `是否存在临时补丁或绕过`：否。

## 经验门禁

- 适用 `docs/backend-development.md#一线生产正式提交必须单事务落链并按唯一组长归属可见`：提交成功后必须由唯一对应生产组长本人在报工列表按事件确认可见，不能用 admin 全量、默认组长或 API-only 替代。
- 适用 `docs/backend-development.md#生产组长报工管理造数必须补齐工序池时间线`：列表按事件提交时间、实际员工和生产组长责任员工集合读取。
- 适用 `docs/e2e-rules.md`、`docs/login-access.md`：最终使用真实页面只读复验，不重复生成正式报工。

## Current Status

completed - 正式幂等迁移已应用并通过二次执行；后端相邻回归 24 项通过，真实 Playwright 已证明事件 `192` 在生产组长“报工管理”显示陈丽、粗洗工序和 123 件，“报工历史”仍不包含未复核事件，MES 写请求为 0。两项 evidence validator 通过，经验已合并到既有后端门禁，cleanup preview/apply 无 blocker 或 warning 并仅删除本任务临时产物。
