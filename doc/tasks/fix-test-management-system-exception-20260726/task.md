# 修复测试管理访问系统异常

## Task Goal

定位并修复访问「测试管理」时提示系统异常的问题，补充回归验证，避免通过 fallback、吞异常或默认成功掩盖根因。

## Milestones

- [ ] 建立复现路径和预期行为
- [ ] 定位前端/后端/API 根因
- [ ] 先补充失败回归测试或可复现验证证据
- [ ] 实施最小正式修复
- [ ] 运行定向回归验证并记录结果
- [ ] 收尾清理、经验沉淀、提交与推送

## Expected Verification

- 访问「系统管理 / 测试管理」不再出现系统异常。
- 定向回归测试覆盖本次根因。
- 相关前端或后端检查通过。
- 不引入 fallback、吞异常、mock 成功或默认成功。

## Current Status

in_progress

## 设计约束检查

- `是否引入 fallback/降级/吞异常`：否。
- `是否从根因和长期维护角度解决`：待验证；若无法定位根因则阻塞。
- `是否存在临时补丁或绕过`：否。

## 经验门禁

- 待读取 `docs/experience-index.md` 命中项后补充。
