# 验证报告：四份材料上传和放行门禁

## 验证范围

本任务验证对象为文档完整性和只读代码符合性，不是生产代码实现验收。

## 已完成验证

- 任务目录已建立：`doc/tasks/20260821-flow-repair-08-four-material-upload-gate/`。
- 五份必需文档存在：`task.md`、`development-plan.md`、`test-plan.md`、`execution-log.md`、`verification-report.md`。
- 文档包含目标态、当前代码事实、根因、修改边界、接口/数据/状态设计、BDD、RED/GREEN/REGRESSION 计划、失败 blocker、迁移/回滚边界和流程修复 6、9、10、11 接口契约。
- 只读代码确认四个独立节点常量、节点初始化、报告负责人快照、上传 token/文件元数据/SHA-256、附件版本 hash 链和操作审计基础存在。
- 未修改生产代码、数据库、配置、测试数据；未启动服务；未运行写入型 E2E。

## 跨线程职责复核

活跃订单点击“完成”是唯一回填节点，由流程修复 4 拥有同一业务事务：双进度 100% 校验、批记录回填、过程检验单回填和损耗分支必须整体成功；流程修复 5 只提供条件损耗判定规则，实际损耗时在该事务内创建并回填损耗单，无实际损耗时不创建或写入损耗单，只保留 `NO_LOSS` 适用性事实。任一回填失败则整体完成/回填失败且不得进入建批。流程修复 6 负责上述事务成功后的建批；流程修复 7 分两阶段负责映射与追溯：pre-release 在材料上传前冻结生产工单、领料单、批记录、过程检验、适用损耗/`NO_LOSS` Origin/TraceLink，post-release 在流程修复 10 成功后消费 release 与 manifest 完成追溯；流程修复 9 负责多入口前置且建批不受材料 gate 阻塞；流程修复 10 负责最终放行；流程修复 11 负责测试、回归、迁移和总门禁。本任务仅负责已完成 pre-release 映射的批次内四材料上传、版本/hash/manifest 和所有放行入口共用的材料硬门禁。

## 代码符合性结论

部分符合，当前不可放行。四份材料的固定口径、独立节点和节点级附件基础符合目标；材料替换或来源/hash 变化必须进入 `MATERIALS_RECHECK_REQUIRED`，旧 manifest 不得提交。缺少流程 7 pre-release Origin/TraceLink 在材料 gate 前完成并被读取的证据时，对外必须传递跨线程稳定码 `TRACE_MAPPING_BLOCKED`，不得使用内部别名；同时缺少服务端固定四节点、旧配置不参与决策以及所有合法放行入口统一复用同一服务端硬门禁的实现与运行态证据。批次创建/复用不属于本任务门禁，也不因材料缺件而阻塞。因而不能宣称“四份材料上传和放行硬门禁已完成”。

## 未解决 blocker

1. 永久必填已确认；需证明服务端无条件执行四节点硬校验，旧配置仅作只读兼容或迁移数据，不能参与放行决策；若仍影响运行时则代码符合性为 FAIL。
2. 需实现流程 7 pre-release Origin/TraceLink 并让材料 gate 强制读取其冻结结果；缺失时统一传递 `TRACE_MAPPING_BLOCKED`；同时实现统一 `FourMaterialReleaseGate`，让所有合法放行入口复用；不得让流程修复 8 参与或阻塞流程修复 6/9 的批次创建/复用。
3. 需明确流程修复 10 只在 `MATERIALS_READY` 后进入最终签名批准，流程修复 8 不写最终放行状态。
4. 需补齐真实测试租户、四份可清理文件、四节点负责人和管理者代表账号，才能运行真实页面 E2E。
5. 需处理或明确隔离损坏构建产物目录，避免后续测试误读构建输出。

## 后续放行条件

完成流程 4（含流程 5 损耗规则）/6/7/9 的上游合同、后端/前端实现、定向 RED/GREEN、回归和真实用户路径验证；必须同时证明点击完成触发的单一事务三类回填原子成功、任一失败不建批、无损耗不生成损耗单、实际损耗必有损耗单、材料为空时建批成功、流程 7 pre-release 映射先于材料 gate、四份 manifest、替换/并发/幂等、所有放行入口复用、流程 10 唯一最终放行和流程 7 post-release 追溯证据全部通过，才可将本专项从设计完成升级为实现完成。

## 文档任务收尾

本任务范围内的五份文档修订、结构核验和 closeout 已完成，任务状态为 completed。生产实现、配置迁移、真实 RED/GREEN/REGRESSION 和 E2E 均为 NOT RUN，不得将文档结构 PASS 写成生产 GREEN；这些事项由后续实现线程单独处理。

收尾证据：task-closeout-cleanup preview/apply 均通过，五份正式文档保留，删除项为零；任务状态为 completed。完成节点单一事务回填、无损耗/实际损耗分支及失败不建批约束已同步，且流程 7 pre-release 映射先于材料 gate、流程 10 唯一最终放行、流程 7 post-release 追溯的跨线程时序已完成一致性复核。

最终一致性复核：五份文档均含 `MATERIALS_RECHECK_REQUIRED` 与 `TRACE_MAPPING_BLOCKED`；历史未定义状态、错误码和笔误均无匹配。该文档 PASS 仅代表文档交付与结构核验，不代表生产 GREEN。
