# 测试管理节点闭环改造

## Task Goal

将 `系统管理 > 测试管理` 当前 14 个测试项改造成“业务节点 + 固定样本/前置复位 + 页面操作 + 页面可见验证 + 闭环清理”的确定性测试说明，消除歧义、程序员视角目标和不可重复执行风险。

## Milestones

- [x] 建立任务记录并加载测试管理闭环门禁。
- [x] 核对当前测试项、检查点数量和数据库字段。
- [x] 重写 14 个测试项的方法与目标项。
- [x] 执行结构、歧义词、内部词和闭环关键词验证。
- [x] 记录验证结论与未完成收尾阻塞。

## Expected Verification

- `system_codex_test_case` 当前项目范围仍为 14 项：工艺路线 4 项、批记录 6 项、智能排产 4 项。
- 每个测试项保留 3 条业务可读测试方法。
- 每个测试项保留 4 条业务可读测试目标。
- 测试方法与目标不包含歧义操作、多选路径、程序员专用词或只能从代码/数据库判断的目标。
- 每个测试项说明固定样本、前置复位、执行动作、页面可见验证和闭环收尾；系统或数据暂不支持时明确“停止并记录”，不冒充成功。

## Current Status

ready_for_closeout

## Closeout Blockers

- 当前工作区在任务开始前已有大量无关未提交改动；本任务未执行基线提交、任务提交和推送，避免混入非本任务文件。
- 本次完成的是测试管理数据库文本更新和只读 SQL 验证，未启动 Runner 执行真实页面回归。
- 批记录和电子批记录固定样本当前缺失；对应测试项已改为明确停止并记录，不冒充业务操作成功。

## 设计约束检查

- `是否引入 fallback/降级/吞异常`：否。
- `是否从根因和长期维护角度解决`：是，直接修正测试管理项的自然语言执行规范，避免 Runner/Codex 每次执行路径漂移。
- `是否存在临时补丁或绕过`：否。

## 经验门禁

### 测试管理测试节点闭环门禁

- Trigger: 新增或修改 `系统管理 > 测试管理` 的自然语言测试项，尤其是按业务系统节点拆分、会新建/修改/删除/作废业务数据的测试项。
- Preflight check: 每个测试节点必须写清业务节点、固定样本或任务自有测试标识、前置复位、页面操作、页面可见验证、清理/恢复方式；测试方法和测试目标必须面向业务测试人员，避免只写接口、内部字段、状态码、hash、英文内部状态或代码视角。
- Blocker: 测试项只创建不清理、只删除不先准备样本、失败后下次运行会被残留数据阻塞、没有固定样本或任务自有标识、目标只能由程序员判断，或需要测试人员在测试说明之外手工猜测清理方式时必须停止。
- Verification: 证据需包含节点数量、每节点方法项数量、每节点目标项数量、固定样本/清理/恢复闭环核验、内部词扫描结果，以及写入租户和项目范围。
- Forbidden action: 禁止用 API-only 清理、生产或 admin 基线数据、隐藏脚本状态、程序员专用字段、一次性人工清库、或“执行失败后手工处理”替代测试节点自身闭环。
- Evidence: `docs/e2e-rules.md#测试管理测试节点闭环门禁`。

### 测试管理 Schema 迁移门禁

- Trigger: 修改或运行 `system_codex_test_case`、测试项分页、测试管理页面相关接口。
- Preflight check: 先用当前真实库核对 `system_codex_test_case.project` 等当前字段是否存在。
- Blocker: 当前代码引用字段在真实库缺失、迁移未应用、或缺少分页 API/DB schema 证据时必须停止。
- Verification: 记录 schema 核对结果、写入目标和受影响范围。
- Forbidden action: 禁止用前端隐藏错误、后端默认 project、吞掉数据库异常、切换数据源、mock 成功或 API-only 代替真实页面恢复来绕过缺字段。
- Evidence: `docs/database-rules.md#测试管理-schema-迁移门禁`。
