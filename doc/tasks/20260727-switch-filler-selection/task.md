# 20260727 Switch Filler Selection

## Task Goal

修复批处理表单中“切换填写人”弹窗无法选择另外 2 个填写人的问题，确保可填写候选人可以被正常切换。

## Milestones

- [ ] 建立任务证据并记录 BDD/TDD 验证要求
- [ ] 定位“切换填写人”候选人禁用或选择失败的根因
- [ ] 增加最小回归测试，先复现失败再修复
- [ ] 实施最小前端修复并运行定向验证
- [ ] 完成验证报告与收尾状态更新

## Expected Verification

- 定向静态或单元测试先 RED 后 GREEN，覆盖“另外 2 个填写人可选择”的行为。
- 受影响前端代码通过相关定向验证。
- 若真实页面 E2E 前置条件不足，记录缺失前置条件和影响，不用 API-only 或 mock 代替真实路径。

## Current Status

in_progress

## 设计约束检查

- `是否引入 fallback/降级/吞异常`：否。
- `是否从根因和长期维护角度解决`：是，目标是在候选人可选状态或选择状态的根因处修复。
- `是否存在临时补丁或绕过`：否。

