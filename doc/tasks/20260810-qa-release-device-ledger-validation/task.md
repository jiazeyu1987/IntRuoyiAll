# 20260810 QA 发布规程设备台账校验

## Task Goal

修复点击“发布规程”时错误提示“外观已填写检验器具及设备说明，但未配置正式设备台账选项。”的问题，确保外观等仅需文字说明/目测的检验项目不会被错误要求配置正式设备台账选项。

## Milestones

- [x] 定位发布规程校验来源和正式设备台账字段契约。
- [x] 先补充可复现该误拦截的 BDD/回归测试并记录 RED。
- [x] 实施最小正式修复，不引入 fallback、默认成功或吞异常。
- [x] 运行目标测试和相关静态/回归验证。
- [x] 记录验证报告并完成任务状态更新。

## Expected Verification

- 目标回归测试先 RED 后 GREEN，覆盖“检验器具及设备说明已填写但不需要正式设备台账”的发布场景。
- 相关前端或后端目标测试通过。
- `git diff --check` 通过。

## Current Status

completed

## 设计约束检查

- 是否引入 fallback/降级/吞异常：否。
- 是否从根因和长期维护角度解决：是，目标是校正发布校验与正式设备台账必填条件的契约。
- 是否存在临时补丁或绕过：否。

## Applicable Experience Gates

- QA/PQC 设备链路门禁：检验设备、设备编号、无设备检验项目和 `equipmentRequired=false` 必须走正式字段语义，不得用文字说明、空值或前端文案掩盖正式设备配置来源缺失。
- QA 多工序发布门禁：发布规程必须按正式 QA 质检工序和项目字段契约校验，不得把不适用项目误判为缺配置。
