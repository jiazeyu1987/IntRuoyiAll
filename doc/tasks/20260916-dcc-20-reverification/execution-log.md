# Execution Log

## 2026-09-16 kickoff
- Read required rules: `docs/task-closeout-rules.md`, backend/frontend DCC rules, DCC 20-item handoff, PowerShell encoding, worktree restrictions.
- Branch/status: `int_qms`, HEAD equals `origin/int_qms` at `a872c31cb66e4d470dc8bc32335c28415bb639f2`; only this task record was untracked at kickoff.
- Previous completed evidence exists in `doc/tasks/20260914-dcc-20-remediation`, but this task performs an independent current-HEAD recheck.
- Initial broad frontend script run found 37 PASS and 1 failure in `scripts/check-dcc-controlled-file-entrypoints.mjs` because it still expects retired `src/views/dcc/controlled-file/mine/index.vue`; router currently uses workbench/training-mine/approval paths. This script is treated as an obsolete entrypoint inventory check, not 20-item evidence.
- 20-item backend static contracts: 21 PASS.
- 20-item frontend behavior contracts: 36 PASS, 4 failures in `browser/checkin-main-flow.spec.cjs` due to test sandbox missing `checkinCleanupLoading`; production `browser/index.vue` defines the ref and uses it in submit/cleanup guards. Treat as RED for stale contract harness, not a confirmed production defect.
## 2026-09-16 verification continuation
- RED/GREEN frontend checkin contract: initial recheck had 4 failures because `browser/checkin-main-flow.spec.cjs` lacked current `checkinCleanupLoading`, `checkinSourceState`, and explicit `versionChangeType` sandbox fields. Updated the test harness only; production `browser/index.vue` already had the current guards. GREEN: `node --test src/views/dcc/controlled-file/browser/checkin-main-flow.spec.cjs` -> 5 PASS.
- GREEN backend static contracts: `Push-Location IntRuoyiBackend; node --test yudao-module-dcc\src\test\js\dcc-*.cjs` -> 21 PASS, 0 failed.
- GREEN frontend 20-item direct contracts: `node --test` over approval PDF task, browser request order, checkin cleanup/upload state, directory cycle, name/content permission, final approval defaults, new upload version entry, ordinary removed actions, project product resolution, related-file pagination, fixed route policy, template invalid items and browser checkin main-flow -> 42 PASS, 0 failed.
- Broad `scripts/(dcc|bpm-dcc)*.test.mjs` inventory was intentionally not used as 20-item gate because it includes stale or unrelated historical contracts such as removed `controlled-file/mine/index.vue`, old BPM compact-detail entrypoint text, training UI shape checks and product-name recognition scripts. These are not DCC-MAIN-01..20 closure evidence.
- RED backend followup transaction rerun #1: `DccPublicationFollowupTransactionIntegrationTest` errored because failure-injection `visibilityUserMapper` leaked into the idempotent approval event test. Isolated the fault injection by restoring the real mapper in the normal approval-event test.
- RED backend followup transaction rerun #2: same class then failed with expected `READY_TO_PUBLISH` but actual `ACTIVE`. This assertion was stale after B1/B2 approval-immediate-activation; updated the test name and assertion to require direct `ACTIVE`, master current-active update, one finalization record, and no old ready-to-publish adapter call.
- GREEN backend followup transaction: `mvn.cmd -pl yudao-module-dcc -am "-Dtest=DccPublicationFollowupTransactionIntegrationTest" "-Dsurefire.failIfNoSpecifiedTests=false" test` -> 4 PASS.
- GREEN backend 20-item targeted regression: `mvn.cmd -pl yudao-module-dcc -am "-Dtest=<27 selected classes>" "-Dsurefire.failIfNoSpecifiedTests=false" test` -> 555 tests, 0 failures, 0 errors, 0 skipped. Covered required candidates, directory cycle/admin, upload HTTP contract, checkout/checkin lifecycle, finalization, query, signature, upload API, workflow, frozen approvals, initial versioning, ordinary removed actions, training, working-submission CAS, project templates, upload-ticket cleanup, publication followup/notification, impact assessment and electronic signature management.
- GREEN frontend type/build: `pnpm ts:check` -> PASS; `pnpm build:local` -> PASS.
- GREEN hygiene: `git diff --check` -> PASS. Git emitted CRLF conversion warnings only for three touched test files.
- Static residual scan: ordinary DCC workflow service public `returnTask`, `transferTask`, and `createSignTask` throw `CONTROLLED_FILE_TASK_ACTION_NOT_ALLOWED`; old route strings found only under external-file-review APIs/controllers, NAS transfer/snapshot internals, and publication impact reassignment. No ordinary controlled-file page/API standalone revision-candidate or create-revision entrypoint was found.
- Cleanup tool check: no local `task-closeout-cleanup` script found under `C:\Users\D01020\.codex`, `C:\Users\BJB110\.codex`, or workspace `.codex`; performed direct cleanup review. Keep list is only this task's three core markdown records; no generated temporary evidence files need deletion.
- Status moved to `ready_for_closeout` after verification; pending Git commit/push under prior user authorization.