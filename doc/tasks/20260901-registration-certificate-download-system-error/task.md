# 注册证下载系统异常修复

## Task Goal

修复用户下载注册证时提示“系统异常”的问题，确保满足正式下载权限的用户能够取得真实注册证文件，失败时返回明确业务错误，不引入降级或绕过下载授权、公司范围和审计要求。

## Milestones

- [x] M1：复现下载失败并定位根因。
- [x] M2：确认 TR3 已包含精确失败回归测试和最小正式修复，无需重复改代码。
- [x] M3：完成注册证下载定向回归与相关合同验证。
- [x] M4：完成任务证据、清理预览和收尾。
- [x] M5：通过 TR3 真实前端登录并下载“批准日期为空、首次获证日期存在”的注册证附件。
- [x] M6：修复真实浏览器下载暴露的审计 JSON 无效问题并完成回归。

## Expected Verification

- 注册证下载回归测试先出现与用户问题一致的 RED，再在修复后 GREEN。
- `yudao-module-dcc` 受影响测试定向通过。
- 注册证附件下载前端静态合同通过（仅当修复涉及前端合同或需要验证现有下载入口时执行）。
- bug-regression evidence validator 通过。
- E2E：使用已授权的真实租户/账号从 TR3 的 `8084` 页面进入注册证详情并点击下载，确认实际文件响应成功且文件名使用首次获证日期。

## Applicable Experience Gates

- `docs/experience-index.md` 存在。
- 注册证业务错误和用户提示必须使用明确中文，不得把内部异常、英文状态或存储错误直接作为成功结果。
- 下载仍必须沿用正式业务文件、授权、公司范围和审计链路；不得绕过为基础设施文件直链。
- 注册部经理直下与普通用户授权下载是两条正式权限路径，测试必须分别锁定。

## 设计约束检查

- `是否引入 fallback/降级/吞异常`：否。
- `是否从根因和长期维护角度解决`：是；先用失败测试锁定真实异常来源，再修改正式下载链路。
- `是否存在临时补丁或绕过`：否。

## Current Status

completed

- 已定位真实根因，确认 TR3 已包含正式修复并通过定向验证；cleanup preview/apply 已在关闭未授权 worktree 集成的模式下完成。
- 用户提供本机 `admin` 登录凭据后，真实登录预检通过。
- 首次真实下载复现“系统异常”，进一步定位为下载审计 `detail_json` 手工拼接导致 MySQL 拒绝无效 JSON；已改为项目标准 JSON 序列化并增加控制字符回归测试。
- TR3 重新构建并启动后，同一真实页面下载通过：文件名 `20260801_5555555_33333333.png`，大小 76,733 字节，页面无“系统异常”、无失败响应或页面错误。
- cleanup preview/apply 通过，仅删除本任务临时 E2E 脚本和结果 JSON；正式任务记录与回归测试保留。验证结束后 TR3 前后端正常停止，`8084/48084` 均未监听。
- 用户后续明确要求融合到 `int_main`；实现提交 `7905873f8` 已形成，等待主工作区快进融合和最终记录收口。
- `int_main` 已快进包含实现提交 `7905873f8` 和任务记录提交 `ee06f18e1`；融合后定向回归通过，TR3 worktree 和运行态均未执行停止、重启、删除或端口释放。
