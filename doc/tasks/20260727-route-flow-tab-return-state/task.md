# 路线流转关系图顶部页签返回状态修复

## Task Goal

修复在路线“流转关系图”页面中，从顶部页签切换到其他任务页签后再切回时，页面错误回到“路线列表”的问题；返回后应继续显示原路线的“流转关系图”视图。

## Milestones

1. 定位顶部页签切换、路由 query 和路线编辑页初始化之间的状态丢失根因。
2. 先新增失败的聚焦回归合同，再实施最小前端修复。
3. 运行聚焦合同、相邻路线静态回归和可用的真实 Playwright 页面验证。
4. 完成任务证据、清理预检和最终状态记录。

## Expected Verification

- `node tests/e2e/mes-route-flow-tab-return-state-static.spec.js`
- 受影响的路线页静态回归合同
- 若本地前后端运行态、登录租户和权限前置均满足，使用 Playwright 从流转关系图页切换顶部页签并返回，断言仍显示“流转关系图”。
- `git diff --check`

## Current Status

in_progress

## 设计约束检查

- 是否引入 fallback/降级/吞异常：否。本修复只保留并恢复已有的 `tab=flow` 状态，不增加降级分支。
- 是否从根因和长期维护角度解决：是。将页签返回状态与现有路由 query/页面状态契约对齐，不以重新加载或默认成功掩盖状态丢失。
- 是否存在临时补丁或绕过：否。不新增测试专用控件，不使用 API-only 替代真实用户路径。

## 经验门禁

- 前端静态契约隔离：若既有宽契约先失败于无关历史问题，必须记录首个无关失败，并使用本任务专用最小静态契约完成 RED/GREEN；不得修改无关合同或产品逻辑绕过。
- 路线页验证：先核对 `RouteEditPage.vue`、`RouteFormContent.vue`、路由 `query.tab` 和顶部页签导航契约，再判断是页面组件状态丢失还是标签页路由缓存/导航问题。

