# PQC 组长职责文档写入

## Task Goal

将“PQC 组长未来一天的系统操作”整理为职责文档，并写入 `C:\Users\BJB110\Desktop\文档\职责\pqc组长.md`。

## Milestones

- [x] 确认目标目录存在。
- [x] 读取任务收尾和 PowerShell UTF-8 编码规则。
- [x] 核对现有职责文档风格。
- [x] 写入 PQC 组长职责文档。
- [x] 验证 UTF-8 读取和关键内容覆盖。

## Expected Verification

- 使用 `python -X utf8` 读取目标文件，确认中文可读。
- 检查目标文件覆盖：角色定位、一天工作主线、PQC 检验任务查看、检验员提交内容复核、确认/退回、过程检验记录、质量异常、日结闭环、禁止混用生产班组长业务。

## Current Status

completed

## 设计约束检查

- `是否引入 fallback/降级/吞异常`：否。
- `是否从根因和长期维护角度解决`：是，本次按角色职责边界整理，不把生产班组长职责混入 PQC 组长职责。
- `是否存在临时补丁或绕过`：否。

## Applicable Gates

- 中文 Markdown 写入必须使用 UTF-8。
- 批记录表单、表单槽位、工序开始配置不得混用。
- PQC 组长只确认检验事实和过程检验记录，不确认生产报工数量、不分配生产订单数量。
