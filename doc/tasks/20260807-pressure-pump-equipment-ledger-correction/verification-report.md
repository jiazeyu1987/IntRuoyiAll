# Verification Report

## Result

PASS - 用户指定的三项本地设备台账变更已完成，事务后数据库、关联读模型、条码边界和租户边界均符合预期。

## Final Records

| 设备 ID | 设备编码 | 设备名称 | 设备类型 | 车间 | 状态 |
| --- | --- | --- | --- | --- | --- |
| 202 | A05075 | 光固机 | 5 / DEFAULT-MACHINERY-TYPE | 900066 / AUTO-WSHOP | 2 / 生产中 |
| 198 | B04091 | 箱型干燥机 | 5 / DEFAULT-MACHINERY-TYPE | 900066 / AUTO-WSHOP | 2 / 生产中 |
| 980007 | C01017 | 撤压机 | 5 / DEFAULT-MACHINERY-TYPE | 900066 / AUTO-WSHOP | 2 / 生产中 |

## Verification Evidence

- 目标唯一性：租户 `122` 的 `A05075`、`B04091`、`C01017` 各恰好 `1` 条。
- 旧编码清除：租户 `122` 的 `A05059`、`B09041` 活动设备均为 `0` 条。
- 数量边界：租户 `122` 活动设备由 `49` 增至 `50`；租户 `1` 保持 `31` 条。
- 稳定身份：光固机保持 `id=202`，箱型干燥机保持 `id=198`。
- 非编码字段：`id=202` 与 `id=198` 的变更前后 MD5 分别保持 `9b04a596ce68241a70a32d2a0904d405`、`d03fcab9d173520de79485ab0f4d678e`。
- 设备工序：两条活动 `mes_dv_machinery_process` 关系分别同步为 `A05075`、`B04091`。
- 工序目录：全局目录 `9003131008/9003131004` 及设备子表 `9003132008/9003132004` 均同步为新编码。
- 工位关系：`id=202` 与 `id=198` 仍各有 `1` 条按设备 ID 关联的活动工位关系。
- 条码边界：租户 `122` 仍无 `biz_type=400` 条码配置，三台目标设备无伪造条码记录。
- 影响范围：本任务 updater 仅落在预期的设备主表、设备工序、工序目录和目录设备子表目标记录中。

## Rollback Evidence

第一次执行因脚本读取 `ROW_COUNT()` 顺序错误在 `COMMIT` 前失败；连接退出后复核三项业务数据均未改变，证明事务整体回滚。修正脚本顺序后，第二次执行通过全部事务内断言并提交。

## Verification Gap

真实页面只读 E2E 未执行：当前会话没有 `测试租户` 的有效登录凭据，且不得切换到 `芋道源码/admin`、复用数据库令牌或临时重置账号密码。此项记录为页面层验证缺口，不替代或削弱已通过的真实数据库和正式读模型验证。
