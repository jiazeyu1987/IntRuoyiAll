# 20260724-batch-fda-audit-log-coverage

## Task Goal

按 FDA 追溯口径补齐批执行相关创建、修改、删除、责任归属、放行决策链和附件链路的不可篡改审计日志，并确保新增日志能在日志表格和批次追溯中体现。

## Milestones

- [ ] M1: 定位现有批执行、附件、任务、放行、归档、操作日志与追溯展示链路。
- [ ] M2: 先补 BDD 场景与失败验证，明确审计缺口。
- [ ] M3: 实现后端审计事件/操作日志写入，覆盖本地状态样本创建、附件删除/预登记/保存、规则保存、候选签名完成、任务重派、放行预检。
- [ ] M4: 确保审计日志能被现有日志表格与批次追溯查询到，必要时补前端展示字段。
- [ ] M5: 运行针对性验证并记录 RED/GREEN 证据。
- [ ] M6: 收尾清理、经验沉淀、任务状态完成。

## Expected Verification

- 后端针对性测试覆盖新增审计日志的 operationType、操作者、时间、对象、原因、前后状态/hash、关联批次/任务/附件/签名/幂等键/请求来源等字段。
- 追溯/日志查询接口返回新增 operationType，并包含批次维度筛选所需字段。
- 前端日志表格/批次追溯展示字段与后端返回数据一致。
- 无新增 fallback、降级、吞异常、默认成功或模拟成功路径。

## Current Status

in_progress

## 设计约束检查

- `是否引入 fallback/降级/吞异常`：否。
- `是否从根因和长期维护角度解决`：是，目标是在统一审计链路补充缺口，而不是在页面或测试侧补丁绕过。
- `是否存在临时补丁或绕过`：否。

## Experience Gate

- `docs\experience-index.md`：缺失。
- 风险记录：本任务涉及受监管批记录审计链路，属于高风险变更；用户已明确要求“按照上面的要求进行设计开发验证”，本轮继续推进，并将所有设计约束、RED/GREEN、验证证据记录在本任务目录。
