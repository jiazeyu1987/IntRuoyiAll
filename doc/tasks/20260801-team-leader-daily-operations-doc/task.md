# 生产班组长每日系统操作文档

## Task Goal

根据用户最新口径，编写一份完整描述生产班组长未来每天如何在系统中操作的产品文档，覆盖生产订单加入、调拨单同步关联、生产报工复核、订单分配、PQC 状态查看、异常处理、日结和班组基础维护。

## Milestones

- [x] 创建任务目录和任务记录。
- [x] 核对现有角色流程文档、班组长相关需求资料和适用经验门禁。
- [x] 编写生产班组长每日系统操作文档。
- [x] 执行 UTF-8、关键口径和空白错误验证。

## Expected Verification

- 使用 UTF-8 读取新增文档，确认中文内容完整可读。
- 搜索关键口径：纸质订单、金蝶 ERP、加入生产订单、关联调拨单、报工复核、订单分配、班组基础维护、审计记录、只影响后续生产。
- 执行 `git diff --check` 检查本任务新增文档和任务记录。

## Current Status

ready_for_closeout

## 设计约束检查

- `是否引入 fallback/降级/吞异常`：否。
- `是否从根因和长期维护角度解决`：是，文档按正式业务链路记录班组长职责，不用系统自动全量同步替代班组长主动加入订单/调拨单。
- `是否存在临时补丁或绕过`：否。

## Applicable Gates

- 文档必须保留用户最新修正：生产订单和调拨单来自金蝶 ERP，但由班组长按纸质订单和实际调拨单主动加入/关联到本系统。
- 班组基础维护是班组长权限范围内的低频操作，必须留审计记录，只影响后续生产，不改写历史报工和历史批记录。

## Closeout Notes

- 文档内容、UTF-8 读取、关键口径搜索和空白错误检查均已通过。
- 当前工作区存在任务开始前已有的其它未提交改动；本任务未执行提交或推送，避免混入并行任务文件。
