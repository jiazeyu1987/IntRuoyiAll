# Task: eDHR Pressure Pump V13.0 Filler Role Update

## Task Goal

为“球囊扩张压力泵”V13.0 的所有表单创建并使用 `压力泵生产1` 角色作为填写员角色，同时将该角色赋给 `wangxin` 账号。

本次追加缺陷修复：批记录表单填写人设置弹窗内，“填写人”选中的人员/角色名称必须完整显示，不得在输入框中被截断为 `压...` 等省略文本。

## Milestones

- [x] 建立任务记录、读取数据库/账号/eDHR 填写人配置门禁。
- [x] 核对本机数据库连接、目标租户、目标用户 `wangxin`、目标版本 V13.0 和表单填写人相关表结构。
- [x] 先执行 RED 查询，证明目标角色或目标填写人配置尚未满足要求。
- [x] 创建或确认 `压力泵生产1` 角色，并绑定给 `wangxin` 账号。
- [x] 将“球囊扩张压力泵”V13.0 下所有表单填写员规则更新为 `压力泵生产1` 角色。
- [x] 验证角色、用户角色绑定、表单规则和批次详情 `fillableUsers` 显示链路。
- [x] 修复批记录表单填写人设置弹窗布局，让“填写人”选择框完整显示选中人员/角色名称。

## Expected Verification

- 数据库核对：角色存在且启用，`wangxin` 账号绑定该角色。
- 数据库核对：“球囊扩张压力泵”V13.0 所有表单填写人规则均为角色 `压力泵生产1`。
- eDHR 详情接口或只读查询核对：相关表单 `fillableUsers` 可解析到 `wangxin` 对应用户昵称。
- 前端静态合同：批记录表单填写人设置弹窗的“填写人”列比相邻字段更宽，且选中标签不再使用默认省略宽度。

## Current Status

ready_for_closeout

实现和必要验证已完成；由于主工作区存在本任务开始前的并行脏改动与未跟踪文件，本任务未执行基线提交/推送，需按项目提交门禁单独收尾。

## 经验门禁

- eDHR 详情回填门禁：必须同时核对配置来源字段、执行任务快照字段、详情接口组装链路和既有优先级；不得从当前登录人、创建人、更新人或角色 ID 推断填写人。
- eDHR 单据填写人显示值门禁：验证页面或接口显示前，先读取详情接口当前 `fillableUsers`，不得硬编码历史候选名称。
- 数据库门禁：写 SQL 前必须核对真实表结构、目标租户、影响范围和回滚方式。
- PowerShell/编码门禁：涉及中文 SQL、Markdown 和日志时使用 UTF-8 路径，禁止 `&&` 和默认编码写入。

## 设计约束检查

- `是否引入 fallback/降级/吞异常`：否。
- `是否从根因和长期维护角度解决`：是，直接维护正式角色、用户角色绑定和 eDHR 填写人规则来源。
- `是否存在临时补丁或绕过`：否。
