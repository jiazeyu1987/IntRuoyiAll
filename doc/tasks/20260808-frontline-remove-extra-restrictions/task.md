# 20260808-frontline-remove-extra-restrictions

## Task Goal

去除一线生产当前代码中与用户口径不一致的额外限制：无设备工序不得被工作站设备强制要求提交设备；设备参数运行态与提交校验必须按同一工序规则；电子签名提示必须明确为所选员工；人员管理创建的正式员工必须进入组长报工管理可见范围。

## Milestones

- [x] 记录 BDD 场景并建立 RED 回归测试
- [x] 修正前端提交上下文和签名提示
- [x] 修正后端设备参数规则与人员 scope 闭环
- [x] 运行定向静态验证并记录证据
- [x] Java/Maven 单元验证解除并发阻塞后重跑
- [x] 技能 evidence validator 校验通过
- [x] 收尾检查并标记完成

## Expected Verification

- 前端静态合同：无设备工序不再 fallback 到 `selectedProcess.deviceId`；签名提示不再要求当前登录账号密码。
- 后端静态或单元合同：运行态参数规则与提交校验均要求当前 `routeProcessId` 精确匹配。
- 后端静态或单元合同：创建正式生产员工时同步维护 `SCOPE_TYPE_EMPLOYEE` 组长负责员工范围。
- 定向测试命令通过，若环境前置缺失则记录阻塞原因。

## Applicable Gates

- 一线运行态 route-start 生产组长来源必须独立于班组设备绑定：工作站正式设备 ID 不等于班组维护设备，不得强制无设备工序提交设备。
- 一线生产正式提交必须单事务落链并按唯一组长归属可见：一线生产不需要匹配工单，但必须按唯一组长归属进入组长报工列表。
- 前端提交前严格验证与草稿态计算隔离门禁：提交前验证只能去除错误限制，不得吞异常或默认成功。

## Current Status

completed: 一线生产额外限制去除已完成，前端/后端静态合同、后端目标 JUnit、技能 evidence validator 和 task-closeout-cleanup 均已通过。

## 设计约束检查

- `是否引入 fallback/降级/吞异常`：否。
- `是否从根因和长期维护角度解决`：是，去除前后端不一致的隐藏限制并保持正式上下文校验。
- `是否存在临时补丁或绕过`：否。
