# 生产组长人员电子签名授权

## 任务目标

在本机 `int_main` 默认业务租户中，为“生产组长 > 人员管理”正式数据源内的全部启用人员开通电子签名授权；不访问或修改测试服、正式服、备用服，不扩大到禁用人员或其他租户。

## 里程碑

- [x] 核对本机数据库、目标租户、生产组长人员正式数据源和电子签名授权表结构。
- [x] 以只读断言形成 RED，确认目标人员中尚未开通电子签名授权的精确集合。
- [x] 在事务内补齐授权并写入授权审计，所有前置与影响行数断言通过后提交。
- [x] 逐人复核授权状态、租户边界、审计记录和非目标数据不变。
- [x] 完成任务清理与最终记录。

## 预期验证

- 本机 MySQL、目标 schema 和正式表结构可用。
- 目标集合只包含当前默认业务租户“生产组长 > 人员管理”中的启用人员。
- 每名目标人员均有唯一、未删除、`electronic_signature_enabled = 1` 且 `authorization_state = 'ENABLED'` 的授权记录。
- 本次新增或更新的授权均有对应授权审计记录，目标人数、变更人数和审计人数一致。
- 不修改其他租户、禁用人员及非目标人员授权。

## 设计约束检查

- `是否引入 fallback/降级/吞异常`：否。
- `是否从根因和长期维护角度解决`：是；按生产组长人员正式数据源和电子签名正式授权表完成精确授权，不通过角色、菜单或默认值替代。
- `是否存在临时补丁或绕过`：否。

## 经验门禁

- `docs/experience-index.md` 存在。
- 适用 `docs/database-rules.md`：写入前核对真实 schema；事务内 DML 后立即保存 `ROW_COUNT()`；前置、目标计数和写后状态不一致时 fail fast 并回滚。
- 适用 `docs/login-access.md` 与 `docs/server-access.md`：未得到远端授权，任务仅操作本机 `127.0.0.1:23306/ruoyi-vue-pro`，不访问远端环境。
- 电子签名授权使用 `dcc_electronic_signature_authorization` 正式链路，不用电子签名管理员角色、菜单权限或默认成功状态代替个人授权。

## Current Status

completed - 本机 tenant 1、leader user 1 下 5 名正式员工已全部获得 `ENABLED` 电子签名授权，3 名临时工原有独立签名密码链路保持有效；授权、审计、租户边界、非目标写入、幂等复跑、经验沉淀和 task-closeout-cleanup preview/apply 均通过。未要求且未执行 Git 暂存、提交或推送。
