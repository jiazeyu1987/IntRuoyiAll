# 测试服务器智能排产问题独立验证

## Task Goal

在测试服务器 `172.30.30.58` 上，通过现有登录会话和真实前端只读路径，独立核对用户列出的智能排产筛选、状态展示、操作可用性、交期风险提示、前置数据校验和权限闭环问题，并给出逐项结论与证据。

## Scope

- 目标环境：测试服务器 `http://172.30.30.58:8081/`。
- 数据来源：测试服务器当前真实页面及只读接口辅助证据。
- 账号来源：浏览器现有登录会话；预期核对页面身份是否为 `zhaojie`，不获取、不记录凭据。
- 操作边界：只读。不得执行入池、手动重排应用、完成、调整交期、冻结、工艺维护提交或任何其它写操作。
- 数据清理：无写入，因此无任务数据需要清理。

## Milestones

- [x] M1：完成规则、经验门禁和测试环境前置核对。
- [ ] M2：通过真实页面复核三个入池状态筛选及其数量、标签和行状态。
- [x] M3：通过部署对应代码复核筛选命名、禁用复选框说明、交期风险提示、空关键日期工单和缺失物料/工序工单的操作边界；真实页面待登录补证。
- [x] M4：通过部署对应代码复核阻断记录的工艺维护入口与 `mes:pro-route:update` 权限闭环；当前账号页面表现待登录补证。
- [x] M5：形成逐项 PASS/FAIL/BLOCKED 验证报告；任务因登录前置缺失暂不收尾为 completed。
- [x] M6：基于用户提供的 `zhaojie` 排产员登录截图，核对工艺路线编辑按钮的真实权限链路并定位缺口。

## Expected Verification

- 真实浏览器打开测试服务器智能排产模块并确认当前账号/租户上下文。
- 对“可入池、阻断、已入池”分别执行页面筛选，记录筛选标签、总数、首屏可见行状态及对应列表请求参数/响应摘要。
- 只读检查工单 `SMOKE-BND-0623212519` 的关键日期、复选框和行操作状态。
- 只读检查非冻结但禁选工单是否存在可见禁用原因。
- 只读检查承诺交期早于计划时间的工单是否存在逾期或交期风险提示。
- 只读检查生产用料清单“缺失”、当前工序“-”时开放的操作及页面说明。
- 只读检查阻断提示、工艺维护入口以及当前账号的 `mes:pro-route:update` 权限表现。
- 记录目标写请求数为 0、页面异常和网络异常。
- 核对 `zhaojie` 的登录权限响应是否包含 `mes:pro-route:update`，并与排产员角色菜单绑定和前端按钮权限判断逐项对照。

## Applicable Experience Gates

- `docs/frontend-development.md#统一列表复合工具栏布局门禁`：逐页签核对多维筛选正式参数；条件草稿、可见标签和已执行结果必须保持可解释的一致性，真实 E2E 不能只断言标签。
- `docs/e2e-rules.md#element-plus-表格选择门禁`：只读检查可见 body 行与业务唯一键；不点击表头全选，不执行写入动作。
- `docs/e2e-rules.md#mes-手动重排全选应用完成门禁`：本任务没有写入授权，仅核对可选性和阻断原因，不执行 preflight/preview/apply。

## 设计约束检查

- 是否引入 fallback/降级/吞异常：否。
- 是否从根因和长期维护角度解决：是；本任务先核对 UI、请求参数、响应数据和权限链路，不以标签或数量单一信号替代正式状态证据。
- 是否存在临时补丁或绕过：否。

## Current Status

blocked

- 当前阶段：已确认测试服务器权限数据根因；测试服务器权限修复属于远端数据写入，当前请求没有授权执行迁移或权限变更。
- 已完成：确认远端发布版本和健康状态；定位筛选草稿标签与已执行结果不一致；确认首屏空条件是现行正式契约；发现旧同步工单静态断言未随 8 月 5 日契约更新；逐项完成代码口径判定并形成 `verification-report.md`。
- 验证证据：测试前端/后端 HTTP 200；部署提交 `19173d2b41442e6b2df3c16c424c11713b3dbbcc`；前后端状态字符串精确映射一致；`20260805-standard-list-empty-tabs` 真实 E2E 记录同步工单首屏仅分页参数。
- 当前新增证据：`zhaojie` 是 tenant `1` 的启用用户 `1074`，有效绑定启用的 `排产员/mes_scheduler` 角色 `910216`；该角色的父菜单 `5720` 和查询权限 `5721` 有效，但更新权限 `5723/mes:pro-route:update` 的 `system_role_menu.deleted=1`，版本查询菜单 `5730` 在测试库不存在。
- 根因：测试库未应用 `20260716_mes_route_version_permission_menu` 和 `20260728_mes_scheduler_route_flow_list_permission`；后者正是为已应用旧版角色范围的环境补齐 `5723/5730`。因此截图中的空操作列是测试服权限迁移缺失，不是账号名称、前端按钮代码或排产员角色绑定错误。
- 剩余阻塞：需用户明确授权测试服务器数据迁移；授权后应执行两条现有正式迁移、核对影响范围、刷新该用户权限缓存，并由 `zhaojie` 重新登录做真实页面 GREEN 验证。
