# QA 检验项目级首检巡检展示修复

## Task Goal

修复 QA 配置页把首检、巡检按全局规则或抽样文字模糊展示的问题，使同一 QA 工序下的每个检验项目直接展示并维护自己的首检启用状态、首检固定数量和巡检启用状态、巡检比例；无后端配置时明确显示未配置且不生成默认任务预览。

## Milestones

- [x] M1：冻结当前不满足项并建立 BDD/RED 回归证据。
- [x] M2：实现检验项目级结构化首检、巡检显示与保存。
- [x] M3：实现未配置空态和按工序、检验项目、检验类型展开的 QA 页预览。
- [x] M4：完成聚焦回归、类型检查、页面验证和证据校验。
- [x] M5：完成经验沉淀与任务清理收尾。
- [x] M6：重启后在 `int_main` 安全集成 task-owned QA 显示改动，并完成合入前聚焦回归。

## Expected Verification

- 聚焦静态合同先 RED 后 GREEN，覆盖项目级首检/巡检控件、结构化后端字段、未配置空态和项目级预览。
- QA 相邻静态合同通过。
- `pnpm ts:check` 通过，或明确记录与本任务无关的既有阻塞。
- 可用运行态存在时，通过真实 QA 页面验证加载、空态、项目级数量和预览；缺少真实数据或运行前置时按规则记录阻塞，不使用 mock。
- `git diff --check` 通过。
- bug regression 与 frontend feature evidence validator 通过。

## Experience Gate

- 已读取 `docs/experience-index.md`，命中“QA 适用检验类型、首检数量、巡检比例、samplingPlanText、末检项目级统一开关”。
- 原有“同一工序内部数量唯一”口径已更新为“同一工序下每个检验项目独立”，并同步更新经验索引。
- 保留门禁：`patrolInspectionRatio` 使用百分比原值，页面不得二次除以 100；抽样方案原文保持独立正式字段，不能由结构化数量反向拼装冒充原文；末检适用性保持项目级统一。

## 设计约束检查

- `是否引入 fallback/降级/吞异常`：否。
- `是否从根因和长期维护角度解决`：是；直接使用后端检验项目级结构化字段作为 QA 页面事实来源，停止由前端抽样文字和全局默认规则推算正式显示。
- `是否存在临时补丁或绕过`：否。

## Current Status

completed：实现、聚焦回归、相邻回归、类型检查、真实页面只读验证、证据校验、经验沉淀及任务清理均已完成；2026-08-18 在独立 worktree `D:\IntRuoyiWorktree\20260818-qa-item-inspection-display` 以 8084/48084 重新验证真实 QA 页面，原“已有配置实值验证超时”阻塞已闭合；重启后已在 `E:\IntRuoyi` 的 `int_main` 安全集成 task-owned QA 前端、测试和任务记录改动。
