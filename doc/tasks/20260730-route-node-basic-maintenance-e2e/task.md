# 工艺路线节点基础维护真实 E2E

## Task Goal

使用 Playwright 通过 `http://127.0.0.1:8081` 的真实页面，在租户 `1` 下完成固定工艺路线的前置复位、新增、详情核验和删除清理闭环。

## Milestones

- [x] 读取本地登录、E2E、运行态、PowerShell、Git 与收尾规则。
- [ ] 核对本机前后端、浏览器、登录和租户前置条件。
- [ ] 通过真实页面完成固定路线前置复位。
- [ ] 通过真实页面新增路线并核验详情页三类信息。
- [ ] 通过真实页面删除固定路线并确认搜索无结果。
- [ ] 写入验证报告并完成任务收尾。

## Expected Verification

- 固定名称 `测试节点-工艺路线-基础维护` 在新增前不存在；若存在，必须从页面删除到无结果。
- 新增保存后按固定名称只命中唯一记录。
- 详情页可见固定编码 `TN-ROUTE-BASIC-001`、固定名称、基础信息、流转关系图和关联产品页签。
- 测试结束后从页面删除固定路线，再次搜索固定名称无结果。

## Applicable Gates

- E2E 必须使用真实前端页面；API 仅可用于最终只读核验。
- 写入型测试必须使用目标租户并创建可追踪、可清理的固定测试数据。
- 删除入口、登录、权限、选择器、服务或数据前置缺失时立即阻塞，不得切换账号、租户、端口或 API-only。
- Element Plus 表格操作必须按可见业务唯一文本定位目标行，写入前核对目标集合。
- 测试节点必须完成前置复位、页面操作、页面可见验证和清理恢复闭环。

## 设计约束检查

- `是否引入 fallback/降级/吞异常`：否。
- `是否从根因和长期维护角度解决`：是；本任务仅执行正式真实页面验收，不修改产品代码。
- `是否存在临时补丁或绕过`：否。

## Current Status

`in_progress`

