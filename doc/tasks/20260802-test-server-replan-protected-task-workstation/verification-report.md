# Verification Report

## Summary

测试服手动重排报错已修复并复测通过。根因是第三方报工导入后正式报工有工作站，但受保护任务 `mes_pro_task.workstation_id` 没有反填，同时测试工作站缺少产线绑定。

## Results

- 修复前复现：4 条目标任务有正式报工且报工工作站单一，但任务工作站为空；4 个目标测试工作站产线为空。
- 数据修复：事务精确更新 4 条任务工作站和 4 个工作站产线/车间绑定，提交后 `post_validation_ok`。
- 真实页面复测：`881MO093613` 手动重排应用成功，生成 81 个任务，保留报工保护任务 `925855/925854`，`blockingIssueCount=0`。
- 真实页面复测：`881MO093615` 手动重排应用成功，生成 82 个任务，保留报工保护任务 `926006/925964`，`blockingIssueCount=0`。
- 数据库后验：目标阻断消息扫描计数为 0，最新操作日志包含两个目标排产工单的 `REPLAN_APPLY`。

## Evidence

- `doc/tasks/20260802-test-server-replan-protected-task-workstation/replan-real-e2e-result.json`
- SQL 后验：目标任务工作站和产线均可解析，排产工单进度分别为 `5.896154%` 和 `2.896154%`。
- 追加工作站同步：本地存在对应工作站 `922725/922726/922727/922731`，测试服事务同步工作站 4 行、设备绑定 7 行、设备工序产能 7 行。
- 追加工作台产能复验：`Z2772=420.000000`、`Z2510=2340.000002`、`Z2775=270.000003`、`Z2971=254.999997`。
- Evidence validators：数据库证据与回归证据 validator 均 PASS；经验门禁已合并到 `docs/backend-development.md` 并通过关键词与 UTF-8 校验。
- 临时工作站对齐：保留测试服被正式报工/任务引用的工作站主键，更新 8 个临时工作站为 `WS-XLSX-00002-*` 口径，新增设备绑定 14 行、设备工序产能 14 行。
- 最终产能复验：12 个截图工序班次产能与本机一致，分别为 `960/420/600/494/2340/1950/270/310/120/960/255/480`。
- 全量继续检查：测试服当前 WIP 26 条，本机 WIP 40 条；按测试服当前 WIP 与本机同工序对比，修复前仍有 14 条差异。
- 全量修复：更新 10 个临时工作站为本机正式口径，插入 4 个缺失工作站，新增设备绑定 23 行、设备工序产能 23 行。
- 全量复验：`TEST_WIP_ROWS=26`、`LOCAL_WIP_ROWS=40`、`MISMATCH_COUNT=0`，测试服当前 WIP 工序资源与班次产能已全部与本机对应工序一致。
- 收尾校验：数据库/回归 evidence validator 通过；UTF-8 读取通过；测试服临时存储过程残留为 0。

## Remaining Risks

- 重排预览仍提示物料缺料和最晚开工时间 WARNING，但不是阻断，不影响本次工作站/产线错误修复。
- 当前工作区存在大量既有未提交改动和分支 ahead 状态，本任务未提交/推送。
