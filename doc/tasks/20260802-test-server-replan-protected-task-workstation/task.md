# 测试服重排受保护任务工作站修复

## Task Goal

修复测试服务器手动重排弹窗中多条 `受保护任务未绑定工作站` 阻塞，确保第三方报工导入后已产生正式报工的任务在重排保护逻辑下具备可追溯的任务工作站与产线资源。

## Milestones

- [x] 建立任务审计目录，记录目标、门禁、BDD/TDD 和回滚方案。
- [x] 只读复现并定位阻塞任务、正式报工、工作站和产线缺口。
- [x] 执行精确测试服数据修复，限制到本次截图对应受保护任务。
- [x] 复验数据库状态与测试服手动重排，不再出现工作站/产线绑定阻塞。
- [x] 记录最终验证和剩余风险。
- [x] 对比本地与测试服当前路线工序工作站，精确同步导致班次产能为 0 的缺失工作站。
- [x] 复验排产员工作台工序列表四个目标工序班次产能不再为 0。
- [x] 将测试服剩余 8 个临时工作站调整为本机同工序正式工作站口径。
- [x] 复验排产员工作台 12 个截图工序班次产能与本机一致。
- [x] 全量复核测试服当前 26 个 WIP 工序与本机对应工序，修复剩余 14 条不一致。
- [x] 复验测试服当前 WIP 工序工作站编码、班次小时、设备绑定数和班次产能与本机一致。

## Expected Verification

- RED：测试服只读查询能复现受保护任务 `mes_pro_task.workstation_id IS NULL`，且正式报工 `mes_pro_feedback.workstation_id` 已存在。
- GREEN：精确修复后，目标任务 `workstation_id` 与正式报工单一工作站一致，相关工作站具备非空 `production_line_id`。
- REGRESSION：测试服手动重排对同一排产工单不再返回 `受保护任务未绑定工作站` 或 `受保护任务未绑定产线`；若后续阻塞为“已满/容量不足”，视为通过当前问题门禁。

## Current Status

ready_for_closeout

## Root Cause

测试服第三方导入已经创建正式报工，且每个目标任务只有一个正式报工工作站；但 `mes_pro_task.workstation_id` 仍为空。手动重排保护已报工任务时，后端会先检查受保护任务工作站，因此直接阻塞为 `受保护任务未绑定工作站`。进一步只读检查还发现 4 个测试工作站均缺少 `production_line_id`，若只修任务工作站会继续阻塞为 `受保护任务未绑定产线`。

## Target Scope

- `PT-3802` / task `925854` -> feedback workstation `922066`
- `PT-3803` / task `925855` -> feedback workstation `922058`
- `PT-3912` / task `925964` -> feedback workstation `922073`
- `PT-3954` / task `926006` -> feedback workstation `922068`
- Test production line: `900040 / AUTO-LINE-01`
- Test workshop: `900011 / 车间1`
- Calendar plan: `900030 / AUTO-PLAN-01`

## Final Verification

- SQL 修复事务提交成功：`mes_pro_task.workstation_id` 更新 4 行，`mes_md_workstation.production_line_id/workshop_id` 更新 4 行。
- Playwright 真实页面复测通过：`881MO093613` 和 `881MO093615` 均从排产工单列表勾选后点击“手动重排 -> 确认应用重排”，观察到 `preflight`、`replanPreview`、`replanApply` 三个真实接口。
- 重排结果：`881MO093613` 应用成功，生成 81 个任务，保留报工保护任务 `925855/925854`，`blockingIssueCount=0`。
- 重排结果：`881MO093615` 应用成功，生成 82 个任务，保留报工保护任务 `926006/925964`，`blockingIssueCount=0`。
- 数据库后验：目标任务工作站和产线均可解析为 `AUTO-LINE-01`，目标阻断消息扫描计数为 0。
- 追加工作站同步：测试服事务插入工作站 4 行、工作站设备绑定 7 行、设备工序产能 7 行。
- 追加工作台产能复验：`Z2772=420.000000`、`Z2510=2340.000002`、`Z2775=270.000003`、`Z2971=254.999997`，四个目标工序班次产能均不再为 0。
- 临时工作站口径修复：保留被正式报工/任务引用的测试服主键，更新 8 个工作站字段，新增工作站设备绑定 14 行、设备工序产能 14 行。
- 最终工作台产能复验：截图 12 个工序班次产能与本机一致，分别为 `960/420/600/494/2340/1950/270/310/120/960/255/480`。
- 全量差异复核：测试服当前 WIP 共 26 个工序，本机 WIP 共 40 个工序；按测试服当前 WIP 工序与本机同 `route_code + process_code + process_id` 对比，仍发现 14 条不一致。
- 全量修复：测试服保留被报工/任务引用的 10 个临时工作站主键并改为本机 `WS-XLSX-00002-*` 口径，另插入 4 个测试服缺失的正式工作站；新增工作站设备绑定 23 行、设备工序产能 23 行。
- 全量复验：测试服当前 26 个 WIP 工序 `workstation_codes`、`shift_hours`、`machine_bindings`、`computed_shift_capacity` 与本机对应工序差异数为 `0`。

## Remaining Notes

- 重排预览仍有物料缺料和最晚开工时间 WARNING，但不是阻断，也不是本次截图中的工作站/产线错误。
- 当前工作区存在大量既有未提交改动和分支 ahead 状态，本任务未执行提交/推送。
- 2026-08-02 追加范围：测试服排产员工作台工序列表中 `Z2772`、`Z2510`、`Z2775`、`Z2971` 班次产能为 0；仅同步本地同租户同 `process_id` 的正式工作站，不覆盖测试服其它非 0 工序。
- 2026-08-02 再次追加范围：测试服剩余 8 个工序仍使用 `TPFB-WS-20260802-*` 临时工作站，班次小时为 7.00；用户要求改成与本机一致。因这些临时工作站已被正式报工/任务引用，本次保留主键，仅将工作站字段、设备绑定和设备工序产能调整为本机同工序口径。
- 2026-08-02 全量追加范围：只以测试服当前 WIP 工序为修复目标；本机额外存在但测试服当前 WIP 不存在的路线/工序未同步，避免扩大数据范围。

## 设计约束检查

- `是否引入 fallback/降级/吞异常`：否。修复只写入正式任务资源绑定，不伪造重排成功。
- `是否从根因和长期维护角度解决`：是。按正式报工记录反填受保护任务工作站，并补齐工作站产线前置条件。
- `是否存在临时补丁或绕过`：否。数据修复限制在测试服务器、精确任务 ID 和测试导入产生的工作站范围内。

## Applicable Gates

- 数据库写入前必须核对真实 schema、目标租户、目标行和回滚路径。
- 写入型测试服数据修复前后必须检查目标范围稳定，不能扩大 WHERE 范围。
- 第三方报工导入链路必须落到正式报工和正式任务进度，禁止直接写进度伪造成功。
