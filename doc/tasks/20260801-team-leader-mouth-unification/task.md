# 生产班组长口径统一

## Task Goal

根据用户最新裁定，将生产班组长每日操作与既有班组长业务统一为同一套产品口径，更新变更记录、旧班组长 PRD、生产全流程角色文档和生产班组长每日操作文档。

## Milestones

- [x] 读取变更评估、产品需求、任务收尾和编码规则。
- [x] 记录用户裁定的正式口径。
- [x] 更新相关需求文档。
- [x] 验证关键口径、UTF-8 和空白错误。

## Expected Verification

- UTF-8 读取更新后的 Markdown 文档。
- 搜索确认关键口径：本地生产订单列表、纸质订单编号、夜间同步、调拨单列表、活跃订单加入时间、生产系数、设备台账、上下限不覆盖原始提交。
- 执行 `git diff --check`。

## Current Status

completed

## 设计约束检查

- `是否引入 fallback/降级/吞异常`：否。
- `是否从根因和长期维护角度解决`：是，直接统一正式产品口径，不通过兼容旧解释绕过。
- `是否存在临时补丁或绕过`：否。
