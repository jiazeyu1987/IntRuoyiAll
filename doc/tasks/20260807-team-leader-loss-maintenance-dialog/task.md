# 生产组长损耗统一维护弹框

## 任务目标

- 将工序配置每行重复的新增、修改、删除损耗按钮合并为一个“损耗”按钮。
- 在单一弹框内展示当前路线工序的损耗列表，支持行内修改、确认删除和列表底部新增。
- 移除顶部通用“新增”中的损耗原因入口，损耗维护只保留行级统一入口。

## 里程碑

- [x] M1：确认现有页面、正式 API、相邻测试和并发修改基线。
- [x] M2：补充聚焦静态合同并取得预期 RED。
- [x] M3：实现统一维护弹框并取得 GREEN。
- [ ] M4：更新真实 Playwright 路径并完成相关回归（脚本与静态回归完成，真实写入前置阻塞）。
- [ ] M5：技能证据、验证报告和经验沉淀已完成；任务收尾受真实 E2E 前置阻塞。

## 预期验证

- 聚焦静态合同锁定单一“损耗”入口、当前工序列表、单行编辑、确认删除和底部新增。
- 相邻统一工序配置、自动编号、顶部新增入口静态合同通过。
- `pnpm ts:check`、真实 E2E 脚本语法检查和 `git diff --check` 通过。
- 具备正式测试租户、生产组长账号、合法运行态和可清理数据时，Playwright 通过真实页面完成新增、修改、删除闭环；缺少任一前置时明确记录 BLOCKED。
- `frontend-feature-evidence.md` 通过技能 validator。

## 设计约束检查

- 是否引入 fallback/降级/吞异常：否；所有写操作继续调用正式 API，失败保持弹框和编辑内容并显示正式错误。
- 是否从根因和长期维护角度解决：是；以 `routeProcessId` 固定维护上下文，正式列表刷新后重新计算当前行，不持有陈旧列表对象。
- 是否存在临时补丁或绕过：否。

## 经验门禁

- 已读取 `docs/experience-index.md`，适用“前端按钮文案与行为一致性门禁”“前端静态契约隔离门禁”“Windows 换行与脚本行为同步”和“表格行定位”。
- 聚焦合同必须同时锁定按钮文案、稳定 `data-*` 锚点、点击处理器、正式列表和写入方法，并禁止旧新增、修改、删除按钮继续存在。
- Playwright 必须按可见业务行或稳定 `routeProcessId` 锚点定位，不使用 API 数组下标映射页面行。

## 并发基线

- 用户于 2026-08-07 确认当前任务可以执行。
- 保留既有任务 `20260807-loss-reason-description-display` 已产生的损耗描述展示改动及其静态合同，不修改、不回滚其任务文档。

## Current Status

blocked：实现、聚焦/相邻静态合同、独立复审、真实脚本语法、`pnpm ts:check`、技能证据校验和 `git diff --check` 已通过；缺少允许写入的测试租户、生产组长账号、匹配的前后端运行地址、目标工序可见业务标识与正式 `routeProcessId`，无法执行真实新增、修改、删除闭环。未使用管理员、mock、API-only 或其它降级路径，未进入 `ready_for_closeout`。
