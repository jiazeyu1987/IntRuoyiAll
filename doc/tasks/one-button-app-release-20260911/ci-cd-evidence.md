# 发布按钮流水线证据

## Environment

## Environments and deployment targets

- 本轮仅在本机应用 worktree `D:/IntRuoyiWorktree/r260911-release-button/a` 修复与验证。
- 后续真实目标仍仅限测试服 `172.30.30.58` 的 `app-release/without-data` publish-test。
- 未操作正式服、审查服、MinIO 数据同步、全量数据库复制或生产晋级。

## Commands

## Build, test, lint, package, deploy, and rollback commands

- Backend targeted regression: `mvn -f IntRuoyiBackend/pom.xml -pl yudao-module-infra -am "-Dtest=RuntimeControlServiceImplTest,ReleaseWorkflowOrchestratorTest" "-Dsurefire.failIfNoSpecifiedTests=false" test`
- Frontend static contract: `node tests/e2e/runtime-control-one-button-static.spec.cjs`
- Frontend type check: `corepack pnpm run ts:check`
- Forbidden-source scan: `rg -n "script/deploy/publish-int-ruoyi.ps1|code-only|with-data|releaseWorkflows\.value\[0\]" <runtime-control source/test roots>`
- Package/deploy command: not run in this corrective slice. R80 is retired; the next package must use a new releaseTag.
- Rollback command: not applicable because this slice did not deploy or switch runtime.

## Pipeline

## Pipeline files changed

- Runtime workflow/service contract under `IntRuoyiBackend/yudao-module-infra/src/main/java/.../runtimecontrol`.
- Local release workflow config in `IntRuoyiBackend/yudao-server/src/main/resources/application-local.yaml`.
- Runtime control frontend page/API type under `IntRuoyiFronted/src/.../runtime-control`.
- Regression contracts under `IntRuoyiBackend/.../RuntimeControlServiceImplTest.java`, `ReleaseWorkflowOrchestratorTest.java`, and `IntRuoyiFronted/tests/e2e/runtime-control-one-button-static.spec.cjs`.

## Secrets

## Required secrets and owners

No new secrets, credentials, registry accounts, service accounts, domains, or external owners were introduced. NAS and server secrets are not changed or recorded.

## Artifact and release output

No release artifact was generated by this corrective slice. The output is source/test correction only. R80 remains non-reusable.

## RED evidence for new config checks

- Maven RED: missing release workflow context fields and working directory contract.
- Frontend RED: static contract caught `releaseWorkflows.value[0]`.

## Verification

## GREEN verification evidence

- Static contract PASS.
- Backend targeted tests PASS, 80 tests.
- Frontend `ts:check` PASS.
- Forbidden-source scan PASS, no old script path, old scope, or first-workflow operation source remains in target roots.

## Blockers

## Manual approvals and blockers

User authorized repair in the direction of a general, long-lived release button. No production or backup promotion approval exists. Blocker before publish-test: application fix must be committed, then maintenance build must use a new releaseTag.

## Rollback

## Rollback procedure and validation

No runtime rollback is needed for this slice because no environment changed. If the next publish-test fails, retire that releaseTag, freeze evidence, fix root cause, and rebuild a new immutable releaseTag.
