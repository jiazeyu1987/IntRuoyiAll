# 一线检验方法详情字段调整

## 任务目标

- 将检验方法详情弹窗顶部红框区域改为展示当前 QA 检验项目的“抽样方案”。
- 将右侧黄框区域改为展示当前 QA 检验项目的“检验器具及设备”。
- 两处展示均直接使用 QA 检验项目列表的正式字段，不使用推断、占位或 fallback。
- QA 来源项目中的业务工序即使尚未识别或绑定批记录表单也必须继续显示，并能进入正式 QA 保存/发布载荷；不得猜测其与其它路线工序的映射。

## 非目标

- 不为未识别业务工序伪造批记录表单绑定，不把“组装螺杆八组件”等名称猜测映射到组装Ⅰ/Ⅱ或其它路线工序。
- 不修改后端契约，除非现有正式响应确实缺少所需字段；若缺少则先阻塞并报告。
- 不调整弹窗之外的页面设计。

## 里程碑

- [x] M1：建立任务文档并记录验收行为。
- [x] M2：定位弹窗、QA 项目响应及正式字段映射。
- [x] M3：补充聚焦测试并取得 RED 证据。
- [x] M4：实现正式字段链路并取得 GREEN/回归证据。
- [ ] M5：完成真实 Playwright 验收与最终收尾（当前阻塞）。

## 预期验证

- 聚焦静态合同先失败后通过，证明顶部展示“抽样方案”、右侧展示“检验器具及设备”，且字段来自当前 QA 检验项目。
- 聚焦静态合同先失败后通过，证明未识别批记录绑定的 QA 业务工序继续显示并归入当前正式 QA 质检工序载荷，已识别路线工序仍按原身份分组。
- 运行相邻静态合同或相关前端测试。
- 运行 `pnpm ts:check`。
- 运行 `git diff --check`。
- 若本机真实页面前置齐备，按项目 E2E 规则使用 Playwright 验证截图对应弹窗；若缺失正式入口或运行态前置则明确记录 blocker，不以 API 或 mock 替代。

## 适用经验门禁

- 先从截图目标区域追溯映射函数、正式响应 VO 与字段来源；用户可见内容直接读取正式名称/描述字段，禁止以编码、默认值或其它字段补齐。
- QA/PQC 检验详情必须区分抽样方案、检验方法、检验器具及设备等正式 QA 项目字段，不得用现有卡片文案或来源标签推断。
- 静态合同应使用稳定 DOM/函数边界，避免宽泛锚点把相邻模板内容误计入。

## 设计约束检查

- `是否引入 fallback/降级/吞异常`：否。
- `是否从根因和长期维护角度解决`：是；从 QA 检验项目正式字段修正展示映射。
- `是否存在临时补丁或绕过`：否。

## Current Status

blocked：已在“芋道源码”租户完成本轮真实 Playwright 复验。QA 来源页选择 `IDI / 按压式球囊扩充压力泵 / 1` 后真实显示“组装螺杆八组件”“光固外套四组件”“检验器具及设备”“抽样方案”，首行正式值为 `目测` 与 `GB/T 2828.1，I，AQL=0.4`。一线 PQC 中存在 5 个同产品 `CODX-AO5-*` 活跃订单，抽查 3 个均被正式设备账号上下文拒绝：`routeProjectItems routeId=980091，missingItemIds=[14]`，因此检验方法按钮和详情弹窗无法渲染。最终 QA 写请求、业务数据写请求、目标网络失败、控制台错误和页面错误均为 0；仅有一次源码确认不持久化的 PQC 员工上下文解析 POST。另有后端历史空字段产品口径冲突仍未解决，任务不能判定完整 E2E 通过。
