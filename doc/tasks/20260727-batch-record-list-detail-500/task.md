# 修复批记录表单详情 500

## 任务目标

修复 `MES 系统 > eDHR批记录 > 批记录表单` 页面选择已发布表单后，右侧详情请求返回 HTTP 500 的问题，恢复可查看、打开、编辑、填写等正常详情能力。

## 里程碑

- [x] 通过真实页面网络请求和后端日志稳定复现 500
- [x] 定位失败接口、异常栈和根因
- [x] 取得先失败的页面与运行 Jar 完整性证据
- [x] 通过标准启动脚本切换到不可变运行 Jar
- [x] 通过真实页面和构建后回归复验
- [ ] 完成任务清理、提交和推送

## 预期验证

- 目标回归测试具备明确 RED/GREEN 证据
- 受影响模块目标测试和相邻回归通过
- Playwright 从真实批记录表单页面选择目标行后，详情接口不再返回 500
- 页面右侧详情正常显示，控制台无目标请求 500

## Current Status

ready_for_closeout

Git 收尾阻塞：当前 `int_main` 存在多个并行任务的未提交改动，其中 `docs\experience-index.md` 和后端测试文件已由其它任务修改。按任务所有权规则不能制作包含其它任务资产的脏工作区基线提交，也不能跳过基线门禁直接提交本任务记录。

## 设计约束检查

- `是否引入 fallback/降级/吞异常`：否
- `是否从根因和长期维护角度解决`：是；必须依据真实异常栈修复正式数据或代码链路
- `是否存在临时补丁或绕过`：否

## 经验门禁

- 主列表成功后详情/辅助请求失败，错误必须归属右侧详情区域，不得清空列表、吞异常或返回默认成功。
- 500 若由 schema、正式数据源或绑定来源缺失导致，必须补齐正式链路，不得用空值、默认模板或其它表单绑定猜测。
- 真实复验必须走 `http://127.0.0.1:8081` 页面和当前 `48081` 后端，不得用 API-only 代替。

## Cleanup Keep

- doc/tasks/20260727-batch-record-list-detail-500/bug-regression-evidence.md
