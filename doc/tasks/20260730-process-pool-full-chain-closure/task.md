# 生产一线报工与资源池全链路补齐

## Task Goal
补齐“绑定数据源 → 一线页面真实提交 → PQC 入池 → FIFO 编排 → 审核副本自动规则 → 班组长工作台 → 完整 E2E”的正式链路，并尽量复用当前报工、记录本、批记录、资源池和电子签名体系。

## Milestones
- M1: 完成当前系统差距复核、PRD 和验收标准。
- M2: 完成依赖图、测试计划和任务状态文件。
- M3: 补齐绑定数据源和一线真实提交链路。
- M4: 补齐 PQC 入池、FIFO 工单消费、审核副本自动规则。
- M5: 补齐班组长工作台入口与完整链路 E2E。
- M6: 完成验证、经验沉淀、提交、推送和收尾。

## Expected Verification
- 后端目标 JUnit / 静态合同覆盖绑定、PQC、FIFO、审核副本规则。
- 前端静态合同或组件测试覆盖一线提交、PQC、班组长工作台。
- Playwright 真实路径 E2E 覆盖报工提交、记录本写入、PQC 入池、FIFO 分配、审核副本和班组长查看。
- `git status --short --branch` 不再显示本任务未提交改动，最终推送到 `origin/int_main`。

## Current Status
blocked

## 设计约束检查
- 是否引入 fallback/降级/吞异常：否。
- 是否从根因和长期维护角度解决：是，要求补齐正式数据链路和真实页面提交，不允许模拟成功。
- 是否存在临时补丁或绕过：否。

## Applicable Experience Gates
- PowerShell / Git 编排：提交前必须复核分支、remote、staged 清单，禁止 `&&`，逐条记录退出码。
- 脏工作区基线：已在本任务开始前保存既有并行改动基线，后续实现提交必须只包含本任务文件。
- E2E 入口存在性：完整 E2E 不能用静态合同或 API-only 冒充真实页面路径。
- 前端静态合同隔离：新增页面/提交链路应先用目标静态合同建立 RED/GREEN，避免被无关全量失败掩盖。
- 数据库迁移门禁：菜单、权限、资源池 schema 或 SQL 变更必须读取数据库规则并提供迁移/静态校验证据。

## Current Blocker
- 2026-07-30: `IntRuoyiFronted/src/views/mes/pro/feedback/FrontlineFixedTemplatePanel.vue` 在本任务基线提交后再次出现非本任务改动；该文件是一线真实提交的核心入口，继续直接修改会混入或覆盖并行任务改动。
- 影响：当前无法安全补齐“一线页面真实提交 / PQC 提交”相关前端代码，后续 FIFO、审核副本和 E2E 也无法形成可信验收。
- 所需决策：等待并行任务完成并提交，或由用户明确授权本任务接管该文件并把现有差异作为当前基线合并处理。