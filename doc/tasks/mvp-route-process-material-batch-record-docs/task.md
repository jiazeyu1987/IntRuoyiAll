# MVP 工艺路线工序物料批记录文档重写

## Task Goal
重写一份最小 MVP PRD 文档和用户操作文档，明确物料绑定放在工艺路线的工序维护中，批次执行使用订单创建/启动时的路线版本快照，并删除/废弃之前已实现但方向不一致的物料逻辑设计说明。

## Milestones
- [x] 定位现有相关文档、已实现逻辑和适用项目经验门禁
- [x] 重写 PRD 文档，限定最小 MVP 范围和非目标
- [x] 重写用户操作文档，覆盖管理员配置与一线生产提交
- [x] 验证文档结构、关键词和 UTF-8 读取

## Expected Verification
- 使用 PRD 技能校验脚本检查产品文档结构。
- 使用 rg 核对新文档包含 MVP、工艺路线工序维护、灰色/绿色、完成数量、ERP 同步表单、最小值、快照、审批流、删除旧物料逻辑等关键要求。
- 使用 UTF-8 方式重新读取关键文档。

## Current Status
completed

## 设计约束检查
- 是否引入 fallback/降级/吞异常：否。系统内没有 ERP 同步表单数据时按“没有数据”处理，不从 ERP 实时读取。
- 是否从根因和长期维护角度解决：是。统一把版本归属放到工艺路线版本，工序本身不维护多版本。
- 是否存在临时补丁或绕过：否。

## 适用经验门禁
- `docs/backend-development.md#一线生产正式提交必须单事务落链并按唯一组长归属可见`：多物料提交不能用单行报工冒充逐物料事实；运行态和服务端快照必须携带同一冻结物料集合；正式请求必须一次提交完整且不重复的物料明细；工序进度取各物料完成数量最小值；批号只按生产订单号和物料编码读取系统内同步事实，不直连 ERP、不读取库存替代源。
- `docs/frontend-development.md#工艺路线附加表单显式全局联动门禁` 与项目术语契约：本功能不得把“批记录物料”混同为表单槽位 `formBindings`、工序开始或批记录表单绑定。
- `docs/e2e-rules.md#工艺路线过程检验映射正式来源门禁`：涉及路线工序映射时必须使用当前路线的正式工序身份，不能按名称或相邻工序猜测。
