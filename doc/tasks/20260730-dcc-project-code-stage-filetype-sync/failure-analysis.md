# Task 35 失败明细分析

## 分析范围

- 来源文件：`task-35-ambiguous-recognition-records.xlsx`、`task-35-unclassified-recognition-records.xlsx`、`task-35-final-verification.json`。
- 分析方式：只读解析导出文件和现有后端识别逻辑；未修改测试服数据、未重跑任务、未直接写 SQL、未改代码。
- 结论状态：仍为 `blocked`。

## 计数核对

- 批量任务 `35`：`ambiguousCount=1207`、`unclassifiedCount=7491`、`successCount=6292`、`failedCount=0`、`conflictCount=0`。
- 歧义导出：`1207` 条，与任务计数一致。
- 未识别导出：`7366` 条，比任务计数少 `125` 条；需要在正式修复前复核导出接口是否仅导出部分记录或是否存在分页/权限过滤。
- 任务后 DCC 项目代码候选复扫：`candidateTotal=8736`、`candidateProjectCount=93`，仍不满足 GREEN。

## 歧义高频原因

- `过程性能确认（PQ）报告、工序卡/作业指导书`：410 条。
- `过程运行确认（OQ）报告、工序卡/作业指导书`：408 条。
- `过程性能确认（PQ）方案、工序卡/作业指导书`：374 条。
- `过程运行确认（OQ）方案、工序卡/作业指导书`：14 条。
- `生产用设备清单、检验用设备清单`：1 条。

## 未识别高频形态

- 文件扩展名分布：`.pdf=4794`、`.sldprt=924`、`.docx=407`、`.slddrw=348`、`.xlsx=211`、无扩展名 `199`、`.sldasm=167`。
- 高频文件名/规范化名：`冠脉球囊扩张导管=67`、`图纸=56`、`无针加药胶塞=55`、`单向阀片=37`、`锁紧盖无内芯=34`、`内螺护帽=32`、`公鲁尔接头=30`。
- 高频目录线索集中在输出、验证、确认、转移相关目录；但当前识别逻辑实际只使用文件名、标题、文件编号，不使用目录阶段线索进行文件类别打分。

## 识别逻辑核对

- 当前启用类别候选来自 `active=true` 且 `fileTypeTaxonomyId != null` 的 `dcc_file_category`。
- 当前匹配文本仅来自受控文件的 `fileName`、`title`、`fileNumber`，会去掉扩展名并归一化中英文括号和空格。
- 当前匹配规则只使用类别名称及代码内置 `CATEGORY_MATCH_ALIASES`；数据表 `dcc_file_category` 没有可维护的别名字段。
- 同一最高分命中多个类别时直接返回 `AMBIGUOUS`；无任何类别名/内置别名命中时返回 `UNCLASSIFIED`。

## 正式修复前置条件

- 需要业务确认 OQ/PQ 与“工序卡/作业指导书”的歧义拆解规则：例如 `OQ报告/PQ报告/OQPQ方案` 应优先命中过程确认类，而不是工序卡类。
- 需要业务确认未识别高频文件名应归属的正式类别：例如 `图纸`、零配件名称、培训/发放/变更记录、OQ/PQ 参数组、验证方案/报告。
- 若仍坚持“不改代码”，则当前系统没有数据级别别名配置入口可覆盖上述缺口；只能通过正式类别名称/文件元数据调整解决，风险较高且不建议自动执行。
- 若允许代码变更，需要另开正式改造任务，为文件类别增加可维护匹配别名或调整现有匹配评分，并按 BDD/TDD 覆盖歧义与未识别样本。

## 禁止动作

- 禁止直接 SQL 修数。
- 禁止循环调用单文件分类或元数据更新接口来绕过分类规则。
- 禁止把 `AMBIGUOUS` 或 `UNCLASSIFIED` 当作成功。
- 禁止跨租户复制规则或修改角色来掩盖规则缺口。
