# 验证报告

## 结论

PASS：当前目标账套凭据可以建立金蝶 WebAPI 会话，并可通过 `ExecuteBillQuery` 查看 `PRD_MO` 生产订单数据。

## 查询结果

| 生产订单号 | 单据状态 | 日期 | 物料编码 | 物料名称 | 数量 |
| --- | --- | --- | --- | --- | ---: |
| `881MO100631` | `A` | 2026-08-07 | `YXN.052.011.1003` | 支撑导管 | 181 |
| `882MO012868` | `C` | 2026-08-05 | `Y03.05.0293` | PTFE涂层不锈钢芯轴（0.35mm） | 400 |
| `881MO100630` | `C` | 2026-08-07 | `YXN.052.011.1004` | 支撑导管 | 181 |
| `881MO100629` | `C` | 2026-08-05 | `PY007.001.104` | 一次性使用电子输尿管内窥镜导管 | 2000 |
| `881MO100628` | `C` | 2026-08-04 | `PY007.001.100` | 一次性使用测压输尿管内窥镜导管 | 1500 |

## 接口证据

- 登录接口：HTTP 200，`LoginResultType=1`，`IsSuccessByAPI=true`，有会话 Cookie。
- 查询接口：HTTP 200，返回数组 5 行，无错误对象。
- 表单：`PRD_MO`。
- 字段：`FID,FBillNo,FDocumentStatus,FDate,FMaterialId.FNumber,FMaterialId.FName,FQty`。
- 排序与限制：`FID DESC`，`Limit=5`。

## 范围边界

- 本次只证明当前账号可读取上述生产订单基础字段。
- 未验证生产订单全部字段、附件、用料清单或其它业务对象权限。
- 未保存目标账套配置，未把查询结果同步到本地数据库，未调用任何 ERP 写入接口。

## 数据与安全

- 未在任务文档中记录密码、Cookie、签名或完整配置。
- 生产订单样本仅用于响应用户明确要求的只读可见性检查。
