# 20260806 工序配置刷新按钮改新增按钮

## Task Goal

将生产组长工作台“工序配置”模块右上角按钮从“刷新”改为“新增”，并保持按钮继续触发当前工序配置列表加载逻辑。

## Milestones

- [x] M1：定位工序配置按钮和现有静态合同
- [x] M2：补充 RED 静态合同锁定“新增”按钮文案
- [x] M3：修改前端组件按钮文案
- [x] M4：运行目标静态验证并记录结果

## Expected Verification

- 目标组件不再在工序配置模块头部展示“刷新”按钮。
- 工序配置模块头部展示“新增”按钮。
- 按钮仍绑定 `processConfigLoading` 和 `loadProcessConfigRows`，不改变现有加载行为。
- 不修改已有 ERP 同步相关脏改动。

## Current Status

ready_for_closeout

已完成目标按钮文案修改和静态验证；尚未提交/推送，因为主工作区存在本任务外既有脏改动，需要保持提交边界。

## 设计约束检查

- `是否引入 fallback/降级/吞异常`：否。
- `是否从根因和长期维护角度解决`：是；本次只调整目标 UI 文案并用静态合同锁定。
- `是否存在临时补丁或绕过`：否。
