# DCC 上传权限延后至审批任务

## Task Goal

允许具备 DCC 提交入口权限的用户完成文件上传和提交流程，不在上传阶段按文件类别 `UPLOAD` 权限阻断；文件类别权限在审批阶段执行并明确拒绝无权限审批动作。

## Milestones

- [ ] M1：定位上传与审批权限校验链路并确定根因。
- [ ] M2：先补充可重复失败的 BDD/TDD 回归测试。
- [ ] M3：实现最小正式修复，使上传不受类别权限限制、审批仍受限制。
- [ ] M4：完成定向回归、证据校验、提交、推送与任务收尾。

## Expected Verification

- 上传阶段：无文件类别 `UPLOAD` 权限的用户仍可选择类别并提交上传。
- 审批阶段：无对应文件类别审批权限的用户无法完成审批。
- 相关前端静态合同与后端单元测试通过。
- `git diff --check` 通过。

## Applicable Experience Gates

- 已发现 `docs/experience-index.md`，待按 DCC 文控审批和上传关键词读取匹配经验并补充门禁。
- 不使用静态合同替代真实业务权限链路；上传和审批必须分别由各自测试证明。
- 不以 mock、默认成功或吞异常掩盖缺失的正式权限来源。

## 设计约束检查

- `是否引入 fallback/降级/吞异常`：否。
- `是否从根因和长期维护角度解决`：是；拆分上传动作与审批动作的权限职责，避免上传页提前复用审批权限。
- `是否存在临时补丁或绕过`：否。

## Current Status

in_progress：正在定位权限校验链路和建立 RED 回归测试。

