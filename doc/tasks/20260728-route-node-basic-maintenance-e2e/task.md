# 工艺路线节点基础维护 E2E

## Task Goal

使用 Playwright 真实浏览器在本机 `http://127.0.0.1:8081` 验证固定工艺路线“测试节点-工艺路线-基础维护”的前置复位、新增、详情可见和删除闭环。

## Milestones

- [ ] 创建任务记录并读取适用门禁
- [ ] 确认本机运行态与登录前置
- [ ] 执行固定路线前置复位
- [ ] 新增固定路线并验证详情页面基础信息、流转关系图和关联产品页签
- [ ] 删除固定路线并确认搜索无结果

## Expected Verification

- Playwright 通过真实页面登录 `芋道源码/admin`。
- 列表搜索固定名称时若命中，先通过页面删除到无结果。
- 新增保存后按固定名称搜索唯一命中，详情页显示固定编码、固定名称、基础信息、流转关系图和关联产品页签。
- 收尾通过页面删除固定路线，再次搜索固定名称无结果。

## Current Status

in_progress

## 设计约束检查

- `是否引入 fallback/降级/吞异常`：否。
- `是否从根因和长期维护角度解决`：是，本任务只做真实浏览器验证与闭环清理。
- `是否存在临时补丁或绕过`：否。

## Applicable Gates

- E2E 必须使用 Playwright 操作真实前端页面，API 只能用于最终状态核验或只读辅助检查。
- 写入型 E2E 必须使用已确认测试租户和账号，并创建带固定任务标识、可清理的数据。
- Element Plus 表格操作必须按可见业务唯一文本定位目标行，再操作同一行按钮。
- Element Plus 下拉若涉及业务对象，必须按页面可见唯一文本选择，不得用下标或隐藏值替代。
