BDD: 清掉旧模板后重导入 -> Given 运行库中存在 5 条旧历史模板, When 清理这些旧模板并重新导入同一份 `.doc`, Then 系统只保留重新导入后的 10 条新模板。

BDD: 新模板按工序粒度落库 -> Given 样本 `.doc` 最新解析结果为 10 个候选表单, When 提交该次导入, Then 新落库模板覆盖产品信息与各工序记录。

GREEN: 运行库数据清理 -> PASS，删除了旧的 5 条模板，并清理了依附旧模板的执行测试数据。

GREEN: 重新提交导入结果 -> PASS，`importId = 11` 的 10 个候选模板已全部提交落库，返回 `createdCount = 10`。

GREEN: 模板列表校验 -> PASS，模板分页接口返回 `total = 10`，且模板标题覆盖产品信息、粗洗、清洁、硅化Ⅱ、组装Ⅱ、检测、光固Ⅱ、单包装、中包装、大包装。
