# 20260711 直接报工导入恢复重构前大弹框

## Task Goal

根据用户最新截图重新校准“重构前”直接报工导入结果界面：恢复为“直接报工导入结果”大弹框、四个统计卡片和空明细提示，而不是系统提示 alert；同时保持直接报工旧业务口径。

## Milestones

1. 定位历史版本中对应的大弹框实现和字段口径。
2. 补充前端静态回归测试，证明直接报工导入后打开大弹框且只展示旧四项统计。
3. 修改当前前端导入结果展示，恢复截图所示界面。
4. 执行前端静态合同、类型检查和后端已恢复旧口径回归验证。
5. 记录验证证据并提交任务相关改动。

## Expected Verification

- `node tests/e2e/mes-direct-work-report-import-result-static.spec.js`
- `pnpm exec vue-tsc --noEmit -p tsconfig.relaxed.json`
- 必要时复跑直接报工旧口径后端目标测试。

## 经验门禁

- PowerShell / Windows shell / 中文编码：中文文件读写必须显式 UTF-8，禁止 Bash heredoc 和 `&&`。
- 前端页面 / 表格 / 样式：恢复用户截图中的既有业务弹框，不引入额外视觉重设计。
- 报工导入旧工序：本任务只改结果展示，不改变已恢复的直接报工导入归属与跳过口径。

## 设计约束检查

- 是否引入 fallback/降级/吞异常：否。
- 是否从根因和长期维护角度解决：是，重新按用户确认的历史 UI 契约恢复前端。
- 是否存在临时补丁或绕过：否。

## Current Status

completed

## Milestone Status

1. completed - 已定位用户截图对应的是历史“大弹框”导入结果界面，不是小 `系统提示` alert。
2. completed - 已更新前端静态回归测试，锁定直接报工导入后打开“直接报工导入结果”大弹框。
3. completed - 已恢复四项统计：工作表数、创建报工数、提交审批数、跳过杂务行，并保留无明细空状态。
4. completed - 已执行静态合同、类型检查和真实 E2E 导入验证。
5. completed - 已记录验证证据，待提交任务相关改动。
