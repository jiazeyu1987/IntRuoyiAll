# eDHR 批次复盘已填写批记录视图

- Task ID: `20260610-edhr-review-filled-records`
- Status: `completed`

## 任务目标

将 eDHR 批次复盘接口从仅返回时间线 JSON 扩展为同时返回按工序排序的已填写单表复盘数据，供前端直接渲染只读批记录内容。

## 里程碑

1. 扩展 `review-timeline` 响应模型，显式返回审批记录和单表复盘列表。
2. 在服务层按批次任务聚合执行记录、填写值、签名、审批、审计链和主数据追溯摘要。
3. 增加目标单测，锁定复盘接口返回 `executionReviews` 与 `approvalRecords`。

## 设计约束检查

- `是否引入 fallback/降级/吞异常`：否。
- `是否从根因和长期维护角度解决`：是；后端直接提供可渲染只读模型，不要求前端从原始 JSON 反推。
- `是否存在临时补丁或绕过`：否。

## 最终状态

- 状态：完成。
- 验证：目标后端单测通过。
