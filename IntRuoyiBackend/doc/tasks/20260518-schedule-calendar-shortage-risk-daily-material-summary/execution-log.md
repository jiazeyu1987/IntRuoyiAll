# Execution Log

- 2026-05-18: Created backend task package `20260518-schedule-calendar-shortage-risk-daily-material-summary`.
- 2026-05-18: Blocked previous backend task `20260518-md-item-erp-bom-sync-button` due user priority switch.
- BDD: Preview keeps shortage as warning -> Given the selected work orders have insufficient material stock but otherwise valid route, workstation, calendar, and capacity data, When auto-schedule preview runs, Then the response records shortage issues with severity `WARNING`, keeps `shortageCount`, and returns `blockingIssueCount = 0`.
- BDD: Apply succeeds with shortage warnings -> Given preview/apply input has only material shortage warnings and no true blocking issues, When auto-schedule apply runs, Then tasks are written, shortage issues are persisted, and no shortage exception is thrown.
- BDD: Missing prerequisite still blocks -> Given the request has a missing BOM, route, line, calendar, or required capacity input, When preview or apply runs, Then the service fails fast exactly as before.
- BDD: Day detail returns first-operation daily material summary -> Given multiple scheduled work orders consume the same material and the first operation for some orders starts on earlier dates, When day detail loads for a selected date, Then each material row reports `scheduledUsageQty`, `remainingAvailableQty`, `shortageQty`, and `affectedWorkOrderCount` based on cumulative prior-date allocation.
- BDD: Month shortage count follows daily material shortage rows -> Given a month contains daily material shortages derived from the first-operation allocation, When month data loads, Then each day `shortageCount` equals the count of materials whose computed `shortageQty` is greater than zero on that date.
- RED: `mvn --% -f D:\ProjectPackage\Int\IntRuoyi\ruoyi-vue-pro\pom.xml -pl yudao-module-mes -am -Dtest=MesProAutoScheduleServiceImplTest,MesProScheduleCalendarServiceImplTest -Dsurefire.failIfNoSpecifiedTests=false test` -> FAIL, `MesProScheduleCalendarDayDetailRespVO.MaterialShortageItem` is missing `scheduledUsageQty`, `remainingAvailableQty`, and `affectedWorkOrderCount`.
- GREEN: `mvn --% -f D:\ProjectPackage\Int\IntRuoyi\ruoyi-vue-pro\pom.xml -pl yudao-module-mes -am -Dtest=MesProAutoScheduleServiceImplTest,MesProScheduleCalendarServiceImplTest -Dsurefire.failIfNoSpecifiedTests=false test` -> PASS, shortage warnings, apply persistence, month shortage counts, and day-detail material summaries all pass together.
