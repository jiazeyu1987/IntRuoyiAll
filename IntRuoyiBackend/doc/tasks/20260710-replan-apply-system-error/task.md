# 应用重排系统异常修复

## 任务目标

- 复现并定位排产工单“应用重排”返回“系统异常”的真实请求链路根因。
- 修复刷新令牌关联用户已不存在时触发空指针、导致业务请求被泛化为“系统异常”的问题。
- 先补失败回归测试，再以明确的未授权错误终止失效登录态，不通过吞异常或伪造成功绕过失败。

## 上一任务检查

- 上一排产说明任务 `doc/tasks/20260710-scheduler-workbench-dynamic-replan-explanation/task.md` 状态为 `completed`，不阻塞本任务。

## 经验门禁

- PowerShell / UTF-8：中文文件显式按 UTF-8 处理，PowerShell 不使用 `&&`。
- Worktree：仅修改后端仓库，工作目录为 `D:\ProjectPackage\Int\IntRuoyiWorktrees\20260710-replan-apply-system-error\ruoyi-vue-pro`，分支为 `codex/20260710-replan-apply-system-error`。
- 排产应用：必须确认请求是否实际到达重排接口；认证刷新失败时不得误判为排产事务失败。
- 运行态：若源码、模块包和运行 boot jar 不一致，先核实际运行包，不在旧运行态上反复修改前端。
- BDD + 严格 TDD：必须先形成可重复的失败测试，再修改生产代码。
- 无 fallback：不得捕获快照异常后继续提交排产任务，不得返回模拟成功或静默跳过说明快照。

## 设计约束检查

- `是否引入 fallback/降级/吞异常`：否。
- `是否从根因和长期维护角度解决`：是；以真实异常和失败回归测试确定修复点。
- `是否存在临时补丁或绕过`：否。

## BDD 场景

- BDD: 失效用户刷新登录态 -> Given 刷新令牌仍存在但其后台用户已删除 / When 应用重排请求触发访问令牌刷新 / Then 后端清理该刷新令牌及关联访问令牌，并返回明确的未授权错误，不抛出空指针或“系统异常”。
- BDD: 有效用户刷新登录态 -> Given 刷新令牌、租户和用户均有效 / When 应用重排请求触发访问令牌刷新 / Then 正常签发新访问令牌，原有刷新成功行为不变。
- BDD: 重排请求边界 -> Given 认证刷新失败 / When 用户点击应用重排 / Then 请求不得进入排产应用事务，不得错误归因为排产快照保存失败。

## 里程碑

1. [完成] 收集运行日志并确定可重复根因。
2. [完成] 新增失败回归测试并记录 RED。
3. [完成] 实施最小修复并记录 GREEN。
4. [完成] 运行相关回归与真实失效登录态验证。
5. [完成] 提交、融合和清理 worktree。

## 预期验证

- 回归测试在修复前稳定复现本次异常，修复后通过。
- `OAuth2TokenServiceImplTest` 新增回归稳定通过，原有刷新令牌测试全部通过。
- 本机测试租户重新登录后，通过真实页面再次发起应用重排，认证链路不再返回“系统异常”。
- 失效用户的刷新令牌与关联访问令牌被清理，不形成重复刷新失败循环。

## 隔离运行目标

- 前端 worktree：`D:\ProjectPackage\Int\IntRuoyiWorktrees\20260710-replan-apply-system-error\yudao-ui-admin-vue3`
- 前端地址：`http://127.0.0.1:8096`
- 后端地址：`http://127.0.0.1:48096`
- 数据库：本机开发库，沿用 `application-local.yaml` 配置。
- Redis：本机 `127.0.0.1:6379`。
- 文件服务：沿用本机配置，不修改受保护的默认文件配置与展厅媒体地址。
- E2E 身份：本机 `测试租户/aoteman`，仅通过真实页面操作测试租户数据。

## Current Status

completed

## 最终收尾

- 实现提交 `ec75ee6653` 已通过 fast-forward 融合到后端 `int_main`。
- 融合后重新运行 `OAuth2TokenServiceImplTest`，14 个测试全部通过。
- 后端与前端临时 worktree、对应任务分支及任务 worktree 根目录均已删除。
- 主后端工作区原有的重启脚本和测试文件改动与本任务无重叠，收尾过程中保持原样，未暂存、未提交。
