# 执行日志：20260626-showroom-product-narration-audio-url-regression

- BDD: 历史产品语音读取必须返回可播放链接 -> Given 产品讲解版本已存在真实 audioFileId 且 infra_file 中仍存在该文件 / When 管理端调用 /showroom/narration/get 读取 PRODUCT + PUBLIC + ZH/EN / Then 返回结果必须同时包含真实 audioUrl，前端可直接播放历史语音。
- BDD: 没有音频文件时仍只返回未生成状态 -> Given 讲解版本存在但 audioFileId 为空 / When 管理端调用 /showroom/narration/get / Then 接口不得伪造 audioUrl，而是明确返回空音频状态。
- BDD: 历史语音可播放不依赖重新生成 -> Given 旧产品的中英文语音文件仍保存在 infra_file / When 用户只打开产品语音弹框不点击生成 / Then 系统直接展示已有语音，不要求再次调用生成接口。

- GREEN: experience-preflight -> PASS，已按经验索引补读 `docs/login-access.md` 与 `docs/release-backup-restore.md`，本轮仅执行本机后端重启与本机真实链路核验，不触发服务器写入、发布、恢复或正式环境操作。
- 调查：`docker exec int-ruoyi-mysql mysql --default-character-set=utf8mb4 -N -B -uroot -p123456 ruoyi-vue-pro -e "SELECT id, config_id, path, url FROM infra_file WHERE id IN (9198110021399,9198110021400);"` -> PASS，确认历史中英文语音文件仍存在且 URL 完整。
- 调查：`docker exec int-ruoyi-mysql mysql --default-character-set=utf8mb4 -N -B -uroot -p123456 ruoyi-vue-pro -e "SELECT id, target_type, target_id, audience_type, language, source_revision_id, version_no, audio_file_id, status, generation_status FROM showroom_narration_version WHERE audio_file_id IN (9198110021399,9198110021400);"` -> PASS，确认产品 1 的 ZH/EN 版本都仍指向真实历史语音文件。
- 调查：`Get-NetTCPConnection -LocalPort 48081 -State Listen`、`Get-CimInstance Win32_Process -Filter "ProcessId = 58240"`、`Get-Item ShowroomApiRuntime.java/ShowroomAdminController.java`、`Get-Content -Encoding utf8 yudao-ui-admin-vue3\.env.local` -> PASS，确认前端 `8081` 代理到本机 `48081`，但当前 `48081` 仍运行旧 jar `E:\Int\CacheData\IntRuoyi\runtime\backend-20260626-134808.jar`，启动时间 `2026-06-26 13:48:20` 早于本次修复源码写入时间，因此用户仍命中旧后端响应。
- RED: `mvn -pl yudao-module-showroom -Dtest=ShowroomHttpApiIntegrationTest "-Dsurefire.failIfNoSpecifiedTests=false" test` -> FAIL，首次实现时 `NarrationVersionRespVO` 缺少 `ShowroomNarrationKey` import，新增管理端 narration 响应类型未编译通过。
- RED: `mvn -pl yudao-module-showroom -Dtest=ShowroomHttpApiIntegrationTest "-Dsurefire.failIfNoSpecifiedTests=false" test` -> FAIL，继续暴露两条真实回归前置条件：1）新音频 URL 断言写得过严，与测试内 mock 文件路径不一致；2）`narrationSubmitShouldStayPendingUntilManualApprovalsAndPublish` 旧测试从未 mock `audioFileId=99121` 对应文件，以前因接口不返回 `audioUrl` 没暴露，现在正式暴露为缺失前置条件。
- GREEN: `mvn -pl yudao-module-showroom -Dtest=ShowroomHttpApiIntegrationTest "-Dsurefire.failIfNoSpecifiedTests=false" test` -> PASS，75 tests passed，确认管理端 `getNarration` 在 `audioFileId` 存在时返回真实 `audioUrl`，且旧待审批场景补齐真实文件后仍可稳定读取。
- GREEN: `python C:\Users\BJB110\.codex\skills\bug-regression-fix-loop\scripts\validate_bug_regression.py --evidence D:\ProjectPackage\Int\IntRuoyi\ruoyi-vue-pro\doc\tasks\20260626-showroom-product-narration-audio-url-regression\bug-regression-evidence.md` -> PASS。
- GREEN: `powershell -ExecutionPolicy Bypass -File D:\ProjectPackage\Int\IntRuoyi\ruoyi-vue-pro\script\deploy\restart-int-ruoyi-local.ps1 -Component backend -WorktreeName int_main` -> PASS，`48081` 已切换到新进程 `PID 59992`，启动时间 `2026-06-26 15:47:51`，运行 jar 为 `D:\ProjectPackage\Int\IntRuoyi\output\runtime\int_main\backend-runtime-control-20260626-154750.jar`。
- GREEN: `node D:\ProjectPackage\Int\IntRuoyi\scripts\preflight\login-preflight.mjs --base-url http://localhost:8081 --tenant 测试租户 --username aoteman --password 111111 --target-path /showroom/product --target-text 产品` -> PASS，真实登录已进入本机展厅产品页。
- GREEN: `Playwright 真实页面复核 http://localhost:8081/showroom/product` -> PASS，当前测试租户首行产品 `product_001 / productId=252` 的语音弹框会并行请求 `targetId=252` 的 ZH/EN narration；两条响应都返回 `code=0` 且带真实 `audioUrl`，弹框内可见 2 个 `<audio>` 播放器，不再出现“讲解音频缺少真实 audioUrl”。
- 调查：带真实登录态直调 `GET /admin-api/showroom/narration/get?targetType=PRODUCT&targetId=1&audienceType=PUBLIC&language=ZH/EN` -> 返回 `SHOWROOM_TARGET_NOT_FOUND`，说明当前测试租户真实产品页使用的不是历史 `targetId=1`，而是当前运行数据中的 `productId=252`；旧报错中的 `targetId=1` 不再是本页当前可操作目标。
