BDD: batch-processing document becomes candidate previews -> Given the pilot `.doc` file contains multiple top-level production-record tables, When the operator uploads it through the MES batch-record template import flow, Then the system returns a parse preview containing `candidates[]`, one candidate per top-level table, and does not yet create official template rows.

BDD: commit persists only selected source-table indexes -> Given a successful parse preview with multiple table candidates, When the operator submits `{ importId, selectedTableIndexes[] }`, Then the system creates template rows only for those source-table indexes using the parse-result default template code and name, and marks the import session as committed.

BDD: template base information remains editable after import -> Given imported template rows already exist, When the operator updates template code, template name, sort, status, process binding, product name, or remark, Then the system persists only those base-information changes without introducing execution, signoff, or export behavior in Phase 1.

RED: `mvn -pl yudao-module-mes -Dtest=MesProBatchRecordWordParserTest,MesProBatchRecordTemplateServiceImplTest,MesProBatchRecordTemplateControllerTest test` -> FAIL, production batch-record import classes and contract endpoints are not implemented yet.
RED: `mvn -pl yudao-module-mes "-Dtest=MesProBatchRecordWordParserTest,MesProBatchRecordTemplateServiceImplTest,MesProBatchRecordTemplateControllerTest" test` -> FAIL, after the batch-record implementation was added the build is blocked by preexisting unrelated `yudao-module-mes` compile errors in `cal/*`, `dv/*`, and `md/*`, so the targeted batch-record tests cannot reach execution.
GREEN: supersession review -> PASS, the newer backend task `D:\ProjectPackage\Int\IntRuoyi\ruoyi-vue-pro\doc\tasks\20260514-electronic-batch-record-doc-report-implementation\task.md` is completed on `int_main` and now covers the chosen electronic batch-record direction with `DOC -> JimuReport` generation and report management APIs.
GREEN: worktree closeout decision -> PASS, `D:\wt\rbt-be` is now classified as an unfinished Phase 1 prototype kept only for reference until the user explicitly discards or removes the worktree.
GREEN: resume decision -> PASS, on 2026-05-14 the user explicitly reprioritized this worktree and requested completion of the original backend Phase 1 template-import slice before commit and merge into `int_main`.
RED: `mvn -pl yudao-module-mes -am "-Dsurefire.failIfNoSpecifiedTests=false" "-Dtest=MesProBatchRecordWordParserTest,MesProBatchRecordTemplateServiceImplTest,MesProBatchRecordTemplateControllerTest" test` -> FAIL, the resumed Phase 1 branch first exposed a broken `ErrorCodeConstants.java`, commit-request contract drift, missing test schema columns, and an unstable `.doc` parsing path.
GREEN: `mvn -pl yudao-module-mes -am "-Dsurefire.failIfNoSpecifiedTests=false" "-Dtest=MesProBatchRecordWordParserTest,MesProBatchRecordTemplateServiceImplTest,MesProBatchRecordTemplateControllerTest" test` -> PASS, after restoring the MES error-code file, aligning the commit contract to `selectedTableIndexes[]`, repairing H2/MySQL batch-record schemas, and switching legacy `.doc` parsing to Word COM conversion plus `.docx` table parsing.
