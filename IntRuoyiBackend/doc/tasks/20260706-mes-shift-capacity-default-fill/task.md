# 20260706 MES 班次产能默认值补齐

## 任务目标
- 解决手动重排时提示“班次产能缺失”的真实数据问题。
- 仅在本机测试数据范围内，将缺失的日期/产线/班次产能按系统默认数值补齐。
- 补齐后验证手动重排前置检查不再因班次产能缺失阻断。

## 里程碑
1. 定位缺失产能对应的真实表、字段、日期范围和产线/班次。（已完成：实际产能表 mes_pro_capacity_actual 缺 tenant_id=122 / line_id=900042 / shift_id=900033）
2. 明确默认数值来源，并生成最小范围补齐方案。（已完成：AUTO-DAY 08:00-15:30，默认 450 分钟）
3. 执行补齐并记录写入证据。（已完成：仅补齐测试租户实际产能缺失日期）
4. 通过只读 SQL 与真实重排链路验证。（已完成：真实登录 + ACTUAL 口径 preflight/preview 验证通过）

## 预期验证
- SQL 验证补齐前后缺失数量变化。
- 手动重排相关前置检查/预览不再返回“产线班次产能缺失”。
- 如存在新的阻断问题，原样暴露真实阻断原因，不做静默兜底。

## 设计约束检查
- 是否引入 fallback/降级/吞异常：否。本任务只补齐缺失基础数据，不改变业务异常处理。
- 是否从根因和长期维护角度解决：是。通过排程基础产能表补齐真实数据，避免前端或接口绕过校验。
- 是否存在临时补丁或绕过：否。

## 经验门禁
- PowerShell/中文/SQL：命令必须显式 UTF-8；中文 SQL 与脚本不得依赖默认编码。
- 数据库写入：写入前必须以真实库 DESCRIBE/SHOW COLUMNS 为准核对字段。
- E2E/写入：高风险动作前必须记录 experience-preflight 通过或阻塞原因。
- 测试租户：真实写入只允许在测试租户/本机环境范围内，不修改正式环境。

## 当前状态
- 状态：已完成
## 最终验证结果
- 已补齐测试租户 `tenant_id=122`、产线 `900042`、班次 `900033` 从 `2026-07-06` 到 `2027-12-31` 的实际班次产能，共新增 475 条。
- 默认值来源为同租户 `AUTO-DAY` 班次：`08:00-15:30`，即 `450` 分钟。
- 真实登录后使用排产工单 `125` 执行实际产能口径前置检查与重排预览：`preflightResult=PASS`、`previewBlockingIssueCount=0`、`previewGeneratedTaskCount=53`。
- 验证响应中不再出现“班次产能缺失”和 `1970-01-01`。
## 运行态追加修复
- 用户复测仍提示相同问题后，已确认前端 `8081` 正在连接后端 `48081`，后端运行参数使用 `127.0.0.2:23306/ruoyi-vue-pro` 当前数据库。
- 不是后端未更新；根因是运行态仍缺 `1970-01-01` 的实际产能行：`tenant_id=122`、`line_id=900042`、`shift_id=900033`。
- 已新增 `mes_pro_capacity_actual`：`1970-01-01 00:00:00`、`450` 分钟；对应计划产能行已存在。
- 已验证 `1970-01-01` 与 `2026-07-06`，`PLANNED` 与 `ACTUAL` 两种口径均不再返回“班次产能缺失”。
## 芋道源码/admin 复验授权与边界
- 用户已在当前任务明确要求使用 `芋道源码/admin` 进行本机验证。
- 本次先执行真实登录和页面路径验证，再读取实际阻断与容量缺口；若必须写入 admin 租户基础产能数据，先只补齐已核验缺失的最小日期/产线/班次范围，并记录 SQL 回查证据。
- 不访问测试服、正式服或远程环境；不引入 fallback、静默成功或绕过校验。

## 芋道源码/admin 复验结果
- 已按用户要求使用本机 `芋道源码/admin` 真实登录 `/mes/pro/schedule-order`，页面列表返回可重排排产工单 `id=88`、`SCH-TESTERP6640C97D318E-20260630-0001`。
- 已补齐 `tenant_id=1 / line_id=900040 / shift_id=900031 / AUTO-DAY` 在 `1970-01-01`、`2026-07-06`、`2026-07-07` 的实际产能，默认值为 `08:00-23:30 = 930` 分钟，共新增 3 条。
- 复验 `PLANNED` 与 `ACTUAL`、`1970-01-01` / `2026-07-06` / `2026-07-07` 六组重排预览：接口均 `code=0`，`hasCapacityMissing=false`，不再出现“班次产能缺失”。
- 当前剩余阻断不是“产能缺失”，而是 `ACTUAL` 口径返回 `BLOCKING:CAPACITY`：`产线可用班次产能不足`；`PLANNED` 口径可生成 47 个任务且无阻断。

