# 任务：MES 手动重排今天/明天整天重排

## 任务目标
- 点击“应用重排”时由用户选择从今天或明天开始重排。
- 手动重排按所选日期整天排程，后端忽略重排请求的时间部分，只使用日期 `00:00:00`。
- 应用前按所选日期重新执行排产前检查、重排预览和应用，避免旧预览与正式应用不一致。

## 经验门禁
- PowerShell / Windows shell：已读取 `docs/powershell-memory.md`；本任务所有中文读写与命令输出使用显式 UTF-8。
- 登录 / 租户 / Playwright E2E：已读取 `docs/login-access.md`；真实 E2E 前必须先跑官方 `login-preflight.mjs` 最小登录路径。
- 前端样式：已读取 `D:\ProjectPackage\Int\IntPP\FRONTEND_STYLE.md`；新增弹框沿用 Element Plus 与现有运维控制台风格，不做额外视觉重设计。

## 设计约束检查
- `是否引入 fallback/降级/吞异常`：否。
- `是否从根因和长期维护角度解决`：是；将手动重排起点语义统一成日期锚点，并让应用时重新计算。
- `是否存在临时补丁或绕过`：否。

## BDD 场景
- `BDD: 应用重排选择今天整天重排 -> Given 已选择可重排排产工单 / When 用户点击应用重排并选择从今天开始 / Then 系统从今天日期锚点重新检查、预览并应用，任务不再按当前剩余分钟切分。`
- `BDD: 应用重排选择明天整天重排 -> Given 已选择可重排排产工单 / When 用户点击应用重排并选择从明天开始 / Then 系统从明天日期锚点重新检查、预览并应用，生成任务不得早于明天可用班次。`
- `BDD: 应用前重算发现阻断 -> Given 用户选择今天或明天后排产前检查或预览存在阻断 / When 确认应用 / Then 系统停止写入并展示真实阻断错误。`

## 里程碑
1. M1：补充后端与前端 RED 测试，锁定旧行为。
2. M2：实现后端手动重排日期归一化，普通自动排产不受影响。
3. M3：实现前端应用时今天/明天选择与重新检查、预览、应用链路。
4. M4：运行定向测试和前端静态契约验证。
5. M5：执行本机真实 E2E 并用数据库只读查询核对最早任务时间。
6. M6：更新证据、完成任务文档并提交本任务相关改动。

## 预期验证
- Maven 定向测试 RED -> GREEN。
- 前端静态契约测试 RED -> GREEN。
- 真实 Playwright E2E：测试租户/aoteman 登录 `/mes/pro/schedule-order`，选择真实工单走应用重排今天/明天路径。
- 数据库只读查询：核对生成任务最早 `start_time` 符合所选日期整天重排语义。

## 当前状态
- completed：已完成后端手动重排日期归一化、前端今天/明天整天重排选择、应用前重新 preflight + preview + apply 链路、阻断问题停止应用、真实 E2E 与数据库只读核对。
- 最终验证：后端 50 个 `MesProAutoScheduleServiceImplTest` 用例通过；前端静态契约、真实 Playwright E2E、排产任务数据库只读核对均通过。

## 完成记录
- M1：已补充后端与前端契约测试，覆盖今天整天、明天整天、普通自动排产不受影响、阻断时不调用最终 apply。
- M2：已实现手动重排 `startTime` 日期归一化，预览和应用均忽略时间部分并使用 `00:00:00` 日期锚点。
- M3：已实现点击“应用重排”弹框选择“从今天开始重排 / 从明天开始重排”，确认后重新执行 preflight、preview，并用同一份 `calendarContextToken` apply。
- M4：已完成后端 Maven 回归、前端静态契约和排产页面类型检查。
- M5：已用测试租户 `aoteman` 通过 Playwright 打开 `http://localhost:8081` 走真实排产工单路径，今天和明天两条应用链路均成功。
- M6：已记录执行证据并完成收尾预览；本任务相关改动进入单独提交。
