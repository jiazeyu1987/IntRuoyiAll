BDD: generic page-type, row-type, layout, and visual layers -> Given batch-record pages with mixed overview sections, process records, and repeated detail blocks, When the shared parser/calibrator/style pipeline is refactored into page-type, row-type, layout, and rendering layers without table-name-specific branches, Then more than one template family should improve together and the system should stop misclassifying overview pages as product-info pages.

BDD: shared page-title rules should separate overview titles from product-info fallback -> Given a page may start with a standalone short overview title such as `装配及包装信息` or `生产记录汇总表`, When the parser or route recognizer decides whether a row starts a new template, Then the shared rule must classify that row as a real page title while still rejecting the document header and field-label rows.

BDD: shared row-type rules should classify long-description and summary rows before generic compression -> Given a process page contains both narrative rows and repeated detail blocks, When the layout calibrator normalizes rows, Then it should recognize title/field/long-description/table-header/detail/summary/footer rows and avoid squeezing long-description or summary rows through the generic narrow-cell path.

RED: isolated shared-page-title test compile -> FAIL, `MesProBatchRecordSharedPageTitleRules` did not exist yet, so the new page-type tests could not resolve the shared rule symbol.
GREEN: isolated shared-page-title test run -> PASS, the new shared page-title rules now classify standalone info/process/summary titles and prefer those titles over the raw product-info fallback.

RED: isolated shared-row-type test compile -> FAIL, `MesProBatchRecordSharedRowTypeRules.RowType` did not exist yet, so the new row-type tests could not compile.
GREEN: isolated shared-row-type and layout test run -> PASS, shared row classification now returns `TITLE / FIELD / LONG_DESCRIPTION / TABLE_HEADER / DETAIL_DATA / SUMMARY / FOOTER`, and the calibrator uses those row types to widen mixed narrative rows and keep summary rows structured after detail blocks.

RED: authenticated live backend `POST /admin-api/mes/pro/batch-record-report/recognize-fixed?routeKey=B` after switching the fixed source contract to `D:\ProjectPackage\Int\IntRuoyi\resource\批记录模板.doc` -> FAIL, the route now reads the new source file successfully but over-splits it with `route_b_expected_15_templates_actual_28`, which means the shared page-type recognition layer is still too permissive for short standalone section titles inside a larger info-summary page.

GREEN: `mvn --% -f D:\ProjectPackage\Int\IntRuoyi\worktrees\batch-record-print-fidelity-phase2\pom.xml -pl yudao-server -am -Dmaven.test.skip=true package` -> PASS after the first full 4-layer subagent-driven patch set was merged into the backend worktree.

GREEN: `mvn --% -f D:\ProjectPackage\Int\IntRuoyi\worktrees\batch-record-print-fidelity-phase2\pom.xml -pl yudao-module-mes -am -Dtest=MesProBatchRecordSharedPageTitleRulesTest,MesProBatchRecordSharedRowTypeRulesTest,MesProBatchRecordRouteBRecognizerTest,MesProBatchRecordRouteDRecognizerTest,MesProBatchRecordReportLayoutCalibratorTest,MesProBatchRecordReportShapeRulesTest,MesProBatchRecordReportJsonBuilderTest,MesProBatchRecordReportStyleEnhancerTest -Dsurefire.failIfNoSpecifiedTests=false test` -> PASS, 65 tests started, 0 failed, 6 skipped by local fixture assumptions.

RED: authenticated live backend `POST /admin-api/mes/pro/batch-record-report/recognize-fixed?routeKey=B` after rebuilding `yudao-server.jar`, restarting the isolated `48082` runtime, and reusing the new fixed source `D:\ProjectPackage\Int\IntRuoyi\resource\批记录模板.doc` -> still FAIL with `route_b_expected_15_templates_actual_28`. This confirms the remaining blocker is no longer the source-file path or runtime packaging; it is specifically the page-type splitting rule over-splitting large combined Word tables into too many templates.

GREEN: `mvn --% -f D:\ProjectPackage\Int\IntRuoyi\worktrees\batch-record-print-fidelity-phase2\pom.xml -pl yudao-module-mes -am -Dtest=MesProBatchRecordSharedPageTitleRulesTest,MesProBatchRecordSharedRowTypeRulesTest,MesProBatchRecordRouteBRecognizerTest,MesProBatchRecordRouteDRecognizerTest,MesProBatchRecordReportLayoutCalibratorTest,MesProBatchRecordReportShapeRulesTest,MesProBatchRecordReportJsonBuilderTest,MesProBatchRecordReportStyleEnhancerTest -Dsurefire.failIfNoSpecifiedTests=false test` -> PASS, 65 tests started, 0 failed, 6 skipped by local fixture assumptions.

GREEN: `mvn --% -f D:\ProjectPackage\Int\IntRuoyi\worktrees\batch-record-print-fidelity-phase2\pom.xml -pl yudao-server -am -Dmaven.test.skip=true package` -> PASS after the shared page-title tightening patch.

GREEN: authenticated live backend `POST /admin-api/mes/pro/batch-record-report/recognize-fixed?routeKey=B` on the refreshed `48082` runtime -> PASS, returned `importedCount=15`, `updatedCount=15`, and the fixed source file is now `批记录模板.doc`.

BDD: the first fixed-source overview page should anchor to the trailing assembly/release section without creating extra templates -> Given the first top-level Word table contains `产品信息`, `配件进货批号信息`, `装配及包装信息`, and `过程放行信息`, When Route B or the shared DOC parser resolves the representative overview section, Then it should keep the template count at `15`, trim away the earlier info-only blocks for the first report body, and expose `装配及包装信息` as the first live report title instead of falling back to `产品信息`.
RED: `mvn --% -f D:\ProjectPackage\Int\IntRuoyi\worktrees\batch-record-print-fidelity-phase2\pom.xml -pl yudao-module-mes -am -Dtest=MesProBatchRecordSharedPageTitleRulesTest,MesProBatchRecordDocParserTest,MesProBatchRecordRouteBRecognizerTest,MesProBatchRecordReportLayoutCalibratorTest,MesProBatchRecordReportJsonBuilderTest -Dsurefire.failIfNoSpecifiedTests=false test` -> FAIL, the new representative-section tests still returned `产品信息` for the first fixed-source report and the new row-budget tests showed detail rows were not being compressed ahead of summary rows.
BDD: the single-page height budget should shrink low-priority row types before summary/footer blocks -> Given process pages contain many `FIELD` and `DETAIL_DATA` rows above `SUMMARY` and `FOOTER` blocks, When the shared calibrator and JSON builder compress the page into the viewer budget, Then ordinary field/detail rows should absorb most of the shrink first while summary/footer rows keep a higher readability floor so lower-half content can enter the first preview screen.
GREEN: `mvn --% -f D:\ProjectPackage\Int\IntRuoyi\worktrees\batch-record-print-fidelity-phase2\pom.xml -pl yudao-module-mes -am -Dtest=MesProBatchRecordSharedPageTitleRulesTest,MesProBatchRecordDocParserTest,MesProBatchRecordRouteBRecognizerTest,MesProBatchRecordReportLayoutCalibratorTest,MesProBatchRecordReportJsonBuilderTest,MesProBatchRecordReportShapeRulesTest -Dsurefire.failIfNoSpecifiedTests=false test` -> PASS after:
- adding shared representative-section anchoring for multi-short-title overview tables
- trimming the first fixed-source overview report body to the trailing `装配及包装信息 / 过程放行信息` block
- removing the extra shared `生产记录汇总表` banner row ahead of the first overview page
- switching the single-page height budget to row-type-aware floors and row-type-aware shrink priority
- tightening the shared single-page height ceiling to better fit the current viewer preview window
RED: `mvn --% -f D:\ProjectPackage\Int\IntRuoyi\worktrees\batch-record-print-fidelity-phase2\pom.xml -pl yudao-module-mes -am -Dtest=MesProBatchRecordSharedPageTitleRulesTest,MesProBatchRecordSharedRowTypeRulesTest,MesProBatchRecordRouteBRecognizerTest,MesProBatchRecordRouteDRecognizerTest,MesProBatchRecordReportLayoutCalibratorTest,MesProBatchRecordReportShapeRulesTest,MesProBatchRecordReportJsonBuilderTest,MesProBatchRecordReportStyleEnhancerTest -Dsurefire.failIfNoSpecifiedTests=false test` -> FAIL once with a local JVM native-memory allocation error before the MES tests started, so the suite had to be rerun with bounded `MAVEN_OPTS`.
GREEN: `$env:MAVEN_OPTS='-Xmx1024m -XX:MaxMetaspaceSize=512m'; mvn --% -f D:\ProjectPackage\Int\IntRuoyi\worktrees\batch-record-print-fidelity-phase2\pom.xml -pl yudao-module-mes -am -Dtest=MesProBatchRecordSharedPageTitleRulesTest,MesProBatchRecordSharedRowTypeRulesTest,MesProBatchRecordRouteBRecognizerTest,MesProBatchRecordRouteDRecognizerTest,MesProBatchRecordReportLayoutCalibratorTest,MesProBatchRecordReportShapeRulesTest,MesProBatchRecordReportJsonBuilderTest,MesProBatchRecordReportStyleEnhancerTest -Dsurefire.failIfNoSpecifiedTests=false test` -> PASS, 72 tests ran, 0 failed, 6 skipped by local fixture assumptions.
GREEN: `$env:MAVEN_OPTS='-Xmx1024m -XX:MaxMetaspaceSize=512m'; mvn --% -f D:\ProjectPackage\Int\IntRuoyi\worktrees\batch-record-print-fidelity-phase2\pom.xml -pl yudao-server -am -Dmaven.test.skip=true package` -> PASS after the representative-section and row-type height-budget changes.
GREEN: restarted `48082` with the freshly packaged `output\runtime\backend-48082-generic-layout.jar`, then ran authenticated live verification -> PASS, `recognize-fixed?routeKey=B` returned `importedCount=15`, `updatedCount=15`, and the first live report title changed to `装配及包装信息`.

BDD: subagent-driven visual-fidelity follow-up should improve overview width usage, rough-wash first-screen completeness, fine-wash density balance, and clean-process repeated-detail readability without introducing per-template hard-coding -> Given the latest Route B live output already fixed the first-page title anchor, When four isolated workers each optimize one remaining visual gap in their owned layer and the main agent reviews the combined result, Then the accepted changes should remain generic, keep Route B at `15` templates, and produce at least one visible improvement in the refreshed four-image compare.
RED: one-shot worker review pass -> FAIL in mixed ways before review closure:
- `1.png` worker proved `MesProBatchRecordReportJsonBuilder` was still collapsing a `960px` overview page to `432px`
- `2.png` worker could compile rough-wash changes but could not get a clean focused JUnit run because unrelated legacy `testCompile` errors polluted its isolated pass
- `3.png` worker had to add checklist-choice semantics to shared dense-tail width rules before the new width-floor tests would pass
- `4.png` worker reported that style-only ownership was enough for denser repeated-detail text, but not enough to solve every remaining page-height issue
GREEN: main-agent review accepted the following generic changes into the current worktree after inspecting the worker outputs:
- unified rendered column widths in `MesProBatchRecordReportJsonBuilder` so overview pages no longer lose their shared-width budget during a second clamp
- applied shared first-screen compression to the fixed rough-wash layout and added a secondary preferred viewport budget for long structured process pages
- widened the generic dense-page width budget and taught shared row-type/shape rules to treat checklist-choice cells as non-narrative semantic tail columns
- added style-layer compaction for long narrative cells and dense repeated-detail rows so repeated pressure-table rows expose more text under the same row-height budget
GREEN: `$env:MAVEN_OPTS='-Xmx1024m -XX:MaxMetaspaceSize=512m'; mvn --% -f D:\ProjectPackage\Int\IntRuoyi\worktrees\batch-record-print-fidelity-phase2\pom.xml -pl yudao-module-mes -am -Dtest=MesProBatchRecordReportJsonBuilderTest,MesProBatchRecordReportLayoutCalibratorTest,MesProBatchRecordSharedRowTypeRulesTest,MesProBatchRecordReportShapeRulesTest,MesProBatchRecordReportStyleEnhancerTest -Dsurefire.failIfNoSpecifiedTests=false test` -> PASS, 61 tests ran, 0 failed, 5 skipped by local fixture assumptions.
GREEN: `$env:MAVEN_OPTS='-Xmx1024m -XX:MaxMetaspaceSize=512m'; mvn --% -f D:\ProjectPackage\Int\IntRuoyi\worktrees\batch-record-print-fidelity-phase2\pom.xml -pl yudao-server -am -Dmaven.test.skip=true package` -> PASS after the reviewed worker changes were kept.
GREEN: restarted `48082`, then reran authenticated live backend `POST /admin-api/mes/pro/batch-record-report/recognize-fixed?routeKey=B` -> PASS, `importedCount=15`, `updatedCount=15`, first live title still `装配及包装信息`.

BDD: detail-heavy process pages with both repeated detail rows and lower-half summary/footer blocks should receive a tighter preferred first-screen budget than ordinary structured pages -> Given some generic process pages still expose many detail rows before the lower-half `SUMMARY / FOOTER` region enters the viewer, When the shared layout calibrator computes the preferred viewport budget, Then pages with many `DETAIL_DATA` rows should tighten that first-screen budget further without introducing route-name-specific branches.
GREEN: `$env:MAVEN_OPTS='-Xmx1024m -XX:MaxMetaspaceSize=512m'; mvn --% -f D:\ProjectPackage\Int\IntRuoyi\worktrees\batch-record-print-fidelity-phase2\pom.xml -pl yudao-module-mes -am -Dtest=MesProBatchRecordReportLayoutCalibratorTest,MesProBatchRecordReportJsonBuilderTest,MesProBatchRecordRouteBRecognizerTest,MesProBatchRecordReportStyleEnhancerTest,MesProBatchRecordReportShapeRulesTest,MesProBatchRecordSharedRowTypeRulesTest -Dsurefire.failIfNoSpecifiedTests=false test` -> PASS after lowering the preferred viewport budget further for pages with `6+` or `8+` repeated detail rows.
GREEN: restarted the batch-record `48082` runtime and reran authenticated live `recognize-fixed?routeKey=B` -> PASS, `importedCount=15`, `updatedCount=15`, first title still `装配及包装信息`, which confirms the tighter detail-row viewport budget did not break Route B structure recognition.

BDD: shared overview pages should not be forced through the generic `72px` per-column cap -> Given the first fixed-source overview report is a six-column standalone info-summary page, When the layout calibrator emits its final parsed table for JMReport, Then the final `columnWidths` and rendered sheet width should preserve the shared target width budget instead of collapsing back to `6 * 72 = 432`.
RED: direct live `jmreport/show` probe for report `EBR_B_T01` after the earlier JSON-side fixes still showed `dataRectWidth = 432`, `area.width = 432`, and six `72px` columns. This proved the remaining `1.png` gap was still in the layout-calibrator width path rather than only in screenshot shell chrome.
GREEN: `mvn --% -f D:\ProjectPackage\Int\IntRuoyi\worktrees\batch-record-print-fidelity-phase2\pom.xml -pl yudao-module-mes -am -Dtest=MesProBatchRecordReportLayoutCalibratorTest,MesProBatchRecordReportJsonBuilderTest,MesProBatchRecordRouteBRecognizerTest -Dsurefire.failIfNoSpecifiedTests=false test` -> PASS after:
- treating `sourceTableIndex = 1` as an extra shared overview-page signal
- generating explicit uniform overview `columnWidths`
- reapplying those overview widths after single-page compression so they survive the generic `resolveSpanWidth()` clamp
GREEN: rebuilt `yudao-server.jar`, restarted the batch-record `48082` runtime, reran authenticated live `recognize-fixed?routeKey=B`, and reprobed `/jmreport/show` -> PASS. The first live overview report now exposes `dataRectWidth = 960`, `area.width = 960`, and six `160px` columns.

BDD: process-record pages without an explicit standalone title row should still inherit the shared full-width budget when their resolved `tableTitle` is already a process-page title -> Given some Route B process pages such as `清洁工序生产记录` are stored with a process `tableTitle` but their row bodies begin directly with metadata and inspection rows, When the shared calibrator normalizes widths and the JSON builder emits final column budgets, Then those pages should still expand to the shared process-page width budget instead of collapsing back to `72px * columnCount`.
RED: `$env:MAVEN_OPTS='-Xmx1024m -XX:MaxMetaspaceSize=512m'; mvn --% -f D:\ProjectPackage\Int\IntRuoyi\worktrees\batch-record-print-fidelity-phase2\pom.xml -pl yudao-module-mes -am -Dtest=MesProBatchRecordReportLayoutCalibratorTest#calibrate_shouldUseProcessWidthBudgetWhenTableTitleMarksAProcessPageWithoutStandaloneTitleRow -Dsurefire.failIfNoSpecifiedTests=false test` -> FAIL, the new regression exposed `maxRowWidth=720`, which confirmed that process pages without a standalone title row were still being flattened into the generic `72px` per-column path.
GREEN: `$env:MAVEN_OPTS='-Xmx1024m -XX:MaxMetaspaceSize=512m'; mvn --% -f D:\ProjectPackage\Int\IntRuoyi\worktrees\batch-record-print-fidelity-phase2\pom.xml -pl yudao-module-mes -am -Dtest=MesProBatchRecordReportLayoutCalibratorTest#calibrate_shouldUseProcessWidthBudgetWhenTableTitleMarksAProcessPageWithoutStandaloneTitleRow -Dsurefire.failIfNoSpecifiedTests=false test` -> PASS after:
- treating shared `PROCESS_RECORD` titles as a process-page signal even when the row body does not repeat the title
- computing the final full-width `columnWidths` budget before single-page compression and reapplying it afterward, so the width budget survives the generic `72px` per-column clamp
GREEN: `$env:MAVEN_OPTS='-Xmx1024m -XX:MaxMetaspaceSize=512m'; mvn --% -f D:\ProjectPackage\Int\IntRuoyi\worktrees\batch-record-print-fidelity-phase2\pom.xml -pl yudao-module-mes -am -Dtest=MesProBatchRecordSharedPageTitleRulesTest,MesProBatchRecordSharedRowTypeRulesTest,MesProBatchRecordRouteBRecognizerTest,MesProBatchRecordRouteDRecognizerTest,MesProBatchRecordReportLayoutCalibratorTest,MesProBatchRecordReportShapeRulesTest,MesProBatchRecordReportJsonBuilderTest,MesProBatchRecordReportStyleEnhancerTest -Dsurefire.failIfNoSpecifiedTests=false test` -> PASS, 81 tests ran, 0 failed, 6 skipped by local fixture assumptions.
GREEN: `$env:MAVEN_OPTS='-Xmx1024m -XX:MaxMetaspaceSize=512m'; mvn --% -f D:\ProjectPackage\Int\IntRuoyi\worktrees\batch-record-print-fidelity-phase2\pom.xml -pl yudao-server -am -Dmaven.test.skip=true package` -> PASS after the shared process-width-budget repair.
GREEN: restarted `48082` with the refreshed `output\runtime\backend-48082-generic-layout.jar`, reran authenticated live `recognize-fixed?routeKey=B`, and recaptured the direct `/jmreport/view` compare images -> PASS, `importedCount=15`, `updatedCount=15`, the first live title still stayed on `装配及包装信息`, and the refreshed `4-clean-process-live.png` shows a visibly wider clean-process page than the previous compare pass.
BDD: shared process pages whose body rows are still individually narrow should still consume the full process-page width budget instead of stopping at the document-header-derived `72px * columnCount` ceiling -> Given a process page can contain mostly narrow detail rows and no wide narrative/header rows strong enough to lift every column on their own, When the shared calibrator resolves final `columnWidths`, Then it should continue distributing any remaining width budget until the process-page table reaches the shared render target.
RED: `$env:MAVEN_OPTS='-Xmx1024m -XX:MaxMetaspaceSize=512m'; mvn --% -f D:\ProjectPackage\Int\IntRuoyi\worktrees\batch-record-print-fidelity-phase2\pom.xml -pl yudao-module-mes -am -Dtest=MesProBatchRecordReportLayoutCalibratorTest#calibrate_shouldStretchSharedProcessColumnsToTargetWidthEvenWhenOnlyDetailRowsStayNarrow -Dsurefire.failIfNoSpecifiedTests=false test` -> FAIL, the new narrow-detail regression proved the process-page width budget still stopped at `720` instead of the expected `1000`.
GREEN: `$env:MAVEN_OPTS='-Xmx1024m -XX:MaxMetaspaceSize=512m'; mvn --% -f D:\ProjectPackage\Int\IntRuoyi\worktrees\batch-record-print-fidelity-phase2\pom.xml -pl yudao-module-mes -am -Dtest=MesProBatchRecordReportLayoutCalibratorTest#calibrate_shouldStretchSharedProcessColumnsToTargetWidthEvenWhenOnlyDetailRowsStayNarrow -Dsurefire.failIfNoSpecifiedTests=false test` -> PASS after adding proportional residual-width distribution for shared process-page column budgets that still fall short of the target render width.
GREEN: `$env:MAVEN_OPTS='-Xmx1024m -XX:MaxMetaspaceSize=512m'; mvn --% -f D:\ProjectPackage\Int\IntRuoyi\worktrees\batch-record-print-fidelity-phase2\pom.xml -pl yudao-module-mes -am -Dtest=MesProBatchRecordSharedPageTitleRulesTest,MesProBatchRecordSharedRowTypeRulesTest,MesProBatchRecordRouteBRecognizerTest,MesProBatchRecordRouteDRecognizerTest,MesProBatchRecordReportLayoutCalibratorTest,MesProBatchRecordReportShapeRulesTest,MesProBatchRecordReportJsonBuilderTest,MesProBatchRecordReportStyleEnhancerTest -Dsurefire.failIfNoSpecifiedTests=false test` -> PASS, 82 tests ran, 0 failed, 6 skipped by local fixture assumptions.
GREEN: rebuilt `yudao-server.jar`, restarted the batch-record `48082` runtime, reran authenticated live `recognize-fixed?routeKey=B`, and reprobed the clean-process `/jmreport/show` payload -> PASS. The same `清洁工序生产记录` live report now exposes `dataRectWidth = 1000`, `area.width = 1000`, and ten `100px` columns instead of the previous `720 / 72px`.
BDD: shared medium-density pages should match the visible paper utilization of the current target screenshots more closely -> Given the current batch-record target images show medium-density process pages with more horizontal paper usage than the earlier live output, When the shared width budget is tuned for medium-density pages, Then the `精洗 / 清洁` pages should expand further without reintroducing template-specific branches.
RED: `mvn --% -f D:\ProjectPackage\Int\IntRuoyi\worktrees\batch-record-print-fidelity-phase2\pom.xml -pl yudao-module-mes -am -Dtest=MesProBatchRecordReportShapeRulesTest#resolveSharedPageWidthBudget_shouldFollowGenericDensityBands -Dsurefire.failIfNoSpecifiedTests=false test` -> FAIL, the medium-density budget still returned `1000` even though the current target paper utilization looked closer to `1040`.
GREEN: `mvn --% -f D:\ProjectPackage\Int\IntRuoyi\worktrees\batch-record-print-fidelity-phase2\pom.xml -pl yudao-module-mes -am -Dtest=MesProBatchRecordReportShapeRulesTest#resolveSharedPageWidthBudget_shouldFollowGenericDensityBands -Dsurefire.failIfNoSpecifiedTests=false test` -> PASS after raising the shared medium-density width budget to `1040`.
GREEN: `$env:MAVEN_OPTS='-Xmx1024m -XX:MaxMetaspaceSize=512m'; mvn --% -f D:\ProjectPackage\Int\IntRuoyi\worktrees\batch-record-print-fidelity-phase2\pom.xml -pl yudao-module-mes -am -Dtest=MesProBatchRecordSharedPageTitleRulesTest,MesProBatchRecordSharedRowTypeRulesTest,MesProBatchRecordRouteBRecognizerTest,MesProBatchRecordRouteDRecognizerTest,MesProBatchRecordReportLayoutCalibratorTest,MesProBatchRecordReportShapeRulesTest,MesProBatchRecordReportJsonBuilderTest,MesProBatchRecordReportStyleEnhancerTest -Dsurefire.failIfNoSpecifiedTests=false test` -> PASS, 82 tests ran, 0 failed, 6 skipped by local fixture assumptions.
GREEN: rebuilt `yudao-server.jar`, restarted the batch-record `48082` runtime, reran authenticated live `recognize-fixed?routeKey=B`, and reprobed the clean-process and fine-wash `/jmreport/show` payloads -> PASS. The clean-process live report now exposes `dataRectWidth = 1040`, `area.width = 1040`, and ten `104px` columns; the fine-wash live report remains at `1120` as expected for the denser 20-column band.
BDD: generic dense process pages should not stay visibly wider than the target screenshots once the medium-density pages are already aligned -> Given the current compare evidence shows `精洗` still renders as a noticeably wider 20-column paper than the target screenshot while `粗洗 / 清洁` are already close, When the shared dense-page width budget is tuned, Then generic 20-column dense pages should contract toward the target width without reintroducing per-process branches.
RED: `mvn --% -f D:\ProjectPackage\Int\IntRuoyi\worktrees\batch-record-print-fidelity-phase2\pom.xml -pl yudao-module-mes -am -Dtest=MesProBatchRecordReportShapeRulesTest#resolveTargetRenderWidth_shouldUseSharedBudgetByColumnDensity,MesProBatchRecordReportShapeRulesTest#resolveSharedPageWidthBudget_shouldFollowGenericDensityBands -Dsurefire.failIfNoSpecifiedTests=false test` -> FAIL, the dense-page budget still returned `1120` when the new target width assertions expected `1044`.
GREEN: the same focused shape-rule test command -> PASS after lowering the shared dense-page width budget from `1120` to `1044`.
GREEN: `$env:MAVEN_OPTS='-Xmx1024m -XX:MaxMetaspaceSize=512m'; mvn --% -f D:\ProjectPackage\Int\IntRuoyi\worktrees\batch-record-print-fidelity-phase2\pom.xml -pl yudao-module-mes -am -Dtest=MesProBatchRecordSharedPageTitleRulesTest,MesProBatchRecordSharedRowTypeRulesTest,MesProBatchRecordRouteBRecognizerTest,MesProBatchRecordRouteDRecognizerTest,MesProBatchRecordReportLayoutCalibratorTest,MesProBatchRecordReportShapeRulesTest,MesProBatchRecordReportJsonBuilderTest,MesProBatchRecordReportStyleEnhancerTest -Dsurefire.failIfNoSpecifiedTests=false test` -> PASS, 82 tests ran, 0 failed, 6 skipped by local fixture assumptions.
GREEN: rebuilt `yudao-server.jar`, restarted the batch-record `48082` runtime, reran authenticated live `recognize-fixed?routeKey=B`, and reprobed the fine-wash and clean-process `/jmreport/show` payloads -> PASS. The fine-wash live report now exposes `dataRectWidth = 1044`, `area.width = 1044`, and nineteen `54/55px` columns, while the clean-process live report stays at `1040`.
BDD: live-like dense process pages should be allowed to exceed the generic `650px` JSON single-page cap without globally loosening every template -> Given `精洗 / 清洁` dense process pages still need more lower-half content in the first preview while `粗洗` fixed layout should stay compact, When the JSON builder decides the final row heights for a real Route B dense process page, Then only those dense live-like pages should be allowed to exceed the generic `650px` JSON cap.
RED: `mvn --% -f D:\ProjectPackage\Int\IntRuoyi\worktrees\batch-record-print-fidelity-phase2\pom.xml -pl yudao-module-mes -am -Dtest=MesProBatchRecordReportJsonBuilderTest#build_shouldAllowLiveLikeDenseProcessPagesToExceedTheGeneric650pxJsonCap -Dsurefire.failIfNoSpecifiedTests=false test` -> FAIL, the new fixed-source Route B regression still produced `totalHeight=650`, which proved the final JSON layer was clipping dense pages even after the width budget had been repaired.
GREEN: the same focused JSON-builder test command -> PASS after:
- keeping the shared `650px` cap as the generic default
- moving the taller-page allowance into `MesProBatchRecordReportJsonBuilder`
- only applying the higher target height to live-like dense process pages detected from shared row-type signals
GREEN: `$env:MAVEN_OPTS='-Xmx1024m -XX:MaxMetaspaceSize=512m'; mvn --% -f D:\ProjectPackage\Int\IntRuoyi\worktrees\batch-record-print-fidelity-phase2\pom.xml -pl yudao-module-mes -am -Dtest=MesProBatchRecordSharedPageTitleRulesTest,MesProBatchRecordSharedRowTypeRulesTest,MesProBatchRecordRouteBRecognizerTest,MesProBatchRecordRouteDRecognizerTest,MesProBatchRecordReportLayoutCalibratorTest,MesProBatchRecordReportShapeRulesTest,MesProBatchRecordReportJsonBuilderTest,MesProBatchRecordReportStyleEnhancerTest -Dsurefire.failIfNoSpecifiedTests=false test` -> PASS, 83 tests ran, 0 failed, 6 skipped by local fixture assumptions.
BDD: live-like medium-density process pages should also be allowed to exceed the generic `650px` JSON single-page cap when they carry a real summary/footer tail -> Given `清洁` 这类中密度共享工序页已经通过共享宽度预算贴近目标纸宽，但底部结构化尾段仍容易被 generic `650px` 首屏上限截断, When the JSON builder decides the final row heights for a real Route B medium-density process page, Then it should allow those pages to exceed the generic `650px` cap without loosening rough fixed layouts globally.
RED: `mvn --% -f D:\ProjectPackage\Int\IntRuoyi\worktrees\batch-record-print-fidelity-phase2\pom.xml -pl yudao-module-mes -am -Dtest=MesProBatchRecordReportJsonBuilderTest#build_shouldAllowLiveLikeMediumProcessPagesToExceedTheGeneric650pxJsonCap -Dsurefire.failIfNoSpecifiedTests=false test` -> FAIL, the new fixed-source `清洁工序生产记录` regression still produced `totalHeight=650`, which showed the JSON layer was still clipping medium-density shared process pages even after width alignment.
GREEN: the same focused JSON-builder test command -> PASS after:
- keeping `MesProBatchRecordReportShapeRules.SINGLE_PAGE_MAX_HEIGHT_PX = 650` as the shared default
- extending `MesProBatchRecordReportJsonBuilder.resolveTargetSinglePageHeight(...)` with a live-like medium-density process-page branch
- only applying the higher `670px` target to pages with the shared `SUMMARY / FOOTER / FIELD / LONG_DESCRIPTION|TITLE` signal mix instead of any process-name-specific branch
GREEN: `$env:MAVEN_OPTS='-Xmx1024m -XX:MaxMetaspaceSize=512m'; mvn --% -f D:\ProjectPackage\Int\IntRuoyi\worktrees\batch-record-print-fidelity-phase2\pom.xml -pl yudao-module-mes -am -Dtest=MesProBatchRecordSharedPageTitleRulesTest,MesProBatchRecordSharedRowTypeRulesTest,MesProBatchRecordRouteBRecognizerTest,MesProBatchRecordRouteDRecognizerTest,MesProBatchRecordReportLayoutCalibratorTest,MesProBatchRecordReportShapeRulesTest,MesProBatchRecordReportJsonBuilderTest,MesProBatchRecordReportStyleEnhancerTest -Dsurefire.failIfNoSpecifiedTests=false test` -> PASS, 84 tests ran, 0 failed, 6 skipped by local fixture assumptions.
BDD: repeated sparse checklist rows that appear after a real structured header should compress like detail rows instead of being preserved as repeated headers -> Given some shared process pages contain repeated `/ + □...` sparse checklist rows after the true inspection header, When shared row typing classifies those repeated rows, Then they should be treated as `DETAIL_DATA` so the height budget and compression priority can shrink them like actual detail lines.
RED: `mvn --% -f D:\ProjectPackage\Int\IntRuoyi\worktrees\batch-record-print-fidelity-phase2\pom.xml -pl yudao-module-mes -am -Dtest=MesProBatchRecordSharedRowTypeRulesTest#classifyRow_returnsDetailDataForRepeatedSparseChecklistRowsAfterStructuredHeader -Dsurefire.failIfNoSpecifiedTests=false test` -> FAIL, the new shared row-type regression still returned `TABLE_HEADER` for the repeated `/ + □30atm压力表` row.
GREEN: the same focused shared-row-type test command -> PASS after teaching `MesProBatchRecordSharedRowTypeRules` to classify repeated sparse checklist rows with large blank spans and prior structured headers as `DETAIL_DATA`.
BDD: pages that still carry a structured tail after `SUMMARY` should reserve more first-screen height than an ordinary six-detail page -> Given some shared process pages such as rough-wash layouts still have `TABLE_HEADER + LONG_DESCRIPTION` tail rows after `生产批量汇总`, When the preferred viewport budget is resolved, Then those pages should keep more first-screen height instead of being compressed like a generic six-detail page.
RED: `mvn --% -f D:\ProjectPackage\Int\IntRuoyi\worktrees\batch-record-print-fidelity-phase2\pom.xml -pl yudao-module-mes -am -Dtest=MesProBatchRecordReportLayoutCalibratorTest#calibrate_shouldReserveViewportHeightForStructuredTailAfterSummary,MesProBatchRecordSharedRowTypeRulesTest#classifyRow_returnsDetailDataForRepeatedSparseChecklistRowsAfterStructuredHeader -Dsurefire.failIfNoSpecifiedTests=false test` -> FAIL, the new structured-tail regression still produced `totalHeight=586`, which confirmed that the preferred viewport budget was still compressing `SUMMARY` + structured-tail pages too aggressively.
GREEN: the same focused structured-tail/shared-row-type test command -> PASS after:
- classifying repeated sparse checklist rows as `DETAIL_DATA`
- detecting `SUMMARY`-to-`FOOTER` structured tails in `MesProBatchRecordReportLayoutCalibrator.resolvePreferredViewportHeightBudget(...)`
- relaxing the detail-row penalty and adding a small shared tail-height bonus only for those structured-tail pages
GREEN: `$env:MAVEN_OPTS='-Xmx1024m -XX:MaxMetaspaceSize=512m'; mvn --% -f D:\ProjectPackage\Int\IntRuoyi\worktrees\batch-record-print-fidelity-phase2\pom.xml -pl yudao-module-mes -am -Dtest=MesProBatchRecordSharedPageTitleRulesTest,MesProBatchRecordSharedRowTypeRulesTest,MesProBatchRecordRouteBRecognizerTest,MesProBatchRecordRouteDRecognizerTest,MesProBatchRecordReportLayoutCalibratorTest,MesProBatchRecordReportShapeRulesTest,MesProBatchRecordReportJsonBuilderTest,MesProBatchRecordReportStyleEnhancerTest -Dsurefire.failIfNoSpecifiedTests=false test` -> PASS, 86 tests ran, 0 failed, 6 skipped by local fixture assumptions.
GREEN: `$env:MAVEN_OPTS='-Xmx1024m -XX:MaxMetaspaceSize=512m'; mvn --% -f D:\ProjectPackage\Int\IntRuoyi\worktrees\batch-record-print-fidelity-phase2\pom.xml -pl yudao-server -am -Dmaven.test.skip=true package` -> PASS after the structured-tail and repeated-sparse-detail shared-rule repair.
GREEN: restarted `48082` with the refreshed `output\\runtime\\backend-48082-generic-layout.jar`, reran authenticated live `recognize-fixed?routeKey=B`, and recaptured the direct `/jmreport/view` compare images -> PASS, `importedCount=15`, `updatedCount=15`; the refreshed live compare shows `2.png` with the post-summary clean-field block closer to the first screen, and `4.png` with flatter repeated pressure-table details plus a more visible `生产后清场记录` tail.
BDD: JSON builder should preserve the compacted height of one-line detail rows on structured-tail pages instead of re-inflating them back to the default row height -> Given a live-like fixed-source `清洁工序生产记录` page where `calibrator` has already compacted repeated pressure-table detail rows, When the JSON builder emits the final JMReport rows for that same page, Then the repeated one-line detail rows should keep the compacted height instead of being restored to the default `28px` height.
RED: `mvn --% -f D:\ProjectPackage\Int\IntRuoyi\worktrees\batch-record-print-fidelity-phase2\pom.xml -pl yudao-module-mes -am -Dtest=MesProBatchRecordReportJsonBuilderTest#build_shouldNotReinflateLiveRepeatedChecklistDetailRowsOnFixedCleanProcessPage -Dsurefire.failIfNoSpecifiedTests=false test` -> FAIL, the live fixed-source `清洁工序生产记录` detail row still came back as `builtDetailHeight=24` while `calibratedDetailHeight=20`, proving the JSON layer was re-inflating compacted rows.
GREEN: `MesProBatchRecordReportJsonBuilder` now preserves compacted one-line row heights on structured-tail pages by keeping the calibrator height for non-narrative rows and only allowing JSON-side growth for long descriptions; the same focused builder test command -> PASS after the fix, and the suite now records `24` builder tests plus the earlier MES regressions all green.
GREEN: `$env:MAVEN_OPTS='-Xmx1024m -XX:MaxMetaspaceSize=512m'; mvn --% -f D:\ProjectPackage\Int\IntRuoyi\worktrees\batch-record-print-fidelity-phase2\pom.xml -pl yudao-module-mes -am -Dtest=MesProBatchRecordSharedPageTitleRulesTest,MesProBatchRecordSharedRowTypeRulesTest,MesProBatchRecordRouteBRecognizerTest,MesProBatchRecordRouteDRecognizerTest,MesProBatchRecordReportLayoutCalibratorTest,MesProBatchRecordReportShapeRulesTest,MesProBatchRecordReportJsonBuilderTest,MesProBatchRecordReportStyleEnhancerTest -Dsurefire.failIfNoSpecifiedTests=false test` -> PASS, 88 tests ran, 0 failed, 6 skipped by local fixture assumptions.
BDD: low-column shared overview pages should expand to the same paper-width band as the current target screenshot instead of staying on the old `960px` narrow budget -> Given the target `装配及包装信息` screenshot uses a visibly wider low-density paper than the current live `960px` overview sheet, When the shared builder resolves final rendered column widths for a low-column overview page, Then it should stretch the page to the shared `1120px` narrow overview budget instead of collapsing back to the generic `72px` clamp or the older `960px` band.
RED: `mvn --% -f D:\ProjectPackage\Int\IntRuoyi\worktrees\batch-record-print-fidelity-phase2\pom.xml -pl yudao-module-mes -am -Dtest=MesProBatchRecordReportShapeRulesTest#resolveTargetRenderWidth_shouldUseSharedBudgetByColumnDensity,MesProBatchRecordReportShapeRulesTest#resolveSharedPageWidthBudget_shouldFollowGenericDensityBands,MesProBatchRecordReportJsonBuilderTest#build_shouldPreserveBudgetSizedWideColumnsForGenericOverviewPages -Dsurefire.failIfNoSpecifiedTests=false test` -> FAIL, the shared narrow-page budget still returned `960`, and the overview builder regression still emitted `dataRectWidth=960`.
GREEN: the same focused narrow-budget/overview builder command -> PASS after:
- raising `SHARED_PAGE_WIDTH_NARROW_BUDGET_PX` from `960` to `1120`
- teaching `MesProBatchRecordReportJsonBuilder.resolveRenderedColumnWidths(...)` to proportionally stretch low-column shared pages up to the shared budget instead of only clamping widths down to `72px`
- preserving the old `1`-column cap path so the legacy single-column width guard still holds
GREEN: direct JDBC probe of `jimu_report.json_str` for live report `EBR_B_T01` -> PASS, stored `dataRectWidth=1120`, which confirmed the backend generation chain had updated even before the browser capture cache was cleared.
BDD: medium-density shared pages should match the current target screenshots' paper width more closely once the narrow overview band is already aligned -> Given the current clean-process target screenshot uses `1044px` paper width while the live shared medium band still stops at `1040`, When the shared medium page width budget is resolved and the report is regenerated, Then medium-density pages such as `清洁工序生产记录` should move to the `1044px` paper-width band as well.
RED: `mvn --% -f D:\ProjectPackage\Int\IntRuoyi\worktrees\batch-record-print-fidelity-phase2\pom.xml -pl yudao-module-mes -am -Dtest=MesProBatchRecordReportShapeRulesTest#resolveSharedPageWidthBudget_shouldFollowGenericDensityBands -Dsurefire.failIfNoSpecifiedTests=false test` -> FAIL, the medium-density shared width budget still returned `1040`.
GREEN: the same focused medium-band shape-rule command -> PASS after raising `SHARED_PAGE_WIDTH_MEDIUM_BUDGET_PX` from `1040` to `1044`.
GREEN: direct JDBC probe of `jimu_report.json_str` for live report `EBR_B_T05` -> PASS, stored `dataRectWidth=1044`, which confirms the clean-process live report now lands on the updated medium-width band.
GREEN: `$env:MAVEN_OPTS='-Xmx1024m -XX:MaxMetaspaceSize=512m'; mvn --% -f D:\ProjectPackage\Int\IntRuoyi\worktrees\batch-record-print-fidelity-phase2\pom.xml -pl yudao-module-mes -am -Dtest=MesProBatchRecordSharedPageTitleRulesTest,MesProBatchRecordSharedRowTypeRulesTest,MesProBatchRecordRouteBRecognizerTest,MesProBatchRecordRouteDRecognizerTest,MesProBatchRecordReportLayoutCalibratorTest,MesProBatchRecordReportShapeRulesTest,MesProBatchRecordReportJsonBuilderTest,MesProBatchRecordReportStyleEnhancerTest -Dsurefire.failIfNoSpecifiedTests=false test` -> PASS, 89 tests ran, 0 failed, 6 skipped by local fixture assumptions.

BDD: source-doc-priority Route B should preserve the full first Word table instead of exposing only its representative trailing assembly section -> Given fixed source `D:\ProjectPackage\Int\IntRuoyi\resource\批记录模板.doc` has a first Word table that spans source pages `page-01 ~ page-03` and contains `产品信息 / 配件进货批号信息 / 装配及包装信息 / 过程放行信息`, When Route B recognizes the fixed source under source-doc-priority mode, Then it should still produce exactly `15` templates while exposing `EBR_B_T01` as `产品信息` and keeping all first-table section content inside that first generated report.
RED: `$env:MAVEN_OPTS='-Xmx1024m -XX:MaxMetaspaceSize=512m'; mvn --% -f D:\ProjectPackage\Int\IntRuoyi\worktrees\batch-record-print-fidelity-phase2\pom.xml -pl yudao-module-mes -am -Dtest=MesProBatchRecordRouteBRecognizerTest#recognizeFixedSourceDoc_doesNotSplitInternalSummarySectionsIntoExtraTemplates -Dsurefire.failIfNoSpecifiedTests=false test` -> FAIL, the fixed-source Route B regression still returned first title `装配及包装信息` instead of source-table title `产品信息`.
GREEN: the same focused Route B recognizer command -> PASS after preserving the full leading multi-short-title overview table and resolving its title from the first shared title row instead of the representative trailing section.
RED: `$env:MAVEN_OPTS='-Xmx1024m -XX:MaxMetaspaceSize=512m'; mvn --% -f D:\ProjectPackage\Int\IntRuoyi\worktrees\batch-record-print-fidelity-phase2\pom.xml -pl yudao-module-mes -am -Dtest=MesProBatchRecordSharedPageTitleRulesTest,MesProBatchRecordSharedRowTypeRulesTest,MesProBatchRecordRouteBRecognizerTest,MesProBatchRecordRouteDRecognizerTest,MesProBatchRecordReportLayoutCalibratorTest,MesProBatchRecordReportShapeRulesTest,MesProBatchRecordReportJsonBuilderTest,MesProBatchRecordReportStyleEnhancerTest -Dsurefire.failIfNoSpecifiedTests=false test` -> FAIL, `MesProBatchRecordReportJsonBuilderTest#build_shouldUse1120WidthForFixedOverviewPage` still looked up the old representative title `装配及包装信息`, so the test fixture could no longer find the source-priority overview report.
GREEN: `mvn --% -f D:\ProjectPackage\Int\IntRuoyi\worktrees\batch-record-print-fidelity-phase2\pom.xml -pl yudao-module-mes -am -Dtest=MesProBatchRecordReportJsonBuilderTest#build_shouldUse1120WidthForFixedOverviewPage -Dsurefire.failIfNoSpecifiedTests=false test` -> PASS after retargeting the width regression to the fixed source's true first report title `产品信息` while keeping the `dataRectWidth = 1120` assertion.
GREEN: `$env:MAVEN_OPTS='-Xmx1024m -XX:MaxMetaspaceSize=512m'; mvn --% -f D:\ProjectPackage\Int\IntRuoyi\worktrees\batch-record-print-fidelity-phase2\pom.xml -pl yudao-module-mes -am -Dtest=MesProBatchRecordSharedPageTitleRulesTest,MesProBatchRecordSharedRowTypeRulesTest,MesProBatchRecordRouteBRecognizerTest,MesProBatchRecordRouteDRecognizerTest,MesProBatchRecordReportLayoutCalibratorTest,MesProBatchRecordReportShapeRulesTest,MesProBatchRecordReportJsonBuilderTest,MesProBatchRecordReportStyleEnhancerTest -Dsurefire.failIfNoSpecifiedTests=false test` -> PASS, 89 tests ran, 0 failed, 6 skipped by local fixture assumptions.
GREEN: `$env:MAVEN_OPTS='-Xmx1024m -XX:MaxMetaspaceSize=512m'; mvn --% -f D:\ProjectPackage\Int\IntRuoyi\worktrees\batch-record-print-fidelity-phase2\pom.xml -pl yudao-server -am -Dmaven.test.skip=true package` -> PASS after the source-doc-priority Route B title/body preservation change.
GREEN: restarted `48082` with refreshed `output\runtime\backend-48082-generic-layout.jar`, reran authenticated live `POST /admin-api/mes/pro/batch-record-report/recognize-fixed?routeKey=B` -> PASS, returned `importedCount=15`, `updatedCount=15`, and `EBR_B_T01` is now `电子批记录[B]-表1-产品信息` with `tableTitle=产品信息`.

BDD: source-doc-priority long repeated operation pages should restore the source document's continuation header rhythm without splitting extra templates -> Given fixed source `D:\ProjectPackage\Int\IntRuoyi\resource\批记录模板.doc` renders `T4 / 清洗工序生产记录` across source pages `page-06 ~ page-07` with the second page resuming after the `手柄` operation segment, When Route B calibrates that long repeated operation table, Then it should keep one `EBR_B_T04` report while inserting a second document header before the later `齿条 / 芯杆 / 螺盖` repeated segments.
RED: `$env:MAVEN_OPTS='-Xmx1024m -XX:MaxMetaspaceSize=512m'; mvn --% -f D:\ProjectPackage\Int\IntRuoyi\worktrees\batch-record-print-fidelity-phase2\pom.xml -pl yudao-module-mes -am -Dtest=MesProBatchRecordReportLayoutCalibratorTest#calibrate_shouldInsertContinuationHeaderBeforeLaterRepeatedOperationSegments -Dsurefire.failIfNoSpecifiedTests=false test` -> FAIL, the fixed-source `清洗工序生产记录` calibration still exposed only one `球囊扩张压力泵生产记录` document header.
GREEN: the same focused layout-calibrator test command -> PASS after detecting long repeated operation-parameter rows and inserting a continuation document header before the fourth repeated segment.
GREEN: `$env:MAVEN_OPTS='-Xmx1024m -XX:MaxMetaspaceSize=512m'; mvn --% -f D:\ProjectPackage\Int\IntRuoyi\worktrees\batch-record-print-fidelity-phase2\pom.xml -pl yudao-module-mes -am -Dtest=MesProBatchRecordSharedPageTitleRulesTest,MesProBatchRecordSharedRowTypeRulesTest,MesProBatchRecordRouteBRecognizerTest,MesProBatchRecordRouteDRecognizerTest,MesProBatchRecordReportLayoutCalibratorTest,MesProBatchRecordReportShapeRulesTest,MesProBatchRecordReportJsonBuilderTest,MesProBatchRecordReportStyleEnhancerTest -Dsurefire.failIfNoSpecifiedTests=false test` -> PASS, 90 tests ran, 0 failed, 6 skipped by local fixture assumptions.
GREEN: `$env:MAVEN_OPTS='-Xmx1024m -XX:MaxMetaspaceSize=512m'; mvn --% -f D:\ProjectPackage\Int\IntRuoyi\worktrees\batch-record-print-fidelity-phase2\pom.xml -pl yudao-server -am -Dmaven.test.skip=true package` -> PASS after the long repeated-operation continuation-header repair.
GREEN: restarted `48082` with refreshed `output\runtime\backend-48082-generic-layout.jar`, reran authenticated live `POST /admin-api/mes/pro/batch-record-report/recognize-fixed?routeKey=B`, and reprobed `EBR_B_T04` through `/jmreport/show` -> PASS, returned `importedCount=15`, `updatedCount=15`; live `EBR_B_T04` now has document header rows `[0, 21]`, with the second header after `手柄` row `17` and before `齿条` row `23`.

BDD: source-doc-priority repeated equipment-matrix pages should restore the second source page header before the later heat-seal matrix rows -> Given fixed source `D:\ProjectPackage\Int\IntRuoyi\resource\批记录模板.doc` renders `T13 / 单包装工序生产记录` across source pages `page-16 ~ page-17` with a long repeated heat-seal equipment matrix, When Route B calibrates that table, Then it should keep one `EBR_B_T13` report while inserting a second document header inside the repeated equipment matrix before `生产自检`.
RED: `$env:MAVEN_OPTS='-Xmx1024m -XX:MaxMetaspaceSize=512m'; mvn --% -f D:\ProjectPackage\Int\IntRuoyi\worktrees\batch-record-print-fidelity-phase2\pom.xml -pl yudao-module-mes -am -Dtest=MesProBatchRecordReportLayoutCalibratorTest#calibrate_shouldInsertContinuationHeaderInsideLongRepeatedEquipmentMatrix -Dsurefire.failIfNoSpecifiedTests=false test` -> FAIL, the fixed-source `单包装工序生产记录` calibration still exposed only one `球囊扩张压力泵生产记录` document header.
GREEN: the same focused layout-calibrator test command -> PASS after detecting long repeated equipment-matrix rows and inserting a continuation document header at the midpoint of that repeated matrix.
GREEN: `$env:MAVEN_OPTS='-Xmx1024m -XX:MaxMetaspaceSize=512m'; mvn --% -f D:\ProjectPackage\Int\IntRuoyi\worktrees\batch-record-print-fidelity-phase2\pom.xml -pl yudao-module-mes -am -Dtest=MesProBatchRecordSharedPageTitleRulesTest,MesProBatchRecordSharedRowTypeRulesTest,MesProBatchRecordRouteBRecognizerTest,MesProBatchRecordRouteDRecognizerTest,MesProBatchRecordReportLayoutCalibratorTest,MesProBatchRecordReportShapeRulesTest,MesProBatchRecordReportJsonBuilderTest,MesProBatchRecordReportStyleEnhancerTest -Dsurefire.failIfNoSpecifiedTests=false test` -> PASS, 91 tests ran, 0 failed, 6 skipped by local fixture assumptions.
GREEN: `$env:MAVEN_OPTS='-Xmx1024m -XX:MaxMetaspaceSize=512m'; mvn --% -f D:\ProjectPackage\Int\IntRuoyi\worktrees\batch-record-print-fidelity-phase2\pom.xml -pl yudao-server -am -Dmaven.test.skip=true package` -> PASS after the repeated equipment-matrix continuation-header repair.
GREEN: restarted `48082` with refreshed `output\runtime\backend-48082-generic-layout.jar`, reran authenticated live `POST /admin-api/mes/pro/batch-record-report/recognize-fixed?routeKey=B`, and reprobed `EBR_B_T13` through `/jmreport/show` -> PASS, returned `importedCount=15`, `updatedCount=15`; live `EBR_B_T13` now has document header rows `[0, 15]`, heat-seal matrix rows `[10, 11, 12, 13, 14, 17, 18, 19, 20, 21]`, and `生产自检` row `[22]`, so the second header lands after the first heat-seal matrix rows and before self-inspection.

BDD: source-doc-priority overview tables should restore the fixed source's three-page information-summary header rhythm without splitting extra templates -> Given fixed source `D:\ProjectPackage\Int\IntRuoyi\resource\批记录模板.doc` renders `T1 / 产品信息` across source pages `page-01 ~ page-03` with later standalone information sections `配件进货批号信息` and `装配及包装信息`, When Route B calibrates that long overview table, Then it should keep one `EBR_B_T01` report while inserting continuation document headers before the later overview sections.
RED: `$env:MAVEN_OPTS='-Xmx1024m -XX:MaxMetaspaceSize=512m'; mvn --% -f D:\ProjectPackage\Int\IntRuoyi\worktrees\batch-record-print-fidelity-phase2\pom.xml -pl yudao-module-mes -am -Dtest=MesProBatchRecordReportLayoutCalibratorTest#calibrate_shouldInsertContinuationHeadersBeforeLaterOverviewSections -Dsurefire.failIfNoSpecifiedTests=false test` -> FAIL, the fixed-source `产品信息` overview calibration still exposed only one `球囊扩张压力泵生产记录` document header.
GREEN: the same focused layout-calibrator test command -> PASS after detecting long-spaced standalone overview `*信息` section titles and inserting continuation document headers before the later major sections.
GREEN: `$env:MAVEN_OPTS='-Xmx1024m -XX:MaxMetaspaceSize=512m'; mvn --% -f D:\ProjectPackage\Int\IntRuoyi\worktrees\batch-record-print-fidelity-phase2\pom.xml -pl yudao-module-mes -am -Dtest=MesProBatchRecordSharedPageTitleRulesTest,MesProBatchRecordSharedRowTypeRulesTest,MesProBatchRecordRouteBRecognizerTest,MesProBatchRecordRouteDRecognizerTest,MesProBatchRecordReportLayoutCalibratorTest,MesProBatchRecordReportShapeRulesTest,MesProBatchRecordReportJsonBuilderTest,MesProBatchRecordReportStyleEnhancerTest -Dsurefire.failIfNoSpecifiedTests=false test` -> PASS, 92 tests ran, 0 failed, 6 skipped by local fixture assumptions.
GREEN: `$env:MAVEN_OPTS='-Xmx1024m -XX:MaxMetaspaceSize=512m'; mvn --% -f D:\ProjectPackage\Int\IntRuoyi\worktrees\batch-record-print-fidelity-phase2\pom.xml -pl yudao-server -am -Dmaven.test.skip=true package` -> PASS after the overview-section continuation-header repair.
GREEN: restarted `48082` with refreshed `output\runtime\backend-48082-generic-layout.jar`, reran authenticated live `POST /admin-api/mes/pro/batch-record-report/recognize-fixed?routeKey=B`, and reprobed `EBR_B_T01` through `/jmreport/show` -> PASS, returned `importedCount=15`, `updatedCount=15`; live `EBR_B_T01` now has document header rows `[0, 18, 36]`, `配件进货批号信息` row `[20]`, `装配及包装信息` row `[38]`, and `过程放行信息` row `[48]`, so the three overview sections align with the source document's page rhythm.

BDD: source-doc-priority repeated cleaning blocks with drying subrows should keep the right-side result columns merged per material block -> Given fixed source `D:\ProjectPackage\Int\IntRuoyi\resource\批记录模板.doc` renders `T4 / 清洗工序生产记录` with each material block spanning cleaning rows plus drying subrows while the right-side quantity and operator columns stay vertically merged across the whole block, When Route B calibrates that repeated operation page, Then the tail blank result columns on those blocks should merge vertically instead of staying as row-by-row blanks.
RED: `$env:MAVEN_OPTS='-Xmx1024m -XX:MaxMetaspaceSize=512m'; mvn --% -f D:\ProjectPackage\Int\IntRuoyi\worktrees\batch-record-print-fidelity-phase2\pom.xml -pl yudao-module-mes -am -Dtest=MesProBatchRecordReportLayoutCalibratorTest#calibrate_shouldMergeTrailingBlankResultColumnsAcrossRepeatedOperationBlocks -Dsurefire.failIfNoSpecifiedTests=false test` -> FAIL, the fixed-source `清洗工序生产记录` still exposed the first repeated block's far-right blank cell with `rowSpan=1` instead of a block-level vertical merge.
GREEN: the same focused layout-calibrator test command -> PASS after teaching the shared calibrator to scan repeated cleaning blocks with drying subrows and merge their trailing blank result columns from right to left so later column positions stay stable.
GREEN: `$env:MAVEN_OPTS='-Xmx1024m -XX:MaxMetaspaceSize=512m'; mvn --% -f D:\ProjectPackage\Int\IntRuoyi\worktrees\batch-record-print-fidelity-phase2\pom.xml -pl yudao-module-mes -am -Dtest=MesProBatchRecordSharedPageTitleRulesTest,MesProBatchRecordSharedRowTypeRulesTest,MesProBatchRecordRouteBRecognizerTest,MesProBatchRecordRouteDRecognizerTest,MesProBatchRecordReportLayoutCalibratorTest,MesProBatchRecordReportShapeRulesTest,MesProBatchRecordReportJsonBuilderTest,MesProBatchRecordReportStyleEnhancerTest -Dsurefire.failIfNoSpecifiedTests=false test` -> PASS, 93 tests ran, 0 failed, 6 skipped by local fixture assumptions.
GREEN: `$env:MAVEN_OPTS='-Xmx1024m -XX:MaxMetaspaceSize=512m'; mvn --% -f D:\ProjectPackage\Int\IntRuoyi\worktrees\batch-record-print-fidelity-phase2\pom.xml -pl yudao-server -am -Dmaven.test.skip=true package` -> PASS after the repeated cleaning-block tail-column merge repair.
GREEN: restarted `48082` with refreshed `output\runtime\backend-48082-generic-layout.jar`, reran authenticated live `POST /admin-api/mes/pro/batch-record-report/recognize-fixed?routeKey=B`, and reprobed `EBR_B_T04` through `/jmreport/show` -> PASS, returned `importedCount=15`, `updatedCount=15`; live `EBR_B_T04` now stores the first repeated block's trailing blank result columns with `merge: [2,0]` on row `10`, which confirms the block-level vertical merge is present in the generated report JSON.
