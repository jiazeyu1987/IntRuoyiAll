# 生产工单名称去除误追加工单编号

## 任务目标

修正本机 MES 生产工单测试数据：工单名称末尾误追加自身工单编号的记录，需要去掉尾部工单编号，只保留产品/工单名称本身。

## 设计约束检查

- 是否引入 fallback/降级/吞异常：否。
- 是否从根因和长期维护角度解决：是，仅更新命中“名称 = 原名称 + 空格 + 自身工单编号”的数据，避免误伤正常名称。
- 是否存在临时补丁或绕过：否。

## 里程碑

- [x] M1：查询确认异常数据范围。
- [x] M2：按精确条件更新本机数据。
- [x] M3：回查确认异常数据清零并记录证据。

## 预期验证

- SQL 回查 `name = <prefix> + ' ' + code` 的异常记录数为 0。
- 回查受影响工单名称不再包含尾部自身工单编号。

## 当前状态

completed
