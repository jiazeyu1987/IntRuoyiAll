# Execution Log：补齐 eDHR 冠状动脉棘突球囊扩张导管产品主数据

BDD: 产品主数据存在性检查 -> Given 测试租户需要按产品建立完整 eDHR 工艺路线 / When 查询 MES 主产品/物料主数据 / Then 如果 `冠状动脉棘突球囊扩张导管` 不存在，必须明确失败并进入正式新建流程。

BDD: 新建产品主数据 -> Given 测试租户产品主数据缺失 / When 插入正式产品记录 / Then 可按产品名称和编码查询到启用状态产品，且不修改 `芋道源码` 租户数据。

- SETUP: 创建任务文档 -> PASS，任务目录 `doc/tasks/20260608-edhr-product-master-data`。

- VERIFY: 查询租户 -> PASS；`system_tenant` 中 `测试租户` id=`122`，`芋道源码` id=`1`。

- VERIFY: 查询产品类型 -> PASS；测试租户产品类型为 `mes_md_item_type.id=7`，`item_or_product=PRODUCT`，状态启用。

- RED: 查询精确编码 `YXN.069.001.1001` -> FAIL，测试租户和 `芋道源码` 均未找到该编码，不能直接把路线编码当作已存在产品编码。

- GREEN: 使用 Unicode escape 查询测试租户 `mes_md_item` 产品名称 `冠状动脉棘突球囊扩张导管` -> PASS，已存在 6 条启用产品记录：`YXN.069.001.1003`、`YXN.069.001.1006`、`YXN.069.001.1009`、`YXN.069.001.1011`、`YXN.069.001.1013`、`YXN.069.001.1014`。

- VERIFY: 查询精确编码 `YXN.069.001.1001` -> PASS，测试租户和 `芋道源码` 均未找到该编码；本任务按用户要求“有则不新建”，未插入重复产品。

- VERIFY: PowerShell 管道中文字面量查询复核 -> PASS；直接在管道脚本里写中文会被当前终端编码压成 `????`，精确中文查询需使用 UTF-8 文件读取或 Unicode escape。

- FINAL: 产品主数据按名称已存在，无数据库写入；后续如要绑定工艺路线，需要业务确认使用现有规格之一，或明确创建精确编码 `YXN.069.001.1001` 的规格。
