# 任务：补齐 eDHR 冠状动脉棘突球囊扩张导管产品主数据

## 任务目标

检查 `冠状动脉棘突球囊扩张导管` 是否已存在于 MES 主产品/物料主数据；如测试租户缺失，则在测试租户正式新建该产品主数据，用于后续绑定工艺路线和 eDHR 执行。

## 范围边界

- 只允许修改 `测试租户` 数据。
- `芋道源码/admin` 仅查询验证，不新增、不修改、不删除。
- 本任务不绑定工艺路线、不修改批记录报表、不修改 eDHR 执行记录。
- 不使用 mock、占位产品或默认成功值。

## 设计约束检查

- `是否引入 fallback/降级/吞异常`：否。
- `是否从根因和长期维护角度解决`：是。先补产品主数据，再由产品-路线-工序-批记录绑定形成完整 eDHR 链路。
- `是否存在临时补丁或绕过`：否。

## BDD 场景

- BDD: 产品主数据存在性检查 -> Given 测试租户需要按产品建立完整 eDHR 工艺路线 / When 查询 MES 主产品/物料主数据 / Then 如果 `冠状动脉棘突球囊扩张导管` 不存在，必须明确失败并进入正式新建流程。
- BDD: 新建产品主数据 -> Given 测试租户产品主数据缺失 / When 插入正式产品记录 / Then 可按产品名称和编码查询到启用状态产品，且不修改 `芋道源码` 租户数据。

## 里程碑

- [x] M1：查询现有产品主数据和表结构约束。
- [x] M2：确认测试租户已存在同名产品主数据，无须新建重复产品。
- [x] M3：验证测试租户产品存在，且 `芋道源码` 未被修改。

## 预期验证

- 查询 `system_tenant` 确认租户 ID。
- 查询 `mes_md_item` / `mes_md_item_type` 确认产品类型与必填字段。
- 查询测试租户 `冠状动脉棘突球囊扩张导管` 产品存在。

## Current Status

completed；已查询测试租户主产品/物料主数据，`冠状动脉棘突球囊扩张导管` 已存在 6 条启用产品记录，本任务未新建重复产品。精确编码 `YXN.069.001.1001` 当前不存在；如后续需要把工艺路线 `ROUTE-YXN.069.001.1001` 绑定到特定产品，需要先由业务确认规格后再新建或选择对应产品。
