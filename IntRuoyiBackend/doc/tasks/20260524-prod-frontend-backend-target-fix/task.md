# 20260524 prod frontend backend target fix

## 任务目标

- 修复正式前端发布链路中 backend 目标错误的问题。
- 已确认现象：正式前端 `http://172.30.30.57:8081` 的 JS 产物包含 `172.30.30.58:48081`，导致正式前端请求测试服 backend。
- 本任务先完成本地构建验证和发布脚本修复；正式服发布必须等用户明确 `PROD` 确认。

## 里程碑

1. 记录 BDD/TDD 与服务仓库任务文档。
2. RED：证明当前正式前端远端产物包含测试服 backend 地址。
3. GREEN：使用正式 backend 目标重建前端产物，并验证产物只包含 `172.30.30.57:48081`。
4. RED：补充 promotion 脚本级测试，证明测试提升正式时缺少正式目标前端重建。
5. GREEN：修复 promotion 脚本，使其在上传正式镜像包前按正式 backend 目标重建前端镜像。
6. 等待用户明确 `PROD` 后执行正式发布并复跑 G6/G7；未确认则保持发布门禁 `BLOCKED`。

## 预期验证

- 远端正式前端现有产物检查必须失败，原因是包含 `172.30.30.58:48081`。
- 本地正式目标构建命令：
  - `pnpm exec vite build --mode test`
  - 环境变量 `VITE_BASE_URL=http://172.30.30.57:48081`、`VITE_BASE_PATH=/`、`VITE_OUT_DIR=dist-intruoyi-test`。
- 构建产物检查：
  - 不得包含 `172.30.30.58:48081`。
  - 必须包含 `172.30.30.57:48081`。

## 当前状态

- 状态：completed
- 当前阶段：promotion 脚本级修复和验证已完成；正式环境发布与 G6/G7 复验仍等待显式 `PROD` 确认。
- 已完成：
  - 当前正式远端产物检查失败，确认 `assets/index-b7bUP0rr.js` 包含 `172.30.30.58:48081`。
  - 使用 `VITE_BASE_URL=http://172.30.30.57:48081` 完成本地前端构建。
  - 构建产物检查通过，只包含 `172.30.30.57:48081`，未包含 `172.30.30.58:48081`。
  - `promote-int-ruoyi-test-to-prod.ps1` 已改为从测试服只导出 backend 镜像，在本机按正式 backend 目标重建 frontend 镜像，并上传目标重建后的镜像包。
  - 脚本测试全集和 PowerShell 语法解析通过。
- 阻塞：
  - 正式服发布需要用户明确回复 `PROD` 后才能执行。
  - 发布前 G6/G7 仍不能 GO。
