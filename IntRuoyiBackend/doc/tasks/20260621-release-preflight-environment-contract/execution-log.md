# 执行日志: 20260621-release-preflight-environment-contract

- BDD: 非目标环境迁移不得阻断同一发布包跨环境推进 -> Given 页面构建发布包会同时携带 test/backup-only 与 test/backup/prod 迁移 / When prod preflight 读取该发布包的 schemaMigrations / Then test/backup-only 迁移必须被显式跳过，而不是把整份 preflight-plan 判成 blocked。
- BDD: 迁移依赖的环境范围必须自洽 -> Given 某个迁移依赖另一个迁移 / When 子迁移允许的环境超出父迁移允许的环境 / Then 发布迁移 policy gate 必须 fail-fast 阻断该不自洽依赖，而不是把问题留到真实页面正式服阶段。
- BDD: 发布包读回 required SQL 元数据时不得丢失环境信息 -> Given release package 的 manifest 已声明每个 required SQL 的 allowedEnvironments / When deploy-release 从包内读取 required SQL 列表 / Then 必须保留并使用真实 allowedEnvironments，而不是硬编码成 test/prod/backup 全量允许。

- GREEN: previous-task-check -> PASS，上一后端任务 `20260621-srm-release-page-flow-sql-blockers` 已 `COMPLETED`。
- GREEN: experience-preflight -> PASS，已读取 `D:\ProjectPackage\Int\IntRuoyi\docs\experience-index.md` 命中的 `release-backup-restore.md` 与 `server-access.md`；本任务只做本机发布输入 / preflight 契约修复，不直接操作远端环境。
- BLOCKER: page-flow-prod-preflight-evidence -> 维护仓真实页面 operation `op-2026-06-21T050013146743700Z-46beb4fb-5f6c-4d2a-ba1c-4f1171a3f54b` 在正式服真实执行阶段失败，日志报 `preflight-plan.json status must be passed before deploy-release: blocked`。
- BLOCKER: page-flow-prod-preflight-env-contract -> 只读核对 `release-20260621-page-full-flow-v4\preflight-plan.json` 与 `manifest.json` 后确认，`prod` 目标环境被 8 个 `allowedEnvironments=test,backup` 的 `post_release_role_e2e_gate*` 迁移阻断；这些迁移当前被 preflight 产出为 `BLOCKED_ENV_NOT_ALLOWED`，而不是环境跳过。
- BLOCKER: release-package-sql-metadata-roundtrip -> 只读核对当前发布包契约与脚本后确认，`deploy-release` 读回包内 required SQL 时仍存在环境元数据丢失风险；当前运行链路需要用 RED 测试先把 planner / policy gate / publish script 的环境契约一并钉住，再进入修复。
- RED: `python -X utf8 -m pytest script\tests\test_release_preflight_plan.py script\tests\test_release_deploy_executor.py script\tests\test_release_migration_policy_gate.py script\tests\test_publish_int_ruoyi_to_test_tooling.py -q` -> FAIL，`7 failed, 101 passed`；失败点依次钉住：`release_preflight_plan.py` 仍把非目标环境迁移判成 `BLOCKED_ENV_NOT_ALLOWED` 且在 dependency 被环境跳过时仍放行 child，`release_deploy_executor.py` 仍拒绝 `SKIP_ENV_NOT_ALLOWED`，`release_migration_policy_gate.py` 尚未阻断 child 环境超出 dependency 环境的反向依赖，以及 `publish-int-ruoyi.ps1` 仍在 build/deploy 两端丢失包内 `allowedEnvironments` 契约。
- GREEN: preflight-contract-fix-implemented -> PASS，已在 `script\release\release_preflight_plan.py`、`script\release\release_deploy_executor.py`、`script\release\release_migration_policy_gate.py`、`script\deploy\publish-int-ruoyi.ps1` 完成最小契约修复：非目标环境迁移改为 `SKIP_ENV_NOT_ALLOWED`，dependency 环境范围不自洽由 policy gate fail-fast，build-release 统一打包跨环境 required SQL，deploy-release 改为从包内 `manifest.json` 读回真实 `allowedEnvironments`。
- GREEN: `python -X utf8 -m pytest script\tests\test_release_preflight_plan.py -q` -> PASS，`11 passed`。
- GREEN: `python -X utf8 -m pytest script\tests\test_release_deploy_executor.py -q` -> PASS，`5 passed`。
- GREEN: `python -X utf8 -m pytest script\tests\test_release_migration_policy_gate.py -q` -> PASS，`7 passed`。
- GREEN: `python -X utf8 -m pytest script\tests\test_publish_int_ruoyi_to_test_tooling.py -q` -> PASS，`85 passed`。
- GREEN: `python -X utf8 script\release\run-release-migration-policy-gate.py --sql-root sql\mysql` -> PASS，返回 `status=passed`，证明新的 dependency allowedEnvironments 子集规则不会阻断当前真实 SQL 树。
- GREEN: `python -X utf8 C:\Users\BJB110\.codex\skills\bug-regression-fix-loop\scripts\validate_bug_regression.py --evidence D:\ProjectPackage\Int\IntRuoyi\ruoyi-vue-pro\doc\tasks\20260621-release-preflight-environment-contract\bug-regression-evidence.md` -> PASS，缺陷证据合同结构有效。
- GREEN: maintenance-runtime-contract-sync -> PASS，已将同样的跨环境发布契约修复同步到维护仓 `D:\ProjectPackage\Int\IntRuoyiMaintance\ops\deploy\publish-int-ruoyi.ps1` 与 `ops\release\*.py`，并通过文本契约核对确认真实页面运行时脚本已具备 `SKIP_ENV_NOT_ALLOWED`、包内 `manifest.json` 读回和跨环境 SQL 打包语义。
- RED: `python -X utf8 -m pytest script\tests\test_release_migration_policy_gate.py -q` / `python -X utf8 -m pytest script\tests\test_publish_int_ruoyi_to_test_tooling.py -q` -> FAIL，本轮提交前复跑时先后暴露 `20260618_mes_edhr_delivery_cockpit.sql` 等 6 条 eDHR SQL 缺少 `release-migration` 元数据、`20260618_mes_edhr_flow_intervention_log.sql` 与 `20260618_mes_edhr_unified_change_impact.sql` 仍使用历史自定义 metadata 前缀，以及 `20260618_mes_edhr_form_instance.sql` 错把文档契约 `20260618_mes_edhr_existing_contract` 写成 SQL migration 依赖。
- GREEN: edhr-release-migration-contract-sweep -> PASS，已为 `20260618_mes_edhr_delivery_cockpit.sql`、`20260618_mes_edhr_flow_intervention_log.sql`、`20260618_mes_edhr_form_instance.sql`、`20260618_mes_edhr_oq_pq_execution_deviation.sql`、`20260618_mes_edhr_report_catalog.sql`、`20260618_mes_edhr_unified_change_impact.sql`、`20260618_mes_edhr_validation_package_matrix.sql`、`20260619_mes_edhr_deployment_license_interface.sql` 统一补齐或修正 `release-migration` 契约；其中 `validation_package_matrix -> delivery_cockpit`、`oq_pq_execution_deviation -> validation_package_matrix`、`deployment_license_interface -> delivery_cockpit`、`flow_intervention_log -> release_transaction_lifecycle`、`unified_change_impact -> record_change_menu` 依赖已按原任务边界固化，`form_instance` 改回独立切片无显式 SQL 依赖。
- GREEN: metadata-regression-rerun -> PASS，`python -X utf8 -m pytest script\tests\test_release_preflight_plan.py -q` -> `11 passed`；`python -X utf8 -m pytest script\tests\test_release_deploy_executor.py -q` -> `5 passed`；`python -X utf8 -m pytest script\tests\test_release_migration_policy_gate.py -q` -> `7 passed`；`python -X utf8 -m pytest script\tests\test_publish_int_ruoyi_to_test_tooling.py -q` -> `85 passed`。
- GREEN: `python -X utf8 script\release\run-release-migration-policy-gate.py --sql-root sql\mysql` -> PASS，返回 `status=passed`，`migrationCount=186`，确认 SQL 树现已满足显式 metadata 和依赖完整性门禁。
- GREEN: `git diff --check` -> PASS。
- GREEN: `python -X utf8 C:\Users\BJB110\.codex\skills\task-closeout-cleanup\scripts\task_closeout.py --task-id 20260621-release-preflight-environment-contract --mode preview` -> PASS，`keep=task.md,execution-log.md,bug-regression-evidence.md`，`delete=<none>`，`blocked=<none>`，`warnings=<none>`。
