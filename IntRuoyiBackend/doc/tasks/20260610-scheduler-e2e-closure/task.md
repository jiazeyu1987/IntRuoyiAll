# 20260610 排产目标 7 测试租户闭环回归

## 任务目标

在独立 worktree `codex/scheduler-e2e-closure` 中完成目标 7：对目标 1-6 的排产闭环能力做测试租户真实 E2E 回归，验证成功后融合进 `int_main`。

验收路径：

- ERP 工单已按工单编码进入生产工单列表。
- 排产员能从 ERP 生产工单生成排产工单并填写承诺交期；排产数量等于生产工单数量，不拆分。
- 排产工单已固化工艺路线版本、工序、设备/人工、班次小时和产能快照。
- 班组长导入的 MES Excel 报工能人工选择排产工单和工序并完成归属。
- 夜间重排配置存在，重排保护已报工部分。
- 排产员工作台能解释今日产能、瓶颈、异常、报工偏差，并跳转到相关页面。

## 设计约束检查

- 是否引入 fallback/降级/吞异常：否。缺少测试租户、真实样本、页面入口、接口响应或运行态归属时直接失败。
- 是否从根因和长期维护角度解决：是。目标 7 不重复业务功能，只固化跨目标闭环回归脚本和证据矩阵。
- 是否存在临时补丁或绕过：否。不用 mock，不用 admin 租户写数据，不通过接口替代已有前端入口。

## 里程碑

1. 现状审计：确认目标 1-6 的已有页面、接口、样本数据和测试覆盖。
2. 闭环矩阵：把目标 1-6 映射到可执行测试步骤和最终验证证据。
3. E2E 脚本：新增测试租户真实闭环回归脚本，覆盖工作台、生产订单、排产工单池、工序快照、报工归属、夜间重排配置。
4. 验证：在 worktree 运行静态/后端回归和真实 E2E。
5. 融合：验证成功后合并进 `int_main`，在合并结果上复跑目标 7 回归。

## 预期验证

- BDD/TDD 证据写入 `execution-log.md`。
- 后端 SQL/单元回归覆盖目标 1-6 的关键契约。
- 前端 Playwright 使用 `测试租户/aoteman` 登录并验证真实页面链路。
- QA 证据写入前端 `docs/qa/mes-scheduler-e2e-closure-evidence.md`。

## 当前状态

- 状态：已完成。
- worktree：`D:\ProjectPackage\Int\IntRuoyi\worktrees\scheduler_e2e_closure`
- 分支：`codex/scheduler-e2e-closure`
- 端口：FE `8095` / BE `48095`
- 已完成：新增目标 7 闭环回归 E2E、QA 证据矩阵；后端 33 个相关测试、SQL 5 个测试、前端静态契约和测试租户真实 E2E 均已通过。
- 最终结果：已融合进 `int_main`；合并后后端回归、SQL 契约、前端静态契约、QA 证据校验和测试租户真实 E2E 均通过。
