# admin 资源大表设备产能统一

## 任务目标

在本机 `芋道源码/admin` 对应的 admin 租户 `tenant_id=1` 中，统一资源大表依赖的设备工序产能数据：同一设备、同一工序只保留一条设备工序产能记录，保留较小/保守产能，逻辑删除重复的较大产能记录。

## 里程碑

1. 建立任务记录并确认修正边界。
2. 只读校验 admin 租户当前冲突、目标记录和引用完整性。
3. 使用带防误删条件的事务逻辑删除重复高产能记录。
4. 校验冲突清零、保留记录正确、测试租户不受影响。
5. 使用 `芋道源码/admin` 只读验证资源大表可打开且 A03388 产能一致。
6. 更新任务记录并提交本次任务文档。

## 预期验证

- admin 租户同设备同工序产能冲突数从 `2` 变为 `0`。
- `A03388 + 外管拉伸2` 仅保留 `id=129`，产能 `25.714286 / 270.000000`。
- `A03388 + 内管拉伸2` 仅保留 `id=130`，产能 `40.000000 / 420.000000`。
- 测试租户 `tenant_id=122` 冲突数仍为 `0`。
- 前端登录 `芋道源码/admin` 打开 `/mes/pro/route`，切换 `资源大表` 后查询 A03388 不再出现接口静态资源错误，且同设备同工序产能一致。

## 当前状态

已完成：admin 租户 `A03388 + 外管拉伸2`、`A03388 + 内管拉伸2` 的重复高产能设备工序记录已逻辑删除；当前 admin 同设备同工序产能冲突数为 `0`，测试租户冲突数仍为 `0`；`芋道源码/admin` 资源大表只读验证通过。

## 设计约束检查

- `是否引入 fallback/降级/吞异常`：否。
- `是否从根因和长期维护角度解决`：是。通过清理底层设备工序产能重复记录，保证资源大表、排程预算和后续维护均按设备+工序唯一产能取值。
- `是否存在临时补丁或绕过`：否。
