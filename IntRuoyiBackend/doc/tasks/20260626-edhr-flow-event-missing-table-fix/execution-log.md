BDD: 查询流程事件时本机旧库不再因缺表失败 -> Given 本机历史库缺少 mes_pro_edhr_flow_event 与 mes_pro_edhr_flow_intervention / When 执行本机标准 schema 保底流程后再查询流程事件 / Then 查询不再报 Table doesn't exist。
BDD: 流程干预正式迁移不得覆盖已存在的验证包矩阵与 OQ/PQ 菜单 -> Given system_menu 中旧号段 900286-900292 已被其他 eDHR 功能占用 / When 执行流程干预正式迁移 / Then 流程干预必须使用独立未冲突号段，并可幂等吸收旧流程干预残留。
BDD: 本机 schema guard 只补齐缺失表结构，不重放已冲突旧菜单 SQL -> Given 本机库缺失流程事件表但现有 system_menu 已有其他功能占用旧号段 / When 执行 restart-int-ruoyi-local.ps1 的本地 schema 保底 / Then 脚本只应用安全的表结构修复 SQL，不把冲突菜单号段写回本机库。
GREEN: experience-preflight -> PASS，本轮仅对本机 Docker MySQL 执行受控 schema 写入；已先确认正式 flow intervention 菜单旧号段与现有 eDHR 功能冲突，因此本机只允许应用无菜单副作用的 runtime SQL。
RED: `python -X utf8 -m pytest script/tests/test_edhr_flow_intervention_schema_sql.py script/tests/test_edhr_flow_intervention_runtime_sql.py script/tests/test_mes_base_schema_edhr_flow_tables.py -q` -> FAIL，新增合同先后暴露：flow intervention 正式 SQL 仍使用冲突旧号段 `900286-900292`、缺少 runtime-only 缺表 SQL、MES 基础 schema 未包含 `mes_pro_edhr_flow_event` / `mes_pro_edhr_flow_intervention`。
RED: `docker exec int-ruoyi-mysql mysql --default-character-set=utf8mb4 -N -B -uroot -p123456 -D ruoyi-vue-pro -e "SHOW TABLES LIKE 'mes_pro_edhr_flow_event'; SHOW TABLES LIKE 'mes_pro_edhr_flow_intervention';"` -> FAIL，真实本机库返回空结果，确认两张 eDHR 流程表均不存在。
INVESTIGATION: `docker exec int-ruoyi-mysql mysql --default-character-set=utf8mb4 -N -B -uroot -p123456 -D ruoyi-vue-pro -e "SELECT id, name, permission FROM system_menu WHERE id IN (900286,900287,900288,900289,900290,900291,900292) ORDER BY id;"` -> PASS，确认旧 flow intervention 正式 SQL 复用的菜单号段已分别被 `验证包矩阵` 与 `OQ/PQ` 占用，不能把原始 `20260618_mes_edhr_flow_intervention_log.sql` 直接用于本机缺表自愈。
CHANGE: `sql/mysql/20260618_mes_edhr_flow_intervention_log.sql` 改为使用独立菜单号段 `900356-900362`，并补齐 legacy menu 映射、旧残留删除、租户包 `menu_ids` 映射吸收与 `system_role_menu` 旧绑定清理。
CHANGE: 新增 `sql/mysql/20260626_mes_edhr_flow_intervention_runtime.sql`，仅补齐 `mes_pro_edhr_flow_event` / `mes_pro_edhr_flow_intervention` 表结构与索引，不写 `system_menu`、`system_tenant_package`、`system_role_menu`。
CHANGE: `sql/mysql/20260512_mes_schema.sql` 补齐 `mes_pro_edhr_flow_event` / `mes_pro_edhr_flow_intervention` 基础建表；`script/deploy/restart-int-ruoyi-local.ps1` 新增 `MES eDHR flow intervention runtime tables` 本机 schema guard；对应合同测试同步更新。
GREEN: `python -X utf8 -m pytest script/tests/test_edhr_flow_intervention_schema_sql.py script/tests/test_edhr_flow_intervention_runtime_sql.py script/tests/test_mes_base_schema_edhr_flow_tables.py script/tests/test_restart_int_ruoyi_local_schema.py -q` -> PASS，20 tests passed。
GREEN: `Get-Content -Encoding utf8 sql/mysql/20260626_mes_edhr_flow_intervention_runtime.sql | docker exec -i int-ruoyi-mysql mysql --default-character-set=utf8mb4 -uroot -p123456 -D ruoyi-vue-pro` -> PASS，本机 Docker MySQL 已成功应用安全 runtime SQL。
GREEN: `docker exec int-ruoyi-mysql mysql --default-character-set=utf8mb4 -N -B -uroot -p123456 -D ruoyi-vue-pro -e "SHOW TABLES LIKE 'mes_pro_edhr_flow_event'; SHOW TABLES LIKE 'mes_pro_edhr_flow_intervention'; SHOW INDEX FROM mes_pro_edhr_flow_event WHERE Key_name IN ('idx_mes_pro_edhr_flow_event_object','idx_mes_pro_edhr_flow_event_instance'); SHOW INDEX FROM mes_pro_edhr_flow_intervention WHERE Key_name IN ('uk_mes_pro_edhr_flow_intervention_idempotency','idx_mes_pro_edhr_flow_intervention_object');"` -> PASS，两张表与关键索引均已存在。
GREEN: `docker exec int-ruoyi-mysql mysql --default-character-set=utf8mb4 -N -B -uroot -p123456 -D ruoyi-vue-pro -e "SELECT COUNT(*) FROM mes_pro_edhr_flow_event WHERE deleted = b'0'; SELECT COUNT(*) FROM mes_pro_edhr_flow_intervention WHERE deleted = b'0';"` -> PASS，读回结果 `0 / 0`，确认仅补结构，不伪造业务数据。
