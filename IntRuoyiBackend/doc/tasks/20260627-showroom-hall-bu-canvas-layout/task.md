# 后台展柜画布按 BU 排布

## 任务目标

在展厅后台展柜画布中新增按 BU 排布能力：后端提供只计算不落库的接口，按照产品 `pipeline_layout` 计算等分 grid 坐标；前端画布弹窗点击“按 BU 排布”后只更新预览，用户继续点击现有“保存布局”后才持久化。

## 经验门禁

- 命中 `D:\ProjectPackage\Int\IntPP\FRONTEND_STYLE.md`：前端按钮保持现有后台操作台风格，使用紧凑工具栏按钮，不引入营销式视觉或额外说明文案。
- 本轮不触发服务器写入、发布、恢复、真实 E2E、worktree 合并或清理；高风险 `experience-preflight` 暂不需要执行。

## 设计约束检查

- `是否引入 fallback/降级/吞异常`：否。
- `是否从根因和长期维护角度解决`：是，新增明确后端计算接口，前端不复制 BU 排布算法。
- `是否存在临时补丁或绕过`：否。

## 里程碑

1. 记录 BDD/TDD 证据并补后端失败测试。
2. 新增后端计算接口和 BU grid 算法。
3. 新增前端 API、按钮与预览更新逻辑。
4. 运行定向验证并记录结果。

## 预期验证

- 后端定向 showroom 测试覆盖产品按 BU 排序、空 BU 未分类、奖项坐标保持不变、坐标范围完整。
- 前端定向静态测试覆盖按钮、接口调用、预览不保存、错误提示。
- 能运行时执行后端模块定向测试和前端定向测试；构建若受现有脏改或环境阻塞需记录阻塞原因。

## 当前状态

已完成。

## 完成记录

- 后端已新增 `POST /showroom/hall/calculate-bu-canvas-layout` 只计算接口。
- 产品按 `pipeline_layout` 分组顺序和原 displayOrder 重排为等大 grid；空 BU 归入“未分类”；奖项坐标原样保留。
- 已补后端回归测试并通过定向 Maven 验证。
- 收尾清理已执行 preview；因 evidence 文件为本次技能验收证据，未执行删除。
