# 任务：展厅芋道源码导出到测试租户导入一致性验证

## Current Status
Completed

## 任务目标
- 使用本机真实环境，从 `芋道源码/admin` 导出展厅产品资源包。
- 使用本机真实环境，将导出的资源包导入 `测试租户/aoteman`。
- 验证导入后测试租户展厅数据与芋道源码导出源数据一致。

## 授权与范围
- 用户已明确要求：在芋道源码导出，在测试租户导入，并验证测试租户数据是否与芋道源码一致。
- 用户追加授权：补齐源租户 `芋道源码` 中 `INT-1`、`INT-15`、`INT-81`、`INT-86`、`INT-97`、`INT-129` 的中文和英文讲解音频，再重新执行导出、导入和一致性验证。
- 仅本机验证，不操作服务器、不发布、不改正式或测试环境。
- `芋道源码/admin` 执行上述 6 个产品讲解音频补齐写入，并执行展厅资源包导出。
- `测试租户/aoteman` 执行展厅资源包导入写入。
- 不使用 mock、不切换账号、不静默跳过失败项。

## 经验门禁
- PowerShell：已读取 `docs/powershell-memory.md`；涉及中文租户名、命令输出、脚本生成与文件写入时必须显式 UTF-8。
- 登录 / 租户 / E2E：已读取 `docs/login-access.md`；真实登录前置必须先跑官方 `scripts/preflight/login-preflight.mjs`，未通过不得执行长链路导入验证。
- Playwright：使用项目既有本机前端入口和真实登录路径，不绕过登录。
- 收尾清理：完成后先运行 `task-closeout-cleanup` preview。

## 设计约束检查
- 是否引入 fallback/降级/吞异常：否。
- 是否从根因和长期维护角度解决：是，本任务为真实链路验证，不改生产逻辑。
- 是否存在临时补丁或绕过：否。

## BDD 场景
- BDD: 跨租户导出导入一致性 -> Given 芋道源码存在展厅产品数据 / When 从芋道源码导出资源包并导入测试租户 / Then 测试租户导入后的展厅产品、旧产品编号、产品主数据与导出源一致。
- BDD: 导入前置失败即停止 -> Given 登录、导出包、产品主数据或导入接口任一阶段失败 / When 执行跨租户导入验证 / Then 停止并报告明确失败原因，不继续比较。

## 里程碑
1. M1：创建任务文档，记录门禁与授权范围。`完成`
2. M2：执行芋道源码和测试租户官方登录前置。`完成`
3. M3：导出芋道源码展厅资源包并保存验证产物。`曾阻塞，待补齐音频后重试`
4. M4：补齐 6 个源租户产品的中文和英文讲解音频。`进行中`
5. M5：导入测试租户并记录响应。`待执行`
6. M6：对比源租户导出 workbook 与测试租户导出/接口数据一致性。`待执行`
7. M7：记录证据，运行 cleanup preview，提交任务文档。`待执行`

## 预期验证
- `login-preflight.mjs`：`芋道源码/admin` 登录 `/showroom/product` PASS。
- `login-preflight.mjs`：`测试租户/aoteman` 登录 `/showroom/product` PASS。
- 导出包结构包含 `产品列表`、`产品主数据`、`奖项`、`讲解音频`、`关键词中英对照`。
- 测试租户导入响应成功。
- 测试租户再导出资源包，与源 workbook 关键业务列一致。

## 当前状态
- 2026-07-06 18:12：最终跨租户链路通过；测试租户导入后再导出与芋道源码导出 workbook 五个 Sheet 全部一致。
- 2026-07-06 18:00：发现并修复覆盖导入既有展品时 `旧产品编号` 未写回的问题，新增回归测试并通过定向 GREEN。
- 2026-07-06 17:50：源租户 6 个缺失讲解音频产品已补齐并可导出；测试租户导入被 15 个冻结产品阻塞，按业务接口解冻后继续验证。
- 已重新打开任务：按用户追加授权，先补齐源租户 `芋道源码` 上述 6 个产品的 ZH/EN 讲解音频，再重新执行导出、测试租户导入和一致性验证。


## 最终验证结果
- 状态：Completed
- 源租户：芋道源码 / admin，仅用于补齐授权的 6 个产品讲解音频并导出。
- 目标租户：测试租户 / aoteman，执行真实导入写入。
- 验证：导出 -> 测试租户导入 -> 测试租户再导出 -> workbook 一致性比较全部通过。
- 结果：产品列表、产品主数据、奖项、讲解音频、关键词中英对照均一致；导入无失败、无跳过。
