# Word 导入历史引用汇总报错

## Task Goal

当 Word 导入因“批记录已无表单但仍有历史业务引用，不能按 V1.0 重新导入”被阻断时，不再只提示第一条技术引用；后端一次性汇总所有阻断引用，并用用户能理解的语言说明“在哪里有引用、应该去哪里删除”。

## Milestones

1. 补充 BDD/TDD，覆盖多个历史引用一次性返回。
2. 后端版本重置保护从单一 blocker 改为汇总 blocker 明细。
3. 验证定向后端测试通过，必要时补前端展示兼容。

## Expected Verification

- 后端定向测试：同一批记录版本同时存在批记录执行、批记录任务、工序权限、工艺流程绑定时，错误消息一次性列出全部引用来源和处理入口。
- 回归测试：无历史引用仍可清理旧版本并重新导入 V1.0。

## 经验门禁

- PowerShell：中文文件读写使用 UTF-8，禁止默认 Get-Content/Set-Content 污染中文。
- 后端 API：不吞异常、不降级；缺少 schema 或测试前置时 fail fast。

## 设计约束检查

- 是否引入 fallback/降级/吞异常：否。
- 是否从根因和长期维护角度解决：是，将后端阻断信息结构化汇总，减少反复试错。
- 是否存在临时补丁或绕过：否。

## Current Status

completed

## Verification Result

- GREEN: 后端定向测试 5 tests PASS，历史引用报错已一次性列出全部处理入口。
