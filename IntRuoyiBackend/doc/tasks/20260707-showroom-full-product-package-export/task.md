# 展厅产品 zip 全量导出导入策略修正

## 任务目标
- 修正展厅产品 zip 资源包导出策略：产品列表、产品主数据、语音、manifest/assets 应覆盖全部当前有效产品，而不是只覆盖展柜已引用产品。
- 展柜名称仍用于恢复展柜映射；未入展柜/未使用产品允许展柜名称为空，但必须随包导入。
- 保持 `INT-*` 产品与中英文语音一一对应的强校验。

## 里程碑
- [x] M1 定位当前只导出展柜引用产品的过滤逻辑。
- [x] M2 补 RED 测试：未入展柜 INT 产品也进入 zip/Excel/语音资产。
- [x] M3 修复导出逻辑并验证 GREEN。
- [x] M4 记录影响与后续正式服同步建议。

## 设计约束检查
- `是否引入 fallback/降级/吞异常`：否。
- `是否从根因和长期维护角度解决`：是；产品全量与展柜映射解耦。
- `是否存在临时补丁或绕过`：否。

## Current Status
completed

## 完成记录
- 根因：导出产品行时用展柜映射过滤产品，导致未入展柜/未使用产品没有进入 zip 包，后续语音 sheet 与 manifest/assets 也随之缺失。
- 修复：产品导出改为基于 `contentService.listProducts()` 全量产品主数据；展柜名称仅作为可选映射字段，未入展柜产品导出为空展柜名称。
- 影响：zip 资源包恢复为全量产品迁移包；导入侧仍按产品编码精确导入，不需要 fallback 或旧编号猜测。
- 验证：`ShowroomProductExcelImportExportIntegrationTest` 55 个用例通过。
