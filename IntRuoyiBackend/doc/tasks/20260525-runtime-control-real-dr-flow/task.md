# 任务：运行控制台真实备份恢复回滚验证

## 任务目标

- 用真实前端 E2E 在测试服真实数据上依次执行 `立即备份`、`恢复数据`、`回滚版本`。
- 每个动作必须通过运行控制台按钮触发，填写操作原因，等待在线日志完成。
- 验证备份、恢复和回滚后测试服后端、管理端前端和 Website 仍符合预期。
- 只操作测试服，不触碰正式服。

## BDD 场景

- BDD: 测试服备份恢复回滚全链路成功 -> Given 本机运行控制台可访问且 admin 拥有运维权限, When 用户依次点击 `立即备份`、`恢复数据`、`回滚版本` 并填写原因确认执行, Then 每个运行操作都成功完成，日志可在线查看，测试服健康检查通过，恢复使用真实备份数据，回滚后应用服务可访问。
- BDD: 测试服 Linux 后端调用本机 DR 脚本 -> Given 运行控制台后端部署在 Linux 测试服容器中, When 用户从测试服页面提交 `立即备份`、`恢复数据` 或 `回滚版本`, Then 后端应调用 `/opt/intruoyi/ops/backup-ops/linux-native/linux/backup-ops-linux.sh` 并传入 linux-local 配置，而不是查找 Windows `script/backup-ops/scripts/backup-ops.ps1`。

## 里程碑

- [x] M1: 梳理运行控制台备份、恢复、回滚动作契约和测试服前置条件。
- [x] M2: 新增或复用真实 E2E 脚本执行三步流程。
- [x] M3: 记录操作 ID、日志路径、备份标识和测试服健康结果。
- [x] M4: 跑恢复/回滚证据校验与任务收尾清理。

## 预期验证

- RED：新增真实 DR E2E 前缺少完整备份、恢复、回滚测试文件。
- GREEN：真实 E2E 提交三个运行控制台动作并全部成功。
- GREEN：备份日志包含 MySQL 和 MinIO 备份结果。
- GREEN：恢复日志显示使用真实备份完成恢复。
- GREEN：回滚日志显示应用版本回滚完成。
- GREEN：测试服 `172.30.30.58:48081/actuator/health`、`172.30.30.58:8081/`、`172.30.30.58:8083/`、`172.30.30.58:8083/showroom` 可访问。

## Current Status

completed

## 当前状态

- 状态：completed
- 已完成：
  - 已创建任务文档。
  - 已确认本地运行控制台的默认 backup-ops 配置指向正式服 `172.30.30.57`，不能用于测试服真实数据验证。
  - 已确认测试服存在 Linux native 备份脚本目录 `/opt/intruoyi/ops/backup-ops/linux-native`，但测试服后端运行控制台未接入该入口。
  - 已通过测试服前端提交 `立即备份`，操作 ID `6555bb3f-0cea-4073-b467-60eb1b18df1c`。
  - 已验证失败后测试服健康检查仍通过，且未生成新备份点、未改变 `IMAGE_TAG`。
  - 已开始修复测试服 Linux 运行控制台 DR 执行入口。
  - 已实现 Linux local DR 命令生成、`.sh` 执行器支持、测试服 backend 容器挂载和镜像工具依赖。
  - 已完成后端单测和脚本契约测试。
  - 已修复 `restore-data`、`rollback-app` 在后端容器内执行时自我重启导致状态无法写回的问题，改为 detached Linux runner 写共享 operation 状态。
  - 已发布修复到测试服 `IMAGE_TAG=20260525_221321`。
  - 已通过测试服真实前端 E2E 完成 `立即备份`、`恢复数据`、`回滚版本` 全链路，备份点 `20260525_222914`。
  - 已将测试服从验证用旧 tag `20260525_200033` 恢复到修复版 `20260525_221321`，最终健康检查全部 HTTP 200。
- 阻塞与影响：
  - 暂无。
