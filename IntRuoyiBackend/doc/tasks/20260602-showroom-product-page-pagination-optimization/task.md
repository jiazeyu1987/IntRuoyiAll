# 任务：优化展厅产品管理分页组装

## 任务目标

- 修复管理端展厅产品分页接口在请求第一页 20 条时先组装全部产品导致响应耗时过长的问题。
- 保持现有筛选、权限可见范围、编辑权限与返回结构不变。
- 将列表页重型详情组装限制在当前页产品内；有筛选时先完成轻量筛选和分页，再组装命中的页。

## 设计约束检查

- 是否引入 fallback/降级/吞异常：否
- 是否从根因和长期维护角度解决：是
- 是否存在临时补丁或绕过：否

## BDD 场景

- BDD: 默认分页只组装当前页产品 -> Given 展厅产品列表存在超过一页的产品 / When 管理端请求第一页 20 条产品 / Then 后端只对第一页 20 个产品执行详情组装，第二页及之后产品不得参与本次详情组装。
- BDD: 筛选分页先筛选再分页再组装 -> Given 产品列表存在关键字、归属公司、生命周期、完整性和审核状态筛选条件 / When 管理端请求带筛选条件的分页 / Then 后端先按现有语义得到匹配集合和总数，再只组装当前页命中的产品。
- BDD: 权限范围分页保持可见性 -> Given 当前用户只可见部分产品且只有部分产品可编辑 / When 管理端请求产品分页 / Then 总数、行数据和 editable 标记仍基于可见与可编辑产品集合返回。

## 里程碑

- [x] M1：建立任务记录、BDD 场景和验证计划。
- [x] M2：补充 RED 回归测试，复现默认分页会组装页外产品。
- [x] M3：实现分页前移与轻量筛选，不改变接口契约。
- [x] M4：完成 GREEN/REGRESSION 验证并记录证据。
- [x] M5：运行收尾清理预览并提交本任务改动。

## 验证计划

- RED：新增 `ShowroomApiRuntimeProductPageTest`，用第 21 个产品的详情组装失败证明旧逻辑请求第一页仍会组装页外产品。
- GREEN：修复后同一测试返回第一页 20 条，且不访问第 21 个产品的最新版本。
- REGRESSION：运行展厅模块相关测试，确认产品分页、筛选和批量发布/封面逻辑不回退。

## 当前状态

状态：已完成。默认产品分页改为数据库分页读取当前页快照，页内批量读取版本后组装；筛选分页改为轻量候选筛选后只组装当前页。目标单测与相关控制器回归已通过。完整展厅模块全量测试已尝试，但阻塞在既有 workflow/role-binding 测试前置条件，不能作为本任务通过证据。

## Current Status

completed - targeted verification passed; full module suite blocked by unrelated workflow and role-binding test prerequisites.
