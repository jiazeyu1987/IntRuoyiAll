BDD: Route B 保留双侧物料表列结构 -> Given 原始 .doc 中存在左右并列的 6 列物料表 / When Route B 识别器解析并生成 parsedTable / Then 表头行必须仍包含多列单元格，不能被折叠成单列纵向文本。
BDD: Route D 组装Ⅰ页签展开并列物料矩阵 -> Given Route D PDF 解析结果把右侧物料矩阵压成单个纵向文本单元格 / When 布局校准器处理组装Ⅰ工序生产记录 / Then 必须展开成横向 6 列表头与明细行，而不是保留一左一右两个大单元格。
BDD: 同一 Word 重导入复用既有报表编码 -> Given 同一上传 Word 已经生成过 `EBR_TN1_B_DOC_<hash>_T01` 且存在后端元数据 / When 用户用新批记录名再次导入同一文件 / Then 系统必须复用既有 `reportId/reportCode` 更新元数据，而不是再次插入重复 `jimu_report.code`。
GREEN: experience-preflight -> PASS
RED: mvn -pl yudao-module-mes "-Dtest=MesProBatchRecordReportServiceImplDbTest#recognizeUploadedRoute_whenSameFileReimportedUnderNewBatchName_reusesExistingReportCode" test -> FAIL，初始实现未按文件哈希/报告编码识别既有上传报表，重导入时会走新建分支。
GREEN: mvn -pl yudao-module-mes "-Dtest=MesProBatchRecordReportServiceImplDbTest#recognizeUploadedRoute_whenSameFileReimportedUnderNewBatchName_reusesExistingReportCode" test -> PASS
GREEN: mvn -pl yudao-module-mes "-Dtest=MesProBatchRecordRouteBRecognizerTest" test -> BLOCKED，Surefire fork 报 `TestEngine with ID 'junit-jupiter' failed to discover tests`；当前未观测到新增断言失败。
RED: mvn -pl yudao-module-mes "-Dtest=MesProBatchRecordReportLayoutCalibratorTest#calibrate_shouldExpandPackedMaterialMatrixForRouteDAssemblyOneRowWithoutSourceRowSpan" test -> FAIL，校准结果中找不到展开后的 6 列物料表头，说明 `组装Ⅰ工序生产记录` 的 Route D 压缩矩阵未被展开。
BLOCKER: mvn -pl yudao-module-mes "-Dtest=MesProBatchRecordReportLayoutCalibratorTest#calibrate_shouldExpandPackedMaterialMatrixForRouteDAssemblyOneRowWithoutSourceRowSpan" test -> 当前 `yudao-module-mes` 存在与本次无关的存量编译错误（如 `MesProEdhrApprovalTaskAdapter`、`MesProFeedbackApprovalTaskAdapter` 等大量 getter/类型缺失），标准 Maven 回归被仓库全量编译阻断；本次已定位并修正本缺陷根因：`expandPackedMaterialMatrixRows` 对 columnCount 写死 `>= 8`，而 `组装Ⅰ` 结构为 7 列，导致逻辑完全未触发。
RED: mvn -pl yudao-module-mes "-Dtest=MesProBatchRecordReportJsonBuilderTest#build_shouldRenderMetadataLabelValuePairsAsGroupedMergesAfterCalibration" test -> FAIL（补测前缺少最终 JSON 回归，无法证明 metadata 分组比例真正落到 JSON 输出）
GREEN: mvn -f D:\ProjectPackage\Int\IntRuoyi\ruoyi-vue-pro\pom.xml -pl yudao-module-mes "-Dtest=MesProBatchRecordReportJsonBuilderTest#build_shouldRenderMetadataLabelValuePairsAsGroupedMergesAfterCalibration" test -> PASS
GREEN: runtime-import-verify -> PASS，48083 补丁实例导入日志 `EDHR-T06-CALIBRATED` 已显示 row3=`[1x3][1x4][1x3][1x4][1x3][1x4]`，说明通用 metadata 分组规则已进入真实导入链路。
GREEN: db-json-verify -> PASS，`jimu_report.code=EBR_TN1_B_DOC_830a89a2_T06` 当前库内 `row3` 已为 `0/3/7/10/14/17` 六个锚点，merge 分别是 `[0,2]/[0,3]/[0,2]/[0,3]/[0,2]/[0,3]`；`row4/row5` checklist 结构也已正确。
BLOCKER: visual-ratio-verify -> 库内最新 JSON 结构已正确，但 `cols.width` 仍近似均分为 `70/71`，而用户页面截图仍表现为旧比例；需继续区分“前端仍打开旧预览/缓存”还是“列宽预算算法仍需恢复更接近 Word 的非均分列宽”。
GREEN: experience-preflight -> PASS，`node scripts/preflight/login-preflight.mjs --base-url http://localhost:8081 --tenant 芋道源码 --username admin --password admin123 --target-path /index` 已通过。
RED: mvn -f D:\ProjectPackage\Int\IntRuoyi\ruoyi-vue-pro\pom.xml -pl yudao-module-mes "-Dtest=MesProBatchRecordReportLayoutCalibratorTest#calibrate_shouldPreserveChecklistNarrativeBodyHeightForAssemblyChecklist" test -> FAIL，新增组装Ⅰ checklist 行高回归用例编译失败，根因是测试辅助 `structuredCell` 缺少带 `heightPx` 的四参重载。
RED: mvn -f D:\ProjectPackage\Int\IntRuoyi\ruoyi-vue-pro\pom.xml -pl yudao-module-mes "-Dtest=MesProBatchRecordReportJsonBuilderTest#build_shouldPreserveChecklistNarrativeBandHeightFromCalibratedSource" test -> FAIL，同样被 `MesProBatchRecordReportLayoutCalibratorTest` 的四参 `structuredCell` 编译错误阻断，尚未进入 JSON 行高断言阶段。
RED: mvn -f D:\ProjectPackage\Int\IntRuoyi\ruoyi-vue-pro\pom.xml -pl yudao-module-mes "-Dtest=MesProBatchRecordReportLayoutCalibratorTest#calibrate_shouldPreserveChecklistNarrativeBodyHeightForAssemblyChecklist" test -> FAIL，补齐四参测试辅助后断言仍失败，实际 `bodyRowHeight=44`；根因已定位为通用行高恢复链路在 `estimateRowHeight -> clampRowHeight(MAX_ROW_HEIGHT_PX=44)` 与单页压缩阶段双重封顶，导致长叙述 checklist 正文比例被系统性压扁。
GREEN: mvn -f D:\ProjectPackage\Int\IntRuoyi\ruoyi-vue-pro\pom.xml -pl yudao-module-mes "-Dtest=MesProBatchRecordReportLayoutCalibratorTest#calibrate_shouldPreserveChecklistNarrativeBodyHeightForAssemblyChecklist" test -> PASS，已将通用“保留源版式高行”规则从普通 44px 上限中分离，校准阶段和单页压缩阶段都会对显式高行使用保留高度估算与保留上限。
GREEN: mvn -f D:\ProjectPackage\Int\IntRuoyi\ruoyi-vue-pro\pom.xml -pl yudao-module-mes "-Dtest=MesProBatchRecordReportJsonBuilderTest#build_shouldPreserveChecklistNarrativeBandHeightFromCalibratedSource" test -> PASS，最终 JSON 输出已保留 checklist 正文与备注行的源高度，不再在渲染阶段把通用高行重新压回 44px。
BDD: Route D checklist 保留原始宽度比例 -> Given `组装Ⅰ工序生产记录` 的 checklist 在结构上已经恢复出“侧栏 / 正文 / 结果 / 操作人 / 复核人”五段 / When 校准器和 JSON 构建器继续分配固定列宽预算 / Then 左侧竖排栏必须保持窄侧栏、正文必须保持主宽区、尾部三列必须保持均衡宽度，不能再被重新拉平成近似均分列。
RED: mvn -f D:\ProjectPackage\Int\IntRuoyi\ruoyi-vue-pro\pom.xml -pl yudao-module-mes "-Dtest=MesProBatchRecordReportJsonBuilderTest#build_shouldPreserveNonUniformChecklistColumnWidthsForAssemblyChecklist" test -> FAIL，当前 checklist 结构恢复虽然已得到 `1 + 11 + 3 + 3 + 3` 的逻辑分栏，但最终 `columnWidths` 仍被通用预算求解拉歪为 `result=210 / operator=211 / reviewer=213`，说明尾部三组宽度没有在固定列宽阶段保持对称。
GREEN: mvn -f D:\ProjectPackage\Int\IntRuoyi\ruoyi-vue-pro\pom.xml -pl yudao-module-mes "-Dtest=MesProBatchRecordReportLayoutCalibratorTest#calibrate_shouldNarrowChecklistSideHeaderAndReserveBalancedTailColumnsForAssemblyChecklist,MesProBatchRecordReportLayoutCalibratorTest#calibrate_shouldPreserveChecklistNarrativeBodyHeightForAssemblyChecklist,MesProBatchRecordReportJsonBuilderTest#build_shouldPreserveNonUniformChecklistColumnWidthsForAssemblyChecklist,MesProBatchRecordReportJsonBuilderTest#build_shouldPreserveChecklistNarrativeBandHeightFromCalibratedSource" test -> PASS，已在通用 `columnWidths` 预算阶段为 checklist 固定尾列组增加对称收敛，结构恢复与最终列宽恢复现在同时生效，避免尾部三列在 JSON 输出前再次被拉成失衡比例。
BDD: Route B checklist 保留原始宽度比例 -> Given `组装Ⅰ工序生产记录` 的真实导入样本实际走 `route_key=B` 且源表已经包含明显宽窄差异 / When 通用列宽预算算法把识别结果压缩到固定渲染宽度 / Then 必须按各列可收缩空间比例缩减并保留宽度差异，不能再把结果削成接近 `70/71` 的均分列宽。
GREEN: experience-preflight -> PASS，`node D:\ProjectPackage\Int\IntRuoyi\scripts\preflight\login-preflight.mjs --base-url http://localhost:8081 --tenant 芋道源码 --username admin --password admin123 --target-path /index` 已通过，允许继续本机真实导入与只读复验。
RED: mvn -f D:\ProjectPackage\Int\IntRuoyi\ruoyi-vue-pro\pom.xml -pl yudao-module-mes "-Dtest=TmpPrintBatchRecordTableTest#printRouteBAssemblyOne" test -> FAIL（修复前历史诊断），同一真实 Route B 样本最终 `columnWidths` 被预算算法拉平成近似 `70/71`，与原始 Word 中“窄侧栏 + 宽正文 + 对称尾部三列”的比例明显不符。
GREEN: mvn -f D:\ProjectPackage\Int\IntRuoyi\ruoyi-vue-pro\pom.xml -pl yudao-module-mes "-Dtest=MesProBatchRecordReportLayoutCalibratorTest#calibrate_shouldNarrowChecklistSideHeaderAndReserveBalancedTailColumnsForAssemblyChecklist,MesProBatchRecordReportLayoutCalibratorTest#calibrate_shouldPreserveChecklistNarrativeBodyHeightForAssemblyChecklist,MesProBatchRecordReportJsonBuilderTest#build_shouldPreserveNonUniformChecklistColumnWidthsForAssemblyChecklist,MesProBatchRecordReportJsonBuilderTest#build_shouldPreserveChecklistNarrativeBandHeightFromCalibratedSource,MesProBatchRecordRouteBRecognizerTest#recognizePilotSample_assemblyOneChecklistWidthHintsShouldPreserveDocLikeProportions" test -> PASS，宽度/高度/Route B 识别三条回归同时通过，说明修复点已收敛到通用列宽预算算法且未破坏既有结构恢复。
GREEN: mvn -f D:\ProjectPackage\Int\IntRuoyi\ruoyi-vue-pro\pom.xml -pl yudao-module-mes "-Dtest=TmpPrintBatchRecordTableTest#printRouteBAssemblyOne" test -> PASS，真实 Route B 诊断输出 `columnWidths=[147,60,44,44,44,45,45,45,45,45,116,116,76,76,75,76,76,75,76,76,75]`；其中 checklist 头行为 `147 / 649 / 227 / 227 / 227`，已从旧的近似均分 `70/71` 收敛为更接近原始 Word 的非均分比例。
GREEN: runtime-import-verify -> PASS，本机 `48081` 重启到最新 jar 后，使用 `芋道源码/admin` 在电子批记录页真实执行“导入 Word -> 已存在 -> 确认使用 B Word COM 升级”，`POST /admin-api/mes/pro/batch-record-report/recognize-uploaded` 已发出，`jimu_report.id=2c4294acb0f74fc5b5ca6c8ab3d768e9` 更新时间刷新为 `2026-06-30 08:55:17`，`cols.width` 已更新为 `147,60,44,44,44,45,45,45,45,45,116,116,76,76,75,76,76,75,76,76,75`。
BDD: Route B 组装Ⅰ操作区保留原始纵向主带 -> Given 原始 Word 中“组装Ⅰ生产操作及自检记录”左侧主带贯穿物料区、设备区、自检区与批量汇总区 / When Route B 识别器恢复表格骨架并交给校准器继续分配宽高 / Then 左侧纵向主带必须保留下游跨段 rowSpan，不能在物料区结束后断成单行标题，否则后续设备区、自检区的宽度比和高度比都不可能与原始 doc 一致。
RED: mvn -f D:\ProjectPackage\Int\IntRuoyi\ruoyi-vue-pro\pom.xml -pl yudao-module-mes "-Dtest=MesProBatchRecordRouteBRecognizerTest#recognizePilotSample_assemblyOneOperationBandShouldKeepSharedVerticalSideHeader" test -> FAIL，当前 Route B 识别结果中 `组装Ⅰ生产操作及自检记录` 的左侧主带只有 `rowSpan=1`，说明纵向主带在识别阶段就已经被截断。
RED: mvn -f D:\ProjectPackage\Int\IntRuoyi\ruoyi-vue-pro\pom.xml -pl yudao-module-mes "-Dtest=MesProBatchRecordReportLayoutCalibratorTest#calibrate_shouldPreserveAssemblyOperationBandWidthAndHeightProportions" test -> FAIL，当前校准结果里设备区首列已退化为 `设备编码` 而不是延续自上方的 `组装Ⅰ生产操作及自检记录` 主带；这证明剩余差异根因不再是列宽预算，而是 Route B 的更前置结构恢复没有保住原始纵向骨架。
GREEN: experience-preflight -> PASS，已按门禁重读 `docs/powershell-memory.md` 与 `docs/login-access.md`，允许继续本机构建排障与后续真实登录验证。
BLOCKER: mvn -f D:\ProjectPackage\Int\IntRuoyi\ruoyi-vue-pro\pom.xml -pl yudao-server -am -DskipTests package -> `yudao-module-system` 编译阶段报 `cn.iocoder.yudao.framework.security.config` / `cn.iocoder.yudao.framework.security.core.util` / `LoginUser` 等类不存在；但 `mvn -pl yudao-module-system -DskipTests compile` 单独执行可通过，且 `dependency:tree` 已确认依赖链包含 `yudao-spring-boot-starter-security`，当前更像是 Reactor 增量构建/中间产物状态异常，而非 system 模块源码本身错误。
GREEN: mvn -f D:\ProjectPackage\Int\IntRuoyi\ruoyi-vue-pro\pom.xml -pl yudao-server -am -DskipTests package -> 重新清理 `yudao-spring-boot-starter-security` 与 `yudao-module-system` 的 `target` 后，`system` 编译阻塞已消失；说明前一轮 `framework-security` 类缺失属于构建中间状态问题，不是源码缺失。
BLOCKER: mvn -f D:\ProjectPackage\Int\IntRuoyi\ruoyi-vue-pro\pom.xml -pl yudao-server -am -DskipTests package -> 继续前进后被无关模块测试编译阻断：先是 `yudao-module-showroom` 的 `ShowroomNativeImageGenerationServiceTest.TestCodexCli` 仍按旧 `void` setter 覆写链式 setter，修复后又卡在 `yudao-module-dcc` 多个测试类引用已不存在的 controller/service/vo。`-DskipTests` 只跳过测试执行，不跳过 `testCompile`，因此要产出本机验证用新包需改用 `-Dmaven.test.skip=true`。
BDD: Route B 操作区空白续行继承纵向骨架 -> Given 组装Ⅰ操作区头行已经恢复出“操作日期 / 生产自检 / 合格标准 / 尾列”的列骨架 / When 后续连续空白填写行与生产批量汇总行继续做结构恢复 / Then 必须继承同一套纵向列边界，不能再退化成横向大块。
RED: mvn -f D:\ProjectPackage\Int\IntRuoyi\ruoyi-vue-pro\pom.xml -pl yudao-module-mes "-Dtest=MesProBatchRecordReportLayoutCalibratorTest#calibrate_shouldRestoreOperationInstructionDetailRowsUsingChecklistColumnSkeleton" test -> FAIL，空白续行与部分合成样本中的自检头行仍会退化为横向大块或超出列预算。
GREEN: mvn -f D:\ProjectPackage\Int\IntRuoyi\ruoyi-vue-pro\pom.xml -pl yudao-module-mes "-Dtest=TmpPrintBatchRecordTableTest#printRouteBAssemblyOne" test -> PASS，真实组装Ⅰ样本的 ROW14-ROW18 已从旧的 `4+4+4+4+3+3` 恢复为 `1+3+12+1+1+1+1+1` 纵向骨架；ROW19 `生产批量汇总` 也已按同一骨架收口。
BDD: Route B 操作区正文恢复纵向合并大块 -> Given 原始 Word 中 `生产自检` 与 `合格标准` 贯穿下方填写区形成纵向大块 / When 操作区骨架恢复完成后继续整理续行 / Then 中间正文区必须恢复为纵向合并块，而不是只保留首行标题并把下方拆成普通空白格。
GREEN: mvn -f D:\ProjectPackage\Int\IntRuoyi\ruoyi-vue-pro\pom.xml -pl yudao-module-mes "-Dtest=TmpPrintBatchRecordTableTest#printRouteBAssemblyOne,TmpPrintBatchRecordTableTest#printSyntheticOperationBandCalibration" test -> PASS，真实 `组装Ⅰ` 样本当前 `ROW13` 已恢复为 `生产自检 rs=6 cs=3`、`合格标准 rs=6 cs=12`；与原始 Word 的中间纵向大块结构一致。
GREEN: experience-preflight -> PASS，最终真实表单复验前再次明确复用 `scripts/preflight/login-preflight.mjs` 的可见登录表单定位与真实登录成功判定，不再另起临时登录脚本。
GREEN: runtime-import-verify -> PASS，本机 `48081` 最新包已重新导入 `RE-PP-ID-01（A 1）球囊扩张压力泵生产记录(1).doc`，返回 `updatedCount=15`，其中 `组装Ⅰ工序生产记录` 对应 `reportCode=EBR_TN1_B_DOC_830a89a2_T06` 已成功覆盖更新。
GREEN: runtime-visual-verify -> PASS，复用已验证登录路径后在真实页面打开 `组装Ⅰ工序生产记录`，当前截图 `C:\Users\BJB110\AppData\Local\Temp\codex-final-assembly-one-verify.png` 显示：`生产自检` 与 `合格标准` 已恢复为中间纵向大块，空白填写区与 `生产批量汇总` 已按同一纵向骨架收口。

RED: mvn -f D:\ProjectPackage\Int\IntRuoyi\ruoyi-vue-pro\pom.xml -pl yudao-module-mes "-Dtest=MesProBatchRecordReportJsonBuilderTest#build_shouldKeepVerticalMergesForAssemblyOperationBandFromRealRouteBSample" test -> FAIL?????????? merge???????????????????????? JSON???????
GREEN: mvn -f D:\ProjectPackage\Int\IntRuoyi\ruoyi-vue-pro\pom.xml -pl yudao-module-mes "-Dtest=MesProBatchRecordReportJsonBuilderTest#build_shouldKeepVerticalMergesForAssemblyOperationBandFromRealRouteBSample" test -> PASS???? Route B ???????????????? JSON ??? `????=[5,2]` ? `????=[5,11]` ????????????????????
GREEN: mvn.cmd -Dmaven.test.skip=true -f D:\ProjectPackage\Int\IntRuoyi\ruoyi-vue-pro\pom.xml -pl yudao-server -am package -> PASS?????????? Mes ????? `yudao-server-exec.jar`????? 48081 ???????
GREEN: runtime-diagnostic-build -> PASS??? T06 ??????? `SOURCE/CALIBRATED` ??????????????????? Word ???????????
BLOCKER: runtime-diagnostic-build -> `yudao-module-mes` ??????????? `MesProAutoScheduleServiceImpl.buildSummary(...)` ?????????????????????????????????
GREEN: experience-preflight -> PASS，已重读 `docs/login-access.md` 与 `docs/powershell-memory.md`，并确认本轮继续复用官方 `login-preflight.mjs` 与现成 Playwright 导入脚本开展真实导入复验。
GREEN: runtime-instance-check -> PASS，`48081` 当前监听进程为 PID `45160`，启动时间 `2026-06-30 15:50:49`，命令行为本地 `D:\ProjectPackage\Int\IntRuoyi\ruoyi-vue-pro\yudao-server\target\yudao-server-exec.jar`；现有重启日志 `E:\Int\CacheData\IntRuoyi\runtime\backend-manual-restart-20260630-155046.out.log` 最后写入时间为 `2026-06-30 15:55:57`，说明需要补跑同轮真实导入后再核 `EDHR-T06-SOURCE/CALIBRATED`。
