BDD: 管理端查看版本应以当前登录租户读取已选版本 -> Given 当前登录租户存在公司历史版本 bundle 且同 `siteKey + stage` 公开绑定指向其他租户 / When 管理端请求版本中心详情 / Then 已选版本必须从当前登录租户读取成功，不得因绑定租户缺 bundle 而报 `SHOWROOM_VERSION_BUNDLE_NOT_FOUND`
BDD: 管理端历史列表应以当前登录租户读取版本包 -> Given 当前登录租户存在历史版本 bundle 且公开绑定指向其他租户 / When 管理端请求版本中心历史 / Then 历史列表基于当前登录租户返回，当前线上版本信息只作为公开 release 诊断信息
RED: `mvn --% -f D:\ProjectPackage\Int\IntRuoyi\ruoyi-vue-pro\pom.xml -pl yudao-module-showroom -Dtest=ShowroomVersionCenterServiceTest#detailShouldReadSelectedCompanyFromCurrentLoginTenantWhenSiteBindingPointsElsewhere -Dsurefire.failIfNoSpecifiedTests=false test` -> FAIL，详情请求在 `siteKey=yingtai-showroom&stage=TEST` 下被错误切到绑定租户，直接抛 `SHOWROOM_VERSION_BUNDLE_NOT_FOUND`。
GREEN: `mvn --% -f D:\ProjectPackage\Int\IntRuoyi\ruoyi-vue-pro\pom.xml -pl yudao-module-showroom -Dtest=ShowroomVersionCenterServiceTest#detailShouldReadSelectedCompanyFromCurrentLoginTenantWhenSiteBindingPointsElsewhere,ShowroomVersionCenterServiceTest#historyShouldReadCompanyFromCurrentLoginTenantWhenSiteBindingPointsElsewhere -Dsurefire.failIfNoSpecifiedTests=false test` -> PASS，管理端历史/详情已固定从当前登录租户读取，公开站点绑定仅用于线上 release 诊断。
REGRESSION: `mvn --% -f D:\ProjectPackage\Int\IntRuoyi\ruoyi-vue-pro\pom.xml -pl yudao-module-showroom -Dtest=ShowroomVersionCenterServiceTest#detailShouldKeepSelectedCompanyReadableWhenCurrentPublicRevisionIsMissing,ShowroomVersionCenterServiceTest#detailShouldKeepSelectedCompanyReadableWhenCurrentContentBundleIsMissing,ShowroomVersionCenterServiceTest#detailShouldKeepSelectedCompanyReadableWhenCurrentContentRevisionIsMissing -Dsurefire.failIfNoSpecifiedTests=false test` -> PASS，辅助版本 blocker 归因行为保持不变。
GREEN: 真实本机接口复验 -> PASS，`tenant-id=1 / admin/admin123` 调用 `GET http://127.0.0.1:48081/admin-api/showroom/version-center/detail?targetType=COMPANY&targetId=1&revisionId=7&siteKey=yingtai-showroom&stage=TEST` 返回 `code=0`；`selectedVersion.revisionId=7`，顶层不再报 `SHOWROOM_VERSION_BUNDLE_NOT_FOUND: COMPANY:1:7`，辅助异常改为 `CURRENT_CONTENT` / `CURRENT_RELEASE` blocker。
CLEANUP PREVIEW: `python -X utf8 C:\Users\BJB110\.codex\skills\task-closeout-cleanup\scripts\task_closeout.py --workspace D:\ProjectPackage\Int\IntRuoyi\ruoyi-vue-pro --task-id 20260602-showroom-version-center-login-tenant-read --mode preview --worktree-closeout off --json` -> PASS，keep 仅 `task.md` / `execution-log.md`，delete=[]，blocked=[]。
