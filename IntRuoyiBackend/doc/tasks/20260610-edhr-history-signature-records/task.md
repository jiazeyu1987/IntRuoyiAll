# eDHR 历史批记录电子签名明细

## 任务目标

补齐 eDHR 批次复盘接口中每张单表执行记录的电子签名明细，使前端历史批记录页可以展示真实签名记录，而不是只显示签名汇总计数。

## 里程碑

1. RED：目标测试证明 `ExecutionReview` 缺少 `signatureRecords`。
2. GREEN：`executionReviews[]` 每项返回当前执行记录的签名明细。
3. REGRESSION：后端目标测试、前端静态测试、类型检查和真实只读 E2E 通过。

## 设计约束检查

- 是否引入 fallback/降级/吞异常：否。
- 是否从根因和长期维护角度解决：是，扩展正式接口数据模型。
- 是否存在临时补丁或绕过：否。

## 当前状态

已完成。
