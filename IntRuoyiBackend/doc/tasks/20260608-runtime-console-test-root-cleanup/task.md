# 20260608-runtime-console-test-root-cleanup

## 任务目标

在 IntRuoyi 运行控制台增加远程根分区空间显示、刷新按钮，以及受控清理按钮。范围扩展为三台固定服务器：测试服 `172.30.30.58`、正式服 `172.30.30.57`、备份服务器 `172.30.30.59`。清理范围仅允许 `/opt/intruoyi/ops/backup/tmp` 和 `/tmp` 下合法临时内容；正式服/备用服务器清理必须输入 `PROD` 二次确认。

## 设计约束检查

- 是否引入 fallback/降级/吞异常：否。
- 是否从根因和长期维护角度解决：是。根分区清理作为显式远程运维动作，服务端硬断言目标环境、主机白名单和允许目录。
- 是否存在临时补丁或绕过：否。

## BDD 场景

- BDD: 显示远程根分区剩余空间 -> Given 运维人员选择 `test`、`prod` 或 `backup` / When 点击刷新 / Then 控制台通过服务端只读探测显示对应固定 IP 根分区总量、剩余量、使用率和 inode 使用率。
- BDD: 清理远程临时目录 -> Given 运维人员点击清理按钮并确认 / When 服务端执行清理 / Then 仅删除 `/opt/intruoyi/ops/backup/tmp` 下内容和 `/tmp` 下允许的历史临时文件，返回清理前后根分区容量证据。
- BDD: 高危服务器清理需要 PROD 确认 -> Given 请求目标是 `prod` 或 `backup` / When 未提交 `prodConfirmText=PROD` / Then 服务端 fail fast，不执行远程清理。
- BDD: 禁止未知或错配目标清理 -> Given 请求目标和固定 IP 白名单不匹配 / When 提交查询或清理 / Then 服务端 fail fast。

## 里程碑

- [x] M1：创建任务文档，写 RED 测试。
- [x] M2：后端补齐远程根分区查询与清理接口/服务。
- [x] M3：运行控制台前端增加展示、刷新、清理按钮和错误态。
- [x] M4：执行 GREEN 与回归验证。
- [x] M5：更新 execution-log，运行 task-closeout-cleanup 预览，只提交本任务相关改动。

## 预期验证

- 后端单元测试覆盖只读容量查询、清理命令构造、目标环境硬断言、高危清理 `PROD` 门禁、允许路径硬断言。
- 脚本静态测试覆盖固定 IP 白名单、清理路径白名单、高危 `PROD` 门禁和运行时 IP 证明。
- 前端静态测试覆盖 API 入口、UI 文案、三目标选择、刷新按钮、清理按钮和高危 `PROD` 确认。
- 回归现有运行控制台静态测试、前端类型检查与备份边界测试。

## 当前状态

completed
