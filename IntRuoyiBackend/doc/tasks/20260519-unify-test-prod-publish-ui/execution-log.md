BDD: unified publish UI should present both release paths in one launcher -> Given an operator starts the repository-root `运维工具.bat` launcher / When they open the publish menu / Then they should be able to choose either publishing the current local workspace to the test server or promoting the current tested runtime from the test server to the production server from the same UI.
BDD: production publish wrapper should promote the tested runtime from the test server -> Given the test runtime on `172.30.30.58` is the verified release source / When an operator runs the production publish wrapper / Then the wrapper should invoke a dedicated promotion script that reads the active tested release from the test server and deploys that exact tested release to `172.30.30.57`.
RED: `python -m pytest D:\ProjectPackage\Int\IntRuoyi\ruoyi-vue-pro\script\tests\test_publish_int_ruoyi_to_test_tooling.py -q` -> FAIL, the repository still exposed a local-to-production wrapper, there was no `promote-int-ruoyi-test-to-prod.ps1`, and the unified publish menu still only offered `Test / Production`.
GREEN: added `script\deploy\promote-int-ruoyi-test-to-prod.ps1` to export the current tested runtime from `172.30.30.58`, mirror MinIO, and deploy that tested release bundle to `172.30.30.57`.
GREEN: changed `script\deploy\publish-int-ruoyi-to-prod.bat` to act as a production promotion wrapper over `promote-int-ruoyi-test-to-prod.ps1` with `PROD` confirmation and `cancel`.
GREEN: updated repository-root `运维工具.bat` so the publish submenu now shows `Test publish / Promote test release to production / Cancel`.
GREEN: updated `OPS.md` and `README.md` so the operator docs describe the new unified publish UI and the test-to-production promotion path.
GREEN: `python -m pytest D:\ProjectPackage\Int\IntRuoyi\ruoyi-vue-pro\script\tests\test_publish_int_ruoyi_to_test_tooling.py -q` -> PASS, `14 passed`.
GREEN: PowerShell parse validation -> PASS, `PROMOTION_SCRIPT_PARSE_OK`.
GREEN: `cmd /c D:\ProjectPackage\Int\IntRuoyi\ruoyi-vue-pro\script\deploy\publish-int-ruoyi-to-prod.bat cancel` -> PASS, returned `[INFO] Promotion cancelled.` without triggering a live promotion.
GREEN: `cmd /c D:\ProjectPackage\Int\IntRuoyi\ruoyi-vue-pro\运维工具.bat help` -> PASS, help output now documents `Publish -> Test publish / Promote test release to production / Cancel`.
GREEN: `python C:\Users\BJB110\.codex\skills\task-closeout-cleanup\scripts\task_closeout.py --workspace D:\ProjectPackage\Int\IntRuoyi\ruoyi-vue-pro --task-id 20260519-unify-test-prod-publish-ui --mode preview` -> PASS, `status: ready`.
