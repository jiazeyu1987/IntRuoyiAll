BDD: restart-only script should reuse the existing remote runtime -> Given `intruoyi-backend` and `intruoyi-frontend` are already deployed on a remote server / When the operator runs the restart-only script / Then it should restart those two containers in place, wait for backend and frontend readiness, and avoid rebuilding images or reimporting data.
BDD: unified ops launcher should expose restart routes -> Given operators may need a faster recovery path than full publish / When they use the repository-root launcher / Then it should expose restart-only routes for test and production in addition to publish routes.
- RED: `python -m pytest D:\ProjectPackage\Int\IntRuoyi\ruoyi-vue-pro\script\tests\test_publish_int_ruoyi_to_test_tooling.py -q` -> FAIL, no restart-only PowerShell script, no test/prod restart wrappers, and no unified launcher restart routes existed yet.
- GREEN: added `script/deploy/restart-int-ruoyi-remote.ps1` as the shared restart-only remote script, plus `restart-int-ruoyi-to-test.bat` and `restart-int-ruoyi-to-prod.bat` wrappers.
- GREEN: extended repository-root `运维工具.bat` with `test-restart` and `prod-restart` routes and matching menu items.
- GREEN: `python -m pytest D:\ProjectPackage\Int\IntRuoyi\ruoyi-vue-pro\script\tests\test_publish_int_ruoyi_to_test_tooling.py -q` -> PASS, `9 passed`.
- GREEN: `powershell -NoProfile -ExecutionPolicy Bypass -File D:\ProjectPackage\Int\IntRuoyi\ruoyi-vue-pro\script\deploy\restart-int-ruoyi-remote.ps1 -ServerHost 172.30.30.58 -RemoteAppDir /opt/intruoyi/runtime -FrontendPort 8081 -BackendPort 48081` -> PASS, the real test runtime restarted and both readiness checks returned HTTP `200`.
- GREEN: `cmd /c D:\ProjectPackage\Int\IntRuoyi\ruoyi-vue-pro\script\deploy\restart-int-ruoyi-to-prod.bat cancel` -> PASS, the production restart wrapper exited safely without touching the live production runtime.
