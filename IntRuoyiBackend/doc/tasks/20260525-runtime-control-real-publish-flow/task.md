# 任务：运行控制台真实发布测试服全链路

## 任务目标

- 通过真实 E2E 路径完成一次 `发布测试服`：登录运行控制台、点击按钮、填写发布原因、提交发布、等待后台发布结束并验证结果。
- 默认使用 `只发代码` 发布范围，不同步 MySQL 和 MinIO，避免覆盖测试服数据。
- 发布动作必须由本机 `localhost:8081 -> 127.0.0.1:48081` 运行控制台执行，因为脚本依赖本机 PowerShell、Docker、SSH、Maven、Node/Vite。

## BDD 场景

- BDD: 发布测试服全链路成功 -> Given 本机运行控制台可访问且 admin 拥有运维权限, When 用户点击 `发布测试服`、保持 `只发代码`、填写原因并确认执行, Then 后端创建运行操作、执行发布脚本、日志可在线查看，最终状态为成功，测试服后端、管理端前端和 Website 健康检查通过。

## 里程碑

- [x] M1: 记录前置检查与阻塞。
- [x] M2: 修复发布脚本 Vite 调用方式。
- [x] M3: 跑发布脚本契约测试。
- [x] M4: 配合前端真实 E2E 执行一次 `发布测试服`。
- [x] M5: 验证测试服健康与发布日志。

## 预期验证

- RED：`pnpm exec vite --version` 在当前前端工作区失败，证明现有发布脚本会在前端构建阶段失败。
- GREEN：脚本契约测试通过，发布脚本显式调用本地 `node_modules/vite/bin/vite.js`。
- GREEN：真实 E2E `发布测试服` 完成，状态为成功。
- GREEN：测试服 `172.30.30.58:48081/actuator/health`、`172.30.30.58:8081/`、`172.30.30.58:8083/showroom` 可访问。

## Current Status

completed

## 当前状态

- 状态：completed
- 已完成：
  - 已确认本机前端 `localhost:8081` 和本机后端 `127.0.0.1:48081` 可访问。
  - 已发现前置阻塞：`pnpm exec vite --version` 失败，当前发布脚本会在 Vite 构建阶段失败。
  - 已将发布脚本改为显式调用本地 `node_modules\vite\bin\vite.js`。
  - 已完成发布脚本契约测试。
  - 已通过前端真实 E2E 提交 `发布测试服`，发布原因为 `E2E真实发布测试服-只发代码-20260525`。
  - 已验证运行控制台操作 `9e6b9fe5-56e9-4651-84b9-9f9b4528f271` 成功，审计参数为 `publishScope=code-only`。
  - 已验证测试服后端、管理端前端、Website 根路径和展厅路径均返回 HTTP 200。
- 阻塞与影响：
  - 暂无。
