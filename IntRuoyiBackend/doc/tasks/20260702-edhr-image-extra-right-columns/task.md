# 20260702 eDHR 图片识别右侧幽灵空列修复

## 任务目标

修复图片识别生成的 eDHR 表格在右侧出现原始表单不存在的空白窄列问题，避免不同图片模板因识别到页面右边界线而多生成尾部空单元格。

## 里程碑

1. 定位图片识别表格列数和布局校准链路：完成。
2. 使用 BDD/TDD 固化“右侧幽灵空列应被裁剪”的行为：完成。
3. 从图片解析输出阶段修复尾部空白列预算，不引入兜底或静默降级：完成。
4. 运行受影响单元测试并记录验证结果：完成，图片解析测试通过；布局校准既有专项测试仍失败，已记录为独立后续问题，不阻塞本次图片解析链路修复。
5. 复修单列右侧幽灵空格、右边界提示词、报表 JSON 默认空列和源图宽度锚点：完成。
6. 使用真实 `RE-PP-ID-01（A 1）球囊扩张压力泵生产记录(1).doc` 验证并收口固定列宽表格推导列外溢：完成。

## 经验门禁

- PowerShell / Windows shell / 中文编码：已命中 `docs/powershell-memory.md`，命令统一显式 UTF-8，不使用 `&&`。
- 任务文档门禁：本任务在生产代码变更前创建任务目录并记录 BDD/TDD 证据。

## 设计约束检查

- 是否引入 fallback/降级/吞异常：否。
- 是否从根因和长期维护角度解决：是，修复图片识别 JSON 转内部表格阶段对右侧幽灵空白列的列预算计算，避免污染后续布局校准。
- 是否存在临时补丁或绕过：否。

## 当前状态

完成。用户复测发现的单列右侧幽灵空格、右侧默认空网格列、以及蓝框区域右边界错位问题已从图片解析和报表 JSON 输出两层收口：解析器裁剪单列/多列右侧空白幽灵列，JSON Builder 不再暴露默认 100 列，并对图片识别高列数表格按源图整行宽度锚定；同时保持 DOC/校准模板的固定列宽权威路径。追加真实 `RE-PP-ID-01（A 1）` 文件验证后，发现固定列宽表格仍可能按推导列数输出额外列，已改为按真实渲染列宽数量输出 `cols.len` 和列定义，并用永久回归测试覆盖。
