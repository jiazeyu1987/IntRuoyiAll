# Execution Log: DCC 站内信真实发送打通

- 2026-05-19: Created backend task package `20260519-dcc-notify-message-delivery`.
- BDD: 公盘下发审批完成后真实发送站内信 -> Given 文件类别要求分发且下发方式为 `PUBLIC_FOLDER` / When DCC 审批通过并完成正式下发 / Then 系统必须调用真实站内信发送 API 给每位接收人发送消息 / And 对应 `dcc_controlled_file_message_job` 状态更新为 `SENT`.
- BDD: 培训任务创建后真实发送站内信 -> Given 文件类别要求培训且培训接收人继承自分发接收人 / When DCC 进入培训链路并创建培训任务 / Then 系统必须调用真实站内信发送 API 给每位受训人发送消息 / And 对应 `dcc_controlled_file_message_job` 状态更新为 `SENT`.
- BDD: 模板缺失或发送失败必须失败并留痕 -> Given DCC 站内信模板缺失或系统站内信发送 API 抛错 / When DCC 尝试发送下发或培训通知 / Then 当前消息任务必须标记为 `FAILED` 并记录明确错误 / And DCC 主链路不得静默吞错伪装成功.
- RED: `mvn --% -f D:\ProjectPackage\Int\IntRuoyi\ruoyi-vue-pro\pom.xml -pl yudao-module-dcc -am -Dtest=DccNotifyTemplateSeedTest,DccControlledFileFinalizationServiceImplTest,DccControlledFileObsoleteServiceTest -Dsurefire.failIfNoSpecifiedTests=false test` -> FAIL, current DCC code only inserted local `PENDING` message jobs, never called `NotifyMessageSendApi`, never updated jobs to `SENT`, and the notify seed lacked `dcc_distribution` / `dcc_training` / `dcc_obsolete`.
- GREEN: `python -m pytest D:\ProjectPackage\Int\IntRuoyi\ruoyi-vue-pro\script\tests\test_dcc_notify_sql_scripts.py -q` -> PASS, the DCC notify seed contains approved/rejected/stamp-failed plus the new distribution/training/obsolete template rows and key copy.
- GREEN: `mvn --% -f D:\ProjectPackage\Int\IntRuoyi\ruoyi-vue-pro\pom.xml -pl yudao-module-dcc -am -Dtest=DccNotifyTemplateSeedTest,DccControlledFileFinalizationServiceImplTest,DccControlledFileObsoleteServiceTest -Dsurefire.failIfNoSpecifiedTests=false test` -> PASS, DCC distribution/training/obsolete flows now call the real system notify send API and update message jobs to `SENT`.
- GREEN: `mvn --% -f D:\ProjectPackage\Int\IntRuoyi\ruoyi-vue-pro\pom.xml -pl yudao-module-dcc -am -Dtest=DccControlledFileFinalizationServiceImplTest,DccControlledFileMessageOutboxTest,DccControlledFilePublicationFlowTest,DccNotifyTemplateSeedTest,DccControlledFileObsoleteServiceTest -Dsurefire.failIfNoSpecifiedTests=false test` -> PASS, targeted DCC finalization/outbox/publication/obsolete regressions all passed after wiring real notify delivery.
- GREEN: `python C:\Users\BJB110\.codex\skills\backend-api-delivery\scripts\validate_backend_api.py --evidence D:\ProjectPackage\Int\IntRuoyi\ruoyi-vue-pro\doc\tasks\20260519-dcc-notify-message-delivery\backend-api-evidence.md` -> PASS, backend evidence file satisfied the delivery contract.
- CLOSEOUT: `python C:\Users\BJB110\.codex\skills\task-closeout-cleanup\scripts\task_closeout.py --workspace D:\ProjectPackage\Int\IntRuoyi\ruoyi-vue-pro --task-id 20260519-dcc-notify-message-delivery --mode preview` -> PASS, preview would keep `task.md` and `execution-log.md`, and only mark `backend-api-evidence.md` as deletable task-local evidence.
