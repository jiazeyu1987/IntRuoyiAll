# 执行日志: 20260622-edhr-release-transaction-permission-rows-fix

- BDD: legacy submit/approve 菜单 permission 漂移不能让 lifecycle 在正式服失败 -> Given 正式库中 900263/900264 旧菜单 id 可能已存在但 permission 与当前发布契约不一致 / When 执行 20260618_mes_edhr_release_precheck_engine.sql 与 20260618_mes_edhr_release_transaction_lifecycle.sql / Then SQL 必须先把 900263/900264 纠正到 submit/approve 正式权限，再继续 lifecycle 的 permission 合并门禁。
- BDD: 修复后真实页面 promote-prod 不得再因 release transaction permission 缺失中断 -> Given 维护仓真实页面 prod(v12) 已证明当前失败点是 release_transaction_lifecycle SQL / When 修复后的 SQL 重新进入 clean 发布包并参与真实页面发布 / Then 正式服发布链路不得再在该 SQL 阶段因 Missing eDHR release transaction permission rows 失败。

- GREEN: previous-task-check -> PASS，上一后端任务 `20260622-edhr-dhr-template-menu-id-collision-fix` 已 `COMPLETED_WAITING_MAIN`，且维护仓真实页面 `上线正式服(v12)` 已证明其修复成功跨过 DHR 模板阶段；当前新的阻塞已切换到后续 release transaction permission 缺失。
- GREEN: maintenance-failure-read -> PASS，只读核对维护仓 operation `op-2026-06-22T074300534524700Z-82f9bc5a-9e16-466d-97ec-fe9173debb83` 日志后确认：真实页面 `上线正式服(v12)` 失败于 `20260618_mes_edhr_release_transaction_lifecycle.sql` line `341`，错误为 `Missing eDHR release transaction permission rows; cannot merge tenant package menu_ids`；同一日志还证明 `20260618_mes_edhr_release_precheck_engine.sql` 已先 `APPLIED`，正式环境发布锁随后被恢复性释放为 `LOCK_RELEASED`。
- GREEN: experience-preflight -> PASS，已读取 `D:\ProjectPackage\Int\IntRuoyi\docs\experience-index.md` 命中的 `release-backup-restore.md`、`server-access.md` 与 `worktree-memory.md`，并完成维护仓 prod 失败证据、业务仓回归入口与任务文档门禁预检；允许进入本任务的只读根因定位与 RED 契约编写。
- GREEN: root-cause-readonly -> PASS，只读核对 `20260618_mes_edhr_release_precheck_engine.sql` 与 `20260618_mes_edhr_release_transaction_lifecycle.sql` 后确认：`precheck_engine` 当前只对 `900263/900264` 做“缺失插入”，不会纠正已存在 legacy 同 id 的错误 permission；`lifecycle` 随后要求 `submit/approve/reject/withdraw/event-query` 五条 permission 全部存在，因此 prod 失败与“旧菜单 id 保留但 permission 漂移未修正”的 SQL 契约缺口一致。
- RED: `python -X utf8 -m pytest D:\ProjectPackage\Int\IntRuoyi\ruoyi-vue-pro\script\tests\test_edhr_release_precheck_schema_sql.py -q` -> FAIL，新增契约要求 release precheck SQL 必须显式纠正 legacy `900263/900264` 的 submit/approve permission 漂移；当前源码缺少 `UPDATE system_menu` 纠偏语句，断言 `UPDATE `system_menu`` 缺失。
- GREEN: root-cause-fix -> PASS，最小修改 `sql/mysql/20260618_mes_edhr_release_precheck_engine.sql`：在 `900263/900264` 缺失插入之后，新增两段 `UPDATE system_menu` 纠偏语句，把 legacy 同 id 菜单统一校正到 `mes:pro-edhr-release:submit` / `mes:pro-edhr-release:approve` 及对应名称、排序、父菜单、可见性契约，再继续后续 `intervene` 菜单插入与 package/menu 合并。
- GREEN: `python -X utf8 -m pytest D:\ProjectPackage\Int\IntRuoyi\ruoyi-vue-pro\script\tests\test_edhr_release_precheck_schema_sql.py -q` -> PASS，新增的 legacy permission 纠偏契约已转绿，6 项 release precheck SQL 契约全部通过。
- GREEN: `python -X utf8 -m pytest D:\ProjectPackage\Int\IntRuoyi\ruoyi-vue-pro\script\tests\test_edhr_release_transaction_schema_sql.py -q` -> PASS，release transaction lifecycle SQL 现有 4 项契约保持通过，修复未破坏后续 lifecycle 菜单/事件/无隐藏捷径门禁。
- GREEN: `python -X utf8 D:\ProjectPackage\Int\IntRuoyi\ruoyi-vue-pro\script\release\run-release-migration-policy-gate.py --sql-root D:\ProjectPackage\Int\IntRuoyi\ruoyi-vue-pro\sql\mysql` -> PASS，迁移策略门禁继续通过，当前 `migrationCount=186`。
- GREEN: `python -X utf8 C:\Users\BJB110\.codex\skills\bug-regression-fix-loop\scripts\validate_bug_regression.py --evidence D:\ProjectPackage\Int\IntRuoyi\ruoyi-vue-pro\doc\tasks\20260622-edhr-release-transaction-permission-rows-fix\bug-regression-evidence.md` -> PASS，bug regression evidence 合同校验通过，当前后端任务已具备交付维护仓重新构建发布包的最小完备证据。
- GREEN: handoff-ready -> PASS，当前业务修复范围仅涉及 `20260618_mes_edhr_release_precheck_engine.sql` 与 `script/tests/test_edhr_release_precheck_schema_sql.py`，验证已通过；下一步由维护仓把新提交并入 clean release backend worktree，使用新的 `releaseTag` 从真实页面重新 `build-release -> deploy-test -> mark-tested -> prod -> backup`。
- GREEN: prod-v14-readonly-compare -> PASS，维护仓真实页面 `上线正式服(v14)` 失败后，已只读对比正式服与测试服 `system_menu` 现场：正式服 `900266/900267/900268` 仍是 `eDHR流转单 / eDHR流转单查询 / eDHR流转单生成`，测试服同一职责位已变为 `900353/900354/900355` 的 `eDHR放行驳回 / eDHR放行撤回 / eDHR放行事务事件查询`。这说明当前 release transaction lifecycle SQL 仍在复用 traveler 号段，正式服会在 permission 合并门禁处失败，必须先修正 SQL 与测试契约。
- RED: `python -X utf8 -m pytest D:\ProjectPackage\Int\IntRuoyiWorktrees\ruoyi-vue-pro-mainmerge-release-clean\script\tests\test_edhr_release_transaction_schema_sql.py -q` -> FAIL，新增契约要求 release transaction lifecycle SQL 必须把 `reject / withdraw / event-query` 切到独立的 `900353/900354/900355`，且不得继续出现放行事务对 traveler 号段 `900266/900267/900268` 的复用；当前 SQL 仍保留旧号段，断言 `SELECT 900353, 'eDHR放行驳回'` 缺失。
- GREEN: root-cause-fix-v14 -> PASS，最小修改 `sql/mysql/20260618_mes_edhr_release_transaction_lifecycle.sql`：将 `eDHR放行驳回 / eDHR放行撤回 / eDHR放行事务事件查询` 三条菜单从 traveler 号段 `900266/900267/900268` 切换到独立未冲突的 `900353/900354/900355`，并同步更新 `system_menu` 计数校验、临时菜单表收集以及 `system_role_menu` 合并逻辑，确保首次在正式库执行时不再与 traveler 菜单冲突。
- GREEN: `python -X utf8 -m pytest D:\ProjectPackage\Int\IntRuoyiWorktrees\ruoyi-vue-pro-mainmerge-release-clean\script\tests\test_edhr_release_transaction_schema_sql.py -q` -> PASS，新增的“独立号段不复用 traveler 菜单”契约已转绿，当前 5 项 release transaction lifecycle SQL 契约全部通过。
- GREEN: `python -X utf8 -m pytest D:\ProjectPackage\Int\IntRuoyiWorktrees\ruoyi-vue-pro-mainmerge-release-clean\script\tests\test_edhr_traveler_schema_sql.py -q` -> PASS，traveler 菜单 SQL 现有 4 项契约保持通过，确认本次修复未篡改 `900266-900271` 的 traveler 号段归属。
- GREEN: `python -X utf8 -m pytest D:\ProjectPackage\Int\IntRuoyiWorktrees\ruoyi-vue-pro-mainmerge-release-clean\script\tests\test_edhr_release_precheck_schema_sql.py -q` -> PASS，上一轮 `submit / approve` legacy permission 纠偏契约保持通过，说明本次 lifecycle 菜单号段修复未回退 precheck 能力。
- GREEN: `python -X utf8 D:\ProjectPackage\Int\IntRuoyiWorktrees\ruoyi-vue-pro-mainmerge-release-clean\script\release\run-release-migration-policy-gate.py --sql-root D:\ProjectPackage\Int\IntRuoyiWorktrees\ruoyi-vue-pro-mainmerge-release-clean\sql\mysql` -> PASS，迁移策略门禁继续通过，当前 `migrationCount=186`。
- GREEN: `python C:\Users\BJB110\.codex\skills\bug-regression-fix-loop\scripts\validate_bug_regression.py --evidence D:\ProjectPackage\Int\IntRuoyiWorktrees\ruoyi-vue-pro-mainmerge-release-clean\doc\tasks\20260622-edhr-release-transaction-permission-rows-fix\bug-regression-evidence.md` -> PASS，bug regression evidence 合同校验通过，当前 release transaction 第二阶段修复已具备交付维护仓重新构建发布包的最小完备证据。
- GREEN: handoff-ready-v14 -> PASS，当前业务修复范围已扩展为 `20260618_mes_edhr_release_transaction_lifecycle.sql` 与 `script/tests/test_edhr_release_transaction_schema_sql.py` 的号段冲突收口，并通过 traveler/precheck/迁移策略回归；下一步由维护仓改用新的 `releaseTag` 从真实页面重新 `build-release -> deploy-test -> mark-tested -> prod -> backup`。
