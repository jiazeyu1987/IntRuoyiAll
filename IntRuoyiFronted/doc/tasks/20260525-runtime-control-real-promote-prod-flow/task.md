# 任务：运行控制台真实提升正式服链路验证

## 任务目标

- 使用 Playwright 通过真实前端路径点击运行控制台的“提升正式服”按钮。
- 使用真实测试服和正式服环境验证默认“只发代码”提升正式服链路是否正确完成。
- 验证前端弹窗默认发布范围、生产确认、提交结果和最近操作展示符合预期。

## 前序任务检查

- 前端上一任务 `20260525-vite-outdated-optimize-dep` 状态为 `completed`，无阻塞。

## BDD 场景

- BDD: 默认只发代码提升正式服成功 -> Given 运维人员在测试服运行控制台打开“提升正式服”弹窗, When 默认选择“只发代码”、填写原因并输入 `PROD` 后确认执行, Then 前端应提交 `promote-prod` 请求，参数包含 `publishScope=code-only`，并展示动作已提交和可查看日志。
- BDD: 正式服提升后前端闭环到正式后端 -> Given 提升正式服完成, When 访问正式环境管理端登录页并执行真实登录, Then 登录请求应闭环到正式后端，不能继续指向测试后端。
- BDD: 操作日志可追溯 -> Given 提升正式服动作完成, When 在运行控制台查看最近操作和日志, Then 最近操作应显示“提升正式服”“只发代码”和成功状态，日志可打开并包含发布完成证据。

## 里程碑

- [x] M1：建立任务文档并确认前序任务状态。
- [x] M2：记录测试前页面和环境状态。
- [x] M3：用 Playwright 完整操作提升正式服按钮。
- [x] M4：验证正式服页面、登录闭环和运行控制台最近操作。
- [x] M5：记录最终证据和剩余风险。

## 预期验证

- Playwright 真实前端操作“提升正式服”。
- 弹窗默认发布范围为“只发代码”。
- 未切换“带数据发布”，请求参数为 `publishScope=code-only`。
- 正式服管理端可访问并可真实登录。
- 最近操作表显示本次提升正式服成功。

## 当前状态

- 状态：completed
- 已完成：
  - 已新增带显式开关的真实 E2E 脚本 `tests/e2e/runtime-control-promote-prod-real-flow.e2e.js`。
  - 已确认默认不设置 `RUNTIME_CONTROL_ALLOW_REAL_PROMOTE_PROD=1` 时脚本会失败退出，不会误触正式提升。
  - 已通过 Playwright 从真实前端路径点击“提升正式服”，默认“只发代码”，填写原因并输入 `PROD`。
  - 已验证操作 `97f81d47-29b4-457d-ad4b-ed42301dd9e0` 状态为 `succeeded`，审计参数为 `publishScope=code-only`。
  - 已验证正式服管理端、Website 和展厅路径可访问，正式服管理端真实登录请求闭环到正式后端。
- 阻塞：暂无。
