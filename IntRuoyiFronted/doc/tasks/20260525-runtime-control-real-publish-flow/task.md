# 任务：运行控制台真实发布测试服 E2E

## 任务目标

- 新增一个显式授权才可运行的真实 E2E，用页面完成 `发布测试服` 全链路。
- E2E 必须走真实登录、按钮点击、发布范围确认、原因填写、确认执行、在线日志轮询、最终健康检查。
- 默认发布范围为 `只发代码`，不执行 MySQL 和 MinIO 数据同步。

## BDD 场景

- BDD: 发布测试服全链路成功 -> Given 本机运行控制台可访问且 admin 拥有运维权限, When 用户点击 `发布测试服`、保持 `只发代码`、填写原因并确认执行, Then 后端创建运行操作、执行发布脚本、日志可在线查看，最终状态为成功，测试服后端、管理端前端和 Website 健康检查通过。

## 里程碑

- [x] M1: 新增真实发布 E2E 文件。
- [x] M2: 用授权环境变量执行一次真实发布。
- [x] M3: 记录操作 ID、日志结果和测试服健康检查。

## 预期验证

- RED：新增 E2E 前缺少真实发布全链路测试文件。
- GREEN：`RUNTIME_CONTROL_ALLOW_REAL_PUBLISH=1` 执行 E2E 通过。
- GREEN：测试服后端、管理端前端和 Website 健康检查通过。

## Current Status

completed

## 当前状态

- 状态：completed
- 已完成：
  - 已确认后端发布脚本前置阻塞由后端任务修复。
  - 已新增真实发布 E2E 文件，默认必须显式设置 `RUNTIME_CONTROL_ALLOW_REAL_PUBLISH=1` 才会执行。
  - 已通过真实页面路径完成登录、点击 `发布测试服`、确认默认 `只发代码`、填写发布原因、确认执行、在线日志轮询和最终健康检查。
  - 已记录操作 ID `9e6b9fe5-56e9-4651-84b9-9f9b4528f271`，后端状态为 `succeeded`。
  - 已验证测试服后端、管理端前端、Website 根路径和展厅路径均返回 HTTP 200。
- 阻塞与影响：
  - 暂无。
