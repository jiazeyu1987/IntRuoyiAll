# eDHR 批次复盘已填写批记录视图

- Task ID: `20260610-edhr-review-filled-records`
- Status: `completed`

## 任务目标

将 eDHR 批次复盘页从默认展示原始 JSON，改为默认展示按工序排序的已填写批记录只读表单，同时保留批次状态、签名、审批和归档信息。

## 里程碑

1. 扩展前端 API 类型，声明复盘接口中的单表复盘、审批、签名和摘要结构。
2. 新增只读表单组件，渲染 `executionSnapshotJson` 与 `cellValuesJson`。
3. 改造复盘页布局，移除默认 `<pre>` JSON 视图，改成复盘摘要和已填写批记录折叠列表。
4. 使用真实本机前后端和 Playwright 验证批次 `32` 的复盘页。

## 设计约束检查

- `是否引入 fallback/降级/吞异常`：否。
- `是否从根因和长期维护角度解决`：是；将只读渲染抽为组件，供复盘页复用。
- `是否存在临时补丁或绕过`：否。

## 最终状态

- 状态：完成。
- 验证：前端类型检查通过，真实 Playwright 验证通过。
