# 执行日志：修复展厅产品管理首屏中的资料状态/指派对象列可见性回归

BDD: 产品管理列表首屏展示资料状态与指派对象 -> Given 用户进入 `http://localhost:8081/showroom/product` 的真实产品管理页并加载真实产品列表 / When 页面以当前默认布局渲染首屏表格 / Then 用户无需额外横向滚动即可看到 `资料状态` 与 `指派对象` 列，且两列继续展示真实列表数据。
