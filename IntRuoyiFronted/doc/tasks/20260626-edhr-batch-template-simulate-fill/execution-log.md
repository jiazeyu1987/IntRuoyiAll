# 执行日志：eDHR 模板模拟填写页

## 2026-06-26

- 初始化任务：根据用户说明，在 eDHR 模板说明页左侧卡片右上角新增 `模拟填写` 入口，并新增左侧模板内填写、右侧表单显示的单模板模拟页。
- BDD: 模拟填写入口可见 -> Given 用户位于模板说明页 When 查看左侧模板卡片 Then 每个卡片右上角显示 模拟填写 按钮。
- BDD: 模拟页按当前模板打开 -> Given 用户点击某张模板卡片的 模拟填写 When 进入新页面 Then 页面只加载该 taskId 对应模板，不再显示批次模板列表。
- BDD: 模拟页左侧按原模板填写 -> Given 用户打开某张模板的模拟页 When 查看左侧区域 Then 看到的是原始模板表格本身，而不是字段列表。
- BDD: 左侧模板内单元格可编辑 -> Given 某个模板单元格配置为文字/数字/日期/勾选/签名 When 用户在左侧对应格内输入 Then 该单元格直接显示可编辑控件并接收输入。
- BDD: 右侧显示填写结果 -> Given 用户在左侧模板格内输入内容 When 右侧渲染 Then 右侧按只读模板显示填写后的结果。
- BDD: 模拟签名实时驱动预览 -> Given 用户填写模拟签名人和签名时间 When 右侧渲染签名单元格 Then 显示签名人和时间；未填写时显示 未签名。
- BDD: 附件字段明确不支持上传 -> Given 当前模板字段带附件规则 When 用户查看左侧该格 Then 页面明确说明仅展示规则与占位，不提供真实上传。
- BDD: 缺少模板前置时明确报错 -> Given 缺少 id/taskId、找不到任务、缺少 reportId、缺少布局或缺少规则 When 打开模拟页 Then 页面明确报错，不渲染伪造内容。
- BDD: 左右工作台等宽 -> Given 用户位于模板模拟填写页 When 同时查看左右两侧 Then 左右工作台按等宽列布局展示。
- BDD: 模板宽度适配容器且高度不受限 -> Given 用户位于模板模拟填写页 When 查看左右模板 Then 模板按容器宽度缩放，高度随内容自然展开，不再固定限制高度。
- RED: `node tests/e2e/edhr-batch-template-simulate-static.spec.js` -> FAIL，断言 `必须新增模板内可编辑组件 EdhrExecutionTemplateEditableForm.vue` 失败，确认左侧仍不是模板内填写。
- CHANGE：`src/views/mes/pro/batchrecordtemplate/batchRecordTemplateRules.ts` 新增模拟填写组件类型映射、附件规则摘要、签名标签解析和 `cellRule -> simulation field` 纯函数。
- CHANGE：`src/views/mes/pro/edhr/components/EdhrExecutionTemplateEditableForm.vue` 新增模板内可编辑组件，直接在原始模板单元格中渲染文字/数字/日期/日期时间/勾选/本地签名输入，并明确附件不支持上传。
- CHANGE：删除原先只服务字段列表方案的 `src/views/mes/pro/edhr/components/EdhrExecutionSimulationForm.vue`，避免继续维持错误交互。
- CHANGE：`src/views/mes/pro/edhr-batch/BatchExecutionTemplatePage.vue` 将模板卡片拆成主点击区与独立操作区，在右上角新增 `模拟填写` 按钮并跳转新路由。
- CHANGE：`src/views/mes/pro/edhr-batch/BatchExecutionTemplateSimulatePage.vue` 新增单模板模拟填写页，按 `id + taskId` 加载模板详情，左侧使用模板内可编辑组件，右侧复用只读模板组件显示结果。
- CHANGE：`src/router/modules/remaining.ts` 新增隐藏路由 `MesProEdhrBatchExecutionTemplateSimulate`。
- CHANGE：根据最新界面反馈，将模拟页双栏工作台列宽改为 `1fr / 1fr`，保证左右区域等宽显示。
- RED: `node tests/e2e/edhr-batch-template-simulate-static.spec.js` -> FAIL，断言 `必须新增模板等比缩放视口组件 EdhrTemplateFitViewport.vue` 失败，确认当前仍缺少左右同尺寸显示体和等比缩放实现。
- CHANGE：`src/views/mes/pro/edhr/components/EdhrTemplateFitViewport.vue` 新增共享模板等比缩放视口，使用 `ResizeObserver` 测量内容与容器尺寸，并通过 `transform: scale(...)` 保证模板在固定显示体内同比缩放。
- CHANGE：`src/views/mes/pro/edhr/components/EdhrExecutionTemplateEditableForm.vue` 与 `EdhrExecutionReadonlyForm.vue` 新增 `fitToViewport` 模式，在模拟页中复用统一缩放视口，保持左右模板使用同一套缩放规则。
- CHANGE：`src/views/mes/pro/edhr-batch/BatchExecutionTemplateSimulatePage.vue` 曾新增统一标题区和 `clamp(...)` 高度限制的双栏模板显示体，作为中间版等比缩放工作台。
- FIX：根据 Vite ESLint overlay 反馈，将 `src/views/mes/pro/edhr/components/EdhrTemplateFitViewport.vue` 中的 `<slot />` 改为 `<slot></slot>`，满足 `vue/html-self-closing` 规则。
- DIAG：针对 `Failed to fetch dynamically imported module: /src/views/mes/pro/edhr-batch/BatchExecutionTemplateSimulatePage.vue` 继续排查；实测目标页面模块及 `EdhrExecutionTemplateEditableForm.vue`、`EdhrExecutionReadonlyForm.vue`、`EdhrTemplateFitViewport.vue`、`batchRecordTemplateRules.ts`、相关 API 模块均由本地 Vite 返回 `200`，确认当前源码已可编译，请求失败更可能来自浏览器标签页缓存了上一次失败的动态导入。
- BLOCKER: experience-preflight -> 官方 `login-preflight.mjs` 脚本在当前登录页结构上未稳定命中目标文本与登录响应，不能直接作为本次真实浏览器结论；后续改由真实 Playwright 探针逐层确认登录、路由和页面加载。
- DIAG：真实 Playwright 探针确认本机登录页可用 `测试租户/aoteman/111111` 完成登录；`/system/tenant/get-id-by-name` 与 `/system/auth/login` 均返回 `code=0` 成功，登录后进入 `/index`。
- FIX：为清理旧的开发态失败缓存，已停止并重启本机 `8081` 的 Vite dev server；新监听进程 `PID=35920` 已恢复。
- DIAG：重启后的真实 Playwright 浏览器验证显示 `http://localhost:8081/mes/pro/feedback/edhr-batch-execution/template-simulate?id=1&taskId=1` 可正常打开，目标模块 `BatchExecutionTemplateSimulatePage.vue` 返回 `200`，页面已渲染 `eDHR模板模拟填写 / 模板内填写 / 表单显示`，确认动态导入错误已不再是当前代码状态。
- GREEN: real-browser-entry-path -> PASS，真实浏览器从 `eDHR批次执行` 列表点击 `模板`，再点击 `模拟填写`，成功进入 `/mes/pro/feedback/edhr-batch-execution/template-simulate?id=900000000445&taskId=2633`，页面已渲染 `eDHR模板模拟填写 / 模板内填写 / 表单显示`。
- RED: `node tests/e2e/edhr-batch-template-simulate-static.spec.js` -> FAIL，断言 `模拟页必须声明模板只按宽度适配容器` 失败，确认旧实现仍固定高度并按宽高同时缩放。
- CHANGE：`src/views/mes/pro/edhr/components/EdhrTemplateFitViewport.vue` 新增 `widthOnly` 模式，改为仅按容器宽度计算缩放比例。
- CHANGE：`src/views/mes/pro/edhr/components/EdhrExecutionTemplateEditableForm.vue` 与 `EdhrExecutionReadonlyForm.vue` 在模拟页缩放模式下切换为 `width-only` 适配，并取消组件级固定高度。
- CHANGE：`src/views/mes/pro/edhr-batch/BatchExecutionTemplateSimulatePage.vue` 去除左右模板显示体固定高度，只保留等宽容器，允许高度按内容自然展开。
- GREEN: `node tests/e2e/edhr-batch-template-simulate-static.spec.js` -> PASS
- GREEN: `node tests/e2e/edhr-batch-template-preview-static.spec.js` -> PASS
- GREEN: `node tests/e2e/edhr-batch-history-static.spec.js` -> PASS
- GREEN: `node tests/e2e/edhr-inline-signature-cells-static.spec.js` -> PASS
- GREEN: `node tests/e2e/edhr-batch-review-summary-labels-static.spec.js` -> PASS
- GREEN: `$env:NODE_OPTIONS='--max-old-space-size=8192'; pnpm ts:check` -> PASS
- DOC: 任务文档与证据文档已统一为最终口径：左右工作台等宽，模板仅按容器宽度缩放，高度不限制。
