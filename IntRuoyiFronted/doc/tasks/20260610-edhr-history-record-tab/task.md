# eDHR 历史批记录页签

## 任务目标

新增只读“历史批记录”页签/页面，只显示已归档的 eDHR 批次记录。用户点击历史批次后，在同页查看该批次对应的已填写工序批记录，工序导航和模板表格预览沿用当前复盘页的左工序、右表格交互。

## 里程碑

1. RED：记录当前无独立历史批记录页签，新增静态测试证明页面、路由和已归档过滤尚不存在。
2. GREEN：新增历史批记录页面、路由入口和只读工序/模板表格预览。
3. REGRESSION：运行静态测试、前端类型检查，并尽量完成真实页面验证。

## 设计约束检查

- 是否引入 fallback/降级/吞异常：否。
- 是否从根因和长期维护角度解决：是，新增专用历史查看页面并复用现有复盘接口和只读模板组件。
- 是否存在临时补丁或绕过：否。

## 预期验证

- 历史批记录列表固定查询 `EDHR_BATCH_STATUS_ARCHIVED`。
- 点击历史批次后加载 `review-timeline` 并显示该批次的工序导航。
- 点击工序后右侧只显示该工序对应的一张已填写模板表格。
- 页面不默认展示原始 JSON，不显示未归档批次，不提供填写/签名/审批/归档操作。

## 当前状态

已完成。

## 完成记录

- 新增历史批记录页面，固定展示 `EDHR_BATCH_STATUS_ARCHIVED` 已归档批次。
- 在 eDHR 批次执行列表顶部新增“批次执行 / 历史批记录”页签入口。
- 历史页点击批次后复用 `review-timeline` 和只读模板组件展示工序与模板表格。
- 静态测试、类型检查和真实只读 E2E 均已通过。
