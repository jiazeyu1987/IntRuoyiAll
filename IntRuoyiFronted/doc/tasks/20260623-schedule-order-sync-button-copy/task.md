# 任务：排产工单同步按钮文案调整

## 任务目标

将排产工单页面工具栏中红框标注的“待同步差异”按钮文案调整为“同步工单”，保持原有按钮行为、弹窗业务含义和排产工单入池流程不变。

## 范围

- 修改 `src/views/mes/pro/scheduleorder/index.vue` 中排产工单工具栏按钮展示文案。
- 更新直接依赖该按钮文案的静态契约测试和真实路径脚本定位文案。
- 不修改接口、权限、入池逻辑、弹窗标题、样式和菜单配置。

## 经验门禁

- `D:\ProjectPackage\Int\IntPP\FRONTEND_STYLE.md`：列表页工具栏应保持紧凑、任务聚焦；本次仅替换按钮文案，不改布局、颜色或按钮层级。
- `clear-frontend-copy`：用户可见按钮文案使用规范简体中文；不新增兜底文案、兼容分支或静默降级。

## 设计约束检查

- `是否引入 fallback/降级/吞异常`：否。
- `是否从根因和长期维护角度解决`：是，直接修改页面源文案并同步测试契约。
- `是否存在临时补丁或绕过`：否。

## BDD 场景

- `BDD: 排产工单同步入口展示新文案 -> Given 用户打开排产工单页面 When 查看工具栏同步入口 Then 按钮展示为“同步工单”，点击后仍进入原待同步差异弹窗。`

## 里程碑

- [x] M1：确认前端子仓库状态、上一任务完成状态、经验门禁和文案定位。
- [x] M2：先更新测试期望并运行 RED，确认旧页面文案导致失败。
- [x] M3：修改页面按钮文案并同步真实路径脚本定位。
- [x] M4：运行静态契约、语法检查和目标文案复核。
- [x] M5：运行收尾清理预览、更新任务记录并准备提交本次前端改动。

## 预期验证

- `node tests/e2e/mes-pro-schedule-order-usability-static.spec.js`
- `node --check tests/e2e/smart-scheduling-smoke-real-flow.e2e.js`
- `rg -n "待同步差异|同步工单" src tests`

## 当前状态

已完成。排产工单页面工具栏按钮已由“待同步差异”调整为“同步工单”；原待同步差异弹窗标题和入池流程未改。静态契约、真实路径脚本语法检查、目标文案复核和收尾清理预览均通过。
