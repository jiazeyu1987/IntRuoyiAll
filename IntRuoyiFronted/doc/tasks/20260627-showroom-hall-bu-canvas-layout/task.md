# 后台展柜画布按 BU 排布

## 任务目标

在展厅后台展柜画布弹窗工具栏增加“按 BU 排布”按钮。点击后调用后端计算接口更新画布预览，不立即保存；用户继续点击现有“保存布局”后才持久化。

## 经验门禁

- 命中 `D:\ProjectPackage\Int\IntPP\FRONTEND_STYLE.md`：按钮沿用后台工具栏紧凑操作风格，不新增说明卡片或大面积视觉改造。
- 本轮不触发服务器写入、发布、恢复、真实 E2E、worktree 合并或清理；高风险 `experience-preflight` 暂不需要执行。

## 设计约束检查

- `是否引入 fallback/降级/吞异常`：否。
- `是否从根因和长期维护角度解决`：是，BU 排布由后端统一计算，前端只负责调用与预览。
- `是否存在临时补丁或绕过`：否。

## 里程碑

1. 记录前端 BDD/TDD 证据并补失败测试。
2. 新增前端 API 方法。
3. 在 `HallCanvasLayoutDialog` 增加按钮、loading、错误提示与预览更新。
4. 运行定向验证并记录结果。

## 预期验证

- 定向静态测试验证按钮存在、调用新接口、不提前保存、失败时提示错误。
- 能运行时执行前端定向测试和 build。

## 当前状态

已完成。

## 完成记录

- 展柜画布弹窗已新增“按 BU 排布”按钮。
- 点击按钮调用后端 `calculateHallBuCanvasLayout`，只更新画布预览，不调用保存接口。
- 保存布局继续调用现有 `updateHallCanvasLayout`。
- 混合产品/奖项布局保存校验已调整：产品块要求完整铺满，奖项块只校验合法坐标范围。
- 收尾清理已执行 preview；因 evidence 文件为本次技能验收证据，未执行删除。
