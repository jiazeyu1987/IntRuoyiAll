# eDHR 批次复盘左工序右表格布局任务

## 目标

把 eDHR 批次复盘页的“已填写批记录”从纵向展开多张表改成左侧工序导航、右侧单张模板表格预览。

## 里程碑

- [x] RED：确认当前页面仍使用 `el-collapse` 纵向展示多张批记录，不支持左侧工序导航和右侧单表预览。
- [x] GREEN：实现左侧有批记录工序列表、选中状态、右侧执行摘要和单张模板表格渲染。
- [x] REGRESSION：运行类型检查和真实复盘页 E2E，确认顶部证据区保留、左侧工序 15 条、右侧一次只显示 1 张模板表格。

## 预期验证

- `$env:NODE_OPTIONS='--max-old-space-size=8192'; pnpm ts:check` 通过。
- Playwright 打开真实复盘页 `id=32`，点击 `B010` 和 `B320` 可切换右侧模板表格。

## 设计约束检查

- `是否引入 fallback/降级/吞异常`：否。
- `是否从根因和长期维护角度解决`：是，复盘页以显式选中执行记录驱动右侧单表展示。
- `是否存在临时补丁或绕过`：否。

## 当前状态

已完成。真实复盘页验证通过，左工序右表格布局生效。
