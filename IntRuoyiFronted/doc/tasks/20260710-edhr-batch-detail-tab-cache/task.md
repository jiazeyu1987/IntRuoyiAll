# 批次详情标签切换缓存修复

## Task Goal

修复批次详情页在已打开标签之间切走再切回时重复加载数据的问题，保留页面实例与当前交互状态，仅在批次标识实际变化时重新加载。

## 经验门禁

- PowerShell 中文与文件读写必须显式使用 UTF-8，不使用 `&&`。
- 行为变更必须先记录 BDD，并按 RED -> GREEN -> REGRESSION 执行。
- 前端真实链路验证必须使用 Playwright 登录后操作，不使用 mock 或接口绕过。
- E2E 前必须记录 `GREEN: experience-preflight -> PASS`。
- 不引入 fallback、静默吞错或用默认成功掩盖加载失败。
- 只修改本任务范围内的标签缓存与批次详情加载逻辑，不扩展后端接口或其他页面行为。

## 设计约束检查

- 是否引入 fallback/降级/吞异常：否。
- 是否从根因和长期维护角度解决：是；从路由缓存与组件生命周期根因修复重复加载。
- 是否存在临时补丁或绕过：否。

## Milestones

- [x] M1：建立任务记录并明确可观察验收行为。
- [x] M2：复现标签切换重复加载并增加失败回归测试。
- [x] M3：实施最小修复并通过目标测试。
- [x] M4：完成相关回归与真实用户路径验证。
- [x] M5：提交实现并完成任务收尾。

## Expected Verification

- 静态或组件回归测试能在修复前稳定失败、修复后通过。
- 批次详情首次进入正常加载。
- 切换到其他已打开标签再返回时，不因标签激活重复请求批次详情、工作台或时间线数据。
- 切换到不同批次标识时仍会加载对应批次数据。
- 相关前端测试、类型检查与真实 Playwright 路径通过。

## Current Status

completed

## Final Verification

- 实现提交：`241e61e62`（任务: 修复批次详情标签切换重复加载）。
- closeout preview/apply 均通过，仅清理本任务的缺陷证据、前端证据和一次性真实 E2E 脚本。
- 保留 `task.md`、`execution-log.md`、`verification-report.md` 与正式静态回归测试。
