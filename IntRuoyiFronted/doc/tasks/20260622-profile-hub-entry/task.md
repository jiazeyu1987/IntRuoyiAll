# 任务：个人中心聚合入口版

## 任务目标

- 基于现有 `/user/profile` 页面扩展“个人中心聚合入口版”。
- 保留当前账号资料与改密能力。
- 在个人中心新增 BPM、DCC、eDHR 的审批/签名/任务分区，展示当前账号相关汇总与快捷入口。
- 本任务只做前端聚合入口，不新增后端聚合接口，不承诺统一单表任务中心。

## 上一任务检查

- 上一个前端任务：`D:\ProjectPackage\Int\IntRuoyi\yudao-ui-admin-vue3\doc\tasks\20260622-dcc-onlyoffice-pure-wheel-zoom\task.md`
- 状态：`COMPLETED`
- 处理：上一任务已完成，不阻塞本次个人中心聚合入口改造。

## 用户要求与执行边界

- 用户确认方案：`A`
- 本任务边界：
  - 只改前端个人中心页及直接相关静态合同测试。
  - 复用现有 BPM / DCC / eDHR API 与既有页面入口。
  - 不新增后端聚合接口。
  - 不把 DCC / eDHR 强行接入 BPM 通用审批动作。
  - 不做与个人中心无关的 UI 重构。

## 经验门禁

- `D:\ProjectPackage\Int\IntRuoyi\docs\experience-index.md`
  - 命中文档：
    - `D:\ProjectPackage\Int\IntPP\FRONTEND_STYLE.md`
- 本任务强制门禁摘录：
  - 页面应遵循蓝/中性运营台风格，不做营销化改版。
  - 先补 RED 测试，再做最小实现，最后运行回归验证。
  - 个人中心若只做入口聚合，必须显式暴露权限/能力边界，不得伪装成统一后端任务中心。

## 设计约束检查

- `是否引入 fallback/降级/吞异常`：否。
- `是否从根因和长期维护角度解决`：是。当前阶段明确为“聚合入口版”，不伪造统一模型；后续若需要统一任务中心，再走正式后端聚合。
- `是否存在临时补丁或绕过`：否。

## BDD 场景

- `BDD: 用户可在个人中心完成当前账号资料维护 -> Given 用户进入个人中心 When 切换到资料或改密页签 Then 仍可使用现有个人资料与修改密码能力。`
- `BDD: 用户可在个人中心看到审批与任务入口汇总 -> Given 用户进入个人中心 When 查看工作台页签 Then 可看到 BPM、DCC、eDHR 的任务汇总、快捷入口和关键说明。`
- `BDD: 个人中心显式保留模块边界 -> Given DCC 与 eDHR 不能走 BPM 通用审批 When 用户点击对应入口 Then 应跳转到各自真实处理页，而不是伪造统一审批按钮。`
- `BDD: 无权限模块在个人中心不误导用户 -> Given 当前账号缺少某模块权限 When 进入工作台页签 Then 该模块应显示无权限或不展示入口，不得报错或假装可用。`

## 里程碑

1. 建立任务文档并记录门禁。`DONE`
2. 补目标静态合同测试，锁定个人中心聚合入口结构。`DONE`
3. 最小实现个人中心聚合页签、汇总与快捷入口。`DONE`
4. 运行目标验证并补证据。`DONE`

## 预期验证

- `node scripts/profile-hub-entry-static.test.mjs`
- `pnpm exec vue-tsc --noEmit -p tsconfig.json`
- 视影响补充已有静态合同测试

## 当前状态

completed

## 当前结论

- 已在现有 `/user/profile` 页面新增“个人工作台”页签，默认进入工作台；若存在社交绑定回跳参数，仍优先回到社交绑定页签。
- 个人工作台已聚合 BPM、DCC、eDHR 三类入口，并展示当前账号相关汇总：
  - BPM：待办审批、已办审批、我的申请
  - DCC：审批任务、我的培训、电子签名
  - eDHR：我的待办、候选审核、我已处理、签名记录、逾期数
- 页面显式保留模块边界说明：DCC / eDHR 的审批与电子签名仍在各自模块完成，个人中心只做聚合入口。
- 无权限模块会显示“无权限”状态，不伪造可用入口；单模块加载失败会在工作台顶部暴露错误。
