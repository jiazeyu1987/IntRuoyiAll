# 20260610 排产目标 7 前端真实闭环 E2E

## 任务目标

新增并执行排产目标 7 的测试租户真实闭环 E2E：用浏览器登录 `测试租户/aoteman`，按排产员路径验证目标 1-6 已形成可用闭环。

## 设计约束检查

- 是否引入 fallback/降级/吞异常：否。脚本必须在登录、页面入口、真实样本、接口响应缺失时失败。
- 是否从根因和长期维护角度解决：是。将分散的排产 E2E 结果串成可复跑的闭环回归。
- 是否存在临时补丁或绕过：否。不新增测试专用页面，不绕过真实前端入口。

## 里程碑

1. 编写目标 7 闭环 E2E 脚本。
2. 编写 QA 覆盖矩阵与证据文档。
3. 执行静态和真实 E2E。
4. 融合后在 `int_main` 复跑。

## 当前状态

- 状态：已完成。
- worktree：`D:\ProjectPackage\Int\IntRuoyi\worktrees\scheduler_e2e_closure`
- 分支：`codex/scheduler-e2e-closure`
- 端口：FE `8095` / BE `48095`
- 已完成：新增 `mes-scheduler-target7-closed-loop-real-flow.e2e.js`，覆盖工作台快捷入口、生产工单搜索、排产工单池、工序快照、生产报工待归属入口和同登录态最终闭环校验。
- 最终结果：已融合进 `int_main`；合并后目标 7 真实 E2E 在 `http://127.0.0.1:8081` 使用 `测试租户/aoteman` 通过。
