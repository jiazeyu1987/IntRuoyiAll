# 20260610 eDHR 881MO090863 测试租户数据补齐与真实 E2E

## 任务目标

在本机测试租户中补齐 `881MO090863` 对应 `ROUTE-YXN.069.001.1001` 的 eDHR 批次执行所需数据，使用 Playwright 走真实前端路径，完成所有必填批处理表格的模拟填写、签名、提交、审批、同步状态，并验证最终可复盘查看填写记录。

## 范围

- 只允许写入本机 `测试租户 / aoteman` 相关测试数据。
- 可从本机 `芋道源码 / admin` 中读取并平移必要主数据、路线、报表模板、菜单权限和角色配置。
- 前端无入口或接口报错时先定位并按正式方案修复；不得通过接口绕过用户流程完成 E2E。
- API 或 SQL 仅用于前置数据核对、受控数据补齐和最终状态校验。

## 非范围

- 不修改正式服务器或测试服务器数据。
- 不改写受保护展厅文件配置、DCC 审核矩阵或无关业务数据。
- 不为 E2E 新增临时前端控件或隐藏真实错误。

## 设计约束检查

- 是否引入 fallback/降级/吞异常：否。
- 是否从根因和长期维护角度解决：是，缺数据补主数据或权限，缺前端入口/接口能力则修正式路径。
- 是否存在临时补丁或绕过：否。

## 里程碑

1. 建立任务文档和 BDD/TDD/E2E 验收口径。
2. 核对本机前后端服务、数据库连接、测试租户与芋道源码租户基础数据。
3. 补齐测试租户中缺失的工单、产品、路线、报表、权限或用户签名前置数据。
4. Playwright 登录测试租户，真实创建/打开批次执行，逐张填写、签名、提交、审批。
5. 同步状态、关闭批次、生成归档，验证复盘页可看到记录。
6. 记录证据、回滚脚本或数据清单，并提交本任务文档和必要修复。

## 预期验证

- `BDD:` 记录用户可观察流程。
- `RED:` 记录补齐前缺失项或失败路径。
- `GREEN:` 记录补齐后 Playwright 真实路径通过。
- `REGRESSION:` 记录芋道源码租户数据未被写入、已有路线和模板未被破坏。

## 完成结果

- 数据补齐结论：本机测试租户已存在目标工单、路线、21 道工序、15 张默认批记录绑定、11 个路线产品规格和 eDHR 权限；本轮未从芋道源码租户新增复制业务数据。
- 已复核完成批次：`PC-E2E-20260610-0210`，批次执行 ID `9`。
- 最终状态：`status=40`，`task_total=21`，`task_approved_count=15`，`blocked_count=0`。
- 归档：`PC-E2E-20260610-0210-edhr-final.pdf`，`SEALED`。
- 前端证据：Playwright 登录测试租户后打开批次详情、复盘页、下载 PDF、触发打印窗口均通过。

## Cleanup Keep

- `doc/tasks/20260610-edhr-881-e2e-data-completion/database-schema-evidence.md`
- `doc/tasks/20260610-edhr-881-e2e-data-completion/test-report.md`
- `tests/e2e/edhr-881-completed-batch-review.e2e.js`

## 当前状态

已完成。
