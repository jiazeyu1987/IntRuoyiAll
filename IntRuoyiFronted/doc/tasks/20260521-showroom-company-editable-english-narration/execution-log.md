BDD: 公司英文介绍允许手动修正后再生成语音 -> Given 用户在 `http://localhost:8081/showroom/company` 打开“编辑公司信息”弹框且 AI 已回填中英文介绍 / When 用户手动修改英文介绍并点击 `生成语音` / Then 前端必须把当前英文草稿一并提交到生成语音链路，而不是忽略英文改动或强制回退自动翻译结果。
BDD: 公司双语语音保存后英文介绍与音频都能保留 -> Given 用户已经基于当前中英文介绍生成新的双语语音草稿 / When 用户点击 `保存语音` / Then 已保存 live 版本必须保留本次英文介绍文本与重新生成的音频，刷新后仍可回放。
BDD: 公司播放入口使用音频组件而不是播放按钮 -> Given 用户查看公司页只读语音区或编辑弹框内的语音草稿 / When 对应语言存在音频地址 / Then 页面应渲染音频播放器组件，不再出现 `播放中文`、`播放英文` 两个按钮。
RED: `node --test scripts/showroom-admin-company-dashboard-history.test.mjs` -> FAIL，`CompanyWorkbench.vue` 仍缺少 `v-model="narrationDraft.introTextEn"` 与 `introTextEn` 请求字段，且页面还保留 `播放中文 / 播放英文` 按钮。
GREEN: `node --test scripts/showroom-admin-company-dashboard-history.test.mjs` -> PASS，源码回归已确认英文介绍可编辑、生成语音请求带 `introTextEn`、播放入口改为 `<audio controls>`。
GREEN: `pnpm exec eslint src/api/showroom-admin/index.ts src/views/showroom-admin/company/CompanyWorkbench.vue scripts/showroom-admin-company-dashboard-history.test.mjs --format stylish` -> PASS。
GREEN: `npx.cmd --yes --package @playwright/cli playwright-cli --session showroom-company-editable-english run-code --filename D:\ProjectPackage\Int\IntRuoyi\yudao-ui-admin-vue3\doc\tasks\20260521-showroom-company-editable-english-narration\scripts\verify-showroom-company-editable-english.mjs` -> PASS，真实页面完成 `AI生成介绍 -> 手改英文 -> 生成语音 -> 保存语音 -> 重新打开弹框 -> 接口核对 live EN narration` 全链路验证。
GREEN: `python C:\Users\BJB110\.codex\skills\frontend-feature-delivery\scripts\validate_frontend_feature.py --evidence doc/tasks/20260521-showroom-company-editable-english-narration/frontend-feature-evidence.md` -> PASS，证据文件结构满足前端技能校验规则。
GREEN: `python C:\Users\BJB110\.codex\skills\task-closeout-cleanup\scripts\task_closeout.py --workspace D:\ProjectPackage\Int\IntRuoyi\yudao-ui-admin-vue3 --task-id 20260521-showroom-company-editable-english-narration --mode preview` -> PASS，预览仅删除本任务附属证据、截图与一次性 Playwright 脚本。
GREEN: `python C:\Users\BJB110\.codex\skills\task-closeout-cleanup\scripts\task_closeout.py --workspace D:\ProjectPackage\Int\IntRuoyi\yudao-ui-admin-vue3 --task-id 20260521-showroom-company-editable-english-narration --mode apply` -> PASS，已按预览结果删除附属证据、截图与一次性 Playwright 脚本，仅保留 `task.md / execution-log.md`。
