# eDHR 复盘聚焦表格与自适应宽度任务

## 目标

复盘页隐藏顶部批次证据区域，只显示“已填写批记录”左工序右表格工作台；右侧模板表格压缩到当前区域内完整显示，不再要求用户拖横向滚动条。

## 里程碑

- [x] RED：确认页面仍显示 toolbar / summary-grid，模板表格仍有 `min-width: 960px` 和横向滚动。
- [x] GREEN：隐藏顶部区域，改造模板列宽为百分比自适应，表格容器不再横向滚动。
- [x] REGRESSION：类型检查和真实页面 E2E 验证右侧只显示 1 张完整表格、无横向滚动、黄框区域内容不出现。

## 预期验证

- `$env:NODE_OPTIONS='--max-old-space-size=8192'; pnpm ts:check` 通过。
- Playwright 打开真实复盘页 `id=32`，确认：
  - 页面不显示 `eDHR 批次复盘` 顶部工具条。
  - 页面不显示 `批次状态时间线`、`签名记录`、`审批记录`、`归档版本`。
  - `.edhr-readonly-form__sheet-wrap.scrollWidth <= clientWidth + 1`。

## 设计约束检查

- `是否引入 fallback/降级/吞异常`：否。
- `是否从根因和长期维护角度解决`：是，去掉不需要的复盘证据区域，并让模板渲染组件按容器宽度分配列宽。
- `是否存在临时补丁或绕过`：否。

## 当前状态

已完成。页面聚焦已填写批记录，模板表格可在右侧区域完整显示。
