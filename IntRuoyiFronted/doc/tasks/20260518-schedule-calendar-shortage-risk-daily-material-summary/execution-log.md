# Execution Log

- 2026-05-18: Created frontend task package `20260518-schedule-calendar-shortage-risk-daily-material-summary`.
- 2026-05-18: Blocked previous frontend task `20260518-md-item-erp-bom-sync-button` due user priority switch.
- BDD: Main auto-schedule drawer allows publish with shortage warnings -> Given the production task page preview contains material shortage issues with severity `WARNING` and no blocking issues, When the preview is shown, Then the `确认发布` action remains enabled.
- BDD: Schedule-calendar auto-schedule panel allows publish with shortage warnings -> Given the schedule-calendar preview contains material shortage issues with severity `WARNING` and no blocking issues, When the preview is shown, Then the `发布排产` action remains enabled.
- BDD: Selected day shows per-material summary inline -> Given the user clicks a schedule-calendar date with scheduled material usage, When the day detail loads, Then the right-side detail panel shows `物料 / 当日用量 / 当日可用余量 / 仍缺 / 影响工单数` for that date.
- BDD: Shortage summary stays visible as a warning, not a blocker -> Given the selected day has one or more shortage materials, When the day detail renders, Then the shortage count remains visible and the user can inspect the detailed per-material shortage rows.
- BDD: Real API errors remain visible -> Given the day-detail or preview API fails, When the frontend request rejects, Then the page surfaces the real error and does not hide it behind fallback data.
- RED: `node D:\ProjectPackage\Int\IntRuoyi\yudao-ui-admin-vue3\doc\tasks\20260518-schedule-calendar-shortage-risk-daily-material-summary\scripts\verify-schedule-calendar-shortage-source.mjs` -> FAIL, the schedule-calendar API type is still missing `scheduledUsageQty` / `remainingAvailableQty` / `affectedWorkOrderCount`, and the day-detail panel does not yet render the required inline material summary labels.
- GREEN: `node D:\ProjectPackage\Int\IntRuoyi\yudao-ui-admin-vue3\doc\tasks\20260518-schedule-calendar-shortage-risk-daily-material-summary\scripts\verify-schedule-calendar-shortage-source.mjs` -> PASS, the schedule-calendar API type and inline material summary labels are present.
- GREEN: `$env:NODE_OPTIONS='--max-old-space-size=8192'; pnpm exec vue-tsc --noEmit -p tsconfig.relaxed.json` -> PASS, relaxed frontend type-check succeeds with the repository heap override.
- GREEN: `npx.cmd --yes --package @playwright/cli playwright-cli --session schedule-calendar-shortage-risk run-code --filename D:\ProjectPackage\Int\IntRuoyi\yudao-ui-admin-vue3\doc\tasks\20260518-schedule-calendar-shortage-risk-daily-material-summary\scripts\verify-schedule-calendar-shortage-real-e2e.mjs` -> PASS, the live page renders one daily material summary row and both preview entry points now report `shortageCount=54` with only `blockingIssueCount=2`.
