# Execution Log: Remove merged auto-schedule frontend worktree

BDD: frontend worktree cleanup -> Given the committed auto-schedule frontend history is already on `int_main`, When the frontend feature worktree is no longer needed, Then the frontend feature worktree registration and directory should be removed without deleting the branch.

RED: pending -> FAIL, frontend worktree and any process dependency not checked yet
