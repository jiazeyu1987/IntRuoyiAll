BDD: compare four batch-record screenshots after viewer cleanup -> Given the current live electronic batch-record preview pages already suppress the JMReport viewer chrome, When we compare the updated live output against the four target screenshots under `resource\\批记录截图`, Then we should be able to state whether each page visually improved and what shared gaps still remain.
RED: `npx.cmd --yes --package @playwright/cli playwright-cli run-code --filename doc/tasks/20260518-four-batch-record-image-compare-post-viewer-cleanup/scripts/capture-four-batch-record-previews.mjs` -> FAIL, `http://127.0.0.1:8082/login?redirect=/index` returned `net::ERR_CONNECTION_REFUSED` because the preview frontend worktree was not running.
GREEN: `node node_modules/vite/bin/vite.js --mode batch-record-preview --host 127.0.0.1` -> PASS with runtime note, the preview frontend worktree started successfully on `http://127.0.0.1:8083/` because `8082` was already occupied by another process.
RED: `FRONTEND_BASE_URL=http://127.0.0.1:8083 npx.cmd --yes --package @playwright/cli playwright-cli run-code --filename doc/tasks/20260518-four-batch-record-image-compare-post-viewer-cleanup/scripts/capture-four-batch-record-previews.mjs` -> FAIL, this Playwright CLI runtime does not expose `process`, so the temporary env-driven script form could not resolve `process.env.FRONTEND_BASE_URL`.
GREEN: `npx.cmd --yes --package @playwright/cli playwright-cli run-code --filename doc/tasks/20260518-four-batch-record-image-compare-post-viewer-cleanup/scripts/capture-four-batch-record-previews.mjs` -> PASS, live screenshots were captured for `粗洗工序生产记录`, `精洗工序生产记录`, and `清洁工序生产记录` on `http://127.0.0.1:8083`.
GREEN: reran the same capture script after extending it to include `产品信息` -> PASS, current live screenshots now cover `产品信息`, `粗洗工序生产记录`, `精洗工序生产记录`, and `清洁工序生产记录`.
Review finding: the current live `产品信息` page is not a valid visual match for target `1.png`. The target image is an `装配及包装信息 + 过程放行信息` overview page, while the current live first page still renders product/batch/material sections instead.
RED: fresh backend `recognize-fixed?routeKey=B` on rebuilt `48082` runtime could not be used to refresh this compare pass because the fixed source file `C:\Users\BJB110\Desktop\2\2\RE-PP-ID-01（A 1）球囊扩张压力泵生产记录(1).doc` is missing. The compare therefore reflects the currently stored live reports, not a freshly regenerated post-fix set.
BDD: refreshed four-image compare after the backend overview-anchor and height-budget repair -> Given the backend Route B live runtime now regenerates from `D:\ProjectPackage\Int\IntRuoyi\resource\批记录模板.doc` and the first report title has switched to `装配及包装信息`, When the frontend capture script reselects the first live report by that new title and captures fresh preview screenshots, Then the compare task should be able to judge the true post-fix first-page match instead of falling back to the old `产品信息` page.
GREEN: updated `doc/tasks/20260518-four-batch-record-image-compare-post-viewer-cleanup/scripts/capture-four-batch-record-previews.mjs` to capture `装配及包装信息` as the first live page and reran `npx.cmd --yes --package @playwright/cli playwright-cli run-code --filename D:\ProjectPackage\Int\IntRuoyi\worktrees\yudao-ui-admin-vue3-batch-record-preview\doc\tasks\20260518-four-batch-record-image-compare-post-viewer-cleanup\scripts\capture-four-batch-record-previews.mjs` -> PASS, fresh live screenshots were captured for `装配及包装信息`, `粗洗工序生产记录`, `精洗工序生产记录`, and `清洁工序生产记录` on `http://127.0.0.1:8082`.
Review finding: target `1.png` is no longer blocked by a wrong live page type. The refreshed live first page now shows the `装配及包装信息 + 过程放行信息` block directly, while `2/3/4` also expose more lower-half content than the previous compare pass.
Review finding after the latest backend worker review: `1.png` still has a large blank right canvas and could use more width utilization, `2.png` and `4.png` improved in lower-half completeness, and `3.png` is the clearest winner on width/row-density balance. The compare is now reading against the live `batch-record-template` pages regenerated from the fixed `批记录模板.doc`.
GREEN: rewired `capture-four-batch-record-previews.mjs` to authenticate through backend API requests and capture the pure backend `http://127.0.0.1:48082/jmreport/view/<reportId>?tenantId=1&token=<refreshToken>` surface instead of depending on the unstable `8082` shell page and iframe `body` screenshot. The refreshed compare still shows `1.png` with too much horizontal whitespace, but it removes the earlier black-line failure and gives a more faithful report-surface capture for `2/3/4`.
GREEN: tightened the same direct-view capture again so the script now screenshots `#jm-sheet-wrapper .jm-sheet` instead of the larger wrapper container. This confirms the remaining large whitespace in `1.png` is not coming from the old `8082` shell or from wrapper-level empty canvas alone; it is still present even when comparing the paper surface itself.
GREEN: after the backend overview-width repair was live-regenerated, reran the same direct `/jmreport/view` paper-surface capture. The refreshed `1-assembly-packaging-live.png` now shows a materially wider first page, confirming the earlier `432px` width bottleneck was real and that the latest backend layout fix improved paper occupancy.
GREEN: after the backend shared process-width-budget repair was live-regenerated on `48082`, reran `npx.cmd --yes --package @playwright/cli playwright-cli run-code --filename D:\ProjectPackage\Int\IntRuoyi\worktrees\yudao-ui-admin-vue3-batch-record-preview\doc\tasks\20260518-four-batch-record-image-compare-post-viewer-cleanup\scripts\capture-four-batch-record-previews.mjs` -> PASS, the latest direct `/jmreport/view` paper-surface capture shows `4-clean-process-live.png` with a visibly wider clean-process page than the previous compare pass.
Review finding: this latest compare keeps the earlier `1/2/3` conclusions, and additionally confirms that `4.png` is no longer as strongly left-compressed as before. The remaining gap on the clean-process page has narrowed to repeated-detail completeness and lower-half content density rather than the earlier obvious narrow-page impression.
GREEN: after the backend proportional residual-width distribution repair was live-regenerated on `48082`, reran the same direct paper-surface capture again -> PASS. The clean-process live report now probes as `dataRectWidth = 1000` instead of `720`, and the refreshed `4-clean-process-live.png` shows another step up in horizontal paper utilization while keeping the same report id and title mapping.
GREEN: after the backend medium-density budget was raised to `1040` and live-regenerated again on `48082`, reran the same direct paper-surface capture -> PASS. The clean-process live report now probes as `dataRectWidth = 1040`, and the refreshed `4-clean-process-live.png` shows a slightly wider paper surface again while preserving the same `清洁工序生产记录` mapping.
GREEN: after the backend dense-page budget was lowered from `1120` to `1044` and live-regenerated again on `48082`, reran the same direct paper-surface capture -> PASS. The fine-wash live report now probes as `dataRectWidth = 1044`, and the refreshed `3-fine-wash-live.png` is visibly closer to the target screenshot’s horizontal paper utilization than the previous `1120`-wide pass.
GREEN: tightened `capture-four-batch-record-previews.mjs` again so each screenshot now clips to the report page’s own `dataRectWidth` from `jmreport/show` instead of always keeping the full `#jm-sheet-wrapper .jm-sheet` width. The refreshed live compare artifacts now render as `3-fine-wash-live.png = 1044x720` and `4-clean-process-live.png = 1040x720`, which removes the artificial right-side viewer whitespace from the compare baseline.
BDD: rerun the four-image compare on the latest live backend after the medium-density first-screen height repair -> Given the compare script already captures the live paper surface by the report page’s own `dataRectWidth`, When we rerun it against the refreshed `48082` backend after the latest medium-density JSON height budget change, Then the artifact set should stay on the same true-paper-width baseline and remain usable for judging the remaining `2/4` lower-half gaps.
GREEN: `npx.cmd --yes --package @playwright/cli playwright-cli run-code --filename D:\ProjectPackage\Int\IntRuoyi\worktrees\yudao-ui-admin-vue3-batch-record-preview\doc\tasks\20260518-four-batch-record-image-compare-post-viewer-cleanup\scripts\capture-four-batch-record-previews.mjs` -> PASS, the refreshed live capture completed again against `http://127.0.0.1:48082/jmreport/view/...`, and the latest artifacts stayed on the true-paper-width baseline with `3-fine-wash-live.png = 1044x720` and `4-clean-process-live.png = 1040x720`.
BDD: compare the four live screenshots again after the shared structured-tail and repeated-sparse-detail fixes -> Given the backend live runtime now compresses repeated sparse checklist rows as detail rows and reserves more first-screen height for `SUMMARY` + structured-tail pages, When the direct `/jmreport/view` capture script is rerun, Then `2.png` and `4.png` should show more of their lower-half tail blocks while staying on the same true-paper-width screenshot baseline.
GREEN: `npx.cmd --yes --package @playwright/cli playwright-cli run-code --filename D:\ProjectPackage\Int\IntRuoyi\worktrees\yudao-ui-admin-vue3-batch-record-preview\doc\tasks\20260518-four-batch-record-image-compare-post-viewer-cleanup\scripts\capture-four-batch-record-previews.mjs` -> PASS after the latest backend live regeneration on `48082`; the refreshed artifacts remain on the same true-paper-width baseline, and the new compare shows `2-rough-wash-live.png` with more visible `生产后清场记录` rows plus `4-clean-process-live.png` with flatter repeated pressure-table details and a more complete `生产后清场记录` tail.
BDD: rerun the compare once more after the JSON builder stopped re-inflating compacted one-line detail rows -> Given the backend live runtime now preserves compacted detail heights in JSON for structured-tail pages, When the same direct paper-surface capture script is rerun, Then the compare artifacts should remain on the true-paper-width baseline while keeping the `4.png` repeated pressure-table rows flatter than the previous pass.
GREEN: `npx.cmd --yes --package @playwright/cli playwright-cli run-code --filename D:\ProjectPackage\Int\IntRuoyi\worktrees\yudao-ui-admin-vue3-batch-record-preview\doc\tasks\20260518-four-batch-record-image-compare-post-viewer-cleanup\scripts\capture-four-batch-record-previews.mjs` -> PASS after the backend JSON-builder fix and live regeneration; the refreshed compare artifacts still stay on the true-paper-width baseline and continue to show the `4.png` repeated pressure-table rows at the flatter compact height instead of the earlier re-inflated form.
BDD: recapture the four live screenshots after the shared overview paper-width budget is raised and the browser capture cache is cleared -> Given the backend now stores `dataRectWidth=1120` for the live `装配及包装信息` overview report, When the compare script clears the browser cache and captures the direct `/jmreport/view` page again, Then the first live artifact should expand to the updated paper width instead of remaining stuck on the stale `960px` cached width.
RED: before clearing the browser cache, the refreshed compare artifact and direct browser capture still remained at `1-assembly-packaging-live.png = 960x720` even though a direct JDBC probe on `jimu_report.json_str` already showed `dataRectWidth=1120`, which proved the compare browser session was still holding stale report-surface cache.
GREEN: updated `capture-four-batch-record-previews.mjs` to clear browser cache/cookies through CDP before login, reran `npx.cmd --yes --package @playwright/cli playwright-cli run-code --filename D:\ProjectPackage\Int\IntRuoyi\worktrees\yudao-ui-admin-vue3-batch-record-preview\doc\tasks\20260518-four-batch-record-image-compare-post-viewer-cleanup\scripts\capture-four-batch-record-previews.mjs` -> PASS, and the latest artifact set now reports `1-assembly-packaging-live.png = 1120x720`, confirming the compare baseline has caught up to the regenerated live overview width.
BDD: align the four-image compare height to the current target screenshot dimensions so the live artifacts are compared on the same paper canvas as the references -> Given the target screenshots have slightly different paper heights (`1=783`, `2=734`, `3=720`, `4=723`), When the capture script uses each target PNG's height as the compare clip height while keeping the live `dataRectWidth` for width, Then the refreshed live artifacts should match the reference canvas sizes more closely without reintroducing the old browser-cache issue.
GREEN: updated `capture-four-batch-record-previews.mjs` to clear browser cache/cookies and clip each screenshot to the corresponding target PNG height, then reran `npx.cmd --yes --package @playwright/cli playwright-cli run-code --filename D:\ProjectPackage\Int\IntRuoyi\worktrees\yudao-ui-admin-vue3-batch-record-preview\doc\tasks\20260518-four-batch-record-image-compare-post-viewer-cleanup\scripts\capture-four-batch-record-previews.mjs` -> PASS, and the new artifact sizes are `1-assembly-packaging-live.png = 1120x783`, `2-rough-wash-live.png = 1031x734`, `3-fine-wash-live.png = 1044x720`, `4-clean-process-live.png = 1040x723`.
BDD: recapture the clean-process compare image after the shared medium paper-width budget moves from `1040` to `1044` -> Given the backend now stores `dataRectWidth=1044` for the live clean-process report, When the compare script clears browser cache and clips by the `showMeta.dataRectWidth`, Then the refreshed `4-clean-process-live.png` should expand from `1040x723` to `1044x723`.
GREEN: reran `npx.cmd --yes --package @playwright/cli playwright-cli run-code --filename D:\ProjectPackage\Int\IntRuoyi\worktrees\yudao-ui-admin-vue3-batch-record-preview\doc\tasks\20260518-four-batch-record-image-compare-post-viewer-cleanup\scripts\capture-four-batch-record-previews.mjs` after the backend medium-band width update -> PASS, and the refreshed artifact set now reports `4-clean-process-live.png = 1044x723` while preserving `1-assembly-packaging-live.png = 1120x783`, `2-rough-wash-live.png = 1031x734`, and `3-fine-wash-live.png = 1044x720`.
BDD: map the full fixed source doc's actual printed pages to the current 15 live Route B reports -> Given `批记录模板.doc` is the sole fixed source and the current live reports are regenerated from that file, When the source doc is exported to PDF pages and its page titles/content are compared with the current Route B report list, Then we should be able to state which source pages/tables already align and which ones still differ by multi-page coverage or complex repeated structure.
GREEN: rendered `D:\ProjectPackage\Int\IntRuoyi\resource\批记录模板.doc` to `C:\Users\BJB110\AppData\Local\Temp\batch-record-source-pages\page-01.png` through `page-19.png` via Word COM + PyMuPDF, extracted the leading page text, and compared it against the current Route B live report list -> PASS. The mapping now confirms:
- source table `1` spans `page-01` to `page-03` and still differs most because the live report anchors to the tail `装配及包装信息 / 过程放行信息` block rather than preserving the earlier `产品信息 / 配件进货批号信息` pages
- source table `4` spans `page-06` to `page-07` and remains a likely repeated-subsection risk page
- source table `13` spans `page-16` to `page-17` and remains a likely second-page equipment-matrix risk page
