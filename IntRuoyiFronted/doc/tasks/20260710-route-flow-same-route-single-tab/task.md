# 工艺路线同一路线单标签修复

## 任务目标
- 修复流转关系图内点击不同工序时，浏览器标签栏重复创建多个“编辑工艺路线”标签的问题。
- 同一条工艺路线只保留一个编辑标签；工序选择仍通过路由查询参数保存，返回页面后继续恢复原工序。
- 不合并不同工艺路线的编辑标签，不改变现有工艺路线 API、保存行为和页面布局。

## 里程碑
1. 已完成：定位标签身份使用完整 `fullPath`，而工序点击会更新 `routeProcessId` 查询参数。
2. 已完成：补充 BDD 场景和 RED 静态回归测试。
3. 已完成：为工艺路线编辑页建立“按路径、忽略查询参数”的标签身份。
4. 已完成：运行目标静态测试、类型检查和相关回归测试。
5. 已完成：测试租户真实点击三个工序后标签数量保持为 1；进入收尾清理和提交。

## 预期验证
- RED：新增静态测试在实现前失败，证明工艺路线编辑路由没有声明稳定标签身份。
- GREEN：同一路线不同 `routeProcessId` 只保留一个标签，不同路线编号仍保留不同标签。
- REGRESSION：现有路线图返回状态、编辑页缓存和标签栏相关静态测试通过。
- E2E：测试租户通过真实前端点击多个工序后，标签栏“编辑工艺路线”数量不增加。

## 设计约束检查
- 是否引入 fallback/降级/吞异常：否。
- 是否从根因和长期维护角度解决：是，在标签身份层区分“路径参数代表业务实例、查询参数代表页面状态”，保留深链和返回恢复能力。
- 是否存在临时补丁或绕过：否。

## 经验门禁
- PowerShell/UTF-8：已读取 `docs/powershell-memory.md`，中文文件使用显式 UTF-8 读取，写入使用 `apply_patch`。
- 前端交付：保持现有路由、状态和 API 契约，不新增测试专用 UI，不隐藏异常。
- BDD/TDD：先新增失败测试，再修改生产代码，记录 RED、GREEN 和回归证据。
- 真实 E2E：执行前必须读取 `docs/login-access.md`，运行官方 `login-preflight.mjs`，只操作测试租户。

## 当前状态
- COMPLETED：根因修复、静态回归、类型检查和本机测试租户真实 E2E 均通过。

## Current Status
completed

## Cleanup Candidates
- tests/output/20260710-route-flow-same-route-single-tab/
