# 排产工作台班次产能与白夜班展示修复

## 任务目标
- 修复排产员工作台“工序列表”中的“班次产能”展示小数，导致数值换行且难以阅读的问题。
- 班次产能按整数四舍五入并保留千分位，例如 `4799.99998` 显示为 `4,800`。
- 用户开启“夜班”后，“班次状态”显示为“白夜班”，并在班次产能数值后显示绿色 `X2`。
- 用户关闭“夜班”后，“班次状态”恢复为“白班”，班次产能不显示 `X2`。
- 仅调整工序列表展示，不改变未完需求、今日报工等允许小数的数量展示，不修改后端计算、保存接口与产能数值。

## 里程碑
1. [已完成] 读取 PowerShell、项目经验、前端样式和缺陷修复门禁，定位目标页面与格式化函数。
2. [已完成] 新增班次产能整数显示回归测试并记录 RED。
3. [已完成] 最小修改班次产能列使用既有整数格式化函数。
4. [已完成] 新增白夜班状态与绿色 `X2` 的失败回归测试。
5. [已完成] 实现统一班次状态展示与班次产能倍数标识。
6. [已完成] 运行目标测试、相关回归、类型检查和测试租户真实用户路径。
7. [已完成] 完成证据校验与收尾预览，进入清理和独立提交。

## 预期验证
- RED：静态契约测试在实现前失败，证明班次产能列仍使用最多 6 位小数的通用格式化函数。
- GREEN：班次产能列改用 `formatIntegerNumber`，示例 `4799.99998` 按 `zh-CN` 显示为 `4,800`。
- RED：夜班开启时，现有页面仍显示后端原始“夜班”状态，班次产能后没有 `X2`。
- GREEN：夜班开启且不是混合状态时显示“白夜班”和绿色 `X2`；关闭时显示“白班”且不显示 `X2`。
- REGRESSION：工序列表短标题、列定义、快速过滤和 TypeScript 检查通过，其他数量列继续使用原格式。

## BDD 场景
- BDD: 班次产能显示整数 -> Given 工序班次产能为带浮点误差的数值 `4799.99998` When 用户查看排产员工作台的工序列表 Then 班次产能显示为带千分位的整数 `4,800`，不显示小数部分。
- BDD: 其他数量精度不受影响 -> Given 未完需求或今日报工包含有效小数 When 用户查看工序列表 Then 这些列继续使用原有通用数量格式，不因班次产能调整而被强制取整。
- BDD: 开启夜班显示白夜班与双倍标识 -> Given 工序当前为白班且夜班未开启 When 用户开启夜班并保存成功 Then 班次状态显示“白夜班”，班次产能数值后显示绿色 `X2`，原始产能数值不被前端重复乘二。
- BDD: 关闭夜班恢复白班 -> Given 工序当前显示“白夜班”和绿色 `X2` When 用户关闭夜班并保存成功 Then 班次状态显示“白班”，班次产能后的 `X2` 消失。
- BDD: 混合夜班不误报双倍产能 -> Given 同一工序聚合数据的夜班配置为混合状态 When 用户查看工序列表 Then 班次状态显示“混合”，班次产能后不显示统一 `X2`。

## 经验门禁
- PowerShell/UTF-8：已读取 `docs/powershell-memory.md`，中文文件显式按 UTF-8 读取，写入使用 `apply_patch`。
- 项目防错：仅修改截图对应的排产工作台班次产能列；不扩展到其他页面、后端计算或兄弟项目。
- 前端样式：保持现有统一列表模板、列宽、对齐和表格样式；倍数标识使用统一成功态绿色小标签。
- 缺陷修复：先复现并新增失败回归测试，再修改生产代码；不添加 fallback、兼容分支或吞异常。
- 并行任务：当前 `20260710-route-flow-detail-partial-refresh` 为不同页面的并行任务，目标文件无重叠，不修改其任务状态或文件。

## 设计约束检查
- 是否引入 fallback/降级/吞异常：否。
- 是否从根因和长期维护角度解决：是，班次产能复用既有整数格式化出口，并由统一班次状态函数同时驱动单元格展示与快速过滤，避免展示口径分叉。
- 是否存在临时补丁或绕过：否。

## 当前状态
- COMPLETED：班次产能整数、白夜班状态与绿色 `X2` 已完成，静态回归、ESLint、TypeScript、证据校验和测试租户真实用户路径均通过。

## Current Status
completed

## Cleanup Candidates
无。

## 最终验证结果
- 测试租户 `tenant_id=122`、账号 `aoteman`、工序 `B050` 真实开启夜班后显示“白夜班”和绿色 `X2`。
- E2E 结束后已恢复该工序原始 `nightShiftEnabled=false`，未遗留测试数据变更。
- 目标静态测试、相关工作台回归、ESLint、TypeScript、前端证据和缺陷证据校验通过。
- 已知旧测试 `mes-scheduler-workbench-process-wip-controls-static.spec.js` 仍断言其他任务已删除的顶部说明区，不属于本任务回归。
