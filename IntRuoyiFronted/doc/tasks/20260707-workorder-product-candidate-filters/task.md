# 生产工单产品候选过滤

## 任务目标

在 MES 生产工单列表查询区新增“产品名称”和“产品编码”两个可输入下拉候选过滤框。用户输入时从真实 MES 物料产品接口拉取候选项，选择候选产品后按产品过滤工单列表，并保留现有“产品”选择器。

追加需求：按用户截图红框，将生产工单列表重点信息列置前并加宽展示，为重点列增加复制按钮；用户复核截图后要求重点列继续显示更宽。

## 非目标

- 不移除现有“产品”选择器。
- 不修改同步、导出、详情打开等既有行为。
- 不新增 mock、本地静态候选列表、fallback 或静默吞异常。

## 经验门禁

- PowerShell / Windows shell / 中文编码：已读取 `docs/powershell-memory.md`，中文读写和命令输出显式 UTF-8。
- 前端页面 / 表格 / 样式：已读取 `D:\ProjectPackage\Int\IntPP\FRONTEND_STYLE.md`，保持当前生产工单列表操作台样式。
- 前端特性交付：已读取 `frontend-feature-delivery` 与 `frontend-contract.md`，补充静态契约测试与证据。

## 设计约束检查

- `是否引入 fallback/降级/吞异常`：否。
- `是否从根因和长期维护角度解决`：是，新增候选筛选参数并由后端按产品 ID 过滤，避免仅前端展示输入框。
- `是否存在临时补丁或绕过`：否。

## BDD 场景

BDD: 产品名称候选过滤生产工单 -> Given 用户打开生产工单列表 / When 在“产品名称”输入框搜索并选择候选产品 / Then 查询请求携带该候选产品 ID，列表只返回该产品相关工单。

BDD: 产品编码候选过滤生产工单 -> Given 用户打开生产工单列表 / When 在“产品编码”输入框搜索并选择候选产品 / Then 查询请求携带该候选产品 ID，列表只返回该产品相关工单。

BDD: 产品名称与编码候选冲突 -> Given 用户分别选择了不同产品的名称候选和编码候选 / When 点击查询 / Then 页面提示“产品名称与产品编码不是同一产品”，不发送工单分页查询。

BDD: 生产工单重点列加宽置前并可复制 -> Given 用户打开生产工单列表 / When 查看红框重点信息列 / Then 工单编号、工单名称、产品编码、产品名称、规格型号、计划数量置于其它列之前，列宽更适合完整展示，并且每列都有复制按钮。

BDD: 生产工单重点列进一步加宽 -> Given 用户查看生产工单列表截图 / When 重点列内容仍出现较多省略 / Then 工单名称、产品编码、产品名称、规格型号、计划数量等重点列进一步加宽，减少可见内容截断。

BDD: 生产工单重点列文本充分利用列宽 -> Given 重点列已经加宽 / When 单元格内仍有空白但文本提前省略 / Then 单元格内部布局应占满整列，文本优先使用可用宽度，复制按钮固定在右侧。

## 里程碑

- [x] M1：新增前端静态契约测试和后端单测，先复现缺少候选过滤参数。
- [x] M2：实现前端两个远程候选过滤框和查询冲突校验。
- [x] M3：扩展后端分页请求 VO 与查询收敛逻辑。
- [x] M4：运行目标验证，更新证据并提交本次相关改动。
- [x] M5：按截图优化生产工单重点列展示、列顺序和复制按钮。
- [x] M6：按复核截图进一步加宽重点列。
- [x] M7：修正重点列内部布局，避免有空白时提前省略。

## 预期验证

- `node tests/e2e/workorder-product-candidate-filters-static.spec.js`
- `node tests/e2e/workorder-key-columns-static.spec.js`
- `pnpm.cmd ts:check`
- `mvn.cmd -pl yudao-module-mes -Dtest=MesProWorkOrderServiceImplTest test`

## 当前状态

completed

## 完成记录

- 查询区新增“产品名称”“产品编码”远程候选过滤框，候选来自 `MdItemApi.getItemPage`；产品名称下拉只展示产品名称，产品编码下拉只展示产品编码。
- 查询前校验产品名称候选和产品编码候选是否指向同一产品；不一致时提示并阻止查询。
- 选择产品名称或产品编码候选后自动触发查询，无需再点“查询”。
- 查询、分页和导出复用 `queryParams`，新增候选 ID 会随工单分页/导出请求传递给后端。
- 生产工单列表重点列已调整为：工单编号、工单名称、产品编码、产品名称、规格型号、计划数量；上述列已进一步加宽并增加复制按钮，生产车间及其它列放在后面。
- 验证：前端静态契约通过；`NODE_OPTIONS=--max-old-space-size=8192 pnpm.cmd ts:check` 通过。

## Cleanup Keep

- `doc/tasks/20260707-workorder-product-candidate-filters/frontend-feature-evidence.md`
