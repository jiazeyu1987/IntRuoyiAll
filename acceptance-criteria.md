# eDHR 偏差管理验收标准

## Purpose and Scope

以下28项为本次目标验收，P1～P6是未来开发阶段，不代表本轮文档任务状态。测试场景逐项映射见test-plan.md。

## Evidence Reviewed

用户最终确认、Word字段、审核表相关条目及PRD。本轮修正不再自动映射原表三级等级，不将PDF扩展作为未授权必交。

## Acceptance Criteria

| ID | 可观察验收 |
| --- | --- |
| P1-AC1 | 有create权限者仅关联一份当前租户正式批记录，一批可多偏差；无权限、跨租户、缺对象拒绝且业务零写入。 |
| P1-AC2 | 上市放行后不能新增关联偏差；UI和后端均给出明确原因。 |
| P1-AC3 | PC-YYYYMM-0001按租户/上海时区年月递增，月切重置、9999后扩位、并发唯一、已提交号不回收。 |
| P1-AC4 | 存在正式批记录但尚未推送PQC时，QA仍可从批记录发起关键偏差NCR；不依赖PQC申请或订单页面。 |
| P1-AC5 | 创建偏差与上市放行并发只有合法顺序可成功；幂等重放不生成第二条偏差/签名。 |
| P2-AC1 | 发起表单覆盖Word发起部分字段，等级仅普通/重大（关键），不得自动把原主要偏差映射成关键。 |
| P2-AC2 | 一偏差至多一份处理记录；未开始可无记录，修改/验证不合格后重验始终复用同一ID，常规关闭前内容完整。 |
| P2-AC3 | 常规关闭要求处理结论为闭环完成，发起编制、处理编制、验证合格、部门负责人、QA关闭确认、质量负责人全部有效签名；QA与质量任意先后，缺任一或持续跟踪仍未处理。 |
| P2-AC4 | 关键偏差常规路径再要求管理者代表；普通偏差无此必签条件，转NCR例外不要求偏差表批准。 |
| P2-AC5 | 修改唯一处理正文保存原因和前后值，旧处理版签名失效但历史可查；未重签不能正常关闭。 |
| P2-AC6 | 同一账号可分别签多个有权节点；同版新增签名不使其他签名失效，不能一次认证自动代签全部节点。 |
| P3-AC1 | 全部/未处理/已处理全部采用标准列表服务端过滤分页排序；query权限看租户内全部而非仅本人。 |
| P3-AC2 | 详情展示发起、唯一处理或未处理说明、签名/审计、关闭方式和NCR链接；转审不伪装验证合格。 |
| P3-AC3 | 正式批记录/活跃表单详情有偏差主Tab，两种追溯抽屉有只读偏差Tab；无逐工序重复Tab或写操作泄漏。 |
| P3-AC4 | 空集合显示“没有偏差”；无权、身份错误、网络失败分别显示原因；旧响应不能串到另一批次。 |
| P3-AC5 | 正常activeOrderId-only入口经后端正式来源得到batchExecutionId再读偏差，不以缺ID错误长期替代功能。 |
| P3-AC6 | 交互包含关联批记录/评审跳转返回、未保存提醒、取消签名、版本冲突、幂等重试和有权动作提示。 |
| P4-AC1 | 偏差转审仅QA且关键等级可用；现有PQC等独立NCR来源不被错误要求先建关键偏差。 |
| P4-AC2 | 一张NCR创建时可多选同批开放关键偏差，空集、跨批、跨租户、普通等级或已关闭项整体拒绝，未选中项不变。 |
| P4-AC3 | 关键偏差可在处理尚未完成、无验证/批准时直接转NCR，成功自动关闭；保留实际资料，不补造缺失签名。 |
| P4-AC4 | 未关闭偏差或待审NCR分别阻断四项正式动作；关键偏差转审关闭仍由NCR接管阻断。 |
| P4-AC5 | QA/PQC处置授权者按让步、返工、作废结果继续控制批次；其他开放偏差/冻结仍有效，让步不自动上市放行。 |
| P4-AC6 | 评审创建、发起签名、冻结、关联和偏差关闭原子化；失败零部分成功，同幂等键可重放成功结果。 |
| P4-AC7 | 已有待审NCR时拒绝第二张及静默追加，保持新偏差OPEN；处理/签署/QA转审/评审处置不被四项门禁误拦。 |
| P5-AC1 | 偏差管理与批记录详情及两种追溯读取同一批全部正式偏差及其关联NCR处置，不混入别批数据。 |
| P5-AC2 | 在线历史保留修订、签名、正常关闭/转审关闭原因；关闭或上市放行后只读，不把PDF导出扩展冒充本次必验。 |
| P5-AC3 | 签名展示真实姓名、服务器精确时间/时区、含义和证据，失效可识别；没有真实签名不拼造成功图样。 |
| P6-AC1 | 按阶段完成实际定向测试证据；E2E须当轮授权且走真实页面，文档校验不可记业务PASS。 |

## Rejection Criteria

- 无权限仍可写；query只查本人导致看不到租户其他偏差。
- 需要先推送PQC才允许QA从批记录发起关键偏差评审。
- 将普通/关键和原表一般/主要/关键混成三档或擅自映射历史。
- 缺部门负责人/编制/验证等必要签名仍常规关闭；把QA先签直接当作关闭。
- 未关闭偏差拦截了处理、签名或评审处置，形成无法解决的流程死锁。
- 自动关闭偏差后直接放开四项动作；让步/返工/作废被统一当作解除全部门禁。
- 关键偏差转审前还要求处理完成或偏差表验证/批准；转审后伪造未执行节点为通过。
- 同版签一个节点导致另一个签名失效；修订后却保留旧签名为有效。
- 两条创建/放行请求竞态同时违规成功；网络重试重复建单或部分关联。
- 仅activeOrderId入口永久报缺ID、或用当前订单/批号猜批记录。
- 历史追溯内出现编辑按钮、空态掩盖请求失败、跨批请求覆盖当前数据。
- 用未找到测试或源码命中声称业务RED/GREEN，或者无授权执行E2E。

## Product Blockers

无新增业务决定阻塞。实施门禁是确认PQC推送前的正式批记录身份与四项写入关联；不能用猜测、mock或已推送样本绕过。待审NCR不事后追加、关闭不重开、导出候选扩展均已在PRD标记首版工程边界。
